# 最速撃破モード（kill-turn 自動目標）設計草案

> **Phase 位置づけ**: ROADMAP の**未確定Phase(ii)**（採番待ち・2026-07-09 再編）。敵HPフェーズ倍率（C7）の
> モデル化で未確定Phase(i)＝敵行動・味方生存と依存を共有するため、(i) と同期させるのが自然。
> **状態: 草案（未実装・2026-07-07 起草／同日 §7 必要性・演算量の検討を追補）**。
> ユーザー構想「シミュ実行時にターン数を指定するのではなく、選択した敵を最速で倒せるよう
> 目標を自動選択できるようにする」の設計たたき台。
> **現在の結論（§7）: S3（目的関数のkill化）は当面保留。S1+S2 は安価で価値が高いが、
> 着手判断は sim02 試行2 の絶対乖離実測をゲートとする**。索引: CLAUDE.md ドキュメント体系。

---

## 1. 目的と現状のギャップ

### 1.1 現状
- `runSim` は**固定ターン数 N**（既定10・UI入力）を探索し、目的関数は**辞書式ターン優先フロントロード**
  （各ターンの `_objective`: FB数→総バースト→総ジャッジ→連理→契晶→ダメージ、`cmpVec` 比較）。
- 敵は `ENEMY_REGISTRY` から def / max_hp / affinity を取り込むが、**HPは探索の停止条件・目的に使われていない**
  （max_hp は表示・逆算用のメタ情報）。

### 1.2 目標
- 敵を選ぶと、**その敵のHPを削り切る最小ターン数（kill-turn）を目的として押し順を自動最適化**する。
- ターン数入力は「自動（最速撃破）」が既定になり、固定Nは検証・較正用途（Phase4のturn-by-turnホライズン等）として残す。

### 1.3 好条件（今の設計と方向が揃っている）
- 既存のフロントロード目的（C6で確定した大域方針）は「固定HPのダメージレースでは前倒し＝早期撃破が実戦的に正」
  という理由で採用されており、**kill-turn 最小化と方向が一致**。∴ 探索の大枠は流用でき、差分は
  「停止条件」「最終ターンの扱い」「絶対値精度」に局在する。

---

## 2. 必須の前提モデル（実装順で先行すべき依存）

| # | 依存 | 現状 | 本モードでの必要性 |
|---|---|---|---|
| P1 | **敵HP追跡**（累計dmg vs max_hp・撃破判定） | max_hp はメタ情報のみ | 必須（kill-turn の定義そのもの） |
| P2 | **C7: HP依存フェーズ被ダメ倍率**（ロキ: HP>50%=×0.7 / 50%以下=×1.2） | deferred（較正時に手補正） | **必須へ昇格**。倍率を無視すると削り速度が系統的にズレ、kill-turn 予測が±1T以上狂う。敵DBにフェーズ定義スキーマ（閾値・倍率の配列）を追加し `_na()` 系の最終乗算に組み込む |
| P3 | **C2: 絶対値乖離の縮小** | open（低優先＝Phase4は序数優先） | **優先度再考**。kill-turn は絶対値予測なので、蓄積誤差がそのまま予測ターンのブレになる。少なくとも較正ボスで「シム予測kill-turn vs 実機kill-turn」の新較正軸を立てる |
| P4 | **def の確定**（walpurgis_loki def=10 は暫定） | 暫定 | P3 と同根。絶対値の一次パラメータ |
| P5 | DOT・反撃等の副次ダメージ | divinus DOT はモデル済みか要確認・ヘカテー反撃（ムーンコード回避時反撃）は未モデル | HP境界付近で±数百万の誤差源。P3 の一部として棚卸し |

> P2〜P5 は「シムの絶対ダメージを実機に寄せる」作業＝**Phase 4 の較正が進むほど本モードの精度が上がる**
> 関係。sim02〜（turn-by-turnホライズン）の完遂が実質的な前提になる。

---

## 3. 探索の設計

### 3.1 目的関数の変更（案）
kill-turn 最小化を辞書式の**最上位**に置く:

```
(kill_turn 昇順, 撃破ターン内の押下位置 昇順, [従来のフロントロードベクトル])
```

- **kill_turn**: 累計dmg ≥ 実効HP に到達したターン（未到達なら +∞ 扱い＝従来ベクトルで比較）。
- **撃破ターン内の押下位置**: 実機では撃破と同時に戦闘終了＝以降の押下は無意味。同じ kill_turn なら
  「より少ない押下で削り切る」ルートが実機の手数・時間で優位（sim02 T7 の実機データも撃破で途中終了している）。
- **未到達時のフォールバック**: 探索上限ターンまでに倒し切れないギア/敵では従来のフロントロード評価に自然移行
  （＝現行挙動と互換）。

### 3.2 適応ホライズン
- N を固定入力せず、**撃破まで伸長**（安全上限 `KILL_TURN_MAX`≈20〜30・到達しなければ上限Nの従来探索として返す）。
- 実装案: まず静的greedy（`_staticPrefixDmg` 相当・数ms）で概算 kill-turn `K0` を推定 → 本探索は `N = K0 + 1〜2`
  で回す（マージン分だけ探索し、目的関数が K0 未満の枝を自動選好）。全ターンを漫然と伸ばすより探索量を抑えられる。
- ロールアウト（`_objective` 内の将来ターン展開）も kill 判定で早期打ち切りでき、**むしろ高速化余地**がある。

### 3.3 較正（calibrateStaticScores）との整合
- 現行の自動較正は「Nターン総ダメージ最大」を目的に override を選ぶ。kill-turn 目的では
  「K0+マージン ターンでの kill_turn 最小」を目的に較正し直すのが筋。
- ただし両者は§1.3 の理由で強相関のため、**まずは現行 override を流用して精度を実測**し、
  乖離が出た場合のみ kill-turn 目的の較正へ拡張する（コスト増を避ける段階案）。

### 3.4 キャッシュ
- `configSig` は既に敵（def/max_hp/affinity）と N を含む。kill-mode はモード識別子と `KILL_TURN_MAX` を
  キーへ追加するだけで既存のヒント検証（リプレイ照合）機構に乗る。

---

## 4. UI（素案）

- ターン数入力の位置に **「🎯 自動（最速撃破）」/「固定ターン」トグル**。既定=自動。
- 結果サマリ: **撃破ターン・撃破押下位置**（例「T7 の 7手目で撃破」）・オーバーキル量・ターン毎の残HP%推移
  （フェーズ境界50%の通過ターンも明示＝C7倍率の切替点）。
- 固定ターンモードは現行表示のまま（Phase4 較正ワークフローが依存するため撤去しない）。

---

## 5. 段階実装計画（案）

| 段 | 内容 | 価値 | リスク |
|---|---|---|---|
| S1 | **敵HPフェーズ倍率のモデル化（C7昇格）**: 敵DBに `phases: [{hp_gt:0.5, taken:0.7},{hp_gt:0, taken:1.2}]` 型スキーマ→エンジン最終乗算 | 較正精度も同時に向上（手補正廃止） | 中（golden変化・ロキ以外は phases 省略で従来動作） |
| S2 | **kill-turn の検出と表示のみ**（探索は従来のまま。「このルートなら T? の何手目で撃破」をサマリ表示） | 低コストで体感価値の大半。予測精度の実測データも貯まる | 小（表示のみ・golden 不変） |
| S3 | **目的関数の kill-turn 化＋適応ホライズン**（§3.1/§3.2） | 本命。押し順が「削り切り最適」へ | 大（golden 体系の再定義: kill-mode 用ゴールデン新設） |
| S4 | **kill-turn 較正軸**（実機撃破ターンとの照合・必要なら較正目的の拡張） | 精度保証 | 中（simNN ワークフロー拡張） |

> S1→S2 の順で入れると、S2 の予測と実機の撃破ターンを突き合わせるだけで P3（絶対値精度）の
> 現在地が数値化でき、S3 に進む判断材料になる。

---

## 6. 未決事項（ユーザーと確定したい点）

1. **撃破ターン内タイブレークの実機的価値**: 「同じ撃破ターンなら押下数最少」を優先してよいか
   （それとも契晶・連理等の残リソース最大化を上位に置くか＝周回の次戦持ち越しは無い認識でよいか）。
2. **倒し切れない場合の挙動**: 上限ターンで未撃破のとき、従来のフロントロード結果を出す（案）でよいか。
3. **C7 フェーズ倍率の一般スキーマ**: ロキ以外の敵（バフデバフ型・時限強化型）をどこまで表現するか
   （Phase6 の敵行動モデルとの棲み分け）。
4. **固定ターンモードの残し方**: Phase4 較正（turn-by-turn）は固定Nに依存するため併存が前提でよいか。

---

## 7. 必要性・演算量の検討（2026-07-07・ユーザーとの検討で確定した現方針）

> 発端: ユーザー「『手段を選ばず最も早く討伐できる押し順』のシミュは演算量が今の比ではない。
> 必要性も含めて検討しよう」。検討の結果、**演算量は本質的障害ではなく、真のブロッカーは絶対値精度**と整理。

### 7.1 演算量 — 爆発しない（現行と同じ近似を使う限り）
- 恐れるべき爆発は「**ターン跨ぎの大域厳密最適**」を求めた場合のみ。しかし現行エンジンは
  **10T総和最適ですらそれをやっていない**（ターン内ビームBW64＋将来ターンは静的greedyロールアウト採点、
  という近似）。kill-turn も**同じ探索構造のまま採点関数を付け替える**なら演算はターン数にほぼ線形。
- 敵が T6〜7 で死ぬ場合は適応ホライズンで N が縮み**固定10Tより軽い**。ロールアウトも撃破判定で
  早期打ち切り可能＝高速化余地。重いのは硬い敵（15〜20T）だけで線形増＋`KILL_TURN_MAX` ガードで抑制。
- 10T総和側が近似で妥協している以上、kill-turn だけ厳密大域最適を要求する理由はない。

### 7.2 必要性 — 価値の大半は「目的関数を変えない」S2 で取れる
| 実用価値 | 取れる段 | 備考 |
|---|---|---|
| (a) N を指定しなくてよい UX | **S2** | 目的関数不変・golden不変・低リスク |
| (b) 「何ターンで倒せるか」の指標 | **S2** | 表示のみ |
| (c) front-load が取りこぼす「±1T早い削り切り」 | S3 | **追加価値は未実証**。C6 のとおり front-load は早期撃破と方向一致＝S3が勝つのは「序盤を犠牲に撃破ターンへ山を作る」例外ケースのみ・差は±1T程度と推定 |

### 7.3 真のブロッカー — 絶対値精度（現状 実機比 ×2級の過小評価）
- kill-turn は**絶対値予測**。序数に頑健だった Phase4 較正方針ではカバーされない領域。
- 実測: sim02 試行1（Manual・実ギア）で実機の総削り9.8億に対しシムreplay累計 約4.84億＝**×2の過小評価**
  （実機T1〜T4は×0.7カット込みでこれ＝補正すると乖離はさらに拡大）。内訳候補: C7フェーズ倍率未モデル・
  def=10暫定・C2蓄積誤差・DOT/ヘカテー反撃の未モデル。
- ∴ 今 S3 を作っても「シムT14撃破予測 vs 実機T7撃破」級の無意味な出力になる。±1Tを競う最適化は
  予測が±数Tで狂っている間は無駄。

### 7.4 判断ゲート（sim02 試行2 データに接続）
1. 試行2（新推奨順・実ギア）の実機 vs シムreplayで**絶対比率を成分別に実測**（C7の×0.7/×1.2は手補正で分離）。
2. **ゲート**: 補正後の絶対乖離が **〜±10%** に収まる → S1→S2 着手（表示だけでも実用精度）。
   **×1.5〜2級のまま** → kill-turn は保留し、C7モデル化・def確定・C2縮小を先行
   （これは kill-turn 抜きでも較正ロードマップと同一の道＝無駄にならない）。
3. **S3 の着手条件**（両方成立時のみ）: ① S2 の撃破ターン予測が実機±1Tに収まる、
   ② front-load が実際に撃破を1T落とす実例が観測される。
