# 外部DB / ビルド導入 検討分析（Next.js・Supabase・Vercel・Googleサービス）

> [!NOTE]
> 本書は「現行の静的構成から、外部DB／ビルドパイプラインへ移行すべきか」を**定量・定性**両面で分析する意思決定資料。
> 結論を先に述べる: **外部DB（Supabase等）・フレームワーク（Next.js）・サーバ演算オフロード（Vercel等）はいずれも本プロジェクトには不適**。
> 条件「演算量に大幅改善が見込める場合のみ検討」を満たす唯一の方向は **クライアント側の WebAssembly 化（＝「ビルド禁止」解除で初めて開く選択肢）** であり、外部インフラではない。

---

## 0. 評価条件（ユーザー指定）
| # | 条件 | 評価上の含意 |
|---|---|---|
| C1 | 「ビルドしない」制約は**解除してよい** | ビルド前提の手段（TS/バンドラ/WASM）を検討可能に |
| C2 | 維持・保守コストは**最小化** | 運用面（アカウント/鍵/ダッシュボード/無料枠ポリシー変動/バージョンドリフト）の増加を強く減点 |
| C3 | **無料** | 各サービスの無料枠の実上限・制約が判定材料 |
| C4 | **Googleサービス可**（Google Drive 5TB 確保済） | Drive/Sheets/Apps Script/Cloud Functions も対象 |
| C5 | 演算量・処理数に**大幅な改善が見込める場合のみ**検討 | 「便利そう」では不可。実測ベースの大幅改善が必須条件 |

---

## 1. 現行アーキテクチャの定量把握

| 指標 | 実測値 | 出典/備考 |
|---|---|---|
| 構成 | 静的 `index.html` + `data/*.js`（`<script>` 直読み）。**ビルド・サーバ無し** | index.html L284-287 |
| 総コードサイズ | index.html **127KB** / データ層 **約38KB**（characters 32KB・weapons 5KB・summons/enemies <1KB） | `wc -c` |
| データ件数 | キャラ **7**・武器 **7**・幻獣/敵 数件 | 現行レジストリ |
| **データの性質** | characters.js に**関数記法79箇所**（`cands.exec`/`guard`/`def.onAbility`/`onBurst`…）＝**行データでなく実行JS（振る舞い）** | `grep => / function` |
| 計算負荷 | フル探索1回 ≈ **12.6秒**（Node単スレ）。ブラウザでは Web Worker で `navigator.hardwareConcurrency` 分**並列化（無料）** | bench実測・index.html Workerプール |
| ホットパス | `_candidates` が探索の約43%。**CPUバウンド・データI/O非依存** | PHASE3_PLAN §1 |

### 1.1 ここから導かれる2つの決定的事実
1. **「DB」が解決する規模問題が存在しない。** データは数十KB・十数件で、JSファイル直読みで瞬時にロード完了。
   SQL/クラウドDB（Supabase=Postgres 500MB枠等）は、本質的に「大量行の永続化・索引・並行書込・認証」を解く道具で、
   本プロジェクトにはそのいずれの要件も無い。**1万行・多人数同時書込・認証が現れるまでDBの便益はゼロ**。
2. **ボトルネックはクライアントCPUであり、データ転送・クエリではない。** よって「DBを速くする」施策は演算を一切改善しない（C5不成立）。
   さらにデータ層は79個の関数（振る舞い）を含み、**リレーショナル/ドキュメントDBには格納不能**（行ではなくコード）。
   DB化するなら関数を文字列で持ち実行時evalする羽目になり、現状の `<script>` 直読みより劣化する。

---

## 2. 技術別 メリット・デメリット分析

### 2.1 Next.js（フレームワーク＋ビルド）
- **メリット（定性）**: TypeScript型安全・モジュール分割・tree-shaking・minify（127KB→圧縮）・ルーティング・SSR/ISR。
- **デメリット（定量/定性）**:
  - C2違反: `node_modules`（数百MB）・依存更新・Nextメジャー追従・設定（next.config/eslint/tsconfig）の**恒常的保守コスト**が発生。現行は保守ゼロ。
  - C5不成立: SSR/ISRは**データ配信**の最適化で、本件のCPUバウンド探索を速くしない。
  - 単一HTML＋Workerの現行設計に対し、フレームワーク導入は**過剰**（オーバーエンジニアリング）。
- **判定: ✕**（重い割に演算改善なし。型安全だけが欲しいなら後述の軽量ビルドで十分）。

### 2.2 Supabase（Postgres＋Auth＋Edge Functions）
- **メリット（定性）**: マネージドPostgres・REST/Realtime自動生成・認証。多人数で編成を共有/保存するなら有効。
- **デメリット（定量/定性）**:
  - C3注意: 無料枠は **DB 500MB・プロジェクト2個・一定期間（約1週間）無アクセスで自動一時停止**（※枠はプロバイダ都合で変動。要都度確認）。趣味プロジェクトでは「久々に開くと停止→手動復帰」が頻発し**C2に逆行**。
  - C5不成立: データ38KBにPostgresは桁違いの過剰。クエリ往復（ネットワークRTT 数十〜数百ms）は**ローカル`<script>`即時読み（0ms）より遅い**。
  - 致命的: **関数79個（振る舞い）を行に格納できない**。DB化できるのは数値フィールドのみで、ロジックは結局JSに残る＝二重管理。
- **判定: ✕**（規模・性質・保守すべてミスマッチ。多人数永続化要件が出て初めて再検討）。

### 2.3 Vercel / サーバレス演算オフロード（探索をサーバ実行）
- **狙い**: 12.6秒の探索を非力なクライアントから剥がしサーバ実行 → 体感改善？
- **デメリット（定量）**:
  - **無料枠の実行時間上限に収まらない**。Vercel Hobbyのサーバレス関数は概ね**10秒級**の上限（※プラン/時期で変動）。現行は1ルート≈15秒。**タイムアウト**。
  - **ブラウザの無料マルチコア並列を失う**。現状はユーザー端末のPコアを無料で使い `ceil((K+1)/P)×15s`。サーバ無料枠は同時実行・GB-secに制限があり、**Pコア相当をfan-outで再現するには複数関数呼び・キュー管理が必要＝C2に強く逆行**。
  - コールドスタート（数百ms〜秒）・帯域/呼出回数クォータ。
- **判定: ✕**（無料枠でクライアント並列に勝てず、保守だけ増える。C5不成立どころか悪化リスク）。

### 2.4 Googleサービス（Drive 5TB / Sheets / Apps Script / Cloud Functions）
- **Drive 5TB**: 大容量だが**静的Webホストではない**（Driveの直接Webホスティングは2016年に廃止、HTML/MIME/CORSの安定配信不可）。→ **アプリ配信用途に不適**。バックアップ/生成物保管庫としてのみ有用。
- **Google Sheets を DB 代用**: 行データには使えるが、**API クォータ（読み書き回数/分）・レイテンシ（数百ms〜）・関数格納不可**。38KB・関数主体の本データには不適。
- **Apps Script で演算**: 実行**6分上限**だが**JSエンジンが低速**＋クォータ厳しく、12.6秒級探索の高速化には逆効果。
- **Cloud Functions（GCP無料枠）**: 月200万呼・540秒上限と寛容だが、**GCPプロジェクト/IAM/デプロイの保守（C2逆行）**＋無料GB-sec上限＋コールドスタート。2.3同様、クライアント無料並列に対する優位が乏しい。
- **判定: ✕（Drive=保管庫としてのみ○）**。演算・DBいずれもクライアント無料実行に勝てない。

### 2.5 【対案・C5を満たしうる唯一の方向】クライアント側 WebAssembly 化 ＋ 軽量ビルド
- **位置づけ**: C1（ビルド解除）が**実際に開く価値はここ**。外部インフラではなく、ホットパス（`use`/`burst`/`_na`/`_candidates` の探索ループ）を **Rust/AssemblyScript → WASM** へ移植。
- **メリット（定量見込み）**: 数値計算中心のホットループは WASM 化で**2〜5倍**級が現実的（JIT非依存・型確定・GC回避）。**同じ無料クライアントコア上**で動き、**外部インフラ・アカウント・クォータ一切不要**。生成物は静的`.wasm`＝**配信はGitHub Pages/Cloudflare Pages等の無料静的ホストで保守最小**。
- **デメリット**: ビルド工程（wasmツールチェーン）追加＝初期コスト。ただし**生成物は静的**なので**運用保守はほぼゼロ**（C2と両立可）。エンジンのWASM/JS二重実装の同期コストに注意（まずPhase3のJS最適化で十分かを見極めてから判断）。
- **判定: △（将来の有力候補）**。ただし**着手前提はPhase 3のJSホットパス最適化（無料・無ビルド・~30-40%）を先行**し、再計測でなお不足なら WASM へ。

---

## 3. 決定マトリクス（条件適合スコア）

| 手段 | C1 ビルド | C2 保守最小 | C3 無料 | C4 Google | C5 大幅な演算改善 | 総合 |
|---|---|---|---|---|---|---|
| 現状維持（静的＋Worker並列） | — | ◎ 保守ゼロ | ◎ | — | — 現状値 | **基準（妥当）** |
| Next.js | 要 | ✕ 重い | ○ | — | ✕ 無関係 | ✕ |
| Supabase | 不要 | ✕ 停止/鍵 | △ 枠制約 | — | ✕ 過剰・関数不可 | ✕ |
| Vercel等 サーバ演算 | 不要 | ✕ fan-out | △ 時間上限 | — | ✕ 並列喪失 | ✕ |
| Google Drive(配信) | — | ✕ 不適 | ◎ | ◎ 容量 | ✕ ホスト不可 | ✕（保管庫のみ○） |
| Google Sheets/Apps Script | — | ✕ クォータ | ○ | ◎ | ✕ 低速 | ✕ |
| **Phase3 JSホットパス最適化** | 不要 | ◎ | ◎ | — | ○ ~30-40% | **◎ 最優先** |
| **WASM化＋静的無料ホスト** | 要 | ○ 生成物静的 | ◎ | △ Drive不可 | ◎ 2-5x見込 | **△ 次点（要先行検証）** |

---

## 4. 結論と推奨

1. **外部DB・フレームワーク・サーバ演算は導入しない。** 本プロジェクトは「データ38KB・件数十数・振る舞い主体・CPUバウンド」であり、
   これらが解決する課題（大規模永続化・並行書込・認証・データ配信最適化）が一つも存在しない。C5（大幅な演算改善）を**いずれも満たさない**。
2. **演算改善の正攻法はクライアント側に閉じる**:
   - **第一手（無料・無ビルド・即効）**: `PHASE3_PLAN.md` §1 のホットパス最適化（`_candidates`/`_stepStatic` のアロケーションフリー化）。~30-40%。
   - **第二手（C1解除で開く・大幅改善候補）**: ホットループの **WebAssembly 化**。2-5x見込。生成物は静的のため保守最小（C2両立）。ただし第一手の再計測でなお不足な場合のみ着手。
3. **Google Drive 5TB の活用は「演算/DB」ではなく「保管庫」**: リプレイ結果・実機スクショ・大容量検証ログのバックアップ先としてのみ有用。アプリ配信・DB・演算には用いない。
4. **配信は無料静的ホスト**で十分（GitHub Pages / Cloudflare Pages / Netlify 無料枠）。Drive配信は不可。ビルド生成物（WASM採用時）もこれら静的ホストに置けば運用保守は実質ゼロ。

### 4.1 「再検討すべき」トリガー（将来この条件が現れたら見直す）
- 多人数が編成データを**共有・永続化・認証付き編集**する要件 → そのとき初めて Supabase 等を検討（DBの本来用途）。
- データが**数千〜万件規模**に膨張し `<script>` 直読みのロード/メモリが問題化 → 静的JSON分割＋遅延ロード（まだDB不要）。
- Phase3 JS最適化＋WASMでも探索時間がUX上不足、かつ多端末で共有計算したい → サーバ演算を**有料枠前提で**再評価（無料枠では不可）。

> 要するに: **「無料・保守最小・大幅演算改善」を同時に満たすのは外部インフラではなくクライアント最適化（Phase3→必要ならWASM）**。
> 外部DB/ビルドフレームワークは、要件が「単一端末の火力シミュレーション」から「多人数の永続サービス」へ変質した時に初めて意味を持つ。
