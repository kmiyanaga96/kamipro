// T1 録画転記 — ROI 定義（canvas 基準の正規化座標）
//
// ★座標系: ゲーム canvas の左上を (0,0)、右下を (1,1) とする比率。
//   画素座標は持たない。∴ スクロール位置・ウィンドウサイズ・ブラウザズーム・録画解像度が
//   変わっても同じ ROI が同じ物を指す（PHASE9_PLAN.md §4 P1 新発見② への構造的な答え）。
//   画素へ落とすのは `roiToPixels(box, roi)`（canvas_detect.js）。
//
// ⚠ provenance（REPO_STANDARDS E1＝測定条件を併記する）:
//   採寸日 2026-08-14 ／ 録画 `pic.mp4` 2288×1440（Microsoft Game DVR・Chrome ブラウザ版）
//   検出 canvas box = { x:290, y:198, w:1684, h:1129 } ／ 採寸者＝ユーザー（T1 ページでドラッグ）
//   ⚠ **1本の録画・1つのウィンドウサイズからの1点採寸**。別条件では未再測（E9）。
//   ⚠ ゲーム側の UI 更新でレイアウトが動いたら採寸し直す（正規化座標でも UI 改変には追随しない）。

export const ROI_PROVENANCE = {
  measuredAt: '2026-08-14',
  source: { file: 'pic.mp4', resolution: '2288x1440', recorder: 'Microsoft Game DVR' },
  canvasBox: { x: 290, y: 198, w: 1684, h: 1129 },
  note: '1点採寸。別の録画・ウィンドウサイズでは未再測（E9）。',
};

/**
 * ★**要素ごとの採寸**（2026-08-18・ユーザー提案で方式を変更）。
 *
 * ⚠⚠ **それまでは「`hp` の中のどこに CT があるか」を幾何で当てにいっていた**（v0.15.0〜0.19.0 の帯探索）。
 *   その機構は**偽陽性1回・偽陰性1回**を出したあと、**採寸1分で決着した**。
 *   ★§1.1 の憲法どおり＝**観測はユーザー／ツールは転記**。位置は「測る」もので「当てる」ものではない。
 */
export const ROI_PROVENANCE_2 = {
  measuredAt: '2026-08-18',
  source: { file: 'M3-1.mp4', at: 23, resolution: '2288x1440' },
  canvasBox: { x: 290, y: 198, w: 1684, h: 1129 },
  measured: ['hpbar', 'modebar', 'ct', 'debuff'],
  note: '1点採寸（1録画・1ウィンドウサイズ・t=23s）。別の敵・UI 更新では未再測（E9）。',
};

/**
 * ★**バーそのものの実寸**（2026-08-18 採寸）。**`ROIS` には入れない**＝
 * ①HP 読み取りには使わない（`hp_bar.js` の帯がバーの縁に乗って flash が 20倍になる・`hpbar` の注記）
 * ②`ROIS` に入れると `findOverlaps()` が `hpbar` と 100% 重なると警告し、画面にも二重に描かれる。
 * ⚠ 帯の割合を内側へ寄せる作業をするときの**一次情報**として保存する。
 */
export const HP_BAR_TIGHT = { x: 0.20902612826603326, y: 0.10363153232949512, w: 0.3859857482185273, h: 0.025686448184233834 };

export const ROIS = {
  /**
   * 敵HPバー＋整数%表示。検算①（総和チェック）の入力。
   * ★同じクロップに **チャージターン（CT）の丸ドット**が写る（ユーザー回答 2026-08-14）。
   *   CT は敵の**限定行動の発動条件**（`gamedata/md/敵/*` に「チャージターンが最大でない」多数）で、
   *   最大になると強力な攻撃をすることが多い。⚠ **シムには未実装**（`charge` 系の記述がゼロ）。
   *   ∴ ドットは「マスクすべき邪魔物」ではなく **敵行動モデルの観測値**（PHASE9_PLAN §4 P2 の⑭）。
   * ⚠ HP% は**塗り画素の計数ではなく塗りの右端エッジ位置**で測る（CT ドットは中央付近で状態も変わるため）。
   */
  hp: { x: 0.19299, y: 0.07529, w: 0.40796, h: 0.11249 },

  /**
   * ★**バーそのものだけ**（敵アイコン・レベル名・下部アイコンを含まない）。P2-5 の入力。
   * ⚠ **`hp` で代用してはいけない**: 敵アイコンが赤系でバーと色分離できず、
   *   ヒューリスティックで分けようとすると破綻する（実装時に HP 20% を 100% と誤読させた）。
   *   **位置で解ける問題を色で解こうとしない**＝ここを人が採寸することで構造的に除く。
   * ✅ 採寸済（2026-08-14・`M3-1.mp4` t=5.0）＝**640×54 px**（canvas 1684×1129 基準）。
   *
   * ⚠⚠ **2026-08-18 に 650×29 px（バーそのもの）へ差し替えたが、実走で退行したので戻した**。
   *   同じコードで **ROI だけを変えた結果**（120秒走・`M3-1.mp4`）:
   *
   *   | | 旧 640×54（y 121〜175） | 新 650×29（y 117〜146） |
   *   |---|---|---|
   *   | `flash`（バーが見えない） | **109** | **2152**（20倍） |
   *   | 読めたフレーム | 3085 | **1020** |
   *   | 単調性違反 | 5 | **36** |
   *
   *   ★**原因は `hp_bar.js` の帯の取り方**＝`stripTop [0.06,0.26]` / `stripBottom [0.74,0.94]` は
   *   **ROI の上下端**を見る。∴ **ROI が「バーぴったり」だと、帯はバーの縁（枠線・グラデ）に乗る**:
   *   - 旧 ROI: 上帯 y 124〜135＝**バーの内側**（バー 117〜146 の 7〜18px 内）→ 赤が乗る ✅
   *   - 新 ROI: 上帯 y 118.7〜124.5・下帯 y 138.5〜144.3＝**上下とも縁から 1.7〜7.5px** → 赤が足りず
   *     `peak < visibleFloor` で **flash 扱い**になる
   *   ⭐ **旧 ROI の下帯（y 161〜172）はバーの外**だが**無害**だった＝v0.12.0 の階段フィットは
   *     **絶対水準に依存しない**ので、上帯だけで信号が立つ（「完全に塗られた列でも占有率 0.375〜0.86」
   *     という既記載の実測値は、**行の約半分がバー外**であることと整合する）。
   *
   * ★∴ **`hp_bar.js` は「バーの周囲に余白のある ROI」を前提にしている**。
   *   タイトな ROI を使いたければ**帯の割合を内側へ寄せる**必要があり、それは
   *   フィクスチャでの再検証を伴う別作業（推測で動かさない）。
   *
   * ⭐ **2026-08-18 の採寸で得た知識は保存する**＝**バーそのものの実寸は y 117〜146（29px）**
   *   ＝`{ x: 0.20902612826603326, y: 0.10363153232949512, w: 0.3859857482185273, h: 0.025686448184233834 }`。
   *   帯の割合を見直すときの一次情報。
   *
   * ⚠ 旧 ROI が CT の行を含むことは**もう問題にならない**＝CT は `ct` ROI で別に読む。
   */
  hpbar: { x: 0.21318, y: 0.10717, w: 0.38005, h: 0.04783 },


  /**
   * ★**敵モードゲージのバー**（ユーザー提案 2026-08-18・⏳未採寸）。
   *
   * ⚠⚠ **これは装飾ではない**＝**一次情報にあるメカニクスで、シムには実装が無い**。
   *   `gamedata/md/敵/ryomen_sukuna.md` §「モードリミット」＝「我々の**敵モードゲージ操作**を減少」／
   *   `variant_chimera.md` `variant_chimera_chi.md` にも同記述。
   *   一方 `src/`・`gamedata/js/` に `modeGauge` 系の実装は**ゼロ**（2026-08-18 に grep で確認）。
   *   ∴ **CT とまったく同じ立場の観測値**＝C45（敵の行動モデル）の intake 経路。
   *
   * ⚠ 命名: `panel_mode.js` の `mode`（右パネルの list/detail）とは**別物**。混同しないこと。
   * ✅ 採寸済（2026-08-18）＝**345×42 px**（canvas x 361〜706 / y 135〜177）。
   */
  modebar: { x: 0.21437054631828978, y: 0.1195748449955713, w: 0.20486935866983372, h: 0.03720106288751107 },

  /**
   * ★**チャージターン（CT）の表示**（ユーザー提案 2026-08-18・⏳未採寸）。
   *
   * ⚠⚠ **「ドット列」と「連続したゲージ」のどちらなのかが未確定**。
   *   2026-08-14 のクロップ受領時の記録は「**バー上の丸ドット5個**」（PHASE9_PLAN ⑭）だが、
   *   2026-08-18 のユーザー提案は「**CT ゲージ**」と呼んでいる。
   *   ★**採寸されたらこの ROI の集約プロファイルが答える**（山が離散的に並ぶか、境界が1つ動くか）。
   *   ⚠ **どちらかを仮定して読み取りを書かない**＝`charge_dots.js` は離散ドットを前提にしている。
   * ✅ 採寸済（2026-08-18）＝**162×37 px**（canvas x 693〜855 / y 138〜175）。
   *   ★**2026-08-17 に「目盛り」と判定した等間隔6山（canvas x 700〜802・間隔 21.3px）は、この枠の中**。
   *   ⚠ 幅 162px に 21.3px 間隔なら **7.6 個ぶん**入る＝**個数は決め打ちせず毎回数える**
   *   （一次情報に「最大チャージターンを2つ減少」があり、**個数の変化自体が観測値**）。
   */
  ct: { x: 0.41152019002375295, y: 0.12223206377325066, w: 0.09619952494061758, h: 0.03277236492471213 },

  /**
   * ★**敵のバフ・デバフアイコン列**（ユーザー提案 2026-08-18・⏳未採寸）。
   *
   * ⚠ **C38（`buffCount`）の近道にはならない**（2026-08-14 に確定済）＝
   *   味方 Status のバフは一部しか表示されない。ここは**敵側**なので別の用途:
   *   デバフの**入り/切れ**はダメージ計算の前提（`damage ∝ 1/enemy_def` 等）に直結する。
   * ✅ 採寸済（2026-08-18）＝**630×52 px**（canvas x 365〜995 / y 164〜216）。
   */
  debuff: { x: 0.2167458432304038, y: 0.14526129317980513, w: 0.37410926365795727, h: 0.046058458813108945 },

  /**
   * ダメージ／`TOTAL` ポップアップが出る範囲。検算③④⑦の入力。
   *
   * ⚠⚠ **狭すぎることが実測で判明した（2026-08-19c・未修正）**:
   *   `M3-1.mp4` のバースト `TOTAL` 表示（`TOTAL STING! 5,044,…`）が**右端で切れており、
   *   下三桁が枠の外**に出ている。∴ **この枠のままでは TOTAL の値は読めない**
   *   ＝検算⑦（`TOTAL` ↔ 個別ヒットの合計）が成立しない。
   *   ★2026-08-14 の採寸は**個別ヒットのポップアップ**を見て決めたもので、
   *     **バーストの TOTAL 表示が桁違いに大きい**ことを知らなかった（1文字 125px 級）。
   *   ⏳ **要・採り直し**（ユーザーがドラッグで採寸＝位置の問題は当てにいかない）。
   */
  dmg: { x: 0.09382, y: 0.25332, w: 0.35511, h: 0.70682 },

  /**
   * 右のアビリティパネル（4枠＋グレーアウト＋CD残ターン）。検算⑧の入力。
   * ⚠ **表示されるのは「そのとき選択されているキャラ」のぶんだけ**（ユーザー回答 2026-08-14＝
   *   キャラアイコンをタップして個別キャラ画面へ遷移する UI）。∴ 検算⑧は**全キャラ同時の状態検査ではなく、
   *   映っているキャラについての機会的な検査**になる。
   * ⭐ 逆に「**どのキャラのパネルが映っているか＝操作対象**」自体が押し順アラインメントの手がかりになる。
   */
  abil: { x: 0.76960, y: 0.57219, w: 0.23040, h: 0.29672 },

  /** `NEXT n TURN`。ターン境界の決定的な読み取り（P1 新発見③）。 */
  turn: { x: 0.61698, y: 0.76882, w: 0.13064, h: 0.04252 },

  /**
   * キャラ Status。ユーザー回答（2026-08-14）＝**5キャラのHP・バフの一部・BG（バーストゲージ）**が映る。
   * ⭐ **BG が読める**＝**C24（ゲージ生成 1ヒット +10 が未実装）の直接の観測経路**になりうる。
   * ⚠ バフは**一部しか表示されない**＝ここから buffCount は復元できない（C38 の近道にはならない）。
   * ⚠ 現状の採寸は右パネルをまるごと覆っており **`abil` を 97.7% 内包している**（`findOverlaps()` で検出）。
   *    害は無いが、P2-3 で Status ブロックだけに詰める余地がある。
   * ⏳ **未確認**: 5キャラぶんが1フレームに同時に映るのか、選択キャラのみか（要 gauge クロップ PNG）。
   */
  gauge: { x: 0.77078, y: 0.23384, w: 0.22506, h: 0.64925 },
};

/**
 * ROI の重なりを検出する（採寸ミスの早期発見用）。
 * @returns {Array<{a:string,b:string,ratio:number}>} 重なり面積 / 小さいほうの面積
 */
export function findOverlaps(rois = ROIS) {
  // ⚠ 未採寸（null）の ROI は飛ばす（`hpbar` のように後から埋める枠がある）
  const names = Object.keys(rois).filter((k) => rois[k]);
  const out = [];
  for (let i = 0; i < names.length; i++) {
    for (let j = i + 1; j < names.length; j++) {
      const a = rois[names[i]], b = rois[names[j]];
      const ow = Math.min(a.x + a.w, b.x + b.w) - Math.max(a.x, b.x);
      const oh = Math.min(a.y + a.h, b.y + b.h) - Math.max(a.y, b.y);
      if (ow <= 0 || oh <= 0) continue;
      const ratio = (ow * oh) / Math.min(a.w * a.h, b.w * b.h);
      out.push({ a: names[i], b: names[j], ratio });
    }
  }
  return out;
}
