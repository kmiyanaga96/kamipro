# Phase 4 計画 — 実機較正の反復（押し順優先）＝エンジンロジックの回収

> Phase 3（高速化）クローズ後の現行フェーズ。シムの探索ロジック・ダメージモデルと**実機**の
> 乖離を、ドキュメント駆動フロー（CLAUDE.md §4）で一つずつ回収する。
> 一次情報は CLAUDE.md・CALIBRATION_ANALYSIS.md。本書は Phase 4 の**進め方と方針**の台帳。

## 1. 目標と優先順位（合意済み）

- **主目標＝押し順（行動順）の実機一致**。ダメージ絶対値の完全一致は二次。
- スコープは**較正に限定**。敵行動・味方生存は**未確定Phase**（ROADMAP §4-i）として分離。
  - ※Phase 番号再編（2026-07-09）: 旧「Phase 6＝敵行動・味方生存」は未確定Phase(i)へ退避、現 Phase 6 は幻獣システム拡張。本書中の以降の「Phase 6（敵DB化等）」表記は**未確定Phase(i)＝敵行動・味方生存**を指す（歴史記述はそのまま）。
  - ただし **敵DB化等が較正に正のフィードバックを与えると判明した場合は一括着手も排除しない**（下記 §7）。

## 2. 戦略の核心 — 「押し順は蓄積誤差に頑健、系統誤差だけを狙う」

実機との**ターン内総ダメージ乖離は各ダメージ算出の許容誤差の蓄積**であり直接の修正は困難（既知）。
しかし**押し順の正しさは一様・ランダムな誤差に対して頑健**：全アビが似た係数でズレても順序ランクは保存される。
**押し順が反転するのは系統的（ability固有）な誤差のときだけ**：

- 減衰 **cap** の誤り（スタックの限界価値→「今撃つ vs 溜める」が反転）
- **フレーム**の誤り（na/abi/burst で減衰slopeが異なる→相対価値が変わる）
- 特定アビの**倍率/トリガー/バフ有無**の誤り

→ **方針: 総ダメージ絶対値の一致を追わず、押し順を実際に反転させる系統誤差だけを狙い撃つ。**
→ **検証は序数（ordinal）**: 「手順A vs B、実機でどちらが上か」の符号がシムと一致するかを許容マージン付きで判定。
  絶対値一致は不要なため蓄積誤差にロバストで、要求データも軽量（点3の都度測定と整合）。

## 3. 押し順較正ループ（駆動フロー）

1. **乖離の検出（無測定）**: 対象形成でシムの推奨押し順をUI（カード）で表示し、ユーザーが実機経験と照合して
   「明らかに最適でない／自分なら打たない」ターンを**フラグ**する（測定不要の最安の検出）。
2. **序数A/B測定（実機）**: フラグ箇所について「シム推奨手順A」vs「ユーザー想定の上位手順B」を実機で打ち、
   どちらが上か（できれば概算差分）を測定。要求データセットを明確化して都度依頼（§6）。
3. **系統原因の診断**: 実機でBが上なのにシムがAを上位にした場合、§2の系統誤差（cap/frame/倍率/トリガー）の
   どれが順序を反転させているかを特定。リプレイモードで起点ターン・バフ・枠を切り分ける。
4. **較正修正**: 原因を `gamedata/js/characters.js`（CHAR_REGISTRY）または `DMG` の宣言的記述として修正
   （エンジン本体へのキャラ名リテラル/個別分岐の追加は禁止＝不変条件）。
5. **回帰固定**: そのA/Bを**序数フィクスチャ**として保存し回帰スイートへ追加（§4）。ゴールデン値 175,023,298 も併せて維持。

## 3.5 較正カデンツ：turn-by-turn ホライズン（採用方針・2026-06-26 決定）

§3 の駆動フローを**ターン単位で前進**させる。**各 sim で「検証済みホライズン」を1ターン伸ばす**
（sim01=T1, sim02=T2, …）。各ターンでは §2 どおり**系統誤差と序数のみ**を見て潰し、**絶対総和の一致は追わない**。

- **採用理由**: 成分の分離測定が実機で不可能（会心RNG・単一成分を切り出せない）。ターンを順に固定し、
  ターン総和＋シムの成分分解で進める方が単一サンプル依存を避けられ堅牢。序数フィクスチャ（§4）も貯まる。
- **後段乖離の局在化**: 前段ターンが固定済みなら、後段の残差は当該ターン固有の系統誤差に局在する。
  例) **C1（テトラHELIX後追撃cap）**は T6〜T7 到達が前提のため単独検証困難だが、ホライズンがそのターンに
  達した **≈sim06-07** で「前段固定済み＝残差が HELIX追撃に局在」する形で確定する（逆算式は CALIBRATION C1 に退避）。
- **緩い結合**: sim#≈ホライズンのターン番号だが厳密固定はしない。各 sim が「どのターンまで担保したか」を
  `simNN/README.md` で宣言する。蓄積運用・テンプレは `simulation/README.md`。

### 3.5.1 【転換・2026-07-12 ユーザー決定】統計的較正 × 反復可能ボス（sim03以降）

**較正対象を回数制限のない反復可能ボスバトルへ転換し、統計的較正を導入**。turn-by-turn の採用理由だった
「会心の分離測定が実機で不可能＝単一サンプル・会心RNGに脆い」制約が、反復により解ける:
- **N試行の集計**: 会心RNGを平均で隔離（我々の与ダメは押下列で決定的＝試行間で振れるのは会心/急所RNGのみ）。
- **会心/急所率の実測**: マーク頻度から `_na()` の critRate/acute 期待値モデルを直接検証・較正。
- **非会心アンカーの反復捕捉**: C25（raw局在）の決定打が1発勝負でなくなる。装備2config（full-crit=決定的／crit-off=マッチペア）
  ×有利/中立ボス（急所=有利属性のみ発動で自動分離）の**2×2で会心・急所を完全分離**。
- **序数A/B（§4）**: 同一stateでのA/B実測が可能に＝序数フィクスチャが本来の形で取れる。
- **未定量の敵耐性への回答**: エンドコンテンツボスの「有利属性以外からの攻撃耐性」（未定量・有志検証なし）は
  最終ダメに乗る**大域スカラ＝形状較正に非依存**。絶対レベルは光有利（闇）ボス1体をアンカーに確定し、
  モデル確定後に各ボス耐性を実測残差として定量化（副産物）。**モデルと耐性を同時にfitしない**（識別不能）。
- turn-by-turn の資産（sim01=T1/sim02=T2 のフィクスチャ・後段局在化の考え方）は継承。C1 等の後段課題も反復ボスの
  長ホライズン走で従来より速く確定しうる。第1走プロトコル＝ `simulation/sim03/README.md`。フォルダ新構造＝ `simulation/TEMPLATE/`。

## 4. 序数比較ハーネス（設計・実装は初回ケースで具現化）

- **フィクスチャ** = `{ formation, gear, enemy, orderA:[turn毎keys], orderB:[...], real:'A>B'|'B>A', margin }`。
- **テスト**: リプレイ経路（`_execKeyNoGuard` 相当の手順強制）で orderA / orderB をシムに再生し総ダメージ simA/simB を取得、
  `sign(simA - simB)` が `real` と一致することをアサート（マージン内の僅差は「判定不能=要再測定」として区別）。
- **狙い**: シムの**ダメージモデルが順序を実機と同じく並べるか**を検証（絶対値非依存）。モデルが正しく並べば、
  既に準最適な探索は実機で良い手順を推奨する。→ モデルの序数フィクスチャを Phase 4 の基盤テストとする。
- **実装方針**: 投機的に作らず、§3 で**最初の実乖離ケースが出た時にそのケースを土台に最小実装**する。

## 5. 乖離バックログ

未解決乖離は CALIBRATION_ANALYSIS.md の「未解決乖離バックログ」で open/investigating/fixed を追跡（失わないため）。
現在の open 項目:
- **テトラHELIX後追撃の減衰上限（推定100万）**: 旧 placeholder を移送。テトラ4アビが連理魔力をT6〜T7まで
  蓄積しないと発動せず単独検証困難。押し順優先方針下では、これが**順序を反転させる**ケースが出た時に併せて確定する。
- **ターン内総ダメージの絶対乖離**: 蓄積誤差由来で直接修正困難。押し順優先方針では**既知・低優先**として保持し、
  序数では問題化しない限り追わない（系統誤差が見つかればその修正の副産物として縮小する）。

## 5.5 重大なエンジン改善の履歴（Phase 4 で判明・押し順較正の副産物）

押し順較正の過程で、火力モデル較正にとどまらない**エンジン根幹の改善**が判明した。記録として残す（一次台帳は各リンク先）。

- **C8（2026-06-28・モデルバグ）**: ヤマト1アシ「集いし願い」のバーストダメージプラス +10万/stack を、
  旧実装はヤマト自バーストのみに加算していた（誤り）。実機は**味方全体のバースト**が対象。
  `gamedata/js/characters.js` で全バースト共通の `burstPartyPassive` へ移管。ゴールデン値 92,031,195→154,711,673。
  副次効果として最適押し順が「funki前出し＋ロボ＋アンプリファ前倒し」の定石へ自発シフト。詳細: CALIBRATION_ANALYSIS.md C8。
- **C9（2026-06-28・探索の準最適性）**: ビーム幅 `BEAM_W` 32→128。BW32は「バフ/デバフ先・ダメージアビ後」の
  最適枝を枝刈りで取りこぼし、実機定石とズレた押し順を出していた（非単調の崖: BW64≈無改善/BW128で回復）。
  **モデルは正しく、探索側の限界**だった。改善幅はギア依存（弱ギア+5.0%／強ギア+0.10%）。
  ユーザー実ギアでスクショ押し順を完全再現して切り分け（古コードでなく gear-cap 相互作用が真因）。
  ゴールデン値 154,711,673→162,398,625・探索量約4倍。詳細: `BEAM_SEARCH_DESIGN.md`／CALIBRATION_ANALYSIS.md C9。
- **C11（2026-06-28・モデルバグ）**: ナイツサプレス（エレイン3）のバースト耐性-20%が非累積（実機確認Q2:a）なのに
  length累積扱いで毎ターン連発していた。guard に `!nights有効` を追加し隔ターン発動化。ゴールデン値 162,398,625→150,445,920。詳細 CALIBRATION C11。
- **C12（2026-06-29・探索の準最適性②＝賢い枝刈り）**: 案B（僅差許容タイブレーク）はダメージを犠牲（generic −0.45%）にするため不採用。
  採用＝**案C: ビーム多様性枠（`BEAM_DIVERSITY_K`）で定石性(orthodoxy)上位枝を枝刈りから保持し、選択は純ダメージ最大（`_objective`/`cmpVec`不変）＝ダメージ単調**。
  ゴールデン値 150,445,920→155,756,325（+3.5%）／強ギア +0.80%。残: exact-tie定石タイブレーク（表示の定石化・同ダメージ時のみ）・tenya分割。詳細 `ORDER_OPTIMIZATION_DESIGN.md`／CALIBRATION C12。
- **教訓**: 押し順が実機定石とズレた時、原因は「火力モデル（計算式）」と「探索（最適化器）」の二層に分かれる。
  まず `_na()` 等の計算式が当該バフ/デバフを正しく適用しているか（モデル層）を確認し、適用済みなら探索層
  （ビーム幅・枝刈り・ルート選抜）を疑う。**強ギア帯では flat/上限系の差が cap飽和で希薄化し、探索の取りこぼしが
  顕在化しにくい**ため、検証は弱ギアと強ギア両方で行う（ギア依存の深刻度を見落とさない）。
- **派生課題**: BW128化で探索が重くなったため、**待機画面の本格刷新**を緩和策として起票（C10）。規模が大きいためユーザー判断で**新 Phase 5（探索UX刷新・PHASE5_PLAN.md）**へ昇格し、既存 Phase 5（敵行動・味方生存）は **Phase 6** にリネーム（2026-06-28）。

## 6. 実機データ要求プロトコル（点3＝都度測定に対応）

> 受領した実機データ・リプレイ・分析は **`simulation/simNN/` に1試行=1サブフォルダで蓄積**する
> （命名規約・テンプレ・ワークフローは `simulation/README.md`。新試行は `cp -r simulation/TEMPLATE simulation/simNN`）。
> 既存試行: `simulation/sim01`（シム推奨 vs 実機勘 ＋ 追撃減衰上限較正 C5）。

ストックは無く都度測定のため、依頼は**最小で再現可能**にする。1依頼に含める情報:
- 形成（英霊＋神姫4・並び順）／装備・幻獣・契晶など初期条件／対象の敵（防御・HP＝測定文脈）。
- 比較する2手順（A=シム推奨、B=対抗）をターン毎キー列で明示。
- 返してほしい値: **A/Bの勝敗（どちらの総ダメージが上か）＋概算差分（任意）**。絶対値の高精度は不要。

## 7. スコープ境界と Phase 6 シナジー判断

- 木人（固定敵）相手の**ダメージレース押し順**には敵AI/生存は不要 → 現時点は**分離が妥当**。
- シナジーが立ち得るのは、**敵のチャージ大技や状態異常が最適タイミングを変える**較正に入った時
  （例: 「この敵行動を見越して溜める方が実機で上」というタイミング依存の乖離）。その兆候が出たら
  Phase 6 の敵DB化を前倒しで一括着手する判断をここで再評価する。
- **前倒し実施（2026-06-26・1体のみ）**: turn-by-turn 較正（§3.5）で T2 以降を観測するには、木人では
  早期撃破して観測不能・実機の生存敵は属性耐性で数値が歪むという制約が判明。**敵DB（Phase6）が較正に
  正のフィードバックを与えるケース**に該当するため、**較正ボス1体だけ**を前倒しで敵DB化した：
  `walpurgis_loki`（ヴァルプルギス・ロキ・幻属性・光有利・ムーンコードで被弾無効化しT7-8ソロ生存）。
  全敵DB構築・敵AIは引き続き Phase 6。intake/スキーマは `gamedata/md/敵/README.md`、現在値は `gamedata/js/enemies.js`。

## 8. 次アクション

- まず **§3-1（無測定の乖離検出）**: ユーザーが実機でよく使う形成のシム推奨押し順を確認し、疑わしいターンをフラグ。
  そこを起点に序数A/B測定（§6）→ 診断 → 修正 → フィクスチャ化、で駆動フローを一周し Phase 4 の回し方を確立する。

---

## 更新履歴

<!-- 直近5件のみ（それ以前は git log）。「波及確認」列が本体＝git が持たない情報はここだけ。 -->

| 日付 | 変更点 | 波及確認 |
|---|---|---|
| 2026-08-05 | 末尾ブロックを新設（DOC_RELATION_PLAN S4・種別=規定・台帳・計画） | 参照関係は `npm run doc:check` がグリーン |

<!-- doc_refs:begin ── 自動生成。手で編集しない（node tools/doc_refs.mjs --write が再生成する） -->
## この md を参照している文書（現役層 6）

- [CALIBRATION_ANALYSIS.md](./CALIBRATION_ANALYSIS.md)
- [CLAUDE.md](./CLAUDE.md)
- [ROADMAP.md](./ROADMAP.md)
- [gamedata/md/敵/README.md](./gamedata/md/敵/README.md)
- [simulation/README.md](./simulation/README.md)
- [workspace/HANDOFF.md](./workspace/HANDOFF.md)
<!-- doc_refs:end -->
