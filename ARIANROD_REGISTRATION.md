# アリアンロッド[健美端麗] 登録較正 — データ取得作業の規定（root一般フロー）

> **位置づけ**: 新規入手キャラの**小規模キャラ登録較正**。sim ではないため専用フォルダは作らず、**root で実施し完了後に archive/ へ移す一般フロー**（ユーザー決定・2026-07-16）。**sim04（較正セッション）と並行して実施可**（§4 の依存注意のみ守る）。
> **背景**: `arianrhod` は 2026-07-11 に wiki 由来スペックで DB 追加済みだが**実機未較正＝9件の解釈仮定に依存**（`archive/arianrhod_impl_notes.md`）。ユーザーが実機で入手したため照合が可能になった。
> **記録**: 小規模のため trial ファイルは作らず**本ドキュメントの §3 記録欄に直接記入**する（一次情報の転記もここ）。

## 1. ゴール

1. 9仮定（`archive/arianrhod_impl_notes.md`）を実機で **confirm / refute** し、`gamedata/characters.js`・`src/constants.js` へ反映。
2. アビ/アシスト/バーストの**実機文言と数値の一次転記**（現DB値はwiki由来＝出典置換）。
3. 完了後: 本ドキュメントに結果を記入 → impl_notes へ「実機確定」を追記 → 本ドキュメントを `archive/` へ移動 → CHARACTER_ANALYSIS.md §2（ヤマトvsアリアン採用論）の再評価要否をユーザーと確定。

## 2. データ取得メニュー（A0〜A9・実機作業）

> 共通: 敵は任意（キャスパリーグ推奨=倒しても再入場可）。**編成・装備・アリアンのLv/限凸/スキルLvをメタ情報として§3に記録**。ダメージ値を読む項目（A1/A8/追撃cap）は会心/急所マークと hit数 も記録。

| # | 目的（仮定との対応） | 観測手順 | 判定 |
|---|---|---|---|
| **A0** | 一次情報の転記 | アビ3種・アシスト2種・バースト効果の**ゲーム内説明文を原文ママ転記**（効果量・CD・効果ターン・スタック上限の表示値含む） | wiki由来値との差分を列挙 |
| **A1** | 追撃の重複（仮定1） | HP80%以上でアリアンがバースト→**追加ダメージ表記の本数**を数える（1本=同一追撃/2本=別枠2重） | 2本なら onBurst を2回加算へ修正 |
| **A2** | 1アビ即発動の上限（仮定2） | 登場〜5T内の同一ターンで「手動1アビ＋自バースト即発動＋ターン終了即発動」を重ね、**実際の発動回数**を数える（可能ならゲージ調整で複数バーストターンも） | 3回以上出れば `T.holy<2` 上限を撤廃/引き上げ |
| **A3** | 1アビ自動発動のアビ計数（仮定3） | テトラ同編成で自動発動の直後に**連理魔力の+表示**を確認（C19でヤマトtenya_re確定に使った方式）。ヘカテー同編成なら moon_acc の進み（12毎発動タイミングのズレ）でも判定可 | 計数されるなら `arianHolyFire` に `_countAbilityUse` 結線 |
| **A4** | 3アビ再発動（elegant_re）のアビ計数（仮定4） | A3と同方式（再発動直後の連理+表示） | 同上（tenya_re と同型化） |
| **A5** | 2アビの奇数/偶数回対象（仮定5） | 2アビを戦闘中2回押し、**バフアイコンの付与先**（自分のみ/味方全体）を各回確認 | 偶数回=自分のみが確定したらモデル化要否を判断（現行は常時全体近似） |
| **A6** | ゲージ付与対象（仮定6） | 1アビ後に**全員のゲージ+10**か／2アシ発動（味方光バースト3回毎）時に**自分+40**かをゲージ読みで確認 | 対象が違えば `addG` 対象修正 |
| **A7** | 1アビ bplus の乗り先（仮定7） | 1アビバフ有効中に**他キャラのバースト**値が+10万/stack 動くか（アリアン自身以外への適用確認） | 対象が「味方光」以外/自身のみなら burstPartyPassive 修正 |
| **A8** | 急所倍率1.1（仮定8） | 2アビ有効中の急所発動hitで**通常hitとの値比≈1.1**を確認（非会心hit同士で比較） | 倍率が別値なら `acute_arian` 更新 |
| **A9** | HP80%条件（仮定9） | HPを80%未満に落としてバースト→**追撃・バースト性能UPの有無** | 発動条件の実挙動を記録（モデル条件化は味方生存モデル実装時＝ROADMAP未確定Phase(i)） |

**追加（絶対値系・データだけ先行取得しfitはsim04後）**: 無バフ非会心のアリアンバースト本体値（a/b係数 a5/b2500 の検証用）・追撃/8hit光ダメの飽和域の値（cap 50万/8万の検証用）。**読むのはsim04完了後**（現エンジンは絶対値×1.43系統過小＋C31構造修正前のため、いま絶対値でfitすると較正後にやり直しになる）。

## 3. 記録欄（実測時に記入）

- 測定日: 2026-07-17（第1回・部分取得） ／ アリアン Lv/限凸/スキルLv: ____ ／ 編成: ____（ロボ＝エジソン入り編成で観測） ／ 装備config: ____（configBなら流用と明記）／ 敵: ____
- A0 文言転記（2026-07-17 実機・原文ママ）:
  - 1アビ ホーリーターボ+（1T/5T・味方全体/敵ランダム）: 光属性キャラのバーストダメージプラス(累積可)・バーストゲージUP／敵ランダムに8回光属性ダメージ ※同一ターンに2回まで使用可能
  - 2アビ ミティゲートペイン+（2T/3T・自分 or 全体）: （効果文言は未転記＝要追記。対象「自分 or 全体」＝A5奇偶と整合）
  - 3アビ エレガントルミナス（8T/-・敵単体）: 即座にゲージ消費なしでバーストを発動 ※バーストゲージを40消費して2回まで(1ターン目は3回まで)再発動可能
  - 1アシ トランジェントマイト: 自分のHPが80%以上の場合、追撃効果／登場から5ターンの間、自分のバースト性能大幅UP・ターン終了時にホーリーターボを即座に発動
  - 2アシ オーバーカムリターン: 光属性キャラが3回バースト攻撃を行う度に、自分のアビリティ再使用間隔1ターン短縮・バーストゲージUP・バーストダメージ上限UP(累積可)
  - バースト セイアッドショット: （文言は未転記＝要追記。A1で追加ダメージ枠を確認）
  - **wiki由来との差分**: 実装値と大枠一致（1アビ8hit/2回・3アビ40消費再発動T1=3/else=2・2アシ3バースト毎）。詳細な効果量（追加ダメ倍率/cap）は絶対値系でsim04後に確定。
- A1〜A9 結果（2026-07-17 第1回。A8 は非会心急所を分離できず未取得＝**将来のアリアン入り統計的較正で取得**。A0の2アビ/バースト文言は要追記）:

| # | 観測内容（生データ） | 判定（confirm/refute＋修正方針） |
|---|---|---|
| A1 | **クリーン編成（エジソン抜き）で再取得**。アリアン単独バースト1回のダメージ表記＝**バースト本体＋追加ダメージ＋アビリティダメージ＋アシスト追撃ダメージ**の4本。前回の「謎の追加ダメージ」は**エジソン(ロボ/英霊武器)由来と判明＝消失**。「追加ダメージ(バースト効果)」と「アシスト追撃(1アシ)」が別表記＝**別枠2重**、「アビリティダメージ」は自動1アビ(8hit光)。 | **仮定1 refute（確定）**。追撃は別枠2本。**反映済**: `onBurst` で追撃(3倍/50万・アビ枠)を**2回加算**（①バースト効果 追加ダメージ ②1アシ 追撃）＋自動1アビ。個別倍率/capの実機値はsim04後の絶対値fitで確定。 |
| A2 | 手動でアビとして発動できるのは同一ターン**2回まで**。自身のバースト効果／アシスト効果による発動には**制限なし**（上限に計上されない）。 | **仮定2 部分refute（確定）**。**反映済**: `arianHolyFire(sim,T,manual)` を導入し、手動のみ `T.holy<2` でゲート、自動発動(burst/assist)は上限なし＆`T.holy`非消費。 |
| A3 | テトラ入り編成で**自動1アビ発動直後に連理魔力が増加**＝自動発動もアビ使用扱い。 | **仮定3 refute（確定）**。**反映済**: `arianHolyFire` の自動経路で `sim._countAbilityUse('holy','y')` を発火（連理+・robot反応・proc）。手動経路は `use()` が計数するため二重計数回避。 |
| A4 | **3アビ再発動(elegant_re)直後も連理魔力が増加**＝赤アビ使用扱い。 | **仮定4 refute（確定）**。**反映済**: `elegant_re.exec` に `sim._countAbilityUse('elegant_re','r')` を追加（ヤマト `tenya_re`/C19 と同型）。 |
| A5 | 2アビの**偶数回は自分のみ**（付与先＝自身）。奇数回は全体（対象文言「自分 or 全体」と整合）。 | **仮定5 refute（確定）**。**反映済（ユーザー決定で局所化実装・2026-07-17）**: `miti.exec` が使用回数の奇偶で global枠(`arian_spec`/`arian_acute`)と self枠(`arian_spec_self`/`arian_acute_self`)へ振り分け、`_na` は self枠を `_naOwner===ownerOf('holy')` 時のみ加算（`ABIL.holy?.[0]` で非編成安全）。arian入り10T=284,325,153・golden不変。 |
| A6 | 修正なし（実装どおり）。 | **仮定6 confirm**（1アビ+10=味方全体・2アシ+40=自分のみ）。 |
| A7 | 修正なし（実装どおり）。 | **仮定7 confirm**（1アビ bplus は味方光の他キャラバーストに適用）。 |
| A8 | 会心かつ急所のhitしか取得できず＝**非会心急所を分離できない**ため通常hitとの≈1.1比を未取得。 | **未取得（保留）**。現行 `acute+0.10` を維持。**将来のアリアン入り編成の統計的較正で取得**（ユーザー方針・2026-07-17）。 |
| A9 | 味方HP<80%でバースト→**追撃なし**（追撃・バースト性能UPが消失）。 | **仮定9 confirm**（HP80%以上ゲートは実在）。モデル条件化は味方生存モデル実装時（ROADMAP未確定Phase(i)）まで保留＝シムは常時フルHP=常時発動のまま。 |

- 絶対値系（sim04後に読む・値だけ記録）: 無バフ非会心バースト本体=____ ／ 追撃=____ ／ 8hit光ダメ（1hit）=____（各・会心急所マークとHP%併記）

## 4. 注意点（sim04 並行時の依存関係）

- **スカラ非依存の観測を優先**: A1〜A9 は「回数・対象・タイミング・アイコン・文言」＝絶対値較正と独立に確定できる項目。**sim04 の完了を待たずに実施してよい**。
- **絶対値 fit だけは sim04 後**（§2 追加項の注記どおり）。データの先行取得は可（記録しておけば後から読める）。
- **エンジン反映の検証**: arianrhod は golden 編成外＝DB修正は golden 不変のはず（反映時に `npm run test:golden` で不変確認・変わったら混入バグ）。仮定3/4 の `_countAbilityUse` 結線はヘカテー/テトラの proc 連鎖に波及するため、反映後に**アリアン入り編成の10T無クラッシュ**（impl_notes の検証手順）も再確認。
- **sim04 の M走とは編成が別**: アリアン測定でメンバーを入れ替えた走を sim04 の trial に混ぜない（本ドキュメント §3 に記録・sim04/data には置かない）。
- 完了後の archive 移動時、CHARACTER_ANALYSIS.md の「実機未較正＝9件の仮定」注記（3箇所）を更新すること。

## 5. 完了基準

1. A0〜A9 の記録欄が全て埋まり、9仮定が confirm/refute 済み。
2. refute 分の `gamedata/characters.js`・`src/constants.js` 修正＋golden不変＋アリアン編成10T無クラッシュ確認。
3. `archive/arianrhod_impl_notes.md` に実機確定の追記・CHARACTER_ANALYSIS.md の暫定注記更新。
4. 本ドキュメントを `archive/` へ移動（root一般フロー完了）。
