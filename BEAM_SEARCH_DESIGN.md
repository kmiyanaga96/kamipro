# 設計レポート — ビーム探索の準最適性（C9）

> 対象: 最適押し順トラッカーの探索エンジン（`index.html` `class Sim` `_beamSearch` / `_objective`）。
> 起点: ユーザー指摘（2026-06-28）— シム推奨押し順で **①エジソン4(アンプリファ)が赤アビの後ろ ②テトラ3(ディウィヌス=防御DOWN)が赤アビの後ろ** になり、実機定石（バフ/デバフ→ダメージアビの順）とズレて火力損失。
> 結論先出し: **モデル（火力計算式）は正しい。原因はビーム探索（BW32）の枝刈り不足**。バックログ索引は `CALIBRATION_ANALYSIS.md` C9。
> 検証スクリプトは scratchpad（`exp4.js`/`exp5.js`/`exp6.js`/`exp7.js`/`exp8.js`）。本書は再現可能な数値台帳。

---

## 1. 総合比較（探索ポリシー別の10ターン総ダメージ）

基準編成 `edison + [yamato, hecate, tetra, elaine]`（ゴールデン値ハーネスと同一・generic gear）。

| ポリシー | 10T総和 | 対BW32 | 備考 |
|---|---:|---:|---|
| 純 static greedy | 116,530,883 | −38.2M | s順のみ。amplifaは#3に出る（定石OK）が、proc連鎖をせず総アビ数が少なく低火力 |
| **ビーム BW32（現行・ゴールデン値）** | **154,711,673** | — | proc連鎖で大幅増。ただし赤アビ前倒し |
| ビーム BW64 | 154,718,400 | +7,517 | **ほぼ無改善** |
| **ビーム BW128** | **162,398,625** | **+7,686,952 (+5.0%)** | 後述のとおり divinus が赤アビ前へ移動 |
| C2（手組み: 赤アビを後ろへ＋proc連鎖維持・T1のみ強制→以降greedy） | 155,145,815 | +434,142 | T1単体でも +630k（後述§2） |

**読み取り**: 「赤アビ後ろ」へ寄せると総和が上がる（C2 > BW32、BW128 ≫ BW32）。**モデルは赤アビ後ろを正しく高評価しており、BW32がそれを発見できていない**。

---

## 2. 敗北要因（BW32が取りこぼす価値の所在）

T1のみを強制し残りをgreedyで完遂した比較（funki発火数4回・renri到達30は全条件で同一＝交絡なし）。

| T1押し順 | T1ダメージ | 該当赤アビの素点 |
|---|---:|---|
| A（スクショ順＝赤アビ前倒し） | 7,151,607 | alone **351,957** / 早期judg 20,025 |
| C2（赤アビをバフ後ろへ） | 7,782,282（**+630,675**） | alone **960,738** / judg 146,626 |

- **alone（エレインのburstTrigger）が 352k→961k（+2.7倍）**。早期に撃つとバフスタック（assault/elem/会心/特殊/C8バーストプラス等）が薄く、`_na()` 基底が小さいまま確定する。バフ積み上げ後に撃てば同じ1回が約2.7倍。
- 早期judgのph0素点も 20k→146k と増えるが、絶対値は小。
- 防御DOWN(divinus)単体の寄与は**小**（§3で詳述）。敗北要因の主体は「バースト/ダメージアビをバフ薄の段階で消費」すること全般。点2/点3はその一症状。

---

## 3. 乖離分析（根本メカニズム）

### 3.1 モデルは正しい（計算式の欠落ではない）
- **防御DOWN**: `_na()`（`index.html:839-843`）で `consort_def/divinus_def/effond_def` を合算し上限50%で `res = (base+royFlat)*(1+defdown)`。`_na()` は通常/アビ/バーストの**全ダメージ基底**なので、divinus -30% は赤アビ(judg ph0)にもバーストにも乗る。
- **アンプリファ**: `edison.onAbility`（`:73`）で赤ロボ反応に `amplifa_flat=+10万`。赤アビ発動時にロボ作動中なら加算。
- → **点2/点3の「効いていない」は計算式の問題ではない**。divinus/amplifaが赤アビの「後ろ」に置かれ、対象赤アビが発動時点でバフ未適用になるという**順序**の問題。

### 3.2 divinus単体の影響が小さい理由（B2実測）
divinus(青・ya列に不寄与)だけを #13→#3 に前出し（funkiゲート不変）した B2 は **+7,795** に留まる。理由:
- 大きい赤アビ（judg#17/effond#18 ≈各96万）は元の順でも divinus 後に発動済み。
- alone のバーストは `_decay('burst')` の cap に張り付き、defdown を上げても頭打ち（早期=バフ薄だが cap 律速）。
- 効くのは「早期 judg ph0」のみで素点が小（20k→26k）。
- ∴ **点3は「divinusを動かす」より「赤アビ自体を後ろへ」が本質**（C2で alone が +608k）。

### 3.3 なぜBW32が取りこぼすか（ビーム探索病理）
- `_beamSearch` は各ノードを「全候補展開→クローンで `_finishStatic()`（static greedy完遂）+`_objective()`（残ターンrollout）採点→上位BW保持」。目的関数第一要素は10T総ダメージ（`_objective` `:1015`）。
- proc連鎖（コヴァレント=アビ12/バースト2毎にrenri+1&judg CD0、モビウス=バースト5毎にヘカテーCD0）を効かせて**より多くのjudg/バースト**を撃つ「深い」枝が高火力。だが**「procも連鎖させつつ全ダメージアビを後ろへ寄せる」枝は、前半に低sのバフを積むため中間採点で上位32に残りにくく、幅32で枝刈りされる**。
- 証拠の決め手＝**非単調の崖**: BW64では +7,517（ほぼ無改善）なのに **BW128で +7.69M（+5.0%）**。最適枝が幅128付近でしか生存しない典型的なビーム探索の取りこぼし。

### 3.4 ユーザースクショの完全再現と「深刻度のギア依存」（最重要）
- **実ギアで完全再現（2026-06-28・scratchpad exp12）**: ユーザー実機設定＝walpurgis_loki / affinity1.5 / 守護幻獣×2(weaponAmp0.80) / サブ=フレイヤ(streak1.1) / 追加補正(assault151・vigor34.6・burstダメUP225・各上限UP等は ×(1+0.80)) / per-char ATK=`DISPLAY_ATK_OVERRIDE`。これで runSim を再現すると **勝者prefix=`["banoshik"]`、T1押し順がスクショと完全一致**（`エジソン2→1→absolute→alone→funki→judg→amplifa(#7)…divinus(#13)`）。
  - **∴ スクショは古いコードではなく「現コード×強ギア」の正しい出力**。3.4旧版の「stale code最有力」は誤り＝**真因はgear-cap相互作用**（下記）。
- **gear-cap相互作用＝深刻度のギア依存**: 強ギアでは全アビがcap飽和し、`[banoshik,droid,amplifa]`(amplifa早出し)の優位＝amplifa+10万flatが**10億規模に対し誤差**化（勝者["banoshik"] 1,082,437,359 vs [banoshik,droid,amplifa] 1,082,122,202＝**−0.029%**）。よってprefixランキングが反転し、BW32は赤アビ前倒しの「Order A」に落ちる。
- **改善幅もギア依存**（空prefix単一ビームBW32→128）:
  - 既定(弱)ギア: +5.0%（§1）
  - **ユーザー実(強)ギア: 1,081,566,442 → 1,082,675,123 = +0.10%**（BW128でdivinus #13→#6＝赤アビ前へ＝点3解消）
- → **点2/点3は実在（C9探索限界）だが、強ギアでの火力損失は約0.10%**。`_na()` のdefdownもamplifa flatもcap飽和下では限界寄与が小。**問題の本質は「火力損失」より「推奨押し順の表示が実機定石とズレてツールの信頼を損なう」点**（弱ギア帯では火力差も顕在）。

---

## 4. 影響度検証

### 4.1 押し順（点3が直るか）— BW32 vs BW128 のT1
（テトラ3=divinus, テトラ1=judg, エジソン4=amplifa）

- **BW32**: …amplifa#5 → absolute#6 → **judg#7** → …(中略)… → **divinus#13**　←judgが防御DOWN前で発動
- **BW128**: …amplifa#5 → absolute#6 → **divinus#7** → **judg#8** → …　←**divinusが赤アビ前へ移動＝点3解消**、judgが防御DOWN込みに

### 4.2 ゴールデン値・回帰
- BW変更は**ゴールデン値を変える**: BW32 `154,711,673` → BW128 `162,398,625`。
- ゴールデン値ワンライナーは単一 `takeTurn`＝BWに直結するため、BW変更時は期待値更新が必須（CLAUDE.md §検証方法 / §2不変条件）。
- ①-A 2段ルート選抜（`PREFIX_TOPK`）はBWと独立だが、本選がBW依存のため最終品質はBWに連動。

### 4.3 性能
- 10ターン通し: **BW32=12.0s / BW128=46.0s（約3.8倍）**。BW256は2分でタイムアウト（未測）。
- 実アプリ(runSim)は1ターン分の最適化をルート分散ワーカーで並列化するため体感は per-turn 換算（BW32≈1.2s → BW128≈4.6s）＋プレフィックス本数。プランニング用途として許容余地はあるが要実測。
- 支配式・並列度は archive/PERF_NOTES.md §1 参照。

### 4.4 BW128が天井かは未確認
BW256未測のため、BW128の +5.0% が上限かは不明。さらに上振れの可能性あり＝「幅を上げ続ける」路線は終点が見えない。

---

## 5. 引継ぎ（修正候補と推奨）

| 案 | 内容 | 長所 | 短所 |
|---|---|---|---|
| ① BW増（32→128） | 1行変更 | +5.0%・点3解消・実装最小 | 約3.8倍遅い／ゴールデン値162.4Mへ／天井不明 |
| ② 賢い枝刈り（推奨候補） | ダメージ専用アビ（burstTrigger赤・judg ph0）の**展開を遅延**、またはビーム採点に「バフ未消費でダメージアビを撃つと減点」のヒューリスティック。バフ配置を優先しつつproc連鎖は維持 | 速度維持・根本対処（幅非依存） | 設計・検証コスト高／タイブレーク不変条件（`>`厳密・走査順）を壊さない配慮が必要 |
| ③ 現状維持 | 記録のみ | 変更リスクゼロ | 準最適（強ギア≈0.10%／弱ギア≈5%）と定石ズレ＝**表示信頼性**の問題が残る |

### 深刻度の再評価（§3.4の実ギア再現を受けて）
- **火力損失は強ギアで≈0.10%・弱ギアで≈5%**（ギア依存）。ユーザー実ビルドのような cap飽和帯では damage 上振れは小さい。
- ∴ C9の主たる価値は**「推奨押し順の表示を実機定石（バフ/デバフ先・ダメージアビ後）に一致させ、ツールの信頼性を担保すること」**。火力指数の改善は副次（弱ギア帯で意味を持つ）。

### 推奨
- **②（賢い枝刈り）を本命**とする。非単調の崖（§3.3）は「幅を上げる」対症療法の脆さを示すため、品質を幅から切り離す設計が望ましい。速度維持で全ギア帯の表示を定石化できる。
- 実装前ゲート: (a) ②のヒューリスティックが proc連鎖の手数（judg/バースト総数）を削らないことを scratchpad で確認（強・弱両ギアで）。(b) ②採用後もゴールデン値を**新基準として固定**（②でも値は変わる）。(c) 強ギア再現フィクスチャ（exp12の設定）を回帰に追加し、divinusが赤アビ前に来ることをアサート。
- ②が困難なら次善は①（BW128・値更新）。ただしBW256も測ってから天井を判断。
- **コスト/便益の注記**: 強ギア帯では火力+0.10%のため、純粋な火力目的なら ③現状維持も合理的。実装判断は「表示信頼性をどれだけ重視するか」で決める＝ユーザー判断事項。

### 未解決の問い（次の設計検討事項）
1. ②のヒューリスティックを「展開遅延」と「採点減点」のどちらで実装するか（前者は探索空間縮小で高速、後者は柔軟だが調整必要）。
2. proc連鎖の「種まき」に最低限必要な早期バースト数（C2は赤を全て後ろにしてもrenri30到達＝種まきは別経路で足りた。一般編成でも成立するか要確認）。
3. ゴールデン値の意味づけ更新（厳密最適でない旨は既記。②/①採用時の新スナップショット定義）。
