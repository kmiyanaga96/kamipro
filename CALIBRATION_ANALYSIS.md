# 神姫PROJECT R — 実機較正 確定仕様＆根拠アーカイブ

> [!NOTE]
> 本書は実機較正の**確定値**と、その**数学的・実機的根拠**を記録するアーカイブ。
> ここに記載の較正（D1/D2/D7/D8）および英霊専用武器（エジソン・ナポレオン）の配線は
> **すべて実装済み**。実効値の最新サマリは `CLAUDE.md`「実機較正ステータス」を、
> 実装の挙動は各ソース（`index.html` / `src/*.js` / `gamedata/*.js`）を正とする。
> 本書は「なぜその値・枠なのか」の根拠を後続セッション／Antigravityエージェントへ残すためのもの。

---

## 1. 調査ソース一覧

| ソース名 | URL / 備考 |
|:---|:---|
| まうらぼ（計算式まとめ） | `kamiprolab.blog.fc2.com/blog-entry-37.html` |
| まうらぼ（上限値・減衰率） | `kamiprolab.blog.fc2.com/blog-entry-220.html` |
| 神プロ計算(改) | `unitia.cloudfree.jp/kamipro` |
| 神プロ攻略まとめwiki | `xn--wiki-4i9hs14f.com` — ゲーム仕様・計算式ページ |
| wikiwiki.jp | 各キャラ個別ページ（ヤマトタケル、テトラ等） |
| ダメージ枠まとめ（ユーザー提供txt） | `gamedata/damage_frames.txt` — バフ/ウェポンスキルの枠別の乗り方一次情報（2026-07-16受領・原文ママ・C31〜C35の根拠） |

---

## 2. 確定較正値（実装済み・サマリ）

実効値の一次情報は `CLAUDE.md`「実機較正ステータス」。本表は根拠記録用の再掲。

| 項目 | 確定値 | フレーム/枠 |
|---|---|---|
| バースト係数 a/b | ヤマト/ヘカテー/テトラ = 5.0/2500、エジソン = 5.0/3000、エレイン = 5.5/3000 | — |
| `burst_inori`（現神の祈り中バースト性能） | +5.0（上限cap2倍化は**無し**） | バースト本体 |
| ヤマト追撃（現神の祈り中のみ） | 3.0倍 / 上限100万 | **アビ枠 `'abi'`**（slope 0.04） |
| ヘカテー追撃（ムーンコード中のみ） | 3.0倍 / 上限100万(実機較正・sim01 C5) | アビ枠 |
| テトラ追撃（HELIX前 / 後） | 3.0倍 100万(実機較正・sim01 C5) / 6.0倍 100万(推定) | アビ枠 |
| エレイン追撃 | 常時1回、契晶80以上で3回 | アビ枠 |
| エジソン英霊武器 追撃 | 2.5倍 / 上限80万 | アビ枠（onBurst実装済） |
| エジソン英霊武器 ロボ反応強化 | 倍率3.0→3.5 / 上限50万→65万 | — |
| ナポレオン英霊武器 ベタイア強化 | 倍率3.0→3.5 / 上限80万、バースト時自アビCD-1 | — |
| ヤマト1アシ バーストダメージプラス（C8） | +10万/stack・**味方全体のバースト対象**（自分のみは誤り） | 全バースト共通 減衰外フラット（`burstPartyPassive`） |
| ナイツサプレス（エレイン3・C11） | バーストダメ+20%・**非累積（refresh）・2T**（有効中は再発動しない＝隔ターン発動） | 全バーストの burst_dmg 枠 |

> 保留（placeholder・エンジン改善フェーズで再検証）: テトラHELIX後追撃の減衰上限（推定100万）。
> テトラ4アビはT6〜T7まで連理魔力を毎ターン蓄積しないと発動条件を満たせず、単独でのダメージ量検証が困難。

---

## 3. 根拠記録（なぜその値・枠なのか）

### 3.1 ヤマト追撃「11倍/バースト枠」→「3倍/アビ枠/上限100万」への補正根拠
旧実装は追撃を `burst_followup_mult = 11`（11倍）かつ `'burst'` フレーム（減衰率 slope 0.10）で計算していた。
この 11倍 は**非公式な逆算値**で、「追撃上限が100万へ拡張されること」「アビダメ枠（slope 0.04）であること」を
考慮せず、バースト減衰枠（上限50万・slope 0.10）で実機観測値130万から逆算したことが原因だった。

正しい仕様（アビ枠・上限100万・slope 0.04）で、実戦級編成（通常攻撃中央値 naB ≈ 280万）に3倍を適用すると:

```
raw = naB × 3.0 = 2,800,000 × 3.0 = 8,400,000
Decayed = cap + (raw - cap) × slope_abi
        = 1,000,000 + (8,400,000 - 1,000,000) × 0.04
        = 1,000,000 + 296,000 = 1,296,000 ≈ 130万
```

実機出力 **130万** が「3倍 / 上限100万 / アビ枠(slope 0.04)」で自然に再現され、非公式な 11倍 は不要となった。
→ 確定: `burst_followup_mult = 3.0`、フレーム `'abi'`、`burst_inori = 5.0`（上限cap2倍化は無し）。

### 3.2 追撃フレームの統一（D7）
バースト発動時の追加ダメージ（追撃）は**すべてアビリティダメージ**として計算される（有志確定）。
→ バースト上限UP（エクシード）は乗らず、アビ上限UP（エラボレイト）が乗る。減衰率もバースト(0.10)ではなくアビ枠(0.04)。
→ ヤマト/ヘカテー/テトラ/エジソン英霊武器の全追撃を `_decay('abi', ...)` に統一（実装済）。

### 3.3 テトラ追撃の欠落バグ（D2）
旧実装は「共通追撃との差分のみ加算」としていたが `burst()` に共通追撃処理が無く、HELIX前は追撃が完全に0、
HELIX後も基礎分（3倍/50万）が欠落していた。→ 差分計算を廃止し、状態に応じた追撃を直接 `_decay('abi', _na()*mult, cap)`
で加算（HELIX前 3倍/50万・後 6倍/100万）。実装は `data/characters.js` テトラ `def.onBurst` を正とする。

### 3.4 バーストストリークの純化（D8）
ストリーク基礎値は**純粋なバースト本体ダメージ（減衰適用後コア）のみ**を指す。旧実装は `this.dmg` 差分で
追撃やフラットアビダメ（ヤマトバーストプラス・ARRIVE等）を巻き込み過大評価していた。→ `burst()` がバースト本体コア
（`core`）を return し、`_attackPhase` が `burstCoreTotal` を集計してストリーク基礎値とする（実装済）。

### 3.5 英霊専用武器の仕様根拠
- **エジソン「自走光砲ランチャータンク」**: メイン装備時、①ドロイド反応の倍率3.0→3.5・上限50万→65万、
  ②バースト追撃 2.5倍/80万（アビ枠）、③味方全体属性攻撃UP+最終ダメUP5%。
- **ナポレオン「光皇刃レス・ボナパルト」**: メイン装備時、①ベタイア（ターン終了時闘気消費ダメ）3.0→3.5・上限80万、
  ②バースト発動時に自身の全アビCD-1、③革命皇の覇気＝味方全体光属性攻撃UP（属性枠+30%相当）。

> 配線（WEAPON_MASTER定義・applyGear・Workerシリアライズ・onBurstフック）は実装済み。
> コードを正とし、本書では仕様の意図のみ残す（旧版にあった実装コード断片・行番号参照は削除済み）。

---

## 4. 未解決乖離バックログ（Phase 4・押し順優先）

> Phase 4（PHASE4_PLAN.md）の作業対象を失わないための追跡表。状態: `open` / `investigating` / `fixed`。
> 押し順優先方針のため、各乖離は「**実機の最適押し順を反転させるか**」を主軸に評価する（総ダメージ絶対値の一致は二次）。

| ID | 状態 | 乖離内容 | 押し順への影響 | 備考 |
|---|---|---|---|---|
| C1 | open（**turn-by-turn ホライズン到達待ち ≈sim06-07**） | テトラHELIX後追撃の減衰上限（推定100万・未確定） | 系統誤差候補（cap誤りは「溜める vs 撃つ」を反転させうる） | テトラ4アビは連理魔力をT6〜T7まで蓄積しないと発動せず単独検証困難。**B案（厳密turn-by-turn）採用により、ホライズンがHELIXターンに達した時点で「前段固定済み＝残差がHELIX追撃に局在」する形で確定する**。<br>**確定用の逆算メソッド（会心相殺・将来使用）**: 両方会心(or両方通常)で naB と追撃を採れば会心倍率が相殺され `ベース上限 = (実ダメ − naB×0.24) / (0.96×(1+総アビ上限UP))`（raw=naB×6.0・slope0.04・crit-before-cap でシム計算の逆関数・raw≫capのため約9割cap項で頑健）。出典: 旧 sim02 C1設計（Antigravity証明＋Claude Code補足）。**較正ボス ロキの2フェーズ被ダメ倍率（C7）を逆算前に必ず除算**（測定ターンのボスHP%で×0.7 or ×1.2）。 |
| C2 | open(低優先) | ターン内総ダメージの絶対乖離 | 蓄積誤差由来＝順序にはほぼ非影響と推定 | 各ダメージ算出の許容誤差の積み重ね。直接修正困難。序数で問題化しない限り追わず、系統誤差修正の副産物として縮小を期待。 |
| C3 | investigating（**第1回実測で焦点がmult→capへ移動・2026-07-11**） | エジソン英霊武器バースト追撃の倍率/上限 | 系統誤差候補（追撃はエジソンバーストの相対価値に影響） | **第1回実測（`simulation/c3_edison_followup_intake.md`・中立敵・会心・T1バフ未除外）**: 本体304→283万（−7%）変動でも**追撃220→222万（±0.9%）とほぼ不変＝追撃はcap飽和**。∴ 較正対象は mult(2.0/2.5)ではなく**cap本体**。cap実機≈220万(会心)/≈147万(非会心換算)。シム実効cap=`800000×(1+GEAR.abi_cap)`＝**アビ上限UPギアでスケール**するため、素の80万は過小に見えるがギア込みで整合しうる（sim02ギア abi_cap0.954なら≈156万≈実機147万）。**確定には非会心の飽和追撃＋その戦闘のabi上限UP%（or gear既知のsim02設定）が必要＝第2回実測待ち**。naBペア欠落（#1/#2バースト samplesと#3/#4通常攻撃 samplesが別バフ状態）で本サンプル単独では確定不可。**★sim03数値確定（2026-07-16・C5と合流）**: キャスパリーグ（耐性=1保証）で ロボ追撃 実機1.58M→2.26M漸増 vs シム≈0.98〜1.01M飽和＝**×1.6〜2.2**・追加ダメージ ×2〜3.3＝**追撃系cap群の一律過小はボス非依存と確定**。実機側の漸増＝実機は非飽和（capがより上）→**cap単独fitは早計・C25 rawモデルとセットで解く**。根拠 `simulation/sim03/analysis/quantitative_analysis.md` §4。 **★sim04 configB実測（2026-07-21・既存データで確認＝sim05不要）**: エンジン期待 vs 実機mean＝ロボ追撃629k/1.46M(×2.32)・edison英霊武器404k/2.79M(×6.90)・hecate追加382k/2.08M(×5.46)・tetra追加424k/2.07M(×4.89)・elaine追加284k/1.53M(×5.38)。**乖離が成分ごとに×2.3〜6.9とバラつき、baseline(×1.835)+abiダメUP加算(mult+2.52)でも説明しきれない**＝raw合成/mult/capが同時に未決＝**現データでは clean な×1.0 fit は非決定的（fudge per-component になる）**。かつ**追加/追撃はabi枠だがcalib_na非適用・abi_dmg非加算のまま**（C31スコープの積み残し）。**判断: 追撃系はpush-order中立（cap近傍で準一定）＝製品主目的への影響小。ナポレオン/アリアン新編成はアリアン追撃が主火力になり得るため、追撃の式是正はその編成のアンカーで解くのが適切（現編成fudgeは非転用）＝ここでは残差として保留**。 |
| C4 | fixed | コード内コメントと実装の整合（`index.html:442` の「streak上限750万＝未実装/無減衰」） | なし（コード衛生） | 実装を正としてコメントを修正完了（2026-06-25）。 |
| C5 | fixed | バースト追撃のベース減衰上限値の乖離（ヤマト/ヘカテー/テトラ: 50万→100万、エレイン: 30万→80万） | 系統誤差（上限誤りはアビ/バースト発動タイミングの優先順を反転させうる） | 実機データ `simulation/sim01/raw_data.txt` および上限UPギアを含めた数学的検証により導出。設計レポート=`simulation/sim01/design_report.md`、統合分析（ゴールデン値不変を実測確認）=`simulation/sim01/integrated_analysis.md`。`DMG` 4値を較正値へ修正完了（2026-06-26・ゴールデン92,031,195不変）。**ユーザー承認済み（2026-06-26）**。 |
| C6 | wontfix（却下） | 探索目的関数を「2T総和最大化」へ変更する案（sim01 で観測: 実機勘ルートはシム抽象2T総和が上） | — | **却下**: シムの大域目標は辞書式（ターン優先）フロントロード（PHASE4_PLAN §2）であり、2T総和重視はこの大域目標に合致しない。固定HP木人のダメージレースでは前倒し＝手数効率・早期撃破が実戦的に正。再起票しないこと。観測の経緯は `simulation/sim01/integrated_analysis.md` §2.1（凍結）。 |
| C7 | **撤回・改訂済（2026-07-12・与ダメ側フェーズ倍率は非実在＝試行2で確定）** | 較正ボス ヴァルプルギス・ロキの**HP依存2フェーズ「与ダメ」倍率**（旧: HP>50%=×0.7 / HP50%後=×1.2）[web] | — | **★試行2実測（B3）が×0.7/×1.2を非支持→撤回**: sim/実機比は de-cut なしで T1〜T5 ほぼフラット（62〜70%）・×0.7/×1.2適用だと50%境界で43%→79%の不連続・実機攻撃フェイズはリリース跨ぎで連続（×1.71級ジャンプ非観測）・表示ダメ累計≒総HP9.8億で整合（隠れ倍率なし）。∴ **与ダメ側フェーズ倍率はモデル化も手補正もしない**（未モデル化のままが実機一致）。B1のde-cut分析（試行1）はこの前提で要再解釈＝F1「蓄積悪化」の大部分はde-cutアーティファクト。**[web]出典は敵→味方被弾側 or 別数値の取り違え疑い＝出典再確認のみ保留**。ボスHP%記録は敵行動/生存モデル(未確定Phase i)・C25位相目印として継続。`enemies/walpurgis_loki.md` §2 改訂済。根拠 `simulation/sim02/b3_trial2_replay_analysis.md` §3。 |
| C8 | fixed | **ヤマト1アシ（集いし願い）のバーストダメージプラス +10万/stack の付与対象が誤り**（旧実装＝ヤマト自バーストのみ／実機＝**味方全体のバースト**）。`data/characters.js` の `onBurst` 内 self消費を、全バースト共通の `burstPartyPassive`（減衰外フラット・`index.html` burst()）へ移管。 | **大（系統誤差）**: 全バースター（最大5人）に毎回 +10万×stack が乗るため絶対火力が大幅増。ヤマト黄アビ（funki等）の相対価値が跳ね上がり、**最適押し順が「funki前出し＋エジソン2⇒1⇒4ロボ＋アンプリファ前倒し」へ自発シフト**（最適化器が定石に追従）。 | 実機指摘（ユーザー・2026-06-28）。ゴールデン値 **92,031,195→154,711,673**（単一 takeTurn・+68%）へ更新。**ユーザー承認済み（2026-06-28・「今すぐ実装＋値更新」）**。⚠ +10万の数値・stack上限の実機正確性は sim02 実機ログで継続照合（funki回数とヤマト以外のバースト素点の相関）。関連: #1アンプリファ後回しは本バグの下流症状で、C8修正により自然解消（formula据置）。 |
| C9 | **fixed（BW32→128採用・2026-06-28）** | **ビーム探索の準最適性＝「バフ/デバフ先・ダメージアビ後」の最適枝を BW32 が枝刈りで取りこぼす**。実機定石（divinus防御DOWN→赤アビ、amplifa→赤ロボ反応）とズレた押し順を出す（divinusが赤アビ後ろに置かれ防御DOWN非適用の赤アビが発生）。 | **中（最適順序不整合）＝主に表示信頼性**。モデル（`_na()` の defdown `×(1+defdown)`・amplifa +10万）は正しく**探索側の限界**。火力損失は**ギア依存（強ギア≈0.10%／弱ギア≈5%）**。 | 設計レポート=`archive/BEAM_SEARCH_DESIGN.md`（5節構成）。**ユーザー実ギア(walpurgis/守護×2/フレイヤ/burstUP225等)で runSim スクショ順を完全再現(exp12)＝古コードでなくgear-cap相互作用が真因**。実測: 弱ギアBW32 154.7M→BW128 162.4M(+5.0%・非単調の崖) / 強ギアBW32 1,081.6M→BW128 1,082.7M(+0.10%・divinus#13→#6)。**修正: `BEAM_W` 32→128（index.html:297）。ゴールデン値 154,711,673→162,398,625。代償=探索量約4倍**。ユーザー判断「最適押し順を優先（装備充実で増分拡大しうる）・0.1%でも追跡」（2026-06-28）。残: 演算量爆発の緩和として待機画面刷新（C10→**Phase 5へ昇格**）。 |
| C10 | **→ Phase 5 として本格策定（PHASE5_PLAN.md）** | **演算量爆発の緩和＝探索中の待機画面の本格刷新**（C9でBW128化し探索約4倍重・装備充実で更に増大しうる）。現状は `#sim-loading`＋`#spin-progress`「ルートX/Y 計算中(N並列)…」のみ。 | なし（UX・体感速度／探索品質は不変） | 規模が大きいためユーザー判断で**新 Phase 5**として分離（既存Phase5=敵行動→Phase 6にリネーム・2026-06-28）。計画・設計は `archive/PHASE5_PLAN.md`（進捗フック配管→可視化→中断→演出の4段／Workerコード抽出不変条件に注意）。 |
| C11 | **fixed（2026-06-28）** | **ナイツサプレス（エレイン3）の「敵バースト攻撃耐性-20%」が非累積なのに、シムは length累積（+20%/stack）扱いで毎ターン連発していた**。 | **中（無駄リキャスト）**: 毎ターン再発動が最適に見え、他アビ枠を圧迫・表示が冗長。 | 実機確認（ユーザー Q2:a・非累積/refresh・2026-06-28）。修正: `knights` guard に `!(sim.buf.nights?.length)` を追加し**有効中は再発動しない**＝2T毎(隔ターン)発動で連続+20%。`data/characters.js`／`index.html` dur_nights・burst_dmg_nights コメント更新。ゴールデン値 **162,398,625→150,445,920**（knights発動 ~10→5回/10T）。 |
| C12 | **fixed（案C＋④b実装・2026-06-29／②exact-tie残）** | sim2押し順の準最適（ユーザー指摘2026-06-28・強ギア再現済）: ①amplifaが赤アビ後（強ギアで+10万固定が誤差化＝Q1:b・表示信頼性） ②effondはデバフ持ちで赤アビより先行すべき ③T2 judgリキャスト空転 ④a ヘカテー順（mobiusがsleur使い切り前） ④b tenya×3が atomic 実装でinterleave不可 | **中（最適順序不整合）**。火力モデルは正（effond defdown登録済・amplifa固定で正・③はjudgCapゲーム仕様で空転はrenri取得のみ＝ダメージ非損）。探索の順序付け・粒度の問題。 | 設計=`archive/ORDER_OPTIMIZATION_DESIGN.md`。**案B（僅差許容タイブレーク）はPoCで不採用**（generic golden −0.45%＝ダメージ犠牲）。**採用＝案C: ビーム多様性枠 `BEAM_DIVERSITY_K`(=24) で定石性(`T.orthodoxy`)上位枝を枝刈りから保持し、最終選択は純ダメージ最大（`_objective`/`cmpVec`不変）＝ダメージ単調**（golden 150,445,920→155,756,325・+3.5%／強ギア +0.80%）。**④b＝tenyaをatomic→「初回tenya＋再発動 `tenya_re`」へ分割**し3発間のinterleaveを探索可能化（旧atomicは到達不能だった最適枝を取りこぼし＝最大ギャップ）。golden 155,756,325→**175,023,298（+12.4%generic／強ギア+0.17%）**・determinism確認・10T 57.9s(+19%)。③＝judgCapはゲーム仕様でシム正（ダメージ非損・sim2をブロックしない）。**②exact-tie定石タイブレーク実装(2026-06-29)**: `_objective`末尾にorthodoxyを追加（cmpVec要素長非依存＝厳密同ダメージ時のみ作動・golden175,023,298不変/決定的を実測）。報酬対象=バフ/デバフ有効中のburst/judg ph0/effond＝effond/divinus先行の表示を定石化。**残(任意・新セッション)**: ①amplifa表示はdamage非依存＝orthodoxyにamplifa先行加点を足せば表示定石化（`edison.onAbility`数行・golden安全）。無駄リキャスト減点はnights/tenyaがC11/④bで構造解消済のため現対象なし（将来フック）。 |
| C13 | **fixed（2026-07-02・commit済）** | **リプレイモードの「⚠Nスキップ」多発**（sim2 シム推奨順を replay すると10T計43スキップ）。原因＝`tenya_re`（天矢乱舞・再発動）が `sim.use` 非経由で初回tenyaのラベル「ヤマト2(再-40)」を表示するため、`parseReplayToken` の末尾()剥がしで別キー `tenya` へ化ける往復ロス。tenya_re がスキップされると走行カウンタ（T.ability/T.burst/yellow_acc/mburst）がズレ、judg/puvoir/sleur/effond/funki のターン内リキャストが連鎖スキップしていた。 | なし（**表示のみ**・シム推奨順は元から合法）。ただし較正replayの見た目を汚す。 | 修正: `tenya_re` に宣言 `refireOf:'tenya'`/`refireSuffix:'(再-40)'` を追加し、`buildReplayNameMap`(src/app.js) が完全一致「ヤマト2(再-40)」→tenya_re を登録（full-token照合が末尾剥がしより先）。**表示ラベル不変**・golden 175,023,298 不変。検証: 43スキップ→0・往復dmg=探索dmg一致（`tools/search_validate.mjs`）。 |
| C14 | **fixed（2026-07-02・①funki毎ターン化を恒久実装＝C15較正とセット確定）** | **① funki(大和の奮起)解禁機構のモデルバグ**。旧実装は funki_cycle（解禁回数）がバトル永続で funki_recharge でしか0に戻らず、T2/T5 が cd=4・cycle上限で開始し funki 使用不可（ブラックアウト）。**実機は毎ターン最大3回まで解禁（カウンタは毎ターンリセット）・yellow_acc は跨ターン持ち越し・4回目契機は翌ターン頭で復活（実機バグ挙動）**（ユーザー実機テスト確認・2026-07-02）。 | **中〜大**: funki の使用機会・inori リキャスト・全体の最適 order に影響。**ただし funki は「アビ12回」proc にカウントされる（実機確認）ため、funki連打は proc整列（judg/ムーンコード）を崩し実は損な場合がある**（成分分解: burst +259K vs judg −551K 他＝net −770K）。 | 修正コードは `archive/SEARCH_ROLLOUT_DESIGN.md §1.1-A・§2`。修正自体はダメージ計算不変（旧orderを新モデルで再生し175,023,298に完全一致）。**単独適用で golden 175,023,298→174,253,492（−0.44%）と退行**＝現行の探索静的スコアが旧モデルに手調整されているため（C15）。∴ **C15（探索自己適応化）とセットで確定・golden更新・ユーザー承認**。 |
| C15 | **closed（fixed・2026-07-02／案(c)自動較正 production実装＝§6・Increment1+2＋粗→細2段＋3変数較正＋step調整/gear汎化まで完了・ブラウザ実機確認済。joint最適 {judg:145,pactcore:1,effond:100}=206,180,726(raw比+18.3%)§6.7/§6.9/§6.10。残は実ギア実測裏取りのみ・4変数以上却下。次セッション=演算高速化）** | **探索が真の最適を取りこぼす＝rollout（将来ターンの静的greedy価値推定）の質問題**。C9(幅の崖)・C12(定石順)とは別軸。①修正下で真の最適 ≥186〜191M が実在するのに現行探索は174.25Mしか出せない。 | **大（最適押し順・総火力）**: 〜9%の取りこぼし（generic gear・①修正下）。 | 設計・全検証データ・再現手順=**`archive/SEARCH_ROLLOUT_DESIGN.md`（C13-C15の一次資料）**。要点: **(1) 幅は無効**（浅ビームrollout・BW384とも改善ゼロ＝採点が同じ静的greedy）。**(2) 効くのは rolloutポリシー(静的s)**（FUNKI_S=30→+6.7%/JUDG_S=130→+9.6%・いずれ検証済だが乱高下＝overfit）。**(3) 決定的＝静的sはモデル固有に手調整**（JUDG_S=130 は①修正モデルで+9.6%だが blackoutモデルで−6.5%＝同一変更が正反対）。∴ 根本原因=**greedyロールアウトが手調整の静的スコアに依存しモデル/ギア変更に脆い**。頑健解=**静的s非依存の lookaheadロールアウト（採点を実シミュレーションturn-end dmgへ）**。計測器=`tools/search_probe.mjs`/`search_validate.mjs`。**STEP2最小PoC実施（2026-07-02・§5.3）: turn-end 実ダメージ採点は静的greedyより約−5%退行・約40×コストで不合格**（原因＝ロールアウト継続ポリシーが静的greedyのままで1手付替えが定石を壊す）。次候補=(a)εタイブレーク限定lookahead/(b)実ダメージ採点の浅ビーム/(c)静的s自動較正。**STEP2案(c)実施（2026-07-02・§5.4）: full-search較正が合格＝①修正でJUDG_S=130→191.14M(+9.6%)・blackoutはdyn維持で175.02M非退行（現行dynをgridに含め最大を採る単調安全）。安価static-greedy proxyはblackoutをミスランクし不採用。コスト=configロード時に|grid|×full-searchの一度きり（推論時0）。harness=`tools/search_autocal.mjs`**。次段=(c)のproduction設計（多パラメータ較正・golden更新・ユーザー承認）。**Increment1実装済（2026-07-02・§6）: `src/app.js` に s-override 機構（`S_OVERRIDE`/`setStaticOverride`/`getStaticOverride`）＋`calibrateStaticScores`（proxy-shortlist+full-verify・単調安全）を追加（空既定=golden不変・inert）。harness=`tools/search_calibrate.mjs`。検証: 現行モデル→override={}→175.02M(退行なし)／①修正モデル→override={judg:130}→191.14M(+9.69%)。** **Increment2実装済（2026-07-02・§6.4/6.5・ユーザー承認済）: ①funki修正を`data/characters.js`へ恒久化（C14 fixed）。runSimを較正phase→本探索phaseの2段化＋worker `calibrate` task＋fallback＋configキャッシュ（`_calibCache`/`CALIB_GRID`）を配線。golden更新: raw 174,253,492 / calibrated（{judg:130}）191,141,005（`test/golden.mjs`は`setStaticOverride({judg:130})`で決定的検証）。CLAUDE.md/AGENTS.md検証ゲート更新。Node e2e（`search_calibrate_e2e.mjs`）でruntime配線論理を検証（root分散191,460,633・火力指数126.2）。⚠ブラウザ実機（npm run preview）は本環境vite不在のため未実施＝残作業。** **多パラメータ較正（2026-07-02・§6.6）: `calibrateStaticScores` を grid直積の多パラメータへ一般化（`_calibCombos`）。funki を候補追加し検証したが judg=130 最適点で override は全値悪化（自然値s=150が最適）＝棄却。judg は 120〜140 プラトー（同値191,141,005）。∴ funki 除外。ブラウザ実機はユーザー確認済（較正中表示/再探索高速/中断OK・実ギア指数A 124.0）。** **第2レバー pactcore 発見（2026-07-02・§6.7）: 全アビ s を proxy足切り→full-verify した結果 pactcore のみ base 超え。judg×pactcore に強い相互作用（pactcore を下げると judg 最適が 130→143〜150 へシフト）＝joint 最適 `{judg:145,pactcore:1}`=201,260,545（raw比+15.50%・judg単独191.14Mを+5.29%）。judg単独/pactcore単独では到達不能＝多パラメータ機構の価値を実証。`CALIB_GRID={judg:[null,100,130,145,160,200],pactcore:[null,1]}`＝較正機構が joint 最適を proxy shortlist で自力捕捉。golden 更新 raw 174,253,492 / calibrated 201,260,545（`{judg:145,pactcore:1}`）。他レバー(tenya/effond/sleur/puvoir 等)は full で base 超えなし。** **粗→細2段＋第3レバー effond（2026-07-02・§6.8/§6.9）: (1) 粗→細2段較正 `_fineGridAround`（proxy段でzoom-in・runSim配線不変）。(2) 新base {judg:145,pactcore:1} で lever再探索→effond 最有力（tenya/sleur/absolute/puvoir も base超えだが3変数上限で effond 採用）。3変数 joint `{judg:145,pactcore:1,effond:100}`=206,180,726（raw比+18.3%・2変数を+2.4%・effondピークs≈100）。(3) shortlist枯渇バグ修正＝粗枠(coarseK=4)/細枠(fineK=3)を別確保し多変数で真の最適coarse領域を保証。`CALIB_GRID=judg×pactcore×effond`。golden 更新 raw 174,253,492 / calibrated 206,180,726。4変数以上はユーザー却下。** **step調整・gear汎化（2026-07-02・§6.10）: shortlist 8→6（coarseK=3/fineK=2・generic品質不変）。`search_gear_probe.mjs` で3ギア較正＝per-gear適応（burstGearは{judg:122,pactcore:1,effond:93}のfine点採用）・全ギア+17〜18%退行なし・grid境界張り付きなし。∴ gear特徴grid自動生成は不要（粗→細＋config別再fitで汎化達成）。** |
| C16 | **fixed（2026-07-03・演算高速化）** | 探索の実行時間（本番〜5分）とコールド再実行コスト | なし（品質は微増） | `BEAM_W` 128→64（較正rollout改善後は幅が品質を買わず逆効果＝両phase約1.4倍）・`PREFIX_TOPK` 10→8（loss0%実測）・結果キャッシュ（ヒント検証＝保存キー列を現行エンジンでリプレイ照合・`ENGINE_VERSION`名前空間）＋越境JSON入出力・火力指数の分母を素直押しへ戻す（C15の分母揃えを逆転＝generic 146.1）。golden **raw 174,697,325 / calibrated（BW64再fit `{judg:122,pactcore:1,effond:93}`）206,846,142**。詳細は git ログ（C16コミット群）と CLAUDE.md。 |
| C18 | **fixed（2026-07-07・ムーンコード実機較正）** | **ムーンコード（ヘカテー2アシ）の発動モデル乖離が真因**。旧実装＝「パーティ全体のアビ12回・ターン内カウントで `mooncode=2` refresh」（毎ターン12押し以上で**実質常時ON**）。実機仕様（ユーザー確認 2026-07-07）＝「**戦闘開始時またはヘカテー自身がアビ12回使用する毎**（戦闘通算）に発動・**持続2T**（開始時も同じ）・12回目の押下で**即時**発動＝発動押下自身のeffondバースト判定にも効く」。この乖離で effond のムーンコードバースト有無がズレ→ターン内バースト数がズレ→**burst-2 proc（コヴァレント・アルカナ）の位置がズレ→judg の arm タイミングがズレ**→シム推奨順が実機で再現不能（実機報告: 試行2 T4 で12アビ目テトラ1(judg)不可→13アビ目に繰下がり・以降総崩れ）。 | **大（最適順序不整合＝実機で再現不能＋火力モデル系統誤差）**。effondバースト（本体+ストリーク+proc連鎖）が周期OFFターンで消えるため火力・押し順の両方に影響。**⚠経緯**: 当初「ジャッジ即発動のターン内未強制」と誤診断（2026-07-05起票）→ ユーザー指摘（JDは強制発火でなく**armされたら押下可・押す位置は裁量**＝エンジンの候補扱いは正しい）で撤回→ arm タイミングの乖離として再診断し真因到達。**判帳簿は正**（judg発火回数・renri・proc上限5・攻撃フェイズproc→次ターンcd.judg=0キャリーオーバー、いずれも実機一致）。 | **決定的証拠＝sim02試行1 raw_data に内在**: ヘカテー2の全14押下中、T1/T2/T3/T5/T7は全てバースト付き・**T4(2回とも)/T6はバースト無し**＝実機のON/OFF交互パターン。旧モデル（常時ON）は再現不能。**修正**（`data/characters.js` hecate）: ① `moon_acc`（自身アビ戦闘通算）を state 追加・`onAbility` で自キーownerのみ++・12毎に `mooncode=2`（use()フックがeffondのバースト判定より先に走る＝即時発動が発動押下自身に効く）。② 戦闘開始発動は `onBattleStart`（tick後）で `mooncode=2`（constructor初期値2だとT1頭tickで食われT2がOFFになり、試行2 T2 #12/#13 judg成立という実機ハード制約に矛盾）。**受入検証**: 旧推奨順replayで T1〜T3全押下成立（実機どおり）＋ **T4 #5 effondバースト無し→judg#12 SKIP→12アビ目(funki)のprocでarm→次judgが実行13アビ目**＝実機トラブル完全再現。**golden再fit**: raw 174,697,325→**173,574,719**／自動較正 override 再fit=同値 `{judg:122,pactcore:1,effond:93}` で calibrated 206,846,142→**202,230,823**（baseline比+16.51%・単調安全OK・FB10/10・決定性2回確認）。ENGINE_VERSION `C16-bw64-ptk8-r2`→`C18-mooncode-r2`（旧キャッシュ無効化）。**r2（2026-07-07・ユーザー仕様追補）**: 判定は**アビ終了後**＝12回目の押下自身のeffondバーストには効かない（effond execが`mcAtPress`で押下時点の状態を捕捉・後続アビからON）。golden再fit: raw **170,955,419** / 較正override **{judg:145,pactcore:1}**（effondレバーはタイミング修正で不要化し脱落）= calibrated **194,778,530**（+13.94%・単調安全OK・受入テストT4再現維持）。**C19/C20（2026-07-09）で再fit**: raw **195,518,168** / override **{judg:160,pactcore:1,effond:100}** = calibrated **212,010,783**（tenya_re のロボ反応/proc計数増で effond レバーが再浮上）。ENGINE_VERSION `C19C20-tenyare-ifishant`。 |
| C19 | **fixed（2026-07-09・実装済）** | **tenya_re（天矢乱舞・再発動）がシムでは「非アビ扱い」**（`sim.use()`非経由＝アビ計数・onAbility フックなし＝旧atomic実装踏襲の**未検証モデリング**）。実機では**再発動も赤アビ使用扱い**: (a) **ロボ反応が発火**（試行1 raw T2/T5 の全ヤマト2行にロボ追撃が付く＝再発動2発分も3/3反応）、(b) **アビ12procにカウント**（試行2 T2#12 judg 押下で「JDリキャストが重複して走る」をユーザー実機観測＝シム計数11回目/実機12回目→バースト2procとアビ12procの**二重発火**・片方は cd=0 に落ち**空転＝機会損失**）。 | **大** — ① ロボ追撃ダメージ欠落（tenya再発動×2/ターン・droid稼働時 各~65万＋droid_buf/banoshikスタック欠落） ② アビ12proc点が tenyaターン（新cache T2/T3/T9）で前方シフト＝judg arm・連理蓄積・proc空転位置がズレ→**押し順の実機再現性/最適性の双方に影響**。 | 検証: 計装リプレイ（scratchpad probe_t2r2）で新T2順の帳簿確定＝sim上は #12 judg 自身の ph1 バーストによる burst-2 proc で即re-arm（有効利用）・**#14 pactcore のアビ12proc が空転**（cd既に0）。実機（tenya_re計数）では proc が #12 に前倒し＝ユーザーの「重複」観測と一致。**探索改善余地**: #14↔#15 入替で空転を arm に変換→5本目 judg 獲得の可能性（C19修正後に再評価）。**裏取り完了（2026-07-09・ユーザー実機確認）: T2#12 で連理魔力+2＝二重発火の直接証拠→実機仕様確定**。**実装（2026-07-09）**: `Sim._countAbilityUse(name,color)`（use()の帳簿部分＝_naOwner→T.ability++→CHARS走査 onAbility を発火）を新設し、`tenya_re.exec` が burst 前に `sim._countAbilityUse('tenya_re','r')` を呼ぶ。use()非経由なのは表示ラベルを初回tenyaと同一「ヤマト2(再-40)」に保つため（C13リプレイ往復・LABEL['tenya_re']は連番ズレ）。効果=再発動もロボ反応(赤アビ)＋連理arcana proc(T.ability%12)に参入。検証: tenya_re で T.ability +1・T.ra +1(droid稼働時)を実測。golden再fit（下記C20とセット）。 |
| C20 | **fixed→C21で修正（2026-07-11・「無条件+1」は過剰付与と判明＝条件付き付与へ後継。本行は経緯アーカイブ）** | **エジソン3（ifishant）でエレインの使用済みアビが再使用可能になる仕様が未モデル化**。実機はエレインのアビに**CD=1（ターン）**が設定されており、ifishant の全アビCD-1 で 0 化→（契晶が足りていれば）再押下可。ただし**ターン内発動可能回数（クォータ）は増えない**＝CDリセットは1回きりで**各アビ最大+1回**のリキャスト。シムはエレイン全アビ **cd=0**＋ターン内クォータguard（`T.alone<2`・`T.legend<契晶連動枠`・knights/pactcore各1回）のみ＝**ifishant がエレインに全く効かない**。 | **中〜大** — 探索が「ifishant→エレイン追い押し」（alone=バースト誘発+1回・pactcore+1回?等）の機会を計上できず**最適順とりこぼし**。実機で押せる手がシム推奨に現れない。 | **仕様確定（2026-07-09・ユーザー回答）**: ① 実機はエレイン**全アビ CD=1**（毎ターン発動可・ただし契晶消費条件で無条件にはならない）。ifishant 対象は**エレインアビすべて**（ただしユーザーの実運用は常に エジソン3⇒エレイン4 の連携＝pactcore再使用で ifishant 含むエジソンCDを再短縮するループのため、4以外は**確度90%**） ③ **契晶コストは再使用でも消費＝確定**。残確認=②クォータとの関係（alone 2回/T の3回目が可能か＝クォータ超過+1かどうか）は実機機会があれば裏取り（90%仮説=超過+1）。**実装（2026-07-09・等価表現を採用）**: 当初案の「エレイン全アビ cd=1 へ置換」は**棄却**＝試行1 raw で alone(エレイン1)が T2〜T6 全て**2回/ターン**発動（ifishantはCD5で毎ターン不可＝この2回は基礎クォータ2）を確認、cd=1にすると1回に落ち実機と矛盾するため。∴ 既存クォータguard（実機一致済み）を保ち、**ifishant.exec で `T.ifishantElaine++`（ターン内枠）を付与し、エレイン各guardの上限に `+(T.ifishantElaine||0)` を加算**する等価表現を採用（alone `<2+枠`・legend `<lu+枠`・pactcore `T.pactcoreN < 1+枠`＝boolean pactcoreUsed→counter pactcoreN に変更）。契晶コストは既存 kc ゲート（`keigyo<kc`）で自然消費・各アビ最大+1（ifishantは同ターン2回不可）。knights は nights-buff guard(C11)で同ターン再発動しない＝+1しても発火せず実機整合。**⚠対象=エレイン全アビはユーザー確度90%**（確実はエレイン4/pactcore・残は sim2 で継続照合）。②クォータ超過可否（alone 3回目）も 90%仮説として実装＝実機裏取り待ち。検証: alone上限が bonus0→2 / bonus1→3 を実測。golden再fit（C19とセット）。 **★実機裏取り完了（2026-07-09・ユーザー実機確認）**: ①CD中のエレインアビが ifishant で1回追加使用可能・契晶不足（例エレイン4）は変わらず不可＝**「上限+1＋契晶ゲート」モデルが実機一致**。②alone の3回目（クォータ超過）が実機で**押せた**＝純CD戻し（クォータ2で頭打ち）ではなく**上限が+1される**採用モデルが正しいことの直接確認。∴ 機構＋alone(エレイン1)＋pactcore(エレイン4)は確定・残る推定は legend(エレイン2)のみ（knightsは refresh 無駄で no-op）＝実装は無変更で実機検証済み。 |
| C21 | **fixed（2026-07-11・実装済）** | **C20モデル（ifishantで無条件に全エレインguard上限+1）が過剰付与**。実機仕様（ユーザー確認 2026-07-11）＝「エジソン3は**CD中のエレインアビを起こすだけ**で、既に発動可能な状態のアビの使用可能回数を+1できない」。∴ +1は**押下時点でクォータ消化済み（=実機CD=1中）のアビにのみ**付与されるべき。発見契機＝sim02試行2（C19C20エンジン推奨順）の実機再現失敗: **T2#25 alone 3回目が実機で発動不能**（T2#15 ifishant押下時点で alone は0/2使用＝CD中でなく実機は+1なし・上限2のまま）。**T3#8 puvoir（ヘカテー1）不能は連鎖**（aloneはburstTrigger＝幻の3回目がバースト1回を余計に計上→モビウスムーンズ[バースト5毎CDリセット]とアビ12proc[アルカナ]の計数が両方ズレ→ヘカテーCD戻りタイミング乖離）→試行2打ち切り。 | **大（最適順序不整合＝実機で再現不能）** — シム推奨順に実機で押せない手が混入。 | **決定的証拠＝旧推奨順トレースが両観測を条件付きモデルで同時再現**: T2#15 ifishant押下時 `T.alone=0`（未消化→+1なし→#25不可 ✓実機一致）/ `T.legend=2=legendCap(cum=19)`（消化済み→+1付与→#17 legend3回目成立 ✓実機一致）。旧C20裏取り（2026-07-09「alone3回目が押せた」）は**aloneが2回消化済み＝CD中の状態でifishantを押したケース**＝条件付きモデルと無矛盾（当時の解釈が「無条件+1」に広すぎた）。**実装**（`data/characters.js`）: ifishant.exec で押下時点の消化判定を行い**アビ別フラット枠** `T.ifAlone`/`T.ifLegend`/`T.ifPactcore` を条件付きで+1（`(T.alone||0)>=2`・`(T.legend||0)>=legendCap(sim)`・`(T.pactcoreN||0)>=1` の場合のみ）。エレイン各guardは自枠のみ参照。legend上限式は共用ヘルパー `legendCap()` へ抽出。**受入検証**: 旧推奨順replayで #25 alone=GUARD-NG（no-op）・#17 legend=通過を実測。**A2実機確認（2026-07-11）でknights（エレイン3）も ifishant対象と判明**＝当初エレイン1/2/4のみ実装だったので **knights を追加**（`T.knightsUsed` bool→`T.knightsN` counter・guard=`used<1+ifKnights`かつ初回はnights有効中不要/ifishant枠はbuff有効でも可・ifishant.exec で `knightsN>=1` 時に `ifKnights++`）。knights再発動は無ダメだがアビ計数(mooncode/arcana proc)＋契晶3消費に参入するため実機整合上モデル化＝**探索が10Tのどこかでknights再発動をproc整列に有効利用しrawが微増**。**golden再fit（2026-07-11・A2含む）**: raw 195,518,168→**203,723,485**（条件付き化＋knights対象化）/ 自動較正 override **{judg:130,pactcore:1}**（knights対象化でjudg最適が160→130へシフト・effondレバー脱落）= calibrated **218,902,146**（baseline比+7.45%・単調安全OK・FB10/10）。ENGINE_VERSION `C19C20-tenyare-ifishant`→**`C21-ifishant-conditional`**（旧キャッシュ無効化）。 |
| C22 | **closed（2026-07-16・sim03で2ボス目も非再現＝クローズ確定）** | **T2ターン内の契晶ストック獲得に+2級の乖離**。sim02試行2 実機観測＝「T2#18あたりでストック15満杯・あふれ発生」に対しシムは同時点13（+2不足）。**T2開始時点は実機読み値「契晶7・累計獲得数14」でシムと完全一致**（トレース keigyo=7/cum=14）＝T1の獲得・消費・攻撃フェイズ分は無罪で、**T2ターン内の獲得のみ**がズレる。シムの獲得モデル＝バースト毎に発動者の `keigyoGain`（エジソン3/他1）+ 4ターン毎+5（`_endBookkeep` t%4===0）+ 初期4（初期値は試行1メタで実機確定済）。**★A1実機確認（2026-07-11）で契晶の per-event 獲得モデルは実機一致と判明**: 誘発バースト（tenya_re/ムーンコードeffond/judg ph1）は**全て+1**・キャラ別蓄積は**英霊のみ+3・他+1**＝シム完全一致。∴ **C22の+2乖離は契晶ロジック自体ではなく、実機の方がバースト事象が多い（＝B1のF1/F2＝シムがバースト/judg押下を取りこぼす）の下流症状**の公算大。試行2は同一順(エンジン推奨)を実機再生したもので、同じ押下列から実機がより多く契晶を得た＝シムがproc/re-arm連鎖を過小発火しバースト数が少ない、と整合。→ **C22は独立バグではなくF1（バースト成長過小モデル化）と根が同じ疑い**。 | **中** — ストック残高が実機より辛い→kc消費アビ（alone4/knights3/pactcore12）の可否判定・押し順に影響しうる。※**累積カウンタ（閾値バフ判定元）はあふれ分もカウント＝実機確認済（2026-07-11）**で現行 `cum` 無上限加算は実機一致＝修正不要。 | **収集プロトコル（sim2リテイク時）**: raw_data に①各ターン開始時（攻撃フェイズ後）の契晶ストック/累計獲得数 ②ストック15到達時の押下番号 を記録。A1で per-event は無罪化されたので、**焦点はバースト事象数の実機vsシム差**（B1 event-level分解と合流）。特定後に修正・golden再fit。**★試行2実測（2026-07-12・B3）: 収集プロトコル実施の結果、契晶ストック/累計/連理魔力が全10チェックポイント（T1終〜T6始の各メインフェイズ開始/終了時）で実機とシム完全一致＝非再現**。旧観測はC18/C21系モデル誤りの下流症状だったとするA1以降の推定と整合。根拠 `simulation/sim02/b3_trial2_replay_analysis.md` §1。**★sim03第1バッチ（2026-07-16）でもキャスパリーグ5走×3チェックポイント全一致（15/15・実機側も5走間で決定的）＝2ボス連続非再現でクローズ確定**。根拠 `simulation/sim03/analysis/quantitative_analysis.md` §5。 |
| C23 | **fixed（2026-07-12・実装済・実機3/3裏取り）** | **真因＝judgフェーズがターン毎リセット（本来は戦闘通算連続）**。judg3フェーズ循環(ph0敵全体/ph1バースト/ph2通常)を現simは`ph=T.ju%3`かつ`T.ju`毎ターン0リセット＝各ターンph0始まり。実機は戦闘通算連続。raw_data2の各ターン最初のjudg表示で照合＝**通算モデルがT4(ph1)/T5(ph0)/T6(ph2)を3/3的中・現simはT4/T6を外す**。#19「リキャストされていない」はこのフェーズズレの下流（ph配置ズレ→バーストproc位置ズレ→re-armズレ）。連理一致はproc総数同じ・位置のみズレのため。旧記述「T4#19 re-arm1押下ズレ」。 | **中（最適順序不整合・局在）→ fixはgolden移動** | 局在: `data/characters.js` judg.exec `ph=T.ju%3`／`src/sim.js` `_beginTurn` の `ju:0`（T.juはフェーズとcapの二重用途・capは据置で正）。**実装（2026-07-12）**: 戦闘通算カウンタ `sim.judgPhase`(constructor0初期化・非リセット・snap/clone伝播)を新設し judg.exec で `ph=sim.judgPhase%3`＋`judgPhase++`。`T.ju`(同ターンcap・毎ターンリセット)は据置。**受入検証**: 旧順replayで T4=[ph1,ph2]/T5=[ph0..]/T6=[ph2..] と実機3/3一致・#19相当がブロックされることを実測。**golden再fit**: raw 203,723,485→**186,634,324**(-8.4%=旧モデルが毎ターンph0リセットでph1バースト判定を過剰計上していた水増しの是正・決定的2回確認)/ 自動較正override **{judg:160,pactcore:1}**(judgフェーズ連続でjudg最適が130→160へ再シフト)= calibrated 218,902,146→**208,347,477**(FB10/10)。ENGINE_VERSION `C21-ifishant-conditional`→**`C23-judgphase-continuous`**(旧キャッシュ無効化=sim03追撃ONキャッシュも要再探索)。詳細 `simulation/sim02/c23_judg_phase_findings.md`。**★sim03追検証パス（2026-07-16）: キャスパリーグ5走×3T の各ターン初judgフェーズ予実15/15一致（T1 ph0/T2 ph2/T3 ph2）＝通算連続モデル確定**。 |試行2実機＝T4#19 judg 発動不能（armされていない）・#20 effond は可。シム＝#19 judg 発動（ph2通常・+1.87M）。連理魔力はT4開始14/終了16で実機と一致＝アルカナproc**回数**は合っているが、**cd.judg=0 の発火位置**がシムは#19より前・実機は後（おそらく#20 effond時）。C18/C21系の proc 整列の残余。 | **中（最適順序不整合・局在）** — シム推奨順に実機で押せない手が1つ混入（T4のみ・被害は judg ph2 通常1回分）。 | T4のアビ計数トレース（12proc到達の押下番号・burst-2 proc位置）をシムで計装出力し、実機のre-arm観測位置と照合して off-by-one の発生点（どのアビ計数/バースト計数が食い違うか）を特定。根拠 `simulation/sim02/b3_trial2_replay_analysis.md` §2。 |
| C24 | **診断済・fixは実機ゲート（2026-07-12・低severity）** | **バーストゲージ経済の微小系統乖離＋エレイン1の条件付き対象**。ターン開始時ゲージ: T2 シム85 vs 実機80（+5）/ T4 シム ヤマト190 vs 実機195（−5）/ **T5 シム85 vs 実機75（+10）**。関連の実機観測（raw_data2 欠陥3）＝T5#1,#2 エレイン1 は「英霊のバーストゲージが100でないため効果対象が英霊のみ」＝**ゲージ付与にゲージ依存の対象条件**がある（シムは味方全体固定）。 | **中** — ゲージはバースト可否（押し順の骨格）を直接ゲート。±10のズレはFB成立ターンを変えうる。 | **診断完了（B3+c24診断）**: ①差は完全に一様（全体付与源）・**+5はMACH_BG=5に一意**・**FB全ターン5で非反転＝押し順骨格に無影響（低severity）** ②**sim赤(攻撃ロボ)反応数が実機ロボ追撃と完全一致**（T1-T3/T5・T4は+1=C23 #19下流）＝attack-robotモデル正 ③反実仮想テストで「赤のみMACH_BG」は−50〜60と大過小＝**黄(補助ロボ)反応もゲージを生む（赤+黄モデルは方向正）**・残差+5/+10は**黄反応の1〜2計数差**に局在（実機は黄反応=バフのみ非表示で帰属確定不可）④**エレイン1(alone)はsimでゲージ付与なし＝欠陥3は逆符号でsim-higherの原因でない**（エレイン2 legendは英霊100条件の対象切替を実装済L423）。**実機ゲート(sim03)**: (1)補助ロボ黄反応のゲージ付与量/計数を黄反応前後のゲージ増分で実測 (2)エレイン1のゲージ効果仕様。根拠 `simulation/sim02/c24_gauge_diagnosis.md`。**★sim03新データ（2026-07-16・診断更新）**: ロボ反応ゲージ増分 実機 T1=80/T2=120（5走同値）。ターン境界: T1 実機**+15一様**（敵フェイズ前100/200/100/100/100 vs シム攻撃フェイズ後85/185/85/85/85）・T2 **ヤマトのみ実機−30（逆符号）**・敵フェイズ跨ぎで実機−20一様＝**「+MACH_BG一様」説では閉じない事例が追加**（シムに敵フェイズのゲージ変動モデルなし）。FBは全走全ターン成立＝低severity継続。根拠 `simulation/sim03/analysis/quantitative_analysis.md` §5。 |
| C25 | **部分fix（2026-07-21 sim04較正）＝通常×1.835・バースト本体×2.07 の枠別calibを実装（configB M走fit・実機mean突合）。残: C5/C3追撃cap・ターン跨ぎ成長の二次残差** ← open・再定式化(2026-07-12) | **絶対値乖離は pre-cap の raw に局在**（cap値・slopeは飽和成分の一致で妥当と検証済み）。経緯: B4で「減衰capが会心・急所の内側なのが誤り＝実機は cap(素ダメ)×会心×急所」仮説を起票→**B5の定量テスト（試作strip変換×実機24サンプル照合）で棄却**: θ（急所実効倍率）不整合＝core 1.1〜1.7 vs 追撃 2.4〜4.3／ヤマトT1（会心のみ・θ自由度なし）で cap外しは32%過剰予測・現行モデルが98%一致／streak統制はstripで崩壊＝**B4のstreak完全一致は「順序」でなく「深飽和による鈍感さ」の証拠**（自己訂正）。**確定した構造**: ① バースト本体=出力比 64〜74%(T1)→49〜51%(T4)＝**baseline不足＋ターン跨ぎ成長不足（C7除去後もB1のF1は正味実在）**・ヤマトのみ大幅に良い（selfBonus過大 or 他キャラraw過小のキャラ不均質併存） ② **バースト追撃=シムはcap飽和(≈200万)だが実機737〜770万＝crit順序をどう置いてもC5較正capでは到達不能→式/capレベルの独立乖離（C5再較正・C3合流）** ③ 通常na=raw≈2.4×不足（同方向）。 | **大（絶対値較正の本丸）** — kill-turn（KILL_TURN_DESIGN §7.4）の前提。 | **決定打＝非会心・非急所の非飽和サンプル**（現行データは全サンプルに会心/急所が乗り raw を直接読めない・streakは飽和で鈍感）。各成分（通常/バースト本体/追撃/ロボ追撃）で non-crit 単発を1点ずつ→raw の枠帰属（GEAR_K/assault/旺盛/…）と成長差の枠局在を確定→修正→golden再fit。**→ sim03 測定プロトコルの中核**（`simulation/sim03/README.md`）。根拠 `simulation/sim02/b4_component_split.md`・`b5_c25_quantitative_test.md`。**★sim03第1バッチでアンカー取得（2026-07-16）**: 隠れ耐性=1保証（ライトレジスト）・affinity1.5既知の環境で**押下単位の実機5走平均/シム比を全数取得**＝全体**×1.430**・バースト系 T1×1.30〜1.49→T2×1.58〜1.78（成長不足F1再現）・judg ph2(na)×~1.95・**深飽和(streak×1.00〜1.03/DOT×1.000)は一致＝pre-cap raw局在をボス3体目で確認**。stack=0ディスペルアンカーは不発（限定行動1T1個仮説＝qualitative §1b）→baseline/成長分離はT1枠回帰で継続。根拠 `simulation/sim03/analysis/quantitative_analysis.md` §4。 |
| C26 | **open（2026-07-12 起票・B4）** | **試行2の探索が「エジソン英霊武器追撃OFF」（`DMG.edison_burst_extra_mult=0`）で走っていた**＝B3 fidelity完全一致（mult=0のreplayがキャッシュ総ダメと1円単位一致）が証明。実機は追撃 T1 445万→T4 1001万と攻撃フェイズ最大級の成分で毎ターン発生＝**推奨順はエジソンのバースト価値を系統的に過小評価した探索の産物**。併発: **結果キャッシュキー（`_configSig`）が `edison_burst_extra_mult` を含まない**＝英霊武器の有無だけ違う設定が同一キーへ衝突（stale cacheハザード）。 | **中〜大→実際は小（下記で再評価）** | **★fixed（2026-07-12・真因確定＋追撃ON再探索受領）**: 真因＝**純粋なシムUI設定忘れ**（英霊武器追撃をUIで有効化し忘れていただけ・applyGearバグではない）。ユーザー確認: 最終限界突破IIはエジソンのステータスUPで**追撃有無とは無関係**。`_configSig` に mult/cap 追加済（2026-07-12・golden不変）。**追撃ONで再探索したキャッシュ受領**（`archive/caches/kamipro_cache_C21-ifishant-conditional_followupON.json`（2026-07-14 archive/caches/へ移設）・dmg 1,565,048,890・override {judg:160,pactcore:1}・gear dmgup:0.09追加）。**★批判的再評価**: 「推奨順の系統バイアス」は**過大評価だった**＝追撃ONでも押し順ほぼ不変（T1は#15/#16入替1箇所・T2完全一致・T3はtenya_re再グループの小差）。overrideのjudg 145→160シフトは主にdmgup:0.09ギア＋追撃。**追撃はcap飽和でsim総ダメの約1.1%（17.5M/10T）にすぎず押し順を構造的に動かさない**。ただし**追撃cap過小はC25追撃点を定量裏付け**＝sim英霊武器追撃 cap≈156万（800000×1.954）で頭打ち vs 実機445〜1001万＝**3〜6×過小**（C5/C3較正ターゲット）。**残データ品質課題（新）**: 結果キャッシュはGEARボックスは埋め込むが**per-char ATK（GEAR_K_C）を埋め込まない**ため、再構成replayが再探索の絶対総ダメを1.7%再現できない（一様ATKスケールでは非閉＝非一様per-char ATK差＝再探索UIのcalcDisplayAtk算出と私の平坦override 92873の食い違い疑い）。→ sim03実機取得時に**per-char表示ATKスナップショット**必須（or `exportResultCache` に `DISPLAY_ATK_C` を含める小改修）。 |
| C17 | **wontfix（見送り・2026-07-04）** | **第4較正レバーの検討**（C15の「4変数以上却下」を BW64・新base `{judg:122,pactcore:1,effond:93}` でデータ再検証） | —（採用せず） | **手順と結果**: (1) proxy掃引（16アビ×8値）→11候補がbase超え。(2) full-verify→**生存2のみ＝sleur:100(+0.491%=207,862,037)/puvoir:100(+0.410%)**。proxy gain上位（funki/droid/inori/amplifa）は全て**full −3.0〜−3.7%**＝myopicミスランクを再々確認（funki前科§6.6と同型）。(3) **sleur×judg×effond joint 27点掃引（3×3×3・pactcore:1固定）→joint最良 `{judg:122,effond:93,sleur:100}`=207,862,037（+0.491%）＝単独加算と完全同値**。pactcore型相互作用（単独+0.3%→joint+5.3%）は**再現せず**＝judg/effond最適は不動。base近傍以外は−1〜−9.5%の崖（例 judg145/effond80/sleur150=−9.48%）＝sleur軸は探索地形を脆くする。**∴ 3変数が実質飽和点**。利得+0.49%（generic単一編成・他ギア未測）に対し、grid直積36→144・崖セル増によるproxy shortlistミスランク面積増（§6.9枯渇バグ再発リスク）・ギア汎化/新キャラ時の再検証面積増＝**工数対効果不成立で見送り**。将来の再掃引は `tools/search_lever_scan.mjs`/`search_lever_verify.mjs`（base は `POC_BASE` env で指定可）。 |

| C27 | **superseded（2026-07-30・C37 局所探索が包含＝production 非経路へ）** ← fixed 2026-07-14（探索品質・単調安全リファイン実装） | **探索が赤アビ(バーストトリガー)をエジソンのロボ設置/アンプリファより前に並べ、攻撃ロボ反応＋ダメージプラス(+10万)の相乗を取り逃す**。証拠キャッシュ `archive/caches/kamipro_cache_C23-judgphase-continuous.json`（ユーザー実gear・C23）T1 推奨順で観測: `alone`(#1・赤)は`droid`(#4)より前＝`sim.droid=0`でロボ反応が**一切発火せず**／`effond`(#5・赤)は`droid`(#4)後だが`amplifa`(#9)より前＝ロボ反応は乗るが**+10万(`DMG.amplifa_flat`)を取り逃す**（機構 `data/characters.js:98-108`: 反応は`color==='r'&&sim.droid>0`条件・+10万は`amplifa_buf`有効時のみ）。 | **中（火力の純損失・押し順反転ではないが局所最適の取りこぼし）** | **反実仮想リプレイで実証済（既定gear・`_replayResult`）**: T1で`alone`/`effond`のみを`amplifa`直後へ移動し他を不変にすると、**FBを5/5に保ったまま T1 +520,491 / 10T合計 +545,459(+0.247%)** の純増。差分がほぼT1に閉じる＝下流副作用なし＝**burst-timingトレードオフではなく探索が見逃した厳密により良い順序**。**根本原因の仮説**: 静的スコア s（`alone`40/`effond`70 vs `droid`201/`amplifa`180）は赤アビ→ロボ反応の動的相乗を織り込まず、2段ルート選抜の proxy-greedy（`_staticPrefixDmg`）が赤アビ前出しの初期分岐を選び、本選(BW64)がその近傍しか掃かないため相乗順が beam に載らない疑い。**次アクション**: (a) ~~実gearでの損失額を再測定~~ **✅実施済（2026-07-14・下記）**(b) ~~定石性報酬 or 順序制約を検討~~ **✅実装済（2026-07-14・whole-route局所改善`_refineRoute`採用・下記★★）**(c) 序数フィクスチャ化（base T1 vs 反実T1・`real:'var>base'`）＝**未着手（低優先follow-up）**。<br>**★(a) 実gear再測定の結果（2026-07-14）**: 証拠キャッシュの実gear（`assault2.826/vigor0.6876/na_dmg0.972/…`・dispAtk 5人分・edison追撃mult2.5/cap80万）を復元し同一反実仮想を実行＝**T1損失 +4,448,410（aff1.0）〜+4,937,780（aff1.5）／10T +5,473,559（+0.384%）〜+5,977,649（+0.395%）**。**既定gearの52万→実gear約450〜490万＝約8〜9×に拡大**（仮説どおり: ロボ反応が`_naForAbi`増でcap飽和＋固定+10万＝実gearで絶対損失が拡大）。**%損失はaffinity1.0/1.5・misc1.0〜1.2の6%スイングでも0.384→0.395%とほぼ不変**＝復元スカラ誤差は base/variant 共通modeでdeltaがrobust。**⚠fidelity注記**: headless復元はキャッシュ総ダメの約87%(aff1.0)〜93%(aff1.5)しか再現せず（C26既知＝UIの`calcDisplayAtk`/misc等の未回収スカラ）＝**絶対額は±数%の不確実性**（delta%は上記のとおり不変）。精密な絶対値はsim03のper-char ATKスナップ必須化で解消。**再現ハーネス**: `tools/c27_refine_probe.mjs`（旧scratchpad cf.mjs/cf_realgear.mjs を統合・キャッシュ引数で実gear復元＋反実仮想を再実行可）。<br>**★★(b) 修正実装（2026-07-14・fixed）＝全設定履歴（ユーザーレビュー用）**:<br>**①機構の選定と根拠**: 3案（(1)定石性報酬=orthodoxy / (2)候補生成の静的スコア順序制約 / (3)確定ルートへの whole-route 局所改善パス）を検討し**(3)を採用**。(1)却下＝orthodoxyは目的ベクトル最終要素の「同ダメージ時タイブレーク」であり、C27の改善順は**ダメージが高い**ため tiebreak では選ばれない（真因はビーム目的関数が将来ターンを静的greedyで**代理採点**するため赤アビ前出しを damage-max と誤選択する点＝tiebreakでは是正不能）。(2)却下＝静的sは既に`droid`201>`alone`40で greedy 単体は既に後出しするが、ビームは**全候補をfork展開してcompleted-rollout damageで採点**するため静的s調整ではforkを止められない。∴根本原因（rollout近似誤差）に直接効くのは**実ルート上で実後続ターンごとreplay採点する局所改善**（=反実仮想の自動化）。<br>**②定石ルール（実装 `src/sim.js` `_refineRoute`）**: 確定 per-turn キー列に対し、各ターン内で「`deploysRobot`(攻撃/補助ロボ設置) または `prelude`(アンプリファ) の**最後の**index より前にある赤アビ(`ABIL[key][1]==='r'`)」を、その最後のsetup**直後**へ移動し、**10T総ダメージが厳密に増える時のみ採用**（`_replayResult`で採点・改善が無くなるまで反復＝固定点）。**タグ駆動**（`deploysRobot`/`prelude`/色 'r'）でキャラ名リテラル不使用＝新キャラ/編成に自動追従。誤った移動（例: 補助ロボ後）は厳密改善ガードで自然に棄却＝安全。<br>**③結線点**: production=`_runRootPlan`（worker/fallback共通・8prefix各routeをrefine後に最大選択）／golden=`test/golden.mjs`（単一ビームrouteに同`_refineRoute`適用）。app.js barrel で `_refineRoute`/`_replayResult` を再公開。<br>**④ENGINE_VERSION**: `C23-judgphase-continuous`→**`C27-red-after-setup-refine`**（押し順変化＝旧キャッシュ無効化。sim03のconfigA探索は本版で実施）。<br>**⑤golden再fit（単調・改善のみ）**: raw 186,634,324→**187,186,834**（+552,510/+0.296%）／calibrated 208,347,477→**208,689,608**（+342,131/+0.164%）・**FB10/10不変**。変化ターン=T1/T5/T9。**override `{judg:160,pactcore:1}` は据置**（overrideはルート**選択**を制御し、リファインは選択後の**単調**パス＝選択最適overrideは不変。∴ formal な `search_calibrate.mjs` 再fitは低優先の follow-up）。<br>**⑥検証**: golden pass・`npm run build`成功・production `_runRootPlan([])` の T1 が `…droid amplifa alone…`（alone後出し=C27解消）・再`_refineRoute`で`improved:false`（**冪等=固定点**）。<br>**⑦残**: (c) 序数フィクスチャ化（base T1 vs 反実T1・`real:'var>base'`）は未着手（テストハーネス化候補・低優先）。**再現ハーネス**: `tools/c27_refine_probe.mjs`（上限測定・冪等/後出し検証・キャッシュ復元を統合した恒久版。引数なし=golden経路/引数=キャッシュexport）。<br>**★★★ superseded（2026-07-30・C37）**: B3b で **LS が C27 の改善を完全に包含**すると実測され（refine 有/無の出発点が1円単位で同一終点へ収束・LS はさらに5.6倍の改善を上乗せ）、`_runRootPlan` の結線は `_localSearchRoute` へ**置換**した。**`_refineRoute` 自体は削除せず残置**（`tools/c27_refine_probe.mjs` ほか5本のハーネスが記録済み数値の再現に使う）が、**production 非経路・新規結線禁止**。C27 の評価は否定ではない＝「ビームの代理採点が赤アビ前出しを誤選択する」という**問題認識は正しく、解法が `deploysRobot`/`prelude` タグに依存していたため一般化しなかった**（タグ不在のナポ/アリアン編成では一度も発火しない＝編成依存の4例目）。詳細 C37 と `simulation/sim05/search_quality_experiments.md` §6e/§6f。 |

| C28 | **open（低優先・2026-07-16 sim03起票）** | **per-hit の乗算ダメージロールRNGが未モデル**（シムは決定的期待値モデル）。sim03第1バッチ: 同一押し順・会心/急所マーク5走一致のhitでも値がスプレッド（median 3.5%/p90 12.4%/max 16.1%）＝記録済み状態では説明不能な per-hit ロールが実機に実在。スプレッドは減衰飽和度と逆相関（深飽和 streak 0.6〜1.2%・非飽和 攻撃フェイズ 4〜17%）＝pre-cap 片側±5〜8%級のロールが cap/slope で圧縮される構造＝**cap/slopeモデルの独立検証にもなった**。 | **ほぼなし（押し順中立・全hitに一様）**。ただし撃破押下が±1動く（実測 T3#16×3走/T3#17×2走）＝**kill-turn予測は分布で語る必要**・単trial照合の解釈にも必須の概念。 | 期待値モデル維持で実害なし＝fixは「較正時に押下単位の複数走平均を使う」運用側で吸収（sim03 quantitative §2）。将来 kill-turn モード（KILL_TURN_DESIGN）実装時に分散パラメータとして再訪。根拠 `simulation/sim03/analysis/quantitative_analysis.md` §2・付録A。 |　**★確定仕様の受領（2026-07-28・ユーザー提供の一次情報）**: **神姫PROJECT のダメージ乱数は「中央値 ±5%・1%刻み・11通り」**（＝{-5,-4,…,+4,+5}% の離散一様と解される）。**出所＝ユーザー申告（2026-07-28）／確定情報として扱う**。**sim03 の推定と整合**＝§2 は観測スプレッド（急所マーク5走一致 hit で median 3.53%/p90 12.44%/max 16.09%）から **pre-cap 片側±5〜8%級**のロールを逆推定していたが、確定仕様 ±5% はその**下端に一致**＝推定は正しく範囲を括っていた（観測値が大きく出るのは cap/slope 通過前後の非線形と非飽和成分ゆえ）。**★総ダメージでの平均化（⚠理論見積り・未検証）**: 離散一様 {-5..+5}% の SD は √((11²−1)/12)=√10 ≈ **3.16%/hit**。**仮に**各hitが独立同分布かつ**等ウェイト**で N 発なら 総ダメの相対SD ≈ 3.16%/√N（N=200 で ≈0.22%）。⚠ただし**実際はバースト等の大hitが偏って効くため実効 N は N より小さく、値はこれより大きい**（有効サンプル数 N_eff=(Σw)²/Σw² 未測定）。**∴「総ダメの乱数 floor はサブ1%オーダー」までが言える範囲で、具体値は要実測**（同一押し順の複数走で総ダメ分散を直接測れば確定する＝sim03 は per-hit しか集計しておらず総ダメ走間分散は未取得）。傍証として sim03 §3 の max_hp 推定区間が5走で **±0.15%** に収束している。**★実務含意（2026-07-28 ユーザー指摘を定量化）**: 装備強化で許容誤差が縮んだ実感の機構は「絶対値が大きいから」ではなく（押し順の優劣は相対比較ゆえ等倍成長では不変）、**離散閾値への近さ**＝撃破ターン・tierゲート・FB成立といった**離散的結果を、連続量の小さな誤差が反転させる**こと。具体例: sim05 の宿儺推奨順は **T5 累積 989,049,835 / HP 980,000,000 ＝ margin わずか 0.92%**（⚠これはシム予測値であり系統誤差 C25/C3/C5/C38 を含む）＝**1%の誤差で撃破が T5→T6 へ反転する領域**。C28 が既に指摘した「撃破押下が実測±1動く（T3#16×3走/T3#17×2走）＝kill-turn は分布で語る必要」が、まさにこの regime。**★測定の識別限界としての使い方**: 押し順比較で「何%以上なら有意か」の物差しになる。実験4（C37）の**上位6本の差 0.13% は乱数 floor をも下回る**＝モデル誤差以前に**実機1走では原理的に区別不能**。一方 C37 実験5cの **+6.9% は floor を大きく超え1走でも判別可能**。
| C29 | **open（低優先・2026-07-16 sim03起票）** | **通常攻撃の連撃（DA/TA）が未モデル**（シムは常に1hit）。sim03実測: judg ph2 の通常攻撃が走・slotにより1〜3hit（T2#1: 3/2/1/1/1・T2#13: 1/1/1/3/1・T3#12: 1/3/1/1/1）＝連続攻撃RNG。キャスパリーグは敵側「連続攻撃確率UP」持ちだが、これは**敵の**攻撃であり味方側連撃とは別（味方側の発生源はDA/TA率）。 | **小** — 通常攻撃成分は1戦の≈1%。ただし judg ph2 の期待値がシム×~1.95過小（C25のna不足と複合）の一部を説明。 | 通常攻撃期待hit数（1+DA率+2×TA率 等）を na モデルに乗せる案。優先度は C25/C5 の後。根拠 `simulation/sim03/analysis/quantitative_analysis.md` §2・§4。 |
| C30 | **fixed（2026-07-21 sim04較正）＝judg ph0 に judg_calib=0.62 を付与(C31加算化後も実機/sim≈0.62過大)。実機N=0非急所428k/hitに整合** ← open(2026-07-16) | **judg ph0（敵全体10回ダメージ）のシム過大＋成長形状不一致**。sim03実測: 実機/sim = 0.76〜0.78（唯一の「シム過大」成分・他は全てシム過小）。シムは `10×hit` フラットが raw 成長で 7.09M→12.2M と増えるのに、実機は 5.8M→7.1M と緩やか＝**hit単価が過大かつ per-hit cap で頭打ちの疑い**（10hitの各hitに小さめのcapがある形状と整合）。 | **中** — judg は静的スコア override（現行 {judg:200}）で探索の主レバー＝**過大評価は judg 前倒し・多用への系統バイアス**になりうる。C25（他成分の過小）と符号が逆のため、絶対較正を総ダメスカラ1本で閉じることも不可能にしている。 | judg ph0 を per-hit `_decay('abi', hit単価, per-hit cap)` ×10 へ改める案を較正セッションで検証（実機の押下値 5.8〜7.1M と成長形状にfit）→**override 再fit・押し順変化に要警戒**（golden再fit必至）。根拠 `simulation/sim03/analysis/quantitative_analysis.md` §4。 |
| C31 | **fixed（2026-07-21 sim04較正で実装）** ← open(2026-07-16起票) | **アビリティダメージUP枠の合成が乗算（実機は倍率へ加算）**。一次情報⑫: アビダメUPはアビ固有倍率へ**加算**される（倍率1のアビ+200%→3倍・倍率20のアビ→22倍）。エンジンは `_naForAbi()*mult*(1+GEAR.abi_dmg+db.dmg)`（src/sim.js:58 ほか judg/effond/consort 等）＝**倍率へ乗算**。configA実ギア abi_dmg=2.16 の judg(mult=3) で sim=3×3.16=9.48 vs 実機式=3+2.16=5.16 → **pre-cap ×1.84 過大**（アビcap slope0.04 の圧縮を通すと sim03 実測 C30 の「実機/sim=0.76〜0.78」と方向・規模が整合）。droid_buf の+3%/反応(db.dmg)も同枠なら加算へ | **高** — C30（judg ph0 シム過大＝唯一の過大成分）の**真因候補**。judg は override 主レバー＝押し順バイアス源 | 較正セッションで `(mult+アビダメUP合計)` 型へ是正し C30 の per-hit cap 案と同時 fit。override/golden 再fit必至。根拠 `gamedata/damage_frames.txt` ⑫ |
| C32 | **resolved＝実装せず（2026-07-21 M3実測で2段cap不支持・現行1.0クランプ維持）** ← open(2026-07-16) | **旺盛枠の上限モデル相違＝シム過小**。一次情報③: ウェポンスキル分は100%上限、ウェポン+アビリティバフの合計は+200%上限。エンジンは全ソース合算を `min(…,1.0)` で一律クランプ（src/sim.js:107）。configA: GEAR.vigor=0.6876＋戦闘中バフ（absolute0.30/legend0.3552/mooncode0.3552）で合算1.34〜1.70 → 1.0 に切られ **(1+vigor) 係数が実機比 ×0.85〜0.74 過小** | **中〜高** — 方向が C25（pre-cap raw シム過小・全体×1.43）と一致＝**C25 の有力寄与候補**。バフ本数依存＝押し順にも二次影響 | `min(ウェポン分,1.0)+アビバフ分 → min(合計,2.0)` の2段capへ。C25 の T1枠回帰に旺盛項を明示的に含める。根拠 `gamedata/damage_frames.txt` ③ |
| C33 | **open（2026-07-16 同上・実装バグ）** | **テクニカ武器スキルの黙殺**。gamedata/weapons.js は `box:'technica'`（directorel 22%・corel_snipe 18%）を宣言するが GEAR_BOXES に `technica` キーが無く、applyGear の `GEAR[sk.box]!==undefined` ガードで**無言ドロップ**（対になる na_cap 7.5%/5% だけ加算される片肺状態）。意図は na_dmg 集約（src/app.js:44 コメント）。configA は装備パネル手入力転記（×1.8）で na_dmg=0.972 が入っているため **sim03 への実害なし**＝武器スロット自動計算経路のみ欠落 | 低（現運用はパネル手入力） | applyGear に technica→na_dmg エイリアス1行（GEAR全0のため golden 不変見込み）。⑤/⑩の +300% cap も未実装（現 na_dmg=0.972 で非活性・武器追加時に要cap） | 
| C34 | **fixed（2026-07-21 sim04較正でバーストダメUP+500%上限を実装）** ← open(2026-07-16) | **バーストダメージUP +500% 上限が未実装**。一次情報⑪: エクシード＋バーストダメUP/性能UPバフの**合計値**に+500%上限（「バースト性能大幅UP」は上限外・別加算）。エンジンは無capで加算（src/sim.js:168 `coef_a+bdmg+GEAR.burst_dmg+selfBonus`）。configA: GEAR.burst_dmg=4.86 単独でほぼ上限、戦闘中バフ（absolute×3=0.90/nights0.20/ARRIVE0.20/sub等）加算で〜6.2 → **シム過大方向** | **中** — sim03 のバースト系は実機/sim=×1.30〜1.78 の**シム過小**＝逆符号。cap導入で見かけの乖離はさらに開く＝**baseline不足がその分大きい証左**として絶対較正の枠帰属に必須 | `min(バフ+ウェポン合計, 5.0)`＋selfBonus（大幅UP=現神/アリアン等）は上限外のまま。C25 絶対較正と同時 fit。根拠 `gamedata/damage_frames.txt` ⑪ |
| C35 | **open（低優先・2026-07-16 同上）** | **枠合成の軽微相違3点**: (a) 恐傷は与ダメUP枠と**同枠加算**が実機（⑥）だがエンジンは `(1+dmgup)×(1+kyo)` の別枠乗算（src/sim.js:137。アルテミス不在編成では実害0） (b) **最終ダメージUP は減衰後適用**が実機（⑬）だがエンジンの DMG.final_dmg は `_na()` 内＝バースト/アビ減衰計算の**前**に乗る（既定1.0で実害0） (c) **急所は有利属性相手のみ発動**（⑧）の affinity ゲート無し（較正ボスは有利1.5で実害0・中立敵シミュで過大） | 低（現編成・現較正ボスでは3点とも実害0） | 絶対較正パスの実装ついでに是正（単体では golden 不変のはずだが要検証）。根拠 `gamedata/damage_frames.txt` ⑥⑧⑬ |
| C36 | **fixed（2026-07-22 sim05前構造修正で実装）** ← 起票同日 | **ダメージ上限UP（減衰上限）枠がキャラ由来では burst しか受け皿がなく na/アビは非対応＝未反映**。一次情報（damage_frames「ダメージ上限UP」＝通常/バースト/アビの各減衰枠へ加算・「特別減衰」は別枠）。具体: テトラ アブソ（通常/バースト/アビ 各+20%）・ヘカテー プヴワール（各+6%/stack累積）・エレイン ARRIVE（バースト+10%）が未実装（各キャラmd §2 に未モデル化と明記されていた）。加えて burst のキャラcap×GEAR上限UPが**乗算**（elaine.md「ARRIVE はエクシードと同枠」＝本来同枠加算） | **中〜高** — cap を上げる＝pre-cap raw がより多く生存＝実gearでシム**過大方向**。C25（pre-cap raw シム過小・baseline不足）/C34（baseline不足の証左）の**符号を埋める是正**＝絶対較正の枠帰属に必須。golden は default gear で cap 未到達につき**不変**（≠実配置） | `_partyCapUp`（abso presence+20% / puvoir 累積+6%＝na/burst/abi 共通加算）＋ burst を同枠加算（自=burstCapBonus[奮起/アリアン2アシ] + パーティ=passiveCap[ARRIVE]/_partyCapUp[abso/puvoir] + sub[アルテミス] + GEAR）へ・**特別減衰[アリアン]のみ別枠乗算**（`burstCapSpecial`）。実装済み・golden不変・単体検証済（abi cap 35万: +abso20%+puvoir18%=+38%→cap 483000 を確認）。**⚠残: abs-calib スカラ（calib_na/burst/judg）は configB[実gear・cap binding]で cap-UP 無しに fit された＝sim05 で cap-UP 込み再検証が必要**。根拠 `gamedata/md/その他/damage_frames.md`「ダメージ上限UP」・tetra/hecate/elaine.md §1 |
| C37 | **open（局所探索は ✅2026-07-30 実装済／幅・代理採点の問題は未解決）** ← 起票 2026-07-28 | **①探索パラメータが編成依存**＝`BEAM_W=64` はナポ/アリアン×両面宿儺で**測定中の最悪点**（BW96以上のどの幅も +3.0〜5.6% 上回り、かつ**非単調**＝BW384 で +5.64%）。根拠の C16 はエジソン測定値で転移せず。**②探索が不安定**＝ATK を1割動かすと押し順の約7割が変わり、**ダメージが ATK に対し非単調**（攻撃力を上げると下がる）。**③ただしエジソンは健全**（A1: ダメージ厳密単調・押し順一致 99.2〜100%）＝エンジン全体の病理ではない。**④原因は `abilCapPerTurn` で確定（B1）**＝宿儺のまま cap のみ無効化すると**非単調が消え押し順一致が 29.5%→85.8% に回復**（4条件すべて単調増加）＝**アリアンの holy は無罪**（maxPress 19→41 に膨らむが不安定性は生まない）。**∴ 較正ボスの選定そのものが探索を不安定にしていた**。 | **大（探索が返す押し順そのものが幅/ATK で変わる）**。ただし本質は「幅不足」ではなく**枝刈りがロールアウト（静的greedy＝代理採点）に依存**していること。害は **(i) ターン内順序＝局所探索で安価に回収可能（+3.02%）** と **(ii) 複数手の同時判断＝回収不能** に分かれ、**本質的課題は(ii)**。なお**上位押し順の差 0.13% はモデル誤差にもゲーム乱数(C28)にも埋もれる**＝シムは上位順を有意に順位付けできていない（実機1走では区別不能）。 | **方針（改訂 2026-07-30）: 局所探索のみ実装・幅と代理採点には手を付けない**（起票時は「起票のみ」だったが、B3b で置換可と確定したためユーザー承認のうえ実装）。**確定した派生結論**: `PREFIX_TOPK=8` は**転移していた**（損失0.017%）／**局所探索は C27 リファインの上位互換＝置換可（B3b）**＝エジソンで C27 単独 +0.140% に対し LS は **+0.779%**（5.6倍）で **refine 有/無が同一終点へ収束**（1円単位一致）。ナポ/アリアンでは**C27 は発火しない**（`deploysRobot`/`prelude` タグがエジソン専用＝**4例目の編成依存**）が LS は +3.02%。<br>**★実装（2026-07-30・`src/sim.js` `_localSearchRoute`）**: 近傍3種（①ターン内move ②ターン内swap ③ターン跨ぎswap）を総当たりし `_replayResult` で厳密改善（+1円）のみ採用・収束まで sweep 反復。**`_runRootPlan` で全 prefix に適用**（実験5c の多点適用の利得＝単点 +3.02% に対し多点 +6.88% を取るため。8ルートは並列ワーカー）。停止条件は**評価回数 `LS_MAX_EVALS=500000`（時間ではない）＝決定的**（時間バジェットは golden の再現性を壊す）。③を move ではなく swap にするのは手数保存＝`abilCapPerTurn` を持つ敵で受け側が黙って落ちるのを防ぐため。`_refineRoute` は**再現性保全のため残置＝production 非経路・新規結線禁止**。付随修正＝`_execKey` が実行可否を返し `greedyTakeTurn(t,forcedKeys)` は**実行できたキーのみ記録**（幽霊キー防止・総ダメージ不変）。**golden 再fit**: raw 197,775,394→**201,909,711**（+2.09%）／cal 211,462,826→**214,213,430**（+1.30%）・FB 10/10 と maxPress 30/33 は不変・napoleon/static 299,534,299 は不変。ENGINE_VERSION `sim04-abscal-C31C34-calib`→**`sim05-ls-route-C37`**。所要 golden 116秒→**約10分**（LS 324s/177s＝評価177,961/124,152・ターン跨ぎswapが評価の73%）。**→ 2026-08-01 の LS 高速化（`_execKey` 短絡＋`_LSReplay` インクリメンタル replay・結果不変）で 約10分→4分07秒**（評価回数・着地点は不変）。⚠**改善幅もギア/敵依存**（B3b の configB 実gear +0.78% に対し golden の default gear では +2.09%）＝**5例目の条件依存**。⚠**override `{judg:145,pactcore:1}` は据置**＝「override はルート**選択**を制御し LS は選択後の**単調**パス」という C27 時と同じ理屈だが、実験5c で「**開始点が違うと LS 後の質が 3.8% 違う**」と判明した以上**選択最適 override が動く余地は C27 時より大きい**＝`search_calibrate.mjs` の formal 再fit は**留保付きの follow-up**（低優先だが C27 時より根拠は弱い）。ただし**探索幅の代替にはならない**（対照実験で BW384+LS が多点LSを +1.90% 上回る）／**押し順の識別可能性は根本的に低い（C1）**＝上位6本の差 0.13% がモデル誤差にも乱数(C28)にも埋もれるうえ、**総ダメが1円単位で同値の別ルートが少なくとも7本存在**（キー列は相違・一致率97.4〜98.9%＝強制開幕手の位置ずれ分のみ）。∴ `enumerateRootPrefixes` のルート分散は**中位prefixでは空回り**（開幕手を変えても同じ値に戻る）・上位prefix(`factor` 一致率23.7%)のみ別解へ導く／**`CALIB_GRID`={judg,pactcore,effond} はナポ/アリアンのアビを含まない構造的欠落**／**Phase 7 のクローズ根拠（§7.2「BW64 が大域最良＝実用上限」）もエジソン依存＝再評価の対象に戻った**。**ボス再選定＝✅2026-07-30 決定**: 較正ボスを**両面宿儺 → `walpurgis_loki` へ切替**（`abilCap`/`barrier` を共に持たない・**ノーダメージ討伐＝シムの常時フルHP近似が厳密成立**・T3〜4安定反復・C7撤回済で交絡整理済／⚠隠れ耐性は未確認＝絶対値較正には乗るが成分比では相殺・def=10 は暫定）。**両面宿儺は不採用**（生存ゲート以前に `abilCapPerTurn:19` が探索の土台を壊す）。実装済みの `barrier`/`abilCapPerTurn` 機構は敵特性として残置（既定敵は null＝golden 不変）。**✅受入検証 B4 完了（2026-07-30・`tools/exp_loki_stability.mjs`・§11）**: loki 実敵で **非単調が完全に消えた**（×0.90/1.00/1.10/1.25 = 3.853/3.996/4.136/4.361億＝厳密単調）。**運用域±10%の押し順も同位置 95.7%・構成 100.0%**（宿儺cap=19 は 55.3%/29.5%・B1 cap=null でも 88.8%/85.8% ＝**B1 より良い**）。1ルート 143〜145s（B1 の256sの約6割）・FB 全条件10/10。⚠**ただし ×1.25 では同位置 52.8%**（構成 95.1%＝顔ぶれ同じで順序のみ入替）＝**根治ではなく「病理が顕在化する条件（abilCap のナイフエッジ）からの回避」**。枝刈りの代理採点という本質は手つかず＝**本 Cx は open のまま**。**一般則**: **編成を移したら探索パラメータは再測する**／**コード内の性能数値は測定条件不明なら前提にしない**（`test/golden.mjs`「edison ~2s」は実測58s＝約29倍の誤り・訂正済）。計測作法の規律は **REPO_STANDARDS §6 の E1〜E8**。**★全実験の条件・数値・解釈は `simulation/sim05/search_quality_experiments.md` を正**（BW掃引/実験1/2/4/5系/A1/コスト/未実施リスト）。本行は要約と状態の管理に徹する。 |
| C38 | **open（★押し順への影響は最大級・実装は buffCount 実機規則の確定待ち）** ← 起票 2026-07-28（旧: workspace/TODO ① から恒久台帳へ昇格） | **`buffCount` が実機と異なるものを数えている＝強化効果数tierが全て常時到達で機能停止**。シム定義は `Σ v.length（DEBUFF_KEYS 除外）`（`gamedata/js/characters.js` L4）＝**同一効果の内部スタックを1個ずつ加算**するため実機のバフ個数より大幅に過大。実測（napoleon編成・静的greedy 10T）: **T1 で既に bc=34**、T5=99、以降88〜100。宿儺config・実推奨順でも T2 以降 bc=25→92 で**全 tier が恒常到達**。膨張の主犯は `arian_bplus`(12→41・**単独で約4割**)・`legend`(2→18)・`puvoir`/`puvoir_acute`(各~12)・`arian_bcap`(4→11) ＝いずれも「1効果のNスタック」であって、ゲームの tier が数える『別種のバフ効果（アイコン）数』ではない公算。 | **最大級**。ナポレオンの4アビは**全て buffCount 閾値でスコアが切り替わる**設計（pike≥15=確実会心 / consort≥20=2hit / roy tier 6/11/16 / factor≥10=全体CD-1）だが、全 tier 常時到達のため `roy`=70・`pike`=92・`consort`=115 の**定数に張り付き＝tier による切り替えが一度も発生しない**。∴ 現行シムには「6で撃つか10で撃つか」という判断が**そもそも存在しない**。ユーザー提案の予測探索（1アビ=tier境界で即撃ち／2アビ=15未満予測なら最速／3アビ=20超え次第）は**本Cxが解決するまで実装しても縮退する**（全ルールが『即撃ち』に潰れる）＝**本Cxが予測探索の前提**。 | **解決には実機データが必要**（シム側の分解は確定済み＝`simulation/sim05/buffcount_sim_side_diag.md`）。取得項目: ①各tierの**実機での初回発動ターン**（pike確実会心/consort2hit/roy tier/factor CD-1）②**実機バフアイコン数**と数え方（同一効果の複数スタックを1個と数えるか stack数か）③味方全体バフは1個か人数分か（③は「1」で確定済・再確認）。記録先＝`simulation/sim05/data/record_skeleton.md` §B。→ 判明後に **(a) `buffCount` を『別種バフ数』へ差替**（累積系を1カウントに畳む）**か (b) 閾値を現行スケールへ再スケール** を決定し、napoleon golden を再fit。⚠**C37(b)『目的地の歪み』の一角**＝探索が完璧でも本Cxが残る限り到達先が誤る（C37 との関係: 本Cxは較正で解ける・C37(a)探索不安定性は較正では解けない＝直交）。 |
| C39 | **open（診断確定・fix は要判断＝golden 再fit を伴う）** ← 起票 2026-08-01 | **`_naOwner`（「今ダメージを出しているのは誰か」を示す一時パラメータ）がターンを跨いで残留し、かつ `clone()` がコピーしない**。現象は2つ。**(a) 残留**: `_naOwner` は `_spendGaugeAbi`／`burst`／通常攻撃が「今の攻撃者」として直前に代入する一時値だが `Sim` のフィールドなので**次に上書きされるまで残る**。`_na()` は `_naOwner` 依存の自己バフ枠を3つ持つ（ムーンコード self／アリアン2アビ self枠 `arSelf`／フレイヤ1アシ `freyja_a_*`）ため、**「直前に別のキャラが撃った」状態のまま `_na()` が呼ばれるとその別キャラの自己バフが乗る**。**(b) クローン非コピー**: `clone()` は `_naOwner` を複製しない＝**ビーム/先読みのクローンは必ず `_naOwner=undefined` から始まる**ので、本線（連続実行）とクローン（探索）で `_na()` の評価条件が食い違う。 | **小〜中（探索側にのみ効く系統ズレ）**。(b) はビームの採点条件を本線とズラすため**押し順を変えうる**（C37 の「代理採点」問題と同じ層の話だが原因は独立）。(a) はダメージ絶対値側。**影響は `_naOwner` 依存の自己バフを持つキャラを含む編成に限られる**＝**edison golden 編成では arSelf/mc/freyja_a がいずれも不成立で影響ゼロ**。 | **発見経緯（2026-08-01）**: LS のインクリメンタル replay（`_LSReplay`）の結果不変性検証で、**napoleon/configC のみ** full replay と食い違った（近傍候補400件中10件・**−893,041 / −903,029**＝総ダメ19.6億に対し約0.05%）。**edison は600件すべて1円まで一致**。全 own フィールドを turn 境界で機械的に差分したところ **`_naOwner` ただ1つ**が `full="arianrhod" / snap=undefined` で相違（`tools/exp_ls_incremental_verify.mjs`）。∴ (a)(b) はいずれも**本 LS 高速化が持ち込んだものではなく既存の挙動**。**現状の扱い**: LS スナップショットは `Sim._snapshotForReplay()` が `_naOwner` を明示的に引き継ぐことで**現行挙動を1円も変えない**（＝高速化は結果不変を維持）。**未対応（本 Cx の本体）**: 望ましい姿は (a) を「攻撃者が未設定なら自己バフ枠を適用しない」へ、(b) を「`clone()` も `_naOwner` を複製する」へ直すことだが、**いずれもダメージが変わる＝golden 再fit＋configC 較正値への波及**があるため、**C25/C3/C5 の較正を確定させるセッションで同時に扱う**（先に直すと較正スカラが差分を吸収してしまう＝A7 と同じ理屈）。 |

> 新規乖離は PHASE4_PLAN.md §3 の駆動フロー（リプレイで起点特定→ここへ起票→`simNN/design_report.md`→`integrated_analysis.md`+修正+序数フィクスチャ）で追加する。


