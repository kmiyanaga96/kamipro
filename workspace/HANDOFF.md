# 引継ぎ — 現状スナップショット

> **種別**: 引継ぎ（現状スナップショット・有界） ／ **用途**: 新セッション起動時に **CLAUDE.md と本書のみ**を読めば「今どこにいて次に何をするか」が分かる状態を保つ。
> **次タスクの詳細リスト**は `workspace/TODO.md`。過去の経緯・provenance は `archive/SESSION_LOG.md`（オンデマンド）。
> **更新規律（セッション末）**: 本書は「今」だけを短く保つ。現状化した記述は `archive/SESSION_LOG.md` の先頭へ1ブロック畳み、本書は最新状態へ上書きする（規律は REPO_STANDARDS §6）。
>
> 最終更新: 2026-08-06（セッション末・クローズ）

---

## 現フェーズ

**Phase 4 = 統計的較正 × 反復可能ボス**（PHASE4_PLAN §3.5.1）。全分析を Claude Code 担当。

**sim05 の主題は 2026-08-03 に入れ替わった**。当初＝【追撃 C3/C5】＋【tier 観測 C38】＋【鬼神障壁】。
現在＝**バーストまわりの成分別較正（C40 / C41 / C44）**。詳細は `simulation/sim05/README.md` §1。
押し順の序数検証は目的外＝**実走はフリー押しでよく、実際に押した順の記録が必須要件**。

## ✅ 直近の成果（2026-08-07）── ★**configC の幻獣枠＝カタス×2 を確定・記録した（数値は既に正しかった）**

**ユーザー申告**（2026-08-07）＝「**sim05 はメイン・サポート幻獣ともにカタス**。スキル効果量と攻撃UPバフが加わる」。

**① 結論: 再計算は不要＝既にシムへ入っていた。** 台帳 `configC_cache_20260803.json` の GEAR を逆算して確認した:
- proper v1 → v2 の枠別比が **assault/elem を除く9枠で厳密に ×10/9** ＝ `(1+weaponAmp)` が **1.8 → 2.0**
  （＝加護 0.8→**1.0** ＝ カタス `weapon_amp:0.50` **×2体**）。武器個別の強化ではこの一律比は作れない。
- **assault だけ残差 +2.00**（`3.204×10/9=3.56` に対し実際 5.56）＝ カタスの `box:{assault:1.0}` **×2体**に厳密一致。
- ∴ **pre-trial / trial01（M2）の分析数値はすべて有効**（E2 bit 一致で再確認・2,823,338,240）。**C40/C44 の結論も不変**。

**② 寄与の実測**（台帳条件・記録ルート10T・押し順固定）: 幻獣なし比 **×1.52**／サポート加護が乗らない場合 **×0.83**。
⚠ ただし `assault` は ATK 項＝**全成分の cap 前 raw を一律に動かす**ので、
**「バースト本体だけ過大・他は過小」という C25/C40/C44 の符号違いは作れない**（説明にならない）。

**③ 本当の欠陥は記録側だった**: **幻獣枠はリポジトリのどこにも書かれていなかった**
（`_configSig` は幻獣キーを持たず**畳んだ GEAR しか持たない**＝キャッシュからは後から復元できない）。
GEAR ハードコード事故（2026-08-03）と**同型の provenance 欠落**。対策として:
`gamedata/md/幻獣/catastrophia_light.md` を新設／`configC_gear_panel.md` 冒頭注記3 ／
`record_skeleton.md` §0 に**幻獣枠を必須記録項目として追加**／`config_c.mjs` に `CONFIG_C_SUMMONS`＋バナー出力。

**④ ★同日夕 ユーザーが §1 一次情報を提出（`catastrophia_light.md`）＝2件が確定した**:

- **C50 が「疑い」から「確定した限定」へ**。加護の原文＝「装備中のスキル**[レイ][シャイン][ルミナ][セイクリッド]**の
  効果量50%UP／光属性キャラの攻撃100%UP」。シムは**全ウェポンスキルに無差別**に掛けている。
  **振れ幅を実測＝最大 −26%**（全枠 2,823,338,240 → 非適用 2,087,634,872。枠別は §2.4）。
  ⚠ **まだ直せない**＝`WEAPON_MASTER` はスキルを**効果種別名**（アサルト/エクシード/…）で持ち、
  **4名の呼称がリポジトリに存在しない**。⏳ゲート＝**実機のウェポン詳細に出るスキル名**（数本で決まる）。
  ★**向きに注意: C50 はシムを下げる修正**＝仮説B が正なら C40/C41 の不足は今見えている以上に大きい。
- **registry を更新**（`SUMMON_REGISTRY.catas`）: **サブ効果 `subEffect.box.elem=0.30` を新規配線**・`atk/hp` 0→4500/900。
  **ダメージ数値は不変**（configC はカタスを**メイン＋サポート**に置く＝サブ効果は発動しない／`atk/hp` はダメージ不使用／golden は幻獣なし）。
  ⚠ 召喚攻撃「イマキュレートレーザー」は**ダメージだけでなく「ダメージ上限UP」も落としている**＝Phase 6 実装時は cap 枠への加算も要る。

**⑤ ★反撃の発動回数が確定（M7 / C45）**: ユーザー回答＝「**敵1回の攻撃に対し反撃1回。三段攻撃なら3回反撃**」。
一次情報は `gamedata/md/その他/attack_phase.md` **§6**（新設）。既存データと矛盾なし（pre-trial T3 の2発＝2ヒット攻撃）。
∴ **ダメージ式（hecate.md §1.2）＋多重度が揃い、C45 の残ブロッカーは「敵の行動モデル」だけ**になった。
⭐**副産物**＝**反撃の発数＝敵が何ヒット当てたかの観測値**。ムーンコード中は回避率ほぼ100%で被ダメが0になるため、
**反撃を数えるのが敵ヒット数の唯一の観測経路**＝M7 の記録価値が上がった。

## ✅ 前回の成果（2026-08-06）── ★**M2（trial01）を受領・分析完了。A トラックのブロッカーが外れた**

**受領**: `simulation/sim05/data/trial01.md`（キャスパリーグ・configC・全力フリー押し **41手**・**T1#41 で撃破**）。
**成果物**: `simulation/sim05/analysis/per_trial/trial01_{quant,quali}.md` ＋ **正式版統合 `simulation/sim05/analysis/integrated_analysis.md`**
（`PROVISIONAL_ANALYSIS.md` は**経緯保全へ降格**＝現在値として引用しない）。

**① C40 は敵横断でも確定**（★本 sim 最大の結論）
**障壁なし・アビ上限なし**の環境で `--cap-probe` を掛けると、**4キャラすべて実機1ヒットがシムの pre-decay raw を超える**
＝**cap / slope / ギア上限をどう動かしても届かない**。pre-trial（宿儺）・M1（エジソン×configA）に続き**3環境目**。
同一押下12点の 追加/本体 比は **実機 0.673（CV 13.2%）vs シム 0.143（CV 46.5%）**＝**実機は共通枠・シムはキャラ別式**。
⚠ **関数形はまだ決まっていない**（1ターン内でバフと本体が共線）＝**M3 が必須**。

**② C44 は分解された**（「`calib_burst` 非転移」は**要撤回**）
キャスパリーグでは **アリアン以外の3キャラが ×1.05（一致）／アリアンだけ ×0.83**。
pre-trial が同一 FB 内のキャラ間比から独立に出した **`arian_cap_boost` +100% → +30〜40%** と**別の敵・別の手法で一致**。
∴ **(a) アリアン固有＝確定に近い ／ (b) 宿儺固有の残り＝障壁/def20/A7 の側（G4.9）**。

**③ C42 確定・C47/C48 を新規起票**
実機は同ターンに **`alone` 3回 / `legend` 3回**（シム上限はどちらも2）。残るのは `holy` の真の上限だけ。
**C47**＝シムがアリアンの①バースト追加ダメと②1アシ追撃を**同一値**で加算（実機は ① が ② の **5.71倍**）。
**C48**＝judg ph2 の通常攻撃をシムは**1ヒット**、実機は**三段攻撃＝3ヒット**（発動あたり ×3.46 不足）。
→ **CLAUDE.md §5 の「`calib_na` は転移していた」を訂正した**（1ヒット ×0.99 はヒット多重度の誤読）。

**④ ハーネスを trial 汎用化**（`tools/calib_replay_compare.mjs`）
`--src` / 列レイアウト自動解決（11列・12列）／敵・表示ATK を md から／台帳駆動 config＋E2 自動検証／
**ミッドターン撃破対応**（`(攻撃フェイズ)` 行が無いターンはシムも攻撃フェイズを回さない）／
`--per-press` `--cap-probe` `--def` `--selftest`。**pre-trial の全数値はビット一致で再現**（回帰基準 T1 シム 380,681,217）。

**⑤ 「M2 は乖離が大きい（×1.59 > pre-trial ×1.41）」は誤読**（ユーザー所感への回答）
総差（net）は不足と過大の**差引**にすぎない。**絶対値の総和（gross）**で見ると:

| 走・T | gross（シム比） | net（シム比） | 相殺率 |
|---|---|---|---|
| pre-trial T1 | **69.2%** | +41.1% | 20.3% |
| pre-trial T2 | **61.5%** | −5.0% | **54.1%** |
| pre-trial T3 | **60.4%** | +5.7% | 45.3% |
| **trial01 T1** | **59.2%** | +59.2% | **0.0%** |

**trial01 は測定4ターンで gross 最小**。×1.59 が大きく見えるのは**過大成分（＝バースト本体）が消えて相殺が無くなった**ため。
⚠ とくに **pre-trial T2 の「×0.95＝ほぼ一致」は 54% が相殺**＝gross 61.5% が「一致」に見えていた。
⚠ ただし **gross は依然 6割**＝楽観材料ではない。減ったのは解釈上の混乱であって誤差ではない。

**⑥ ハーネスのバグを1件自己検出・修正**: `--def` を `loadConfigC()` の後に代入しており
`recalcGearK()` が走らず**完全な no-op** だった（→「def を動かしても比が動かない」という誤観測を生んでいた）。
修正後の掃引＝def 5/10/20/40 でシム T1 は 325.7M/256.2M/221.3M/189.0M＝**`1/def` ではなく cap が大半を吸収**。
**def をどちらへ動かしても宿儺の ×0.77 は再現しない**＝C40/C44 の結論は def の不確かさに影響されない。

**⑦ M3 の設計をユーザーと詰めた**（★次セッションはここから）
- **設計はバフ別 ablation 梯子**（0本 → 特定バフ1本 → 2本 …を各バフに対して）＝**ユーザー確定**。
  Claude Code が一度「全部盛り」と読み違えて反論したが**誤読**。手順と優先順位は [workspace/TODO.md](./TODO.md) の M3 節。
- **`knights`（エレイン3・青）が C40 の決定打**＝`burst_dmg_nights` +20%＝**バースト枠のみ**（`exec` は `buf.nights` push だけ＝クリーン）。
  ON/OFF を振って**追加ダメが +20% 追随するか**で本体連動 / 別枠が1対比較で決まる。
- **`puvoir` は純粋な cap レバーではない**（⚠ Claude Code の前版の誤り）＝`cap_puvoir` +6%/stack と
  **`elem_puvoir` +15%/stack（raw）の両方**（一次情報 `gamedata/md/神姫/hecate.md` §1 アビ1）。
  ただし **cap は等倍・raw は slope 0.04 倍**で効くので **cap/raw 比が違う2成分を読めば分離できる**
  （elem/cap 感度比＝holy 0.258 / judg ph0 0.176 / consort 0.112・見積り `node tools/exp_puvoir_lever.mjs`）。
- **★1 stack で足り、バーストも1回も要らない**（ユーザー指摘「複数 stack はバースト混入で測れない」への回答）:
  `holy` / `judg` ph0 / `consort` はいずれも **`burstTrigger` 非保持**（保持は `effond` と `elegant` だけ）
  ＝**ボタン OFF のまま押せる**。精度＝trial01 の holy 8ヒットから**ヒット間 CV 1.04%** を実測＝
  **効果 3.11% は3押下ずつで約10σ**。⚠**バフ状態を揃えることが絶対条件**。
  ⚠ `judg` は押すたびに ph0→ph1→ph2 と巡る＝**1走1回**に留める。

**⑧ C49 を起票（低優先）**: **急所枠がシムは期待値 `発動率×(倍率−1)`・実機は確定発動**。
⚠ さらに **有利属性走からは同定できない**（一次情報 ⑧「複数発動時は倍率が加算」＝**表示は二値だが実効倍率は可変**）。
**同定できるのは非有利属性の敵だけ**（急所は有利属性でしか発動しない＝確定ゼロにできる）。
**会心はズレていない**（一次情報 ⑨「何本積んでも発動時1.5倍」＝シムも rate 1.0 クランプで厳密一致）。
∴ sim05 README §4.3 の「**非会心・非急所アンカーで fit する**」原則は **retire した**。
**低優先の根拠**＝cap 拘束下では raw の効きが 1/25 に潰れ、**乖離の 5.4% しか埋まらない**。クラスタ①の後に効く。

**⚠ M2 の想定は半分外れた**: README §4.8 は「T1 撃破＝**全成分が1ターンに凝縮**」と想定していたが、
実際は **T1 で撃破＝攻撃フェイズにもターン終了処理にも到達せず**、
**betaia（C41）／DOT／反撃（C45）／ストリーク／FB バーストが1つも取れていない**。**M4 は省略できない。**

## ✅ 前回までの土台（2026-08-05・**詳細は畳んだ**）

**全文は [archive/SESSION_LOG.md](../archive/SESSION_LOG.md) の 2026-08-05 の4ブロック**。要点のみ:

1. **ハーネスの config 汚染を根治**＝15本を台帳駆動（`tools/lib/config_c.mjs`）へ移行し **E10** を制定。
   ⏳ 残＝**汚染された数値の再取得は未実施**（C37 の BW 掃引ほか）。**C43 実装後に**・優先度は A トラックより下。
2. **md 相互参照の整備 S1〜S5 完了＝運用フェーズ**（`npm run doc:check` ／ 常駐サブタスクのカウンタは TODO 冒頭）。
   ⏳ 残＝常駐が**2周**回ること（DOC_RELATION_PLAN 完了条件③）。
3. **高被参照 md の可読性整理**（CLAUDE.md 32%減）＋ **2世代前の golden 値など陳腐化を3件検出・修正**。

## ⏳ 次の一手 ── ★**M3 の受領・分析**（ユーザーが実機で測定中）

> **★新セッションはここから**（2026-08-06 セッション末の申し送り）。

**現況**: **M2（trial01）は受領・分析完了**＝A トラックのブロッカーは外れた。
**ユーザーは 2026-08-06 時点で M3 を実機測定中**＝**次セッションの第一手は M3 データの受領と分析**。

⚠ **M3 の記録では幻獣枠（メイン/サポート/サブ5）も埋めてもらうこと**（`record_skeleton.md` §0 に追加済・2026-08-07）。
⚠ **C50 の残ゲート＝実機のウェポン詳細に表示されるスキル名**（`[レイ][シャイン][ルミナ][セイクリッド]` が
どの効果種別に当たるか）。configC の10本のうち**数本で決まる**＝M3 とセットで依頼する。
⚠ **M7 は「反撃の発数」を必ず数える**（＝敵のヒット数の唯一の観測経路・attack_phase.md §6）。

**受領したときの手順**（M2 と同じ・[workspace/TODO.md](./TODO.md) の「走を受領したあと」節が正）:
<!-- doc_refs:ignore-begin ── 未作成: M3 受領時に作成するファイル群への前方参照 -->
1. `simulation/sim05/data/trial02.md` として格納（**複製と push はユーザー**）
2. `node tools/calib_replay_compare.mjs --src trial02.md --per-press --cap-probe --lever`（約3秒・config は台帳駆動＋E2 自動）
   ⚠ **ハーネスは trial 汎用化済**＝`--src` と列レイアウト自動解決でそのまま読めるはず。読めなければ様式差を先に潰す
3. `simulation/sim05/analysis/per_trial/trial02_{quant,quali}.md` を作成
<!-- doc_refs:ignore-end -->
   → [simulation/sim05/analysis/integrated_analysis.md](../simulation/sim05/analysis/integrated_analysis.md) を再統合 → Cx 更新
4. ⚠ **C40/C41/C44 の実装は M2〜M6 が揃うまで着手しない**（部分修正は較正スカラに誤差を吸わせる）

**M3 で何が決まるか**（設計の詳細と根拠は TODO の M3 節）:
- **C40 の関数形**（追加ダメが本体連動か別枠か）＝**`knights` の ON/OFF 1対比較**が決定打
- **クラスタ①**（アビ枠 cap-UP の共通欠落 ×1.48）＝**`puvoir` 1 stack を2成分で読む**
- **C44(a) アリアン**（`arian_cap_boost`）＝FB 走で最終確認
- ★**FB 行（攻撃フェイズ）とストリークを必ず記録**＝M2 が T1 撃破で到達しなかったため**M3 が唯一の機会**
- ★**§2 のゲージ5人分を記入**（trial01 は空欄＝C24 の「1ヒット +10」検算が未了）

**M3 のあとの順序**: **M5 → M4 → M6**
（`simulation/sim05/README.md` §4.8「依存順」＝**カウントで閉じる → 比で閉じる → 絶対で閉じる**。
⚠ 当初は M5 を先頭に置いていたが、**ユーザーが M3 に着手済み**なので実順序は M3 が先になった。
M5 はダメージモデル非依存なのでどちらの順でも成立する。）

| ID | 何を閉じるか | 走らせ方の要点 | 実機コスト |
|---|---|---|---|
| **M5** | **C42 の残り**（`holy` の真の上限・`knights`）＋ **C38**（tier） | CD0 の `holy`/`alone`/`legend`/`knights` を押せなくなるまで連打。**毎押下で契晶を記録**し、押せない理由を CD / 契晶 / 上限 で書き分ける。⚠**`alone` 3 / `legend` 3 は trial01 で確定済**＝残りに絞ってよい | 1〜2走（当初想定より安い） |
| **M3** | **C40 の関数形**（本体連動 か 別式か）＋ **C44(a) アリアン** | **バーストボタン OFF で溜め**、黄アビの本数を変えた4条件で**1ターンだけバースト**。本体と追加ダメを**別行で**記録。★**FB 行（攻撃フェイズ）も必ず記録**（ストリーク／5人バーストの唯一の機会） | 3〜4走 |
| **M4** | **C41**（betaia cap）＋ DOT ＋ **C45**（反撃） | **ボタン OFF のまま赤アビを一切押さない**。HP 4億で溶けないようにするのが設計の要。⚠**trial01 が turn-end 成分をゼロしか出していない＝M4 が唯一の在庫源**（省略不可） | 1〜2走 |

⚠ **M7 は全走に相乗り**（反撃の押下/値/キャラ＋**直前の敵の行動**）。
⚠ **バーストボタン OFF はシムに無い（C46）**＝そのターンは C46 の影響下と明記して測る。

## 3トラックとゲート状況

| トラック | 内容 | ゲート |
|---|---|---|
| **A. ダメージ較正** | **C40 / C41 / C44**（＋C42/C43）／C3/C5 は縮小／C45/C46/**C47/C48/C49** は新規。★**14本だが原因は7クラスタ**（`simulation/sim05/analysis/integrated_analysis.md` §1.5） | ✅ M2 完了 → ⏳ **M3 測定中 → M5 → M4 → M6**（宿儺での fit は変数過多＝G4.9 へ後ろ倒し） |
| **B. 探索の安定化** | C37 | ✅ LS 実装＋ボス切替で回避。⚠**根治せず**（代理採点は手つかず）＝open。**C43 が入ると探索空間が変わる＝測り直し** |
| **C. 押し順・tier** | C38（buffCount）・予測探索 | ⏳ 実機データ待ち。**実機では tier 切替が実在すると確認**（bc<15→≥20 が同一ターン内で動く）。⚠**trial01 では T1#8 で既に `pike`（bc≥15）が成立**＝立ち上がりは pre-trial 観測より速い |

## アクティブ作業ライン

| ライン | 状態 | 次アクション |
|---|---|---|
| **★M3 → M5 → M4 → M6** | ✅ M2 完了（2026-08-06）／⏳ **M3 はユーザーが実機測定中** | **次セッションの第一手＝M3 の受領と分析**。手順は TODO 冒頭。⏳ **`configC_slot.json` は依然未受領**（走行は受領キャッシュから復元できているので非ブロッカー） |
| **宿儺での最終検証（G4.9）** | ⏳ **後ろ倒し** | ≦19手の走を1本／C43 は「≦19手運用」で無期限に後回し可 |
| **★メタトロン A7（AnotherLink 重複規則）** | ⏳ **M6 で閉じる** | `final_dmg` 1.10 vs 1.21。**サブ枠は `[metatron, artemis]` で確定**。⚠ **`calib_burst` の configA/configC 食い違い 13% の第1候補**（1.21 が正なら configC 0.800→0.73 で configA 0.705 に近づく）。⚠ trial01 で「本体は宿儺だけ過大」と分かったため、**A7 は (b) 宿儺固有の残りを説明する候補**という位置づけに変わった |
| **★他ハーネスの config 汚染** | ✅ **2026-08-05 根治**（15本を台帳駆動へ・E10 制定） | ⏳ 残＝**汚染された数値の再取得**（C37 の BW 掃引ほか）。⚠**C43 実装後に**（探索空間が変わる）・優先度は A トラックより下 |
| **★override の再fit** | ⏳ **未履行・ただし着手禁止** | C39 でモデルが動いたが、**C40/C41/C44 でまた動く**＝二重fitになるので**確定後に1回だけ** |
| **長時間ジョブ** | ✅ ①②並列化済 | 残＝③LS内部の投機並列・他ハーネス横展開・ローカル移行 |
| **Phase 8 アクセ実装** | ⏳ intake ゲート | §6 intake をユーザーと確定後 |

→ 全タスクの優先順・チェックは **`workspace/TODO.md`**。

## ドキュメント・ポインタ（詳細は必要時のみ）

- **開発ルール・コード地図・確定仕様・検証・実測コスト**: `CLAUDE.md`（本書と対で必読）
- **較正の確定値・根拠・Cx バックログ**: `CALIBRATION_ANALYSIS.md`（**C37/C38/C40〜C49 が open**。★**Cx は 14本だが原因は7クラスタ**＝`simulation/sim05/analysis/integrated_analysis.md` §1.5）
- **攻撃フェイズの仕様（一次情報・2026-08-03 ユーザー回答）**: `gamedata/md/その他/attack_phase.md`
  （バーストと通常攻撃は**排他**／ボタンOFFで溜められる／**反撃は被回避でも発動**／**ゲージは1ヒット +10**＝**C45/C46/C24**）
- **sim05**: `simulation/sim05/README.md`（**§4.4=較正ボス2段構え / §4.4.2=T1局在[決着済] / §4.6=tier観測 / ★§4.8=アイソレーション走 M1〜M7 / §6=残ゲート / §7=受入基準**）
  ／**`simulation/sim05/analysis/integrated_analysis.md`（★統合＝ここから読む・統合判断の正）**／`simulation/sim05/analysis/m1_history_replay.md`（★M1 の全成分突合）
  ／`simulation/sim05/analysis/per_trial/trial01_{quant,quali}.md`（★M2）／`simulation/sim05/analysis/per_trial/pre-trial_{quant,quali}.md`
  ／`simulation/sim05/data/trial01.md`・`simulation/sim05/data/pre-trial.md`（実機原本）／`simulation/sim05/data/record_skeleton.md`（記入テンプレ）
  ／⚠`simulation/sim05/analysis/PROVISIONAL_ANALYSIS.md` は**経緯保全へ降格**（現在値として引用しない）
  ／**`simulation/sim05/data/configC_gear_panel.md`（★冒頭注記2＝GEAR の正）**／**`simulation/sim05/data/configC_cache_20260803.json`（config の正・E2 通過）**
- **計測ハーネス**: `tools/`（`tools/README.md` が索引・**§0 に並列実行**・**★§0.5 に config の台帳駆動（`tools/lib/config_c.mjs`）**・**`calib_replay_compare.mjs` が sim05 の較正方式そのもの**＝2026-08-06 に **trial 汎用化**（`--src` / 列レイアウト自動解決 / ミッドターン撃破対応 / `--per-press` `--cap-probe` `--lever` `--def` `--selftest`）／`tools/exp_puvoir_lever.mjs`＝M3 の設計支援）
- **実験・計測の作法**: `REPO_STANDARDS.md` §6 の **E1〜E10**（着手前に通す。**★E10=config は台帳から読む**）
- **探索品質の実験の全数値**: `archive/SEARCH_QUALITY_EXPERIMENTS.md`（C37 の根拠アーカイブ）
- **新キャラ/幻獣の一次情報と Ax**: `gamedata/md/神姫/metatron.md`（§3.2 A0〜A10）／`gamedata/md/幻獣/`
- **Phase 一覧**: `ROADMAP.md` ／ **現行フェーズ**: `PHASE4_PLAN.md` ／ **規約**: `REPO_STANDARDS.md`
- **過去セッションの経緯**: `archive/SESSION_LOG.md`（**2026-08-03 ブロックは冒頭の訂正対応表から読む**）

## 検証（作業後は必ず）

```bash
npm run test:golden   # edison/raw 202,005,923・edison/cal 215,161,915・napoleon/static 299,523,354（全 FB10/10）
```
⚠**実測 2分07秒**（fixture 並列。`--serial` で逐次・`--fixture <name>` で単体）。**背景実行を推奨**。docs のみの変更なら golden は不変。
`_replayResult`/`_execKey`/`clone`/`_snapshotForReplay` を触ったら **`node tools/exp_ls_incremental_verify.mjs`（約4分）も回す**。

---

## 更新履歴

<!-- 直近5件のみ（それ以前は git log）。「波及確認」列が本体＝git が持たない情報はここだけ。 -->

| 日付 | 変更点 | 波及確認 |
|---|---|---|
| 2026-08-07 | **configC の幻獣枠＝カタス×2 を確定・記録**（ユーザー申告）。直近の成果を差し替え／M3 依頼事項に幻獣枠と加護テキストを追加／**C50 を起票** | **★申告内容は既に台帳 GEAR に入っていた**＝逆算（9枠一律 ×10/9 ＋ assault +2.00）で裏取りし、**E2 bit 一致**で既存分析の有効性を確認した。∴ **再計算・再fit は発生しない**。新設 `gamedata/md/幻獣/catastrophia_light.md`（§1 未受領）。同時更新＝`gamedata/md/幻獣/README.md`・`CALIBRATION_ANALYSIS.md`・`configC_gear_panel.md` 注記3・`record_skeleton.md` §0・`tools/lib/config_c.mjs`・`summons.js`（コメントのみ）。golden 3/3 不変 |
| 2026-08-06 | **セッション末クローズ**: 次の一手を「**M3 の受領・分析**（ユーザー測定中）」へ差し替え／M3 設計の確定事項（`knights`・`puvoir` 1stack・バースト不要）と **C49** を追記／2026-08-05 ブロックを SESSION_LOG へ畳んだ（232→約190行） | **次セッションは本書の「次の一手」から入れば足りる**ように書いた。数値は全て `node tools/calib_replay_compare.mjs` で再現確認済（pre-trial 3T・trial01 T1 とも記録値と一致）。golden 3/3 不変 |
| 2026-08-06 | **M2（trial01）受領・分析完了**を反映（直近の成果／次の一手／3トラック／アクティブ作業ライン／ポインタ） | 統合の正を `PROVISIONAL_ANALYSIS.md` → **`integrated_analysis.md`** へ切り替え（前者に降格注記）。CLAUDE.md §5 と較正ステータス索引、CALIBRATION_ANALYSIS の C30/C40/C42/C44＋**C47/C48**、sim05 README §4.8/§6、tools/README を同時更新。golden 3/3 不変 |
| 2026-08-05 | 末尾ブロックを新設（DOC_RELATION_PLAN S4・種別=現状スナップショット） | 参照関係は `npm run doc:check` がグリーン |

<!-- doc_refs:begin ── 自動生成。手で編集しない（node tools/doc_refs.mjs --write が再生成する） -->
## この md を参照している文書（現役層 4）

- [CLAUDE.md](../CLAUDE.md)
- [DOC_RELATION_PLAN.md](../DOC_RELATION_PLAN.md)
- [REPO_STANDARDS.md](../REPO_STANDARDS.md)
- [workspace/TODO.md](./TODO.md)

_他に 凍結sim/archive/essays から 1 件（更新対象外）_
<!-- doc_refs:end -->
