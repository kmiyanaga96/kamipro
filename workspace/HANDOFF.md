# 引継ぎ — 現状スナップショット

> **種別**: 引継ぎ（現状スナップショット・有界） ／ **用途**: 新セッション起動時に **CLAUDE.md と本書のみ**を読めば「今どこにいて次に何をするか」が分かる状態を保つ。
> **次タスクの詳細リスト**は `workspace/TODO.md`。過去の経緯・provenance は `archive/SESSION_LOG.md`（オンデマンド）。
> **更新規律（セッション末）**: 本書は「今」だけを短く保つ。現状化した記述は `archive/SESSION_LOG.md` の先頭へ1ブロック畳み、本書は最新状態へ上書きする（規律は REPO_STANDARDS §6）。
>
> 最終更新: 2026-08-18（セッション末・クローズ）

---

## 現フェーズ ── **Phase 9（実機観測 intake の自動化）**

**sim05（Phase 4）は凍結**し、転記の自動化基盤を先に作る（2026-08-07 ユーザー決定）。
**憲法**＝「**観測と判断はユーザー／導出・分析は Claude Code／転記・検算・整形はツール**」。
系として「**既に記録されたものから導けることは二度と人に訊かない**」。

**計画の正＝[PHASE9_PLAN.md](../PHASE9_PLAN.md)**（一次設計＝`TRANSCRIPTION_DESIGN.md`）。

## 進捗

| 段 | 状態 |
|---|---|
| **P0** 先行整備 ／ **§10** 確定仕様 | ✅ 完了（⚠§10 の変更にはユーザー承認が要る） |
| **P1** 実測ゲート | ✅ **通過（2026-08-14）＝進む**。撤退条件3つとも非該当 |
| **P2** 抽出基盤 | ✅ **実装は完了**。P2-1 ✅ / P2-1b ✅＋**除振 ✅** / P2-3 ✅ / **P2-5 ✅** / **P2-5b CT 🔶（検出器の穴を3つ塞いだ＝v0.19.0・居場所と点灯は未確定）** / P2-2 ❎close / P2-4 ➡️P3-2。⏳ **残るは CT の居場所の確定だけ** |
| P3〜P5 | ⏳ OCR＋検算 → **P3-2 イベント畳み込み（旧 P2-4）** → config 版管理 → 受入 |

**成果物**: `transcribe/index.html` ＋ `src/transcribe/*.js`（12本）。Vite マルチページ・
**シム本体と非結線＝golden に非干渉**。回帰は **`npm run test:t1`（249件）**。

## ⏳ 次の一手 ── **走査1回（v0.27.0）**

```bash
git pull && npm run dev
```
→ 120秒走査（`version` **`0.27.0`**）。見るのは **`## CT` の `★★色づいた区間`**。

★**期待する答え（ユーザー確定）＝1個目だけが 3回**。
それ以外（2〜5個目に区間が出る／`coin` が 3 以上）が出たら**まだ演出を拾っている**。
⚠ 各区間の **`bg`**（差し引いた画面全体の色）も見る＝大きければ演出の最中。

## ⚠⚠ 2026-08-18h ── **v0.26.0 の区間は CT ではなく演出だった（撤回）**

★ユーザー指摘＝**M3-1.mp4 の CT 点灯は 5個中 1個だけ・それが 3回**。
実際に出た区間は **8個中 7個が `coin=5`**（＝全ピップ同時＝画面全体の演出）だった。

**原因＝色みの層が4つあった**:

| 層 | 値 | 正体 |
|---|---|---|
| 灰 | ≈ −1.5 | 消灯 |
| 全体演出（弱） | 13〜20 | 画面全体（**谷も色づく**） |
| ★**CT の点灯** | **100〜130** | **ピップだけ**（1個目 t≈51〜57 / 117〜120） |
| 全体演出（強） | 160〜205 | 画面全体（**谷も色づく**） |

大津法2段の切れ目は **140.25** に落ち、**★CT の点灯を切り捨てて強い演出だけを拾った**。
⚠ **層が増えるたびに段を足すのは、際限がないうえに毎回外す。**

⭐⭐ **正しい切り分けは「大きさ」ではなく「どこが色づいたか」**:
**全体演出はピップも谷も等しく色づけるが、CT の点灯はピップだけを色づける**。
∴ **谷（山と山の中間）の色みを差し引く**＝**共通モード除去**。
これは新しい閾値ではなく**参照点の選び方**で、**演出が何段階あっても消える**。
差し引いた後は灰と点灯の2層しか残らないので、**大津法は1段でよい**。

⚠ **参照に「他のピップ」を使ってはいけない**＝**全部点灯したとき（CT 最大）に差が消える**。
谷なら**何個点灯していても**常に参照になる。

★**合成で通しで確認済み**（実測の4層をそのまま再現）:
**弱い演出も強い演出も 0 区間**、**1個目だけに 2区間**（2.0秒と 0.17秒）、他のピップは 0、`coin=1`。

## ★★2026-08-18g ── **色で読めた。モードゲージは完全に、CT も点灯を捕捉**## ★★2026-08-18g ── **色で読めた。モードゲージは完全に、CT も点灯を捕捉**

### ✅ モードゲージ ── **読めた**（色みの段差がきれい）

| 標本 | 色み（R−B） | 判定 |
|---|---|---|
| t=5 | 全域 ≈ −1 | **空** |
| t=39.3 | bin 4〜113 が **96〜103**、bin114 で 91→54→7 | **満** |
| t=73.7 | 全域 ≈ −1 | **空** |
| t=107.8 | bin 3〜112 が **92〜102** | **満** |

★**満 ≈ 102 / 空 ≈ −1 のきれいな段差**＝**HP バーとまったく同じ階段フィットで塗り率が読める**。
∴ v0.26.0 で **毎フレームの塗り率＋その時系列**を出すようにした（`chromaFillSeries`）。
⭐ 走全体の平均色みは **左 69〜73（=0.68〜0.72）／右 51〜52（=0.50）**＝
**左ほど長くオレンジ＝ゲージは左から溜まる**（右端まで届くのは全体の約半分の時間）。
★ユーザー確定情報より、これは**与えたダメージの観測値**になる。

### ✅ CT ── **点灯を捕捉した**（ただし信号は2種類ある）

| 種類 | 見え方 | 例 |
|---|---|---|
| **画面全体のオレンジ演出** | **5個すべてが同時に 13〜20** | 11 バケットで観測（t=9,15,21,30,36,75,81,87,96,102…） |
| ★**ピップの点灯** | **1個だけ 104〜130** | **1個目が t≈51〜57 と t≈117〜120** |

★**大きさで分かれる**（17前後 vs 100超）。輝度の標本 t=56.4 / 124.9 で見えた 210/198 と一致する。
⚠ **2〜5個目も p95 が 78/73/37/49 と高い**＝**短い点灯がある**が、
**3秒バケットの平均では薄まって消える**（バケット平均の最大は 20）。
∴ v0.26.0 で **フレーム解像度の「色づいた区間」**に切り替えた。

### ⚠ テストが出荷前に設計の欠陥を捕まえた ── **大津法は2クラス法**

切れ目を大津法で決めたが、**実データは3層**（灰 ≈−1.5 ／ 演出 13〜20 ／ 点灯 70〜130）。
1段だけだと切れ目が **16.8** に落ち、**演出が点灯側に混ざる**。
∴ **上位クラスへもう一度かける**（`otsuSplit2`）＝演出と点灯の間に入る。**両方の切れ目を出力に載せる**。
⭐ **合成テストで先に踏んだので出荷せずに済んだ**（実走 14分を1回節約した）。

## ★2026-08-18f ── ユーザー確定情報（これが読み方を決めた）## ★2026-08-18f ── ユーザー確定情報（これが読み方を決めた）

| 項目 | 内容 |
|---|---|
| 見た目 | **モードゲージも CT もオレンジ**（互いに別の色） |
| モードゲージ | **与えたダメージに応じて蓄積** |
| CT | **ターン1回につき1つ蓄積** |
| ★**M3-1.mp4 での観測** | **バースト攻撃中・攻撃後に、モードゲージと CT の点灯を観測済み** |

⭐ **最後の1行が効いた**＝「この録画では CT が動かなかった」という可能性が**消えた**。
∴ **動いているのに我々が取れていない**＝**測り方の問題**と確定した（輝度では表されていない）。

∴ v0.25.0 で **色みの時系列**（山ごと・3秒バケット×40）と**色みプロファイルの標本**を出す。
合成で **途中でオレンジに変わる山の立ち上がりが時系列に出る**ことを確認済み
（前半 ≈0 → 後半 大／消灯のままの山は分布が割れない）。

⚠ **digest を1つ畳んだ**＝**帯ごとの探索（8ブロック）を要約1行に**。
これは「CT が縦のどこにあるか**探す**」ための出力で、**採寸方式へ移行して役目を終えた**。
⭐ **役目を終えた出力を出し続けると、判断に効く行が埋もれる**。生データは完全 JSON に全部残る。
実測見積り＝digest **約 8,800字**（畳む前は 10,000字）。**絶対量の番人（12,000字以下）を新設**した。

## ✅ 2026-08-18e ── **CT は 5個で確定。左端の構造はモードゲージの右端だった**

★**2つの ROI を突き合わせて決着**（`modebar` x 361〜706 と `ct` x 693〜855 は 13px 重なる）:

| canvas x | modebar の値 | ct の値 | 差 |
|---|---|---|---|
| 693 | 74.3 | 78.0 | −3.7 |
| 699 | 63.9 | 63.7 | **+0.2** |
| 703 | 64.0 | 63.0 | +1.0 |

★**別々に手で採寸した2つの ROI が、重なり領域で一致した**。
そして **modebar の右端（bin 108〜119）は 100→64 へ単調に減少＝山は無い**。
∴ **`ct` の左端の盛り上がりはモードゲージの右端が消えていく裾**であって、6個目のピップではない。
⭐ **CT のピップは 5個で確定**（canvas x = **716 / 738 / 760 / 782 / 804**・間隔 **22.01px・CV 0.37%**）。

## ⚠ 2026-08-18e ── **点灯は「輝度」では表されていない**

山ごとの明るさの分布（走全体）:

| 山 | p25 / p50 / p75 | p95 | 明るいフレームの割合 |
|---|---|---|---|
| 1 (23.2) | 119.7 / 119.9 / 120.3 | 197.5 | **0.1467** |
| 2 (45.1) | 119.9 / 120.1 / 120.5 | 178.9 | 0.0843 |
| 3 (67.2) | 119.8 / 120.0 / 120.3 | 168.3 | 0.0866 |
| 4 (89.3) | 120.0 / 120.1 / 120.4 | 172.6 | 0.0855 |
| 5 (111.3) | 119.9 / 120.0 / 120.3 | 161.7 | 0.0883 |

★**5つとも p25〜p75 が 120±0.5**＝**ほぼ常に同じ明るさ**。
★★**明るいフレームの割合は 2〜5個目が 0.084 / 0.087 / 0.086 / 0.088 と横並び**＝
**単調に減る階段になっていない**。充電が進むなら「1個目 ≥ 2個目 ≥ …」と減るはずで、そうなっていない。
∴ **平均輝度は充電の状態を表していない**（1個目だけ +6pt 高いのと p95 の左下がりは、左寄りのアニメの痕跡）。

⭐ **残る仮説＝点灯は色で表されている**（`lum()` は色を潰す＝輝度が同じでも色は違いうる）。
∴ v0.24.0 で **R/G/B と色み（R−B）**を出すようにした。合成で
**輝度がほぼ同じ暖色と灰色を R−B が桁で分ける**ことを確認済み（60 vs −2.4）。
⚠ **モードゲージにも同じ疑い**＝輝度は bin 3〜113 が **99〜110 で平坦**（塗り境界が見えない）。

⚠ もう一つの可能性は「**この録画の 120秒間、CT が動かなかった**」＝色でも差が出なければそちら。
★どちらでも次の一手は同じ（色を見る）。

## ✅ 2026-08-18c ── **v0.22.0 実走。HP は完全復旧、CT の「7枠」は撤回**

**①HP は旧採寸の水準へ完全復旧** ＝違反 **5**／読めた **3084**／`flash` **109**／`noEmptyRegion` **315**。
違反の時刻（12.30 / 51.60 / 69.63 / 72.52 / 118.25）も v0.18.0 と一致＝**`hpbar` 撤回は正しかった**。
★新設の `捨てたフレームの中身` が効いた＝`flash` フレームは **peak=0 / red=0 / 山ゼロ**＝
**ROI が本当に真っ暗**＝演出であって採寸の問題ではないと確認できた。

**②⚠ 前回の「CT は 7枠・5点灯」は誤りだった（撤回）**。全120値を見ると:

| 領域 | 中身 |
|---|---|
| bin 12〜88 | ★**5つの山**（重心 17.40 / 33.65 / 49.98 / 66.38 / 82.59）。**間隔 22.00px・CV 0.4%** |
| bin 91〜119 | **56〜63 の平坦**（幅わずか 7）＝**山は1つも無い** |

★**「6・7枠目」と読んだ 99/108/110/114/116 は、平坦な背景の上の ±1 のノイズ**だった。
⚠ 原因は **digest がプロファイルを「上位10山」に畳んでいた**こと＝
**局所最大は、平坦なところでも必ず見つかる**。
⭐ **山は「局所最大か」ではなく「どれだけ高いか」で数えるべき**＝
`rawHumps()`（**振幅の中点でしきり、連結した塊の重心**）へ変更。実データで 5個・CV 0.45% を再現。

**③★ただし `ct` ROI は左端で切れている疑いがある**（未決着）:

| | 値 |
|---|---|
| 山と山の谷（bin 26/42/58/75） | 平均 **67.0** |
| **左端**（bin 0〜9） | 78, 73, 68, 67, 65, 62, 62, 63, 63, 63 ＝水準 **63.0**（谷とほぼ同じ） |
| **右端**（bin 100〜119） | 水準 **56.6**（谷より **6.4 低い**＝これが真の背景） |

★**左右が非対称**＝右端は背景だが、**左端は「谷」の水準で、しかも端に向かって上昇している**（bin 0 で 78）。
∴ **ROI の左外に山がある**（外挿位置 canvas x≈694＝ROI 左端 693 のすぐ外）。
⚠ **ただし `modebar`（x 361〜706）と `ct`（x 693〜855）は 13px 重なっている**＝
その構造は **6個目のピップかもしれないし、モードゲージの右端かもしれない**。
★**決着方法＝`modebar` にも同じ生プロファイル抽出をかける**（v0.23.0 で実装済み・追加の走査コストはほぼ無い）。

**④⚠ `sigmaProfile` は設計の逆に振る舞う**＝山の中心で **29.6〜30.4（最小）**、谷で 36〜38、背景で 39.9。
**ピップは最も安定した列**だった。∴ σ は「変化した列」の localizer**ではない**が、
**「動かない UI 要素」の検出器としては有効**（意味を読み替えて使う）。

**⑤点灯のエンコードは未確定**。5つの山は平均輝度では**ほぼ同じ（126〜133）**で 120秒ほぼ不変。
⚠ ただし標本 t=56.4 と t=124.9 で **1番目の山だけ 210/198 に跳ぶ**。
∴ v0.23.0 で **山ごとの明るさの分布（p05〜p95・max・明るいフレームの割合）**を出すようにした＝
**二峰かつ明るい割合が山ごとに階段状なら「明るい＝点灯」**が確定し、**それが CT の時系列そのもの**になる。

## ⚠ 2026-08-18b ── **HP は `hpbar` の採り直しで退行したので戻した**

同一コードで **ROI だけ**を変えた結果:

| | 旧 640×54（y 121〜175） | 新 650×29（y 117〜146） |
|---|---|---|
| `flash` | **109** | **2152**（20倍） |
| 読めた | 3085 | **1020** |
| 単調性違反 | **5** | **36** |

★**原因は `hp_bar.js` の帯の取り方**＝帯は **ROI の上下端**（0.06〜0.26 / 0.74〜0.94）を見るので、
**ROI が「バーぴったり」だと帯がバーの縁に乗る**（新 ROI は上下とも縁から 1.7〜7.5px）。
⭐ 旧 ROI の下帯（y 161〜172）は**バーの外**だが**無害**だった＝階段フィットは**絶対水準に依存しない**。
∴ **`hpbar` は 2026-08-14 の値へ戻した**。実寸は `HP_BAR_TIGHT` として保存（帯を内側へ寄せる作業の一次情報）。

★**診断の欠陥を1つ塞いだ**＝flash が 20倍になったのに**捨てた側の生データが無く、推測でしか
説明できなかった**。∴ `hpSkipSamples`（原因ごとに最初の3件の生プロファイル）を追加。
⭐ **「読めなかった」は最も情報量の多い出来事なのに、それを捨てていた。**

## ✅ 2026-08-18 ── 採寸で決着。**「バーの目盛り」は CT だった**

ユーザーが4枠を採寸（`M3-1.mp4` t=23）。画素へ落とすと **`ct` = canvas x 693〜855 / y 138〜175**。
★**2026-08-17 に「バーの目盛り・模様」と判定された等間隔6山は x 700〜802**＝**`ct` の中に完全に入る**。

⚠ **誤りの本体は回答ではなく質問だった**。旧 `hpbar` は **54px 高**で、実際のバー（**29px**）の
**下にある CT の行まで含んでいた**＝`charge_dots.js` が見ていた中央帯は **y 137〜159＝CT の行そのもの**。
「**HPバーの中の**構造ですが CT ですか」と訊けば「バーの模様でしょう」と答えるのが自然です。
**検出器は最初から正しい場所を見ていました。**

⭐⭐ **教訓＝1つの回答から恒久的な抑止規則を作らない**。2026-08-17 は「誤報告は見つからないより悪い」への
対策としてその位置を `knownDecorX` に**ハードコードして抑止**しましたが、これは**静かで恒久的な
ブロッカー**になり、しかも新しい `ct` ROI では**中央付近＝ピップそのものを拒否する**規則でした。
∴ **撤回**（既定 `null`・機構だけ残す）。★位置の問題は**抑止規則ではなく採寸**で解きます。

**★合成での事前確認が、本番前に読み取り経路の誤りを捕まえた**（採寸された実寸 162×37px で実行）:

| 経路 | 結果 |
|---|---|
| `found`/`bestPeriod`（|高域通過|＋自己相関） | ❌ 5・6個で found=false／7個で**周期を約半分**（縁）に取り個数も誤る |
| ★**`rawProfile`（生の輝度）** | ✅ **6ケース全部で正解**（5個→山5つ 21.6px／6個→山6つ 21.1px・真値 21.3px） |
| ★**`sigmaProfile`（時間σ）** | ✅ **状態が変わった列だけ**が立つ＝点灯のエンコードを仮定しない位置特定 |

★理由は構造的＝`meanProfile` は |高域通過| なので**山がドットの縁に立つ**。
**ROI が要素そのものになった今は、生の輝度をそのまま読むのが正しい**。
⚠⚠ **ここで作り込みを止めました**＝合成の見た目は**私の推測**で、調整を続けると**推測に過適合**します。
∴ 判定は確定させず、**走査1回が答えられる生データ**を出すようにしました。

## （参考）採寸で解消した食い違い



**2026-08-14**「バー上の丸ドット5個＝CT」と **2026-08-17**「等間隔6山は目盛り」の食い違いは、
**2026-08-18 の採寸で決着**（上記）＝**2026-08-14 が正しく、2026-08-17 の判定が誤り**だった。
⚠ 当時立てた仮説 (a)/(b) は**どちらも外れ**＝正解は「**旧 `hpbar` の採寸が CT の行まで含んでいた**」。
★**採寸せずに仮説を立てていたから、選択肢の中に正解が無かった**。

## ★2026-08-18 ── 実走 v0.18.0 は「見つからない」と答えた。**それは検出器の可視範囲の外の話だった**

走査自体は正常（119.9秒 / 3510フレーム / 29.27fps / FATAL 0 ERROR 0 WARN 3）。
帯探索は**8帯すべてが「低い」か「decor」**＝**事前に決めた読み方なら「CT は ROI の外」**だった。

⚠⚠ **その読み方を適用しなかった。測定器の可視範囲を先に確かめたら、穴が2つあった。**

**①★★探せる周期の上限が 43px に潰れていた**（v0.17.0 で帯探索を足したときに入った）。
帯探索は**固定幅 `0.25×W` の窓**で自己相関を測るので、「窓に4周期入ること」と掛かって
上限が **W/16**＝`hp` 687px なら **43px**。★**指紋は結果に出ていた**＝**8帯中5帯が上限側の 43/43/43/41**
（最良値が探索範囲の端に張り付くのは、範囲の外に真の構造があるときの典型）。
⚠ **一次情報の観測は「バー上の丸ドット5個」**＝640px のバーなら間隔 **≈107px＝最初から範囲外**。

**②平坦な帯が偽の構造を作る**。空の帯は高域通過後の残差が 1e-14 級の浮動小数のゴミになり、
「分散1」へ標準化すると **score 0.995 が最良候補に浮上する**（合成で、空の帯が本物のドット列 0.50 を
押しのけて1位になった）。実走では帯に中身があり顕在化しなかった＝**ROI を広げていたら踏んでいた**。

**★直したのは閾値ではなく「窓の決め方」**（v0.19.0）＝窓幅 `max(4×period, 0.25W)`
＝**試している周期ごとに窓を広げる**。「4周期入ること」という成立条件は保ったまま上限だけ外れる（17〜171px）。
あわせて ③**周期は自己相関で見つけ、櫛フィルタで測り直す**（整数ラグは繰り返し4〜5回で数%外し、
累積して**5個を3個と数えていた**）／④**等間隔の山の列を自己相関に依らず直接拾う**（`peakRun`）。

| | v0.18.0 | **v0.19.0** |
|---|---|---|
| 帯探索が間隔 106px のドット列を報告 | ❌ 43/17 しか返せない | ✅ **P=106** |
| `solveGeometry`（687px ROI・5ドット） | ❌ found=false（0.3253 < floor 0.35） | ✅ found=true・**dotCount 5** |
| 空の帯 | ⚠ score **0.995**（偽） | ✅ score 0 |

⭐⭐ **教訓＝「見つからない」を結論にする前に、測定器が何を見られるのかを確かめる**。
2026-08-17 の教訓（**見つけた ≠ 探していたものを見つけた**）の裏返しで、**範囲外の「無し」は情報ではない**。
∴ **`searchRange` を digest に常時表示**するようにした＝可視範囲を測定結果と同じ画面に置く。

⚠ **digest から読み取れた他のこと**（いずれも既存の結論を動かさない）:
- **モード遷移は 生18 → 除振後8**。★**残った8件は 2026-08-17 と時刻まで一致**（滞在 2.27/1.93/7.15/1.92 秒）
  ＝**生の本数が 12→18 とぶれても除振の出力は再現する**＝除振の設計が効いている追加証拠。
- **HP 違反 5 件**も時刻まで一致（t=12.30 / 51.60 / 69.63 / 72.52 / 118.25）＝**HP 抽出は完全再現**。
- ⚠ **`lifetimeFrames` は 2 → 3 になった**（完全重複 231 枚を除いた後の値）。
  ★**「重複を除けば L は 2 以下・おそらく 1」という予測は外れた**（上がった）。
  ⚠ ただし**結論は動かない**＝`stride = floor(L/2)` は 1 のままで間引きは不可能・削減は P3-2 の担当。
- Fisher 分離 **1.966**（既知の 1.92〜1.96 帯）＝**P2-2 は close のままでよい**。

## ★2026-08-17 の実走で分かったこと（重要な軌道修正）

**① モード遷移の除振は予測どおり効いた** ✅
生 12 → 除振後 **8**。落ちた4件は **33ms の往復ちょうど2組**（t=28.45 / 110.03）＝
★**事前に「人には不可能な速さだから偽」と名指ししていたものが、そのまま落ちた**。
残った8件の滞在は **2.23 / 1.93 / 7.15 / 1.92 秒**＝すべて本物の操作の尺度。

**② ⚠⚠ CT ドットだと思って見つけた構造は、CT ではなかった**
`hpbar` の中央帯に **6つの山が 21.3px 間隔**（x 341〜443px＝バー幅の 53〜69%）で並んでいたが、
★**ユーザー確認の結果「バーの目盛り・模様」**＝**CT ドットではない**。

⭐**ここで得た一番大事な教訓**＝**「等間隔の構造を見つけた」は「探していたものを見つけた」ではない**。
UI には**探しているもの以外の等間隔構造がふつうにある**（目盛り・模様・文字の字送り）。
∴ **見つけたと報告する前に、それが何かを確認する**。
⚠ **誤報告は「見つからない」より悪い**（偽の CT ドットを作り、その上に較正を積んでしまう）。

**対処（v0.17.0）**:
- **既知の装飾領域（バー幅 50〜72%）を記録**し、そこで見つけても `found:false, decor:true` と返す
  （provenance＝ユーザー確認 2026-08-17・**1点観測**なので別の敵・UI 更新では未再測）。
- ★**縦方向にも探す**＝ROI 高さを8本の帯に割り、**どの帯に構造があるかを走査に答えさせる**
  （どの行にあるかを人に訊く代わりに）。
- 探索対象を `hpbar`（バーのみ・高さ54px）から **`hp`（バーの上下を含む広い枠）** へ広げた。

**③ 検出の「探し方」も2つ直した**（②の前に見つけた件）
周期 21.3px は**探索下限 45px の外**だった／構造がバー幅の 16% にしか無く**全域では希釈**
（-0.047 → 窓を切ると 0.477）。∴ 下限を 1/40 へ拡大＋窓を切って探索
（窓には「周期が4回以上入ること」を要求＝`panel_mode` と同じ根拠）。

## ✅ P2-5b（CT ドット）と P2-1b′（モード遷移の除振）を実装（v0.15.0・2026-08-15）

**CT ドット** ── ★**幾何は走全体から決める**のが要点。
**バーの塗り境界は HP とともに動くが、ドットは動かない**＝時間方向に集約すると段差が均されドットだけ残る。
1フレームから周期を取ろうとして2度失敗した末の形（①段差が周期検出を支配 ②消灯ドットは塗りとほぼ同輝度）。

- ⚠⚠ **ドット数を定数にしていない**。一次情報に**最大CT が戦闘中に変わる**記述がある
  （`cath_palug.md`「最大チャージターンを**2つ減少**」）＝**個数の変化そのものが観測すべきイベント**。
- ✅ **検証済み**: 合成で**ドット数 3/5/7 を復元**・点灯数 1〜4 に追随・ドットが無ければ「見つからない」と言う。
- ⏳ **未検証（意図的にアサートしていない）**: **点灯/消灯のエンコードが未確認**＝
  **全消灯と全点灯を区別できない**（どちらも境界が無い）。★**実物の見え方は走査1回が答える**。
- 実装中に踏んで直したもの: 高域通過の**端の残差で偽ドットが1個生える**／**倍音（2P）を掴む**／
  **ドットの読みが背後の塗りに汚染される**（ベースラインの尺度をドット間隔にして解決）。

**モード遷移の除振** ── 最小滞在 **0.25秒**。
★**閾値の根拠は「人が開いて閉じるのに要する時間の下限」**＝当てはめて合わせた値ではない。
⚠⚠ **「再現する」＝「本物」ではない**: 安定な芯の中にも **33ms の往復**（t=28.45 / 110.03）があり、
毎回同じフレームが標本に入るから再現するだけ。**人には不可能な速さ**なので落とす。
実測の谷＝**偽の往復 ≦160ms / 本物の滞在 1.9〜7.2秒**（0.25秒は偽の 1.6倍・本物の 1/7.6）。
⚠ **生の遷移列も必ず併記**（何を落としたかが見えないと検証できない）＋落としすぎの番人（`T1-ROI-011`）。

## ★P2-4 の決着 ── 間引きは不可能と実測し、削減を P3-2 へ移設（ユーザー承認 2026-08-15）

**実測 `lifetimeFrames = 2`**（3つの J で p50=2 が一致＝受入条件クリア）。
⚠ さらに**交絡を1つ潰した**＝**seek が同じフレームを二度返す**（261/3510＝7.4%）と
**重複は全セルの |Δ| を 0 にする＝偽の「長さ2の freeze」を作る**（実測 frozenCount の 45〜61% を説明）。
修正版で測れば **真の L は 2 以下・おそらく 1**。

| L | `stride = floor(L/2)` | 削減率 | 旧出口条件（10×） |
|---|---|---|---|
| 2 | **1** | **1.0×＝間引きゼロ** | ❌ |
| 2（安全率1でも） | 2 | 2.0× | ❌ |

★**ユーザー確定情報**: ダメージ数値は「**わずかにフェードイン・消失があるが、位置は基本的に不動**」。
∴ ①**「同一位置」は使える**（位置アンカーの照合が成立する）
②⚠ **freeze 長 2 は「数字が動いている」ことを意味しない**＝**数字に重なる演出**（発見⑫のキラキラ）が
セルを動かし続けるため。★**読める長さは 2 より長い。ただし画素の凍結では測れない。**

∴ **「同じポップアップか」は値を読まないと判定できない**。P2-4 の定義自体が「同一位置・**同一値**」で、
**「同一値」は OCR を要求している**＝**畳み込みは P3 の後段（P3-2）が正しい置き場所**。
⚠ **要求を下げたのではない**＝**置き場所が間違っていた**。

⭐ **副次的に P2-2 の長期棚上げが決着した＝「不要」**。OCR 前に間引く動機そのものが消えた
（v2 は採用でも棄却でもなく**用途消滅**）。⚠ 不利な証拠（同一窓3走で Fisher 1.92〜1.96）は台帳に残した。

⚠ **診断の severity を2つ落とした**（`T1-DETECT-002` / `T1-DEDUP-001` → INFO）＝
**もう出口条件でないものを WARN で鳴らし続けるのは誤導**だから。

## P2-5（HPバー抽出）✅ **実走で成立を確認**（v0.12.0・2026-08-15）

| | v0.11.0（3走とも） | **v0.13.0 実走** |
|---|---|---|
| 単調性違反 | **118 / 118 / 118** | **5**（3088フレーム中 **0.16%**） |
| 読めず捨てた | 108（flash のみ） | 423（flash 108 ＋ **noEmptyRegion 315**＝9%） |

★**v0.11.0 が3走とも寸分違わず 118** だったので、**5 への低下は実装の効果と断言できる**（ノイズではない）。
残る5件は**すべて「低く誤読した直後の復帰」**（`from` 0.12〜0.50 で `to` は正しい）＝
**バーの一部を暗く覆う遮蔽が「バーの終わり」に見える**型。⚠ 遮蔽部は暗いので空区間検査を通ってしまう。
`noEmptyRegion` 9% は**設計どおりの拒否**（HP は毎秒30回測るので 12% 捨てても実害なし）。

### 経緯（v0.11.0 → v0.12.0）

**v0.11.0 の生プロファイル持ち帰りが効いて、推測なしで原因が確定した**。
★真因は閾値の高さではなく**「右端の探し方」**＝`閾値を超えた最後の列` は**塗りの連続性を見ていない**。
実測では真の境界（idx 105）より右に**孤立した 1〜2 列**が立つことがあり、それだけで右端が末尾へ飛んでいた。

**違反は1種類ではなく2種類だった**:

| 型 | 割合 | 中身 | 対処 |
|---|---|---|---|
| **孤立列** | 9/10 | 末尾 1〜2 列だけ 0.11〜0.31（間は厳密に 0） | 右端を**2区間の階段フィット**へ＝**閾値パラメータを廃止** |
| **ROI 汚染** | 1/10 | 平坦部 0.2083（正常 0.375〜0.4583）／中央と末尾に**占有率 1.0 のブロック**＝赤い演出が覆う | **読めないと言う**（旧実装は「HP 100%」と報告していた） |

- 階段フィットは **`peak` にも塗り水準（0.375〜0.86）にも依存しない**＝演出で peak が 1.0 に化けても動かない。
- ⚠ **代償**: **満タンのバーも「読めない」になる**（空の区間が無いと汚染と区別できない）。
  取りこぼすのは戦闘開始前の 100% 区間だけ＝許容。理由は `skipCauses`（`flash` / `noEmptyRegion`）で出る。
- ⭐ **回帰の作り方を変えた**＝`tools/fixtures/t1_hp_profiles_M3-1.json` に**実走のプロファイルを現物のまま**収録
  （画像ではなく 120個の集計値＝§10.3 に非抵触）。**旧実装ならこのプロファイルで 1.0 に跳ぶ**ことまで固定した。
- ⏳ **残る5件は「遮蔽が空区間検査を通ってしまう」型**（優先度低・0.16%）。

## ★出力の構造（2026-08-17 変更・ユーザー指摘）

**走査の結果は2本に分かれる**:

| 出力 | 中身 | 使い方 |
|---|---|---|
| **digest**（`digest をコピー`） | 意思決定に要る数値だけを数十行に | ★**これを Claude に貼る** |
| **完全な診断JSON**（`保存`） | プロファイル全値・帯8本・keptSample 等 | 手元に保存。digest で判断がつかないときだけ該当箇所を貼る |

⚠ **診断（WARN/ERROR/INFO）は digest にも全部載る**＝畳んでよいのは**生データの本体だけ**。
★何が起きたかを要約で落とすと**測定系の欠陥を見落とす**（v0.7.0 の 1/3 間引き事故の型）。
実測で **完全JSON の 1/25 程度**（プロファイルは「山の位置:高さ」に畳む）。

## ⚠ 引き継ぐ注意（今後の判断に効くもの）

- ⚠⚠⚠ **`main.js` を 0 バイトで2コミット出荷した**（2026-08-18c・ユーザーの走査が無反応になって発覚）。
  原因は書き換えスクリプトの**評価順のバグ**＝`open(path,'w')` が先に評価されてファイルを空にしてから、
  その**空ファイルを読んで**書き戻していた。★**そして 190 件のテストが全部通った**＝
  `main.js` は DOM 依存で import できないため、**唯一「全部を配線しているファイル」だけが無検査だった**。
  ∴ **セルフテスト [14] ページ配線の健全性**を新設（存在・ESM 構文・`$('id')` と `index.html` の
  双方向の配線・VERSION）。**空の main.js で4件落ちることを確認済み**。
  ⭐ 教訓＝**「壊れたら気づける」より弱い保証でよいから、気づけない箇所をゼロにする**。
- ⭐⭐**「見つからない」も、測定器の可視範囲を確かめるまでは結論にならない**（2026-08-18・実際に踏みかけた）。
  v0.18.0 の帯探索は**探せる周期の上限が 43px**で、探していたもの（間隔 ≈107px）は**最初から範囲外**だった。
  ★**指紋は結果に出る**＝**最良値が探索範囲の端に張り付いていたら、範囲の外を疑う**（8帯中5帯が 43/43/43/41）。
  ∴ `searchRange` を digest に常時出すようにした＝**可視範囲を測定結果と同じ画面に置く**。
  ⚠ **平坦な入力を標準化すると偽の構造が生まれる**のも同時に発見（空の帯が score 0.995 で1位になった）。
- ⭐⭐**「見つけた」は「探していたものを見つけた」ではない**（2026-08-17・CT ドットで実際に踏んだ）。
  UI には**探しているもの以外の等間隔構造がふつうにある**（目盛り・模様・文字の字送り）。
  幾何の性質（周期性）だけで探すと**別物を掴む**。∴ **見つけたと報告する前に、それが何かを確認する**。
  ⚠ **誤報告は「見つからない」より悪い**＝偽の観測値の上に較正を積んでしまう。
- ★**推測で閾値を置かない**。2026-08-14〜15 で**同型の失敗を7回**踏んだ（詳細は SESSION_LOG ④）。
  対策は仕組み化済み＝**診断は必ず生データを返す**／**正解ラベル無しの健全性検査**（二峰性・HP単調性）／
  **実走条件をフィクスチャに焼き直す**／**読めないときは読めないと言う**（数値を捏造しない）。
  ⭐ **この仕組みが 2026-08-15 に初めて配当を出した**＝P2-5 の残り 3.4% を、
  **クロップも追加の走査も人に頼まず**、持ち帰った `colProfile` だけで確定させた（8回目を踏まずに済んだ）。
- ★**閾値を疑う前に「探し方」を疑う**（P2-5 の教訓）。v0.10.0→v0.11.0 は閾値を絶対→相対にして
  違反を 237→117 に減らしたが、**真因は閾値ではなく走査の形**（連続性を見ていない）だった。
  ∴ 半分しか直らなかった。**パラメータを動かして改善したときこそ、構造が違う可能性を残す**。
- ⚠ **測定系の欠陥は、その上で回した分析を全部無効にする**（走査が 1/3 間引きだった件で結論を一度撤回した）。
  `T1-DETECT-005`（サンプリング被覆）が番人。
- ❎ **P2-2（色ベースの存在検出）は close＝不要**（削減が P3-2 へ移り用途が消滅）。⚠ 不利な証拠は台帳に残す＝
  **全フレーム被覆の3走すべてで Fisher 1.92〜1.96**（基準 2.5 未満）・**最良キーも走ごとに入れ替わる**。
  以前の「3.02」は**別の窓**の値。**採用でも棄却でもなく用途消滅**という扱い。
- ✅ **モード遷移の再現しなさ（3走で 18/14/12）は除振で対処済み**（v0.15.0・最小滞在 0.25秒）。
  ⚠⚠ 教訓として残す＝**「再現する」は「本物」を意味しない**（安定な芯にも 33ms の往復が2件あった＝
  毎回同じフレームが標本に入るから再現するだけ）。**物理的にありえない速さは、再現しても偽**。
- ⚠ **決定性が厳密でない**（3走で 3506/3509/3510＝0.11%）。★**HP 抽出は完全再現**（違反数・first/last が3走一致）だが、
  **1〜2フレームの excursion を見る指標では 0.11% が 50% に増幅される**。§9.1-3 の厳密さは **P5 の受入で確定**させる。
- ⭐ **実運用は放置実行＝所要時間は二の次**（ユーザー確定）→ seek ベース（実時間の6倍）は恒久的に正しい。
- ⚠ **動画は git に入れない**（セッションへの共有は可）。**静止画は `ffmpeg -fps_mode passthrough` の抽出フレーム**で
  受け取る（デスクトップキャプチャは再標本化される）。**録画は貯めてよい**（`PHASE9_PLAN.md` §4.0.1）。

## ⏸ sim05 は凍結中（2026-08-07〜）

**M3〜M6 の実走・分析は Phase 9 完成まで止める**。**C40 / C41 / C44 は open のまま＝意図的に受け入れたコスト**。
**凍結解除**＝P5 受入通過。⚠ **ランクアップで走を捨てない**（E11）。

**到達点**: M1・M2 完了＝C40 を編成横断＋敵横断で確定／C44 を分解／C42 確定／C45・C47〜C50 起票。
詳細は `simulation/sim05/README.md` §2.2 と `simulation/sim05/analysis/integrated_analysis.md`。

**★Phase 9 で得た Cx の観測経路**（状態と根拠の正は `CALIBRATION_ANALYSIS.md`）:
**C47**＝`TOTAL` が①バースト追加ダメージ（内側）と②1アシ追撃（外側）を**画面上で分離**／
**C40**＝`追加 = TOTAL − 本体` が厳密に取れる（⚠**M3 は依然必要**）／
**C45**＝**CT（チャージターン）が録画から読める**＝敵行動モデルの intake の目処／
**C24**＝`gauge` の BG／**C38**＝⚠**録画からの近道は無い**（バフアイコン部分表示・区間推定方針は不変）。

## アクティブ作業ライン

| ライン | 状態 | 次アクション |
|---|---|---|
| **★Phase 9** | ⏳ P2 実測確認 | ✅ HP 復旧・**CT は 5個で確定**（22.01px・CV 0.37%）・左端はモードゲージの裾と判明 → ⚠ **点灯は輝度では表されていない**（ユーザー確定＝**録画中に点灯を観測済み**＝測り方の問題） → ✅ **色で読めた**（モードゲージは完全・CT も点灯を捕捉） → ⚠ v0.26.0 の区間は**演出を拾っていた**（撤回）→ **共通モード除去**へ → ⏳ **走査1回（v0.27.0）**で「1個目だけ3回」が出るか → P2 通過 |
| sim05（M3〜M6 / C40・C41・C44） | ⏸ 凍結 | Phase 9 完成後 |
| エジソン英霊武器の追加ダメは `TOTAL` の内外か | ⏳ 未確認 | **edison 編成を扱う前に**確認（C40 の留保に直結） |
| `configC_slot.json` の受領 | ⏳ 長期未受領 | Phase 9 の **P4** で効く |
| サポート幻獣の加護が全量スタックするか | ⏳ 未確認 | シムの前提。外れると全成分が一律 ×0.83 |
| override の再fit／他ハーネスの数値再取得 | ⏳ 着手禁止 | C40/C41/C44 確定後に1回だけ |
| Phase 8 アクセ実装 | ⏳ intake ゲート | — |

## ドキュメント・ポインタ

- **開発ルール・コード地図・確定仕様・検証**: `CLAUDE.md`（本書と対で必読）
  ／**不変条件の詳細**: `ENGINE_INVARIANTS.md`（**該当コードを触るときだけ開く**）
- **Phase 9**: `PHASE9_PLAN.md`（**§4 P2 に実装の設計判断と失敗の記録**・**§10 確定仕様**・§10.5.1 エラーコード台帳）
- **較正の Cx**: `CALIBRATION_ANALYSIS.md`（**C37/C38/C40〜C49 が open**・C50 は closed）
- **実験・計測の作法**: `REPO_STANDARDS.md` §6 **E1〜E11**（着手前に通す）
- **過去の経緯**: `archive/SESSION_LOG.md`（**2026-08-15e ブロックが最新**）

## 検証（作業後は必ず）

```bash
npm run test:t1       # T1 転記ツールの回帰（249件・1秒未満）
npm run doc:check     # md 相互参照（現役層に壊れた参照があれば exit 1）
npm run test:golden   # edison/raw 202,005,923・edison/cal 215,161,915・napoleon/static 299,523,354
```
⚠**golden は実測 2分07秒**（背景実行を推奨）。`src/transcribe/` はシム本体と非結線なので **golden は不変**。
`_replayResult`/`_execKey`/`clone`/`_snapshotForReplay` を触ったら **`node tools/exp_ls_incremental_verify.mjs`（約4分）も回す**。

---

## 更新履歴

<!-- 直近5件のみ（それ以前は git log）。「波及確認」列が本体＝git が持たない情報はここだけ。 -->

| 日付 | 変更点 | 波及確認 |
|---|---|---|
| 2026-08-18h | **v0.26.0 の区間は CT ではなく画面全体の演出だった（撤回）**。**共通モード除去**（谷の色みを差し引く）へ作り直し（v0.27.0） | ⚠ ユーザー指摘（**点灯は1個だけ・3回**）で気づけた＝出た区間の **8個中7個が coin=5**。★原因は**色みの層が4つ**（灰／弱い演出／**CT 点灯 100〜130**／強い演出 160〜205）あり、大津法2段の切れ目 **140.25** が**点灯を切り捨てて強い演出だけ**を拾ったこと。⭐⭐ **層が増えるたびに段を足すのは際限がなく毎回外す**＝切り分けは**大きさではなく「どこが色づいたか」**。**全体演出は谷も色づけるが点灯はピップだけ**＝**谷を参照に差し引く**（新しい閾値ではなく参照点の選び方）。⚠ **他のピップを参照にしてはいけない**＝全部点灯で差が消える。★合成で通し確認＝**弱い演出も強い演出も0区間・1個目だけ2区間・coin=1**。`test:t1` **244→249件** |
| 2026-08-18g | **色で読めた**＝モードゲージは**満102/空−1のきれいな段差**、CT は**1個目の点灯（104〜130）を t≈51〜57 と 117〜120 で捕捉**。**フレーム解像度の区間切り出し**と**塗り率の時系列**を追加（v0.26.0） | ⭐ **信号が2種類ある**と分かった＝**全5個同時 13〜20＝画面全体の演出**／**1個だけ 100超＝点灯**。大きさで分離できる。⚠ **2〜5個目も p95 78/73/37/49**＝短い点灯があるが**3秒バケットでは薄まって消える**＝区間へ切り替えた理由。⭐⭐ **テストが出荷前に設計の欠陥を捕まえた**＝**大津法は2クラス法**なので3層データでは切れ目が 16.8 に落ち**演出が点灯側に混ざる**→ **2段掛け**にした（実走14分を1回節約）。★モードゲージは**左から溜まる**（平均色み 左0.70/右0.50）＝**与ダメージの観測値**。digest は畳み直して **5,200字**（10,000→8,800→5,200）。`test:t1` **232→244件** |
| 2026-08-18f | **ユーザー確定情報**（モードゲージ＝与ダメで蓄積／CT＝ターン1回で1つ／**どちらもオレンジ**／**M3-1.mp4 で点灯を観測済み**）を受けて、**色みの時系列**（山ごと・3秒×40バケット）と**色みプロファイルの標本**を追加（v0.25.0）。**帯ごとの探索を要約1行に畳む** | ⭐ **「録画中に点灯を観測済み」が効いた**＝「CT が動かなかった」可能性が消え、**測り方の問題**と確定した。★合成で**途中でオレンジに変わる山の立ち上がりが時系列に出る**ことを確認（前半≈0→後半大／消灯のままの山は分布が割れない）。⚠ **役目を終えた出力（帯ごとの探索）を畳んだ**＝採寸方式へ移行して不要になり、**出し続けると判断に効く行が埋もれる**。生データは完全JSON に全部残る。★**絶対量の番人（12,000字以下）を新設**＝比だけだと完全JSON が膨らめば digest も膨らめてしまう。実測見積り 8,800字。`test:t1` **226→232件** |
| 2026-08-18e | **v0.23.0 実走＝CT は 5個で確定**（左端はモードゲージの裾）。**点灯は輝度では表されていない**と判明し、**色（R/G/B・R−B）の抽出**を追加（v0.24.0） | ⭐ **別々に手で採寸した2つの ROI が重なり領域で一致した**（x=699 で差 0.2）＝`modebar` 右端は単調減少で山が無く、`ct` 左端の盛り上がりはその裾。∴ **6個目のピップは無い**。★**点灯の否定は分布が示した**＝5つとも p25〜p75 が **120±0.5** で、明るいフレームの割合が **2〜5個目で横並び**（0.084〜0.088）＝**階段になっていない**。⭐ 残る仮説＝**色**（`lum()` は色を潰す）。合成で**輝度がほぼ同じ暖色と灰色を R−B が桁で分ける**ことを確認（60 vs −2.4）。⚠ モードゲージも輝度は平坦（99〜110）＝同じ疑い。`test:t1` **220→226件** |
| 2026-08-18d | **v0.22.0 実走＝HP 完全復旧を確認し、CT の「7枠」を撤回**。山の数え方を `rawHumps()`（振幅でしきる）へ変更、**`modebar` の生プロファイル抽出**と**山ごとの明るさ分布**を追加（v0.23.0） | ⚠ **前回の読みが誤りだった**＝全120値では右端 29bin が **56〜63 の平坦**で、「6・7枠目」は **±1 のノイズ**。⭐ **原因は digest が「上位10山」に畳んでいたこと**＝**局所最大は平坦なところでも必ず見つかる**。∴ **山は「どれだけ高いか」で数える**。★HP は違反 5・flash 109 で **v0.18.0 と時刻まで一致**＝`hpbar` 撤回は正しかった。★新設の `捨てたフレームの中身` で **flash は本当に真っ暗**（peak=0/red=0）と確認。★**新たな未決着**＝`ct` の左端が切れている疑い（左端の水準 63.0＝谷と同じ・右端 56.6＝背景）＝`modebar` と 13px 重なるので**どちらの構造か**は modebar の profile で決める。⚠ **σ は設計の逆**（山で最小）＝「動かない UI 要素」の検出器として読み替える。`test:t1` **217→220件**・シム本体未変更 |
| 2026-08-18b | **v0.20.0 実走で CT を「離散ピップ 7枠・5点灯」と読んだ**。**`hpbar` の採り直しは退行したので撤回**し、`hpSkipSamples`（捨てたフレームの生データ）を追加（v0.21.0） | ★**「ドットかゲージか」は決着**＝明るい5つが間隔 21.94px・CV 2.7%・高さ ±2.6% の**等間隔等輝度**で、格子を延ばすと**暗い2つ（45% 輝度）が 6・7枠目にぴたり乗る**。8枠目は ROI 外。⭐ 2026-08-14 の目視「丸ドット5個」と一致（人は点灯側を数えていた）。⚠ 枠数の確定には**完全JSON の rawProfile 120値**が要る（digest は上位10山に畳む・**走査不要**）。⚠ **σ は localizer として機能せず**（ROI 全体で一様＝演出が分散を支配）＝頑健化は要るが推測で入れない。★HP は**同一コードで ROI だけ変えて flash 109→2152**＝`hp_bar.js` の帯が**ROI の上下端**を見るため**タイトな ROI だと帯がバーの縁に乗る**。⭐ **診断の欠陥**＝捨てた側の生データが無く推測でしか説明できなかった＝`hpSkipSamples` で塞いだ。`test:t1` **187→188件**・golden 3/3 不変 |
| 2026-08-18 | **実走 v0.18.0 の digest を分析**し、**検出器の穴を3つ塞いだ（T1 v0.19.0）**＝①帯探索の窓が固定幅で**探せる周期が 43px で頭打ち** ②**平坦な帯を標準化して score 0.995 の偽構造** ③周期の数%誤差が累積して**5個を3個と数えていた**。`test:t1` **166→177件** | ⭐⭐ **教訓＝「見つからない」も、測定器が何を見られるのか確かめるまでは結論にならない**（2026-08-17 の「見つけた ≠ 探していたものを見つけた」の裏返し）。★**事前に決めた読み方に従えば「CT は ROI の外＝採寸やり直し」だったが、適用しなかった**＝**指紋が結果に出ていた**（8帯中5帯が探索上限に張り付き）。⚠⚠ **未解決の食い違いを起票**＝2026-08-14「バー上の丸ドット5個＝CT」と 2026-08-17「等間隔6山は目盛り」＝**(a)/(b) で次の一手が正反対**。★**新テストが旧実装で落ちることを確認**してから採用（回帰として効くことの検証）。**digest に `searchRange` を常時表示**＝可視範囲を測定結果と同じ画面に。シム本体未変更＝**golden 3/3 実行して不変** |
| 2026-08-17 | **セッション末クローズ**。**CT ドット抽出**（`charge_dots.js`）・**モード遷移の除振**・**digest 出力**（`digest.js`）を実装し、**main へマージ**（ユーザー承認）。`test:t1` **115→166件** | ⭐⭐ **本セッション最大の教訓＝「見つけた」は「探していたものを見つけた」ではない**＝CT だと思った等間隔構造は**ユーザー確認で「バーの目盛り」**だった。**誤報告は見つからないより悪い**（偽の観測値の上に較正を積む）。∴ **既知の装飾を記録**し、**縦方向にも探す**（帯8本）ようにした。★除振は**予測どおり 33ms の往復2組だけを落とした**。★**digest でトークンを 1/25 に**（⚠ 診断は全件残す＝畳むのは生データだけ）。⚠ **CT の居場所は未確定**＝次の走査が答える。シム本体未変更＝**golden 3/3 不変** |
| 2026-08-15d | **P2 の残り2件を実装（T1 v0.15.0）**＝**CT ドット抽出**（`charge_dots.js`）と**モード遷移の除振**。`test:t1` **115→146件** | ★CT は**幾何を走全体から決める**のが鍵＝**塗り境界は動きドットは動かない**を利用（1フレームからの周期検出は2度失敗）。⚠⚠ **ドット数を定数にしていない**＝一次情報に**最大CT の増減**があり、**個数の変化自体が観測値**。⏳ **点灯/消灯の読みは未較正**（実物の見え方が未確認）＝意図的にアサートせず走査1回に委ねた。★除振の閾値 0.25秒は**人の操作の下限**が根拠＝**「再現する」は「本物」を意味しない**（安定な芯にも 33ms の往復があった）。シム本体未変更＝**golden 不変** |
| 2026-08-15c | **★計画を組み替えた（ユーザー承認）**＝実測で**等間隔の間引きは原理的に不可能**と確定（L≦2＝stride 1）し、**削減を P2 から P3-2（OCR 後のイベント畳み込み）へ移設**。P2 の出口条件を「HP% が自動で取れる」＋「正規化・モード判定・ROI が実フレームで成立」に改訂 | ⚠ **要求を下げたのではない＝置き場所が間違っていた**（P2-4 の定義「同一位置・**同一値**」の“同一値”が OCR を要求している）。⭐ **副次的に P2-2 の長期棚上げが「不要」で決着**＝OCR 前に間引く動機が消えた（v2 は採用でも棄却でもなく**用途消滅**）。★ユーザー確定情報「**わずかにフェード・位置は不動**」＝①「同一位置」は使える ②**freeze 長 2 は数字が動いている意味ではない**（重なる演出がセルを動かす）。⚠ **診断 severity を2つ INFO へ落とした**＝もう出口条件でないものを WARN で鳴らすのは誤導。`test:t1` **115件**・シム本体未変更＝**golden 不変** |
| 2026-08-15b | **★P2-4 に着手（T1 v0.13.0）**＝`src/transcribe/dedup.js`。**発見⑧（ポップアップ寿命）の実測を走査に組み込み**、間引きは**保証付きの等間隔方式**にした。⏳ `stride` は**未較正のまま出荷**（＝間引かない） | ★**問いを変えたのが本体**＝P2-2 の「変わったか」→ P2-4 の「**取りこぼさないと保証できる最大の間引きは**」。⚠⚠ **保証は s ≦ 寿命 L のときだけ**で、**L が 1〜2 なら間引きでは出口条件に到達できない**という結論もありうる＝`T1-DEDUP-003` がその番人。⭐ **回帰フィクスチャを実走の距離分位点に一致させた**（p50 6.72/p90 40.34/p99 112.63 → 合成 6.70/41.7/110）＝**P2-2 を失敗させた動きを合成が持つ**。⚠ **テストで主張が1つ偽と判明し書き直した**（「全部捕まる」→「寿命が stride 以上のものは捕まる」）。⭐ **自分で作った指標を自分で反証して捨てた**＝`persistenceRatio` は**ポップアップゼロの背景だけの列で 0.537**＝「持続性あり」に見えた（背景の遅さと寿命を区別できない）→ `eventContrast` へ差し替え、**その反証を回帰に固定**。`test:t1` **93→110件**。シム本体未変更＝**golden 3/3 不変**（実行して確認済） |
| 2026-08-15 | **★P2-5 を完了（T1 v0.12.0）**。ユーザー走査の診断JSONから**原因を推測なしで確定**し、右端の判定を**階段フィット（閾値なし）**へ、ROI 汚染フレームを**「読めない」**へ。次の一手を **P2-4** に前進 | ★**違反は1種類ではなく2種類**だった（孤立列 9/10 ＋ ROI 汚染 1/10）＝**片方は境界を直しても解けない**。⚠ **代償を1つ受け入れた**＝満タンのバーも「読めない」になる（空区間が無いと汚染と区別不能・戦闘開始前のみ）。⭐ **回帰の作り方を変更**＝実走プロファイルを現物で収録（`tools/fixtures/`）し、**旧実装なら 1.0 に跳ぶことまで固定**。`npm run test:t1` **75→93件**。`src/`・`gamedata/js/` のシム本体は未変更＝**golden 3/3 不変**（実行して確認済） |
| 2026-08-15 | **セッション末クローズ＝本書を Phase 9 P2 中心へ全面書き換え**（290行→約120行）。当日の成果は `archive/SESSION_LOG.md` の 2026-08-14〜15 ブロックへ畳んだ | **P1 通過（進む）と T1 ページの実装が本セッションの成果**。⭐ 記録として最も価値があるのは**同型の失敗7回とその対策の仕組み化**（SESSION_LOG ④）＝本書には要約のみ残した。★`TOTAL` 発見により **C47/C40 の測定経路が変わった**／**CT が C45 の観測値**と判明。⚠ **C38 は録画からの近道が無い**ことが確定（方針不変）。`src/`・`gamedata/js/` のシム本体は未変更＝**golden 3/3 不変** |
| 2026-08-14 | **P2-1b（右パネルのモード判定）を追加実装** | ⑮＝右パネルが同じ領域に2状態を持ち無数に行き来すると判明。⚠モード判定を通さずに ROI を解釈すると静かに壊れる |
| 2026-08-14 | **★P1 実測ゲート通過＝「進む」を確定** | 撤退条件3つとも非該当。★VFR は実害なしと実測（最大ギャップ 50ms）→ P2 の固定fps 正規化を撤回 |
| 2026-08-14 | **2026-08-14 の CLAUDE.md 分冊セッションを追補** | ★申し送り漏れを検出して埋めたもの。実害は台帳の記録漏れのみ |
| 2026-08-07 | Phase 9 採番・sim05 凍結・§10 確定仕様を反映 | 詳細は SESSION_LOG 2026-08-07 |

<!-- doc_refs:begin ── 自動生成。手で編集しない（node tools/doc_refs.mjs --write が再生成する） -->
## この md を参照している文書（現役層 5）

- [CLAUDE.md](../CLAUDE.md)
- [DOC_RELATION_PLAN.md](../DOC_RELATION_PLAN.md)
- [REPO_STANDARDS.md](../REPO_STANDARDS.md)
- [simulation/sim05/README.md](../simulation/sim05/README.md)
- [workspace/TODO.md](./TODO.md)

_他に 凍結sim/archive/essays から 1 件（更新対象外）_
<!-- doc_refs:end -->
