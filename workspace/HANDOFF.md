# 引継ぎ — 現状スナップショット

> **種別**: 引継ぎ（現状スナップショット・有界） ／ **用途**: 新セッション起動時に **CLAUDE.md と本書のみ**を読めば「今どこにいて次に何をするか」が分かる状態を保つ。
> **次タスクの詳細リスト**は `workspace/TODO.md`。過去の経緯・provenance は `archive/SESSION_LOG.md`（オンデマンド）。
> **更新規律（セッション末）**: 本書は「今」だけを短く保つ。現状化した記述は `archive/SESSION_LOG.md` の先頭へ1ブロック畳み、本書は最新状態へ上書きする（規律は REPO_STANDARDS §6）。
>
> 最終更新: 2026-08-02（セッション末）

---

## 現フェーズ

**Phase 4 = 統計的較正 × 反復可能ボス**（PHASE4_PLAN §3.5.1）。全分析を Claude Code 担当。

**sim05 の主題＝【追撃 C3/C5 較正】＋【tier 観測（C38）】＋【鬼神障壁の実測】**。
押し順の序数検証は**目的から外した**（測り分け不能）＝実走は実機勘のフリー押しでよく、
代わりに**実際に押した順の記録が必須要件**。詳細は `simulation/sim05/README.md` §1。

## ⏳ 次の一手 ── **pre-trial.md の受領（次セッション）**

ユーザーが両面宿儺の試走を **`pre-trial.md` ＋ その元になった録画サンプル**としてまとめて共有・格納予定（**次セッション**）。
受領後の最初の作業は **T1 局在の切り分け**（下記・sim05 README §4.4.1）。

> **★優先順位（ユーザー明示 2026-08-02）**: **pre-trial とその検証目的が最優先。転記ツールはあくまでサブタスク。**
> 録画サンプルが来ても、まず pre-trial の分析（T1 局在の切り分け → 追撃 per-hit → C3/C5 fit）を進める。
> 転記ツールの実測（§6.1）は**その手が空いてから**。
>
> ⚠ **動画は git に格納しない**（`.gitignore` 済み）。リポジトリは packed 1.37MiB で、5分の 1080p mp4 は
> 全履歴の30〜350倍かつ**コミットすると削除しても履歴に残る**。共有は可・格納は不可。
> 実測に必要なのは **静止画3枚＋`ffprobe` の出力テキスト**で足りる（`TRANSCRIPTION_DESIGN.md` §6.1）。

**較正ボスは両面宿儺で確定**（2026-08-01 実機検証）。両ボスとも**被ダメージ0**で討伐（テトラ1アシのT1無敵＋ヘカテー反撃）
＝生存ゲートは論点ごと消滅。主ダメージ源は**バースト**と**ナポのベタイア**。

| ボス | 実機 撃破 | 実機 残HP | シム予測 | 実機/シム |
|---|---|---|---|---|
| ロキ | T2 攻撃フェイズ | T1 36% | T3 撃破 | T1 ×1.37 |
| **両面宿儺（採用）** | **T3 ベタイア** | T1 46% / T2 24% | T5 撃破 | T1 **×2.43** / T2 ×1.78 |

- ⚠ **clean な測定ターンは ロキ1 / 宿儺2**（要件は4）＝**複数走で稼ぐ**か**高HPボス再確認**（即死の懸念は被ダメ0 実績で弱まった・未解放は依然ブロッカー）。
- ★ **絶対値の乖離は T1 に局在**（増分比 T1 ×2.43 / T2 ×1.07）＝**一律スカラでは説明できない**。
  仮説1（cap 19 が T1 で不成立）は**定量化済＝乖離の約2/3まで**。残りは (2)T1バースト過小 /(3)障壁 rate /(4)ベタイア cap 過小 の取り分。
  **⚠ T2 が ×1.07 で合っていること自体が情報**＝≈19手の条件ではダメージモデルはほぼ正しい。→ 詳細 **sim05 README §4.4.1**

**転記で最優先の取得物**（`record_skeleton` §1〜§3）:
①各ターンの押下数（特に T1） ②**ベタイア1ヒットの表示ダメージ**（実機 cap を直接与える）
③consort のヒット数と表示ダメージ（倍率 `2+0.5×bc` ＝bc を逆算できる） ④factor の CD-1 が出たか（bc≥10・最も明瞭）
⑤バースト/追撃の成分別ダメージ

> ⚠ **tier 観測の取得方式は 2026-08-02 に変更**: 実機UIは付与先を表示せず効果量のストック表示＝**アイコン計数は非現実的**。
> ∴ **tier 効果の成立/不成立から bc を区間推定する**（`record_skeleton` §3）。

## 3トラックとゲート状況

| トラック | 内容 | ゲート |
|---|---|---|
| **A. ダメージ較正** | C3/C5（追撃・実機比×2.3〜6.9） | ⏳ **pre-trial 受領待ち**（ボス＝両面宿儺で確定・configC は proper v2） |
| **B. 探索の安定化** | C37 | ✅ LS 実装＋ボス切替で回避（B4 検証済）。⚠**根治せず**（代理採点は手つかず）＝open |
| **C. 押し順・tier** | C38（buffCount）・予測探索 | ⏳ 実機データ待ち（`record_skeleton` §3） |

## 直近セッションの成果（2026-08-02・詳細は SESSION_LOG）

- **✅ C39 fix（★ダメージモデル変更・golden 再fit）**: `_naOwner` の是正＝(a) `effond`/`betaia` が**他キャラのギア係数・自己バフで自分のダメージを計算していた** (b) `clone()` が `_naOwner` を落とし**ビーム/先読みだけ本線と評価条件が違った**。→ **raw 202,005,923 / cal 215,161,915 / napo/static 299,523,354**（`sim05-c39-naowner`）。⚠**override は未再fit**。
- **✅ LS 高速化＋ジョブ並列化**: `_execKey` 短絡＋`_LSReplay`（結果不変）で golden 約10分→4分07秒、さらに **fixture 並列化で 2分07秒**。`tools/lib/parallel_map.mjs` 新設＝**4コアのうち1コアしか使っていなかった**問題への対処（`extract_order` の8 prefix は 約19分→6.5分）。
- **✅ 運用原則を明文化**: CLAUDE.md 開発ルール **§5「編成間の転移可能性」**＋REPO_STANDARDS §6 **E9**。
- **✅ sim05 の整理**: 12ファイル→4ファイル・README 400→172行・`record_skeleton` 218→86行（sim04 と同じ「データを入れるだけ」の形へ差し戻し）。
- **📝 `TRANSCRIPTION_DESIGN.md`**（未実装・設計のみ）: 実機録画→転記の半自動化。**転記コストが較正の律速**という認識。実機の画面仕様（ポップアップは静止／ラベルは数値の真上／重なりは成分で予測可能／HP は整数%＋バー／成分は表示だけでは判別不可）を受領して詳細化。**★HP%の分解能が 1%＝980万しかない**と判明し、主役を総和チェック→**リプレイ照合のヒット数突合**へ入れ替えた。

## アクティブ作業ライン

| ライン | 状態 | 次アクション |
|---|---|---|
| **★pre-trial の受領・分析** | ⏳ **次セッション** | T1 局在の切り分け → 追撃 per-hit → C3/C5 fit |
| **★メタトロン A7（AnotherLink 重複規則）** | ⏳ **要実機・最優先** | metatron+artemis を両方サブ＝同枠が必ず競合。`final_dmg` 1.10 vs 1.21。**確定前に絶対値較正をやると較正が誤差を吸収する** |
| **★override の再fit** | ⏳ **未履行** | C39 でモデルが動いた＝`search_calibrate.mjs` の再fit が規約上必要（`{judg:145,pactcore:1}` は据置で出荷中） |
| **転記の半自動化** | 📝 設計のみ・**サブタスク** | ⚠**pre-trial の分析を優先**。手が空いたら S1（リプレイ照合・AI不要）＋録画の実測（§6.1） |
| **長時間ジョブ** | ✅ ①②並列化済 | 残＝③LS内部の投機並列・他ハーネス横展開・ローカル移行 |
| **Phase 8 アクセ実装** | ⏳ intake ゲート | §6 intake をユーザーと確定後 |

→ 全タスクの優先順・チェックは **`workspace/TODO.md`**。

## ドキュメント・ポインタ（詳細は必要時のみ）

- **開発ルール・コード地図・確定仕様・検証・実測コスト**: `CLAUDE.md`（本書と対で必読）
- **較正の確定値・根拠・Cx バックログ**: `CALIBRATION_ANALYSIS.md`（**C37/C38 が open・C39 は fixed**）
- **sim05**: `simulation/sim05/README.md`（**§4.4=較正ボス / §4.4.1=T1局在 / §4.6=tier観測 / §2=依存チェーン**）
  ／`data/record_skeleton.md`（記入テンプレ）／`data/configC_gear_panel.md`（§2.1 ATK・HP / §3 装備パネル）／`buffcount_sim_side_diag.md`
- **転記ツールの見立て**: `TRANSCRIPTION_DESIGN.md`
- **計測ハーネス**: `tools/`（`README.md` が索引・**§0 に並列実行**）
- **実験・計測の作法**: `REPO_STANDARDS.md` §6 の **E1〜E9**（着手前に通す）
- **探索品質の実験の全数値**: `archive/SEARCH_QUALITY_EXPERIMENTS.md`（C37 の根拠アーカイブ）
- **新キャラ/幻獣の一次情報と Ax**: `gamedata/md/神姫/metatron.md`（§3.2 A0〜A10）／`gamedata/md/幻獣/`
- **Phase 一覧**: `ROADMAP.md` ／ **現行フェーズ**: `PHASE4_PLAN.md` ／ **規約**: `REPO_STANDARDS.md`
- **過去セッションの経緯**: `archive/SESSION_LOG.md`

## 検証（作業後は必ず）

```bash
npm run test:golden   # edison/raw 202,005,923・edison/cal 215,161,915・napoleon/static 299,523,354（全 FB10/10）
```
⚠**実測 2分07秒**（fixture 並列。`--serial` で逐次・`--fixture <name>` で単体）。**背景実行を推奨**。docs のみの変更なら golden は不変。
`_replayResult`/`_execKey`/`clone`/`_snapshotForReplay` を触ったら **`node tools/exp_ls_incremental_verify.mjs`（約4分）も回す**。
