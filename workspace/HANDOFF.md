# 引継ぎ — 現状スナップショット

> **種別**: 引継ぎ（現状スナップショット・有界） ／ **用途**: 新セッション起動時に **CLAUDE.md と本書のみ**を読めば「今どこにいて次に何をするか」が分かる状態を保つ。
> **次タスクの詳細リスト**は `workspace/TODO.md`。過去の経緯・provenance は `archive/SESSION_LOG.md`（オンデマンド）。
> **更新規律（セッション末）**: 本書は「今」だけを短く保つ。現状化した記述は `archive/SESSION_LOG.md` の先頭へ1ブロック畳み、本書は最新状態へ上書きする（規律は REPO_STANDARDS §6）。
>
> 最終更新: 2026-08-23e（**P3-1b は残り1つ＝マスクの効果の実測**。👤 `dmg` 枠を**型ごと**に待ち。次は **P3-2**）

---

## 現フェーズ ── **Phase 9（実機観測 intake の自動化）／P3-1b 実装済・次は P3-2**

**sim05（Phase 4）は凍結**（2026-08-07 ユーザー決定）。憲法＝「**観測と判断はユーザー／導出・分析は Claude Code／転記・検算・整形はツール**」。
計画の正＝[PHASE9_PLAN.md](../PHASE9_PLAN.md)（一次設計＝`TRANSCRIPTION_DESIGN.md`）。

| 段 | 状態 |
|---|---|
| **P0** 先行整備 ／ **§10** 確定仕様 | ✅ 完了（⚠§10 の変更にはユーザー承認が要る） |
| **P1** 実測ゲート | ✅ 通過（2026-08-14）＝進む |
| **P2** 抽出基盤 | ✅✅ 通過（2026-08-19・出口確認済）。P2-1 / 1b / 1b′ / 3 / 5 / 5b ✅ ／ P2-2 ❎close ／ P2-4 ➡️P3-2 |
| **P3** OCR＋検算 | ⏳ P3-0 ✅ ／ P3-1 アトラス ✅ ／ **P3-1b 読み取り＋検算 ✅実装（2026-08-23）＝ただしラベルは👤教示待ちで実機未測定** ／ **P3-2 が次の着手点** |

## 1. 直近の実装内容

**本セッション＝P3-1b（読み取り＋検算）**。⚠ **シム本体は未変更**＝振る舞いは1行も動いていない（golden 3/3 実行確認）。

| 層 | 変更 | 証拠 |
|---|---|---|
| **検算（新設）** | `src/transcribe/verify.js`＝③値域・先頭ゼロ／⑦`checkTotal`・**`reconcileWithTotal`**（残差から誤読を逆算） | `test:t1` [17-1〜17-5] |
| **ラベル層** | `glyph.js` に `teachLabel` / `labelBandAbove` / `detectLabels`（検出→マスク） | [17-6〜17-8]（**機構のみ**） |
| **実データ計測** | `tools/t1_verify_probe.mjs`（`measure()` を [17-9] と共用＝計測を二重に持たない） | 下表 |
| **打ち間違いの検査** | 教示の入口に `checkCommaGrammar`（実使用で `5,,553,703` が届いた）。弾かないと**囲みが誤った文字数で割られ**アトラスが静かに壊れる | `test:t1` [17-10] |
| **ラベル3語（👤受領）** | `CRITICAL!` / `STING!` / `TOTAL` をフィクスチャへ焼いた。実画素で**自己0.999 / 他者0.25〜0.37**＝明確に分離（`minScore 0.45` がちょうど間） | `test:t1` [17-12] |
| **production 経路** | `glyph.js` `readScene`＝**ラベル検出 → マスク → 読み取り**を1本に。★**マスク有無の両方を返す**（「効いた」は実測でしか言えないため） | `test:t1` [17-13] |
| **シーン保存＋実測** | ページ「★ダメージ枠をJSONで保存」（`kind:'scene-frame'`＝枠まるごと＋**見えている数値**）／`tools/t1_scene_probe.mjs` | 合成シーンで通し実行を確認 |
| **ラベル教示経路** | `transcribe/index.html` に「このラベルを教える」＋`main.js` 配線（`LABEL_KEYS` プルダウン・`kind:'teach-label'` で保存）。`t1_teach_probe.mjs` が受けて `atlas.labels` へ | `test:t1` [17-11] |
| **差し替えマージ** | `t1_teach_probe.mjs --merge`＝**同じ表示の教示回だけ**新しい切り抜きで置換（過去の切り抜きは手元に残らないため） | — |
| ドキュメント | `PHASE9_PLAN.md` §4 P3-1b／**§4.3.0j（囲みの真因）**／**§4.3.0k（ラベルの教え方）**／§5／§4.3.1 ／ `CLAUDE.md` ／ `tools/README.md` | `doc:check` グリーン |

**★★実データで測った結果**（`t1_verify_probe.mjs`・実切り抜き14枚 101文字を教示回ごと leave-one-out）:

| | k=2 | k=3 | k=4 | k=6 |
|---|---|---|---|---|
| ★**検出**（誤読を含む束に気づけた） | **100%** | **100%** | **100%** | **100%** |
| ★★**偽の訂正** | **0** | **0** | **0** | **0** |
| 一意に直せた | 6 | 5 | 2 | 0 |

**★★本セッションの中心的な発見 ──「合計が合う」は「読めている」ではない**:
真 `4957011+5553703` を `4957711+5553005` と読むと **+700 と −700 が打ち消し合い**、残差には +2 しか出ない。
∴「残差 −2 を1文字で説明する」直し方が**一意に**見つかり、**合計は合うのに中身は誤ったまま**通る（＝偽の訂正）。
★効いた唯一の手＝**採用する深さより深く探す**（`searchSwaps=3 > acceptSwaps=2`）＝
**採用の条件は「解が在る」ではなく「他に説明が無い」**。
∴ **⑦ は合計の整合を証明するが1文字ずつは証明しない**／**⑦ の主たる価値は訂正ではなく検出**。

**★★2つ目の発見 ── 「囲みが緩い個体」説を棄却。ただし原因は未解明**:
囲み直しを受領して測ったところ、**まったく同じ誤読を再現**（`5125605→5123003` / `5553703→5·530·5`）
＝**囲みが原因ではない**。掃引でも `shift.x`/`shift.y`/`lambda` とも **誤6 が床**＝**パラメータでもない**
（x=5 が良く見えたのは**コーパス依存の見せかけ**＝旧アトラスでは誤7）。

⚠⚠ **いったん「字を切っている」と結論して検査（`checkCropFraming`）まで作ったが、同日に撤回した**＝
`glyphMask` が**画像全体の 42〜73% をインクと判定**しており（演出が金色）、縁の 71〜85% は
**ベースラインと同じ**＝何も分けていなかった（受領7枚すべてで警告）。
★**誤った説明のために作った検査が、その説明を裏書きする**循環に入りかけた＝検査は説明より先に自分を疑う。
⏳ **∴ 残る誤読6件の原因は未解明**。次に疑うのは**特徴量**（`glyphMask` の注記＝「特徴量の選択は暫定」）。
⚠ フィクスチャの数字は差し替えていない（囲み直しは 誤6→5 だが `validateAtlas` が落ち、
1件差は 101 標本で有意でない＝関門を緩める理由にしない）。

**回帰**: `test:golden` **3/3 不変**（202,005,923 / 215,161,915 / 299,523,354）・`test:t1` **377件**（337→+40）・
T1 精度 **不変**（正80/誤6/曖昧15）・`doc:check` グリーン・`skill:invariants` 違反0・`skill:doctor` 違反0。

**★一次情報を受領**（2026-08-23）: **バースト本体の演出はキャラで3型**（①単発＝ナポレオン/テトラ・**`TOTAL` は出る**／
②2発＝アリアンロッド／③多段＝ヘカテー/エレイン・**重なりあり**）＝`gamedata/md/その他/burst_display.md`。
★**取得計画が変わった**＝重なるのは③だけなので**型を混ぜずに集める**。
⏳ ついでに確定できる＝**単発型で `TOTAL` は本体と一致するか大きいか**（**C40** の直撃・sim05 凍結中なので記録のみ）。

**★副産物**: ①`fieldScale` の**小数の帯で NaN を返す潜在バグ**を修正（`detectLabels` が初めて小数帯を渡して露出）
②`test:t1` の「**1秒未満**」が陳腐化していた（[17] 追加前で既に **13.1秒**）＝実測へ訂正。

## 2. 現在の課題・ブロッカー

| # | 課題 | 状態 / 誰待ち | 根拠 |
|---|---|---|---|
| 1 | ⏳ **P3-1b の残り1つ＝「マスクすると誤読が減るか」の実測**。経路（`readScene`）と保存・測定ツールは実装済 | ⏳ **👤 ユーザー待ち**＝`dmg` 枠まるごとを**型ごとに**（依頼＝§4.3.1 の **1c**） | ★**バースト本体の演出は3型**（一次情報＝`gamedata/md/その他/burst_display.md`）＝**重なるのは多段型だけ**。∴ ★1 多段＝**効くか** ／ ★2 単発＝**壊さないか**（対照）。⚠ **1枚に全ヒットは写らない**＝検算⑦は1フレームでは閉じない（P3-2） |
| 2 | ⏳ **数字の残る誤読6件＝原因未解明**。囲みでもパラメータでもないと確定 | ⏳ **Claude 側**（👤 追加の囲み直しは**不要**） | 次に疑うのは特徴量＝`glyphMask` が明るい場面で選択的でない（全体の42〜73%をインク判定） |
| 2b | ✅ UI はユーザー確認済（「問題なし」2026-08-23）。⚠ この環境では `npm run build`/`dev` は動かない（`node_modules` 不在） | — | — |
| 3 | `LABEL_DEFAULTS` は**全て未較正**（`offsetRatio`/`heightRatio`/`minScore`）＝広く探す側に倒してある | ⏳ ①の受領で締める | 実機のラベルで未測定 |
| 4 | **決定性の残差 0.2%**（走ごとに 3506〜3515 フレーム） | ⏳ P5 の受入で確定 | P2 の持ち越し |
| 5 | HP 違反の残り5件（**0.16%**） | ⏳ 低優先 | 3090 フレーム中5件 |
| 6 | sim05 の本丸 **C40 / C41 / C44** は open（**意図的コスト**＝Phase 9 先行のため凍結） | ⏸ 凍結中 | `CALIBRATION_ANALYSIS.md` の Cx 行が正 |
| 7 | **S6〜S10 は機械化できていない**（人が見る） | ⏳ | `tools/skills/README.md` §4 |

⚠ ユーザー手元の**アトラスJSON（v0.35.0・cell 12×20）は世代が古い**。正は `tools/fixtures/t1_glyph_atlas_M3-1.json`
（cell 16×26・カンマを覚えない）。新しい切り抜きが来たら `node tools/t1_teach_probe.mjs --atlas …` で**作り直す**（`tools/README.md` §4）。

## 3. 次回の Next Steps

1. **⏳ P3-1b を閉じる**＝👤 から `dmg` 枠まるごとが届いたら `node tools/t1_scene_probe.mjs <scene>.json` で
   **マスク有無を突合**。効いていれば `LABEL_DEFAULTS`（現在**全て未較正**）を実測で締める。
   ⚠ 効かなければ**そう書く**（マージンが安全網にならなかったのと同型を繰り返さない）。
2. **⏳ P3-2 イベント畳み込み**（Phase 9 の本線・[PHASE9_PLAN.md](../PHASE9_PLAN.md) §4 P3-2）＝
   同一位置・同一値の連続フレームを1イベントに畳む。**出口条件＝OCR 後のイベント数が1桁減る**。
   ★**判定材料は画素の静止ではなく「読んだ値と位置」**（キラキラがセルを動かし続けるため＝P1 発見⑫）。
3. **スキルを実戦で1周する**は本セッションで**一部達成**（`skill:invariants` / `skill:transcribe` / `skill:doctor` /
   `skill:handoff` を実走）。⏳残＝`run-sim-experiment`（重い掃引でまだ通していない）。
4. DOC_RELATION_PLAN の常駐サブタスク＝**`burst_display.md` 新設＝トリガ該当**につき `doc:check` と `doc_refs --write` を実行し**カウンタ 0 に戻した**。

**ポインタ**: 成果物＝`transcribe/index.html` ＋ `src/transcribe/*.js`（**13本**・シム本体と非結線＝golden に非干渉）。
工具の版＝**T1 v0.36.0**。オフライン検証＝`tools/t1_teach_probe.mjs`（アトラス）／**`tools/t1_verify_probe.mjs`（検算）**。
スキルの索引＝[tools/skills/README.md](../tools/skills/README.md)。

---

## 更新履歴

<!-- 直近5件のみ（それ以前は git log）。「波及確認」列が本体＝git が持たない情報はここだけ。 -->

| 日付 | 変更点 | 波及確認 |
|---|---|---|
| 2026-08-23 | **P3-1b（読み取り＋検算）を反映**＝現フェーズ表・直近の実装・課題・Next Steps を全面上書き。押し出した 2026-08-22 の内容は `archive/SESSION_LOG.md` へ畳んだ | `src/`（シム本体）未変更＝**golden 3/3 実行確認で不変**。`test:t1` 337→**362件**・T1 精度不変・`doc:check` 現役層グリーン |
| 2026-08-05 | 末尾ブロックを新設（DOC_RELATION_PLAN S4・種別=現状スナップショット） | 参照関係は `npm run doc:check` がグリーン |

<!-- doc_refs:begin ── 自動生成。手で編集しない（node tools/doc_refs.mjs --write が再生成する） -->
## この md を参照している文書（現役層 5）

- [CLAUDE.md](../CLAUDE.md)
- [DOC_RELATION_PLAN.md](../DOC_RELATION_PLAN.md)
- [REPO_STANDARDS.md](../REPO_STANDARDS.md)
- [simulation/sim05/README.md](../simulation/sim05/README.md)
- [workspace/TODO.md](./TODO.md)

_他に 凍結sim/archive/essays から 2 件（更新対象外）_
<!-- doc_refs:end -->
