# 引継ぎ — 現状スナップショット

> **種別**: 引継ぎ（現状スナップショット・有界） ／ **用途**: 新セッション起動時に **CLAUDE.md と本書のみ**を読めば「今どこにいて次に何をするか」が分かる状態を保つ。
> **次タスクの詳細リスト**は `workspace/TODO.md`。過去の経緯・provenance は `archive/SESSION_LOG.md`（オンデマンド）。
> **更新規律（セッション末）**: 本書は「今」だけを短く保つ。現状化した記述は `archive/SESSION_LOG.md` の先頭へ1ブロック畳み、本書は最新状態へ上書きする（規律は REPO_STANDARDS §6）。
>
> 最終更新: 2026-08-22（★P3-1 アトラス取得 完了＝14枚・101点で 正80/曖昧15/誤6・実データを回帰に焼いた）

---

## 現フェーズ ── **Phase 9（実機観測 intake の自動化）／P3-1 完了・次は P3-1b（読み取り＋検算）**

**sim05（Phase 4）は凍結**し、転記の自動化基盤を先に作る（2026-08-07 ユーザー決定）。
**憲法**＝「**観測と判断はユーザー／導出・分析は Claude Code／転記・検算・整形はツール**」。
系として「**既に記録されたものから導けることは二度と人に訊かない**」。

**計画の正＝[PHASE9_PLAN.md](../PHASE9_PLAN.md)**（一次設計＝`TRANSCRIPTION_DESIGN.md`）。

## 進捗

| 段 | 状態 |
|---|---|
| **P0** 先行整備 ／ **§10** 確定仕様 | ✅ 完了（⚠§10 の変更にはユーザー承認が要る） |
| **P1** 実測ゲート | ✅ 通過（2026-08-14）＝進む |
| **P2** 抽出基盤 | ✅✅ **通過（2026-08-19・出口確認済）**。P2-1 / 1b / 1b′ / 3 / 5 / 5b ✅ ／ P2-2 ❎close ／ P2-4 ➡️P3-2 |
| **P3** OCR＋検算 | ⏳ **ここ**。P3-0 設計 ✅／**P3-1 アトラス取得 ✅完了（2026-08-22）**＝教える方式・実切り抜き**14枚**で **正80/曖昧15/誤6（101点）**・`validateAtlas` 通過／**P3-1b 読み取り＋検算 ⏳＝次の着手点** |

**成果物**: `transcribe/index.html` ＋ `src/transcribe/*.js`（12本）。Vite マルチページ・
**シム本体と非結線＝golden に非干渉**。回帰は **`npm run test:t1`（337件）＝うち [16-15] が実データ回帰**。工具の版＝**T1 v0.36.0**（照合パラメータは実データ掃引で確定）。
**オフライン検証**＝`tools/t1_teach_probe.mjs`＋`tools/lib/png.mjs`（依存なし PNG コーデック）。

## ✅ P2 で取れるようになったもの（P3 の入力）

| 観測値 | 状態 | 読み方の要点 |
|---|---|---|
| **HP%** | ✅ 違反 **5 / 3090（0.16%）**・時刻まで6走再現 | 上下帯＋階段フィット。`flash`/`noEmptyRegion` は**設計どおりの拒否** |
| **CT（チャージターン）** | ✅ **5ピップ・実機の正解と一致** | ★**点灯＝谷に対する色みの超過 120±0.5・`bg`≈6・`coin`=1**。偽物は `bg`≥20 か単一フレーム |
| **モードゲージ** | ✅ **塗り率が読める**（満 ≈102 / 空 ≈−1・左から溜まる） | **与ダメージの観測経路**（一次情報にある未モデル化メカニクス） |
| **モード遷移** | ✅ 除振後の8件が**6走連続で同一** | 生は 12〜20 とぶれる＝**除振後を使う** |
| ROI | ✅ **9枠すべて採寸済** | `hp`/`hpbar`/`modebar`/`ct`/`debuff`/`dmg`/`abil`/`turn`/`gauge` |

⚠ **持ち越す穴**（出口条件ではない）: **決定性の残差 0.2%**（走ごとに 3506〜3515 フレーム／**P5 の受入で確定**）／
`T1-DETECT-005` の死角（採用 fps を完全重複込みで数えている）／HP 違反の残り5件（遮蔽が空区間検査を通る型・0.16%）。

## ⏳ 次の一手（引継ぎ後の再開点）

### A. すぐ効く ── 👤 **緩い囲み2枚を教え直す**（枚数を足すより効く）

実切り抜き**14枚**で **1回抜き＝正 80 / 曖昧 15 / 誤 6（101点）**・**完全一致 3枚**（`5,044,101`×2・`5,314,682`）。
★★**誤り6件のうち5件が2枚に集中**＝`5,125,605`(153.53s) 誤3 ／ `5,553,703`(25.33s) 誤2 ／ 他11枚は**誤0**。
∴ **照合器の限界ではなく、囲みが緩い個体**の疑いが濃い（囲みは測定そのもの＝§4.3.0e）。
枚数 `0`×15 `1`×13 `5`×13 `4`×11 `6`×11 `3`×9 `8`×8 `9`×8 `2`×7 **`7`×6**。混同は **`3`/`5` の対**が中心。
★依頼の本文は **PHASE9_PLAN §4.3.1 の「次にお願いしたいこと」**（①2枚の教え直し ②`3`/`7` を含む表示 ③条件を混ぜる）。

### B. ✅ **実データは回帰に焼いた**（切り抜きが無くても詰めを続けられる）

`tools/fixtures/t1_glyph_atlas_M3-1.json`（280KB・**署名の数値だけ**＝画像ではない＝§10.3 の例外に明記済）。
`samples` 101点を持つので **`leaveOneTeachingOut` がオフラインで回る**＝**照合パラメータの掃引は次セッションでも続行可能**。
関門＝`npm run test:t1` [16-15]（**誤≦6 / 正≧80 / 曖昧≦20**）。裏取り＝**ずらし許容を切ると誤 6→21 で落ちる**。

⚠ ユーザー手元の**アトラスJSON（v0.35.0・cell 12×20・カンマ込み）は世代が古い**。
受け取っても**そのままは使わない**（現行は cell 16×26・カンマを覚えない）＝**上のフィクスチャが正**。
新しい切り抜きが来たら `tools/t1_teach_probe.mjs` 系で**フィクスチャを作り直す**（下記「再構築の手順」）。

**再構築の手順**（切り抜きJSONを受け取ったとき）＝**1コマンド**（`tools/README.md` §4）:

```bash
node tools/t1_teach_probe.mjs --atlas tools/fixtures/t1_glyph_atlas_M3-1.json \
  --source 'M3-1.mp4 の教え切り抜き（ユーザー提供）' <切り抜き>.json ...
npm run test:t1     # [16-15] の閾値を実測へ**締め直す**（緩めない）
```

⚠ **同じ表示の切り抜きが複数届いたら、囲みの狭い方（＝後から届いた方）を使う**。
実際に踏んだ＝`5,044,101`(20.0s) が 588×134 と 573×113 の2通りあり、**広い方だと `validateAtlas` が比 1.82 で落ちた**
（誤読も 6→7）。★**関門が緩い囲みを実際に捕まえている**＝§A の見立ての裏取りでもある。

### C. ★★P3-1b の設計前提が1つ変わった ── **安全網は検算に置く**

⚠ 合成では「**誤読の 100% がマージン割れ**」だったが、**実画素ではマージンを上げても誤りが減らない**
（0.04→誤8 / 0.12→誤8・73点）。★**実機の誤読は「自信のある誤読」**。
∴ P3-1b は **検算（§5）を安全網として設計する**＝`TOTAL` ↔ 個別ヒットの合計・カンマ文法・値域。
**マージンで守れる前提で設計してはいけない**。

### D. その先

P3-1b（ラベル検出→マスク→読み取り→検算③④⑦）→ **P3-2 イベント畳み込み** → **P4 config 版管理** → **P5 受入**。

## ⚠ 引き継ぐ注意（Phase 9 で繰り返し効いたもの）

> **軌跡の詳細（数値つき）は [essays/PHASE9_MATERIAL.md](../essays/PHASE9_MATERIAL.md)**（essay 素材・開発に拘束力なし）。
> **経緯は `archive/SESSION_LOG.md` の 2026-08-18〜19 ブロック**。

- ⭐⭐**測定器の性質を、観測対象の性質と取り違えない**。3回踏んだ＝**要約の畳み方**（上位10山→平坦のノイズを山と数えた）／
  **探索範囲の上限**（範囲外の「無し」を「無い」と読みかけた）／**大津法の2クラス性**（層が4つあった）。
  ∴ **可視範囲を測定結果と同じ画面に出す**（`searchRange`）・**判断に効く配列は畳まず全値で出す**。
- ⭐⭐**1つの回答から恒久的な抑止規則を作らない**＝静かで恒久的なブロッカーになる（`knownDecorX` を撤回）。
  ★**位置の問題は抑止規則ではなく採寸で解く**（人に1分で訊けることを機械に当てさせない＝憲法）。
- ⭐**閾値の段を足すより、参照点を変える**＝共通モード除去（谷を引く）で**演出が何段階あっても消えた**。
- ⚠**「再現する」は「本物」を意味しない**（除振で落とした 33ms の往復2組＝毎回同じフレームが標本に入るだけ）。
- ⚠**実データで改善を確認できない変更は入れない**（局所基準を試してやめた）。
- ⚠**検査できない場所にだけ事故が起きる**＝`main.js` は唯一 import できず、0バイトのまま2コミット出荷した。
  ∴ **DOM スタブで実際に実行する**（`npm run test:t1` [15]）＋**壊れたら画面が赤い帯で理由を言う**。
  ⚠⚠ **3度目（2026-08-19c）**: リファクタで `const rois` の宣言ごと消し、**押した瞬間に
  `ReferenceError`**。294件は全部通っていた＝**ハンドラが付いていることと、押して動くことは別**。
  ∴ スモークで**全ボタンを実際に押す**ようにした（バグを戻して落ちることを確認済）。
  ⚠⚠ **4度目（2026-08-21）**: 探索を捨てて `fit.contrast` を無くしたのに**画面側が
  `contrast.toFixed()` を呼び続け**、「教える」を押した瞬間に落ちた（319件は全通過）。
  ★**押した先の書式まではスモークが届かない**＝**書式を純関数へ出す**（`teachSummary`）＋
  **`fit.○○.toFixed(` を main.js に直書きしない**静的検査を追加した。
- ⭐**実運用は放置実行＝所要時間は二の次**（ユーザー確定）→ seek ベース（実時間の6〜8倍）は恒久的に正しい。
- ⚠**動画は git に入れない**（セッションへの共有は可）。**録画は貯めてよい**（`PHASE9_PLAN.md` §4.0.1）。

## ⏸ sim05 は凍結中（2026-08-07〜）

**M3〜M6 の実走・分析は Phase 9 完成まで止める**。**C40 / C41 / C44 は open のまま＝意図的に受け入れたコスト**。
**凍結解除**＝P5 受入通過。⚠ **ランクアップで走を捨てない**（E11）。

**到達点**: M1・M2 完了＝C40 を編成横断＋敵横断で確定／C44 を分解／C42 確定／C45・C47〜C50 起票。
詳細は `simulation/sim05/README.md` §2.2 と `simulation/sim05/analysis/integrated_analysis.md`。

**★Phase 9 で得た Cx の観測経路**（状態と根拠の正は `CALIBRATION_ANALYSIS.md`）:
**C47**＝`TOTAL` が①バースト追加ダメージ（内側）と②1アシ追撃（外側）を**画面上で分離**／
**C40**＝`追加 = TOTAL − 本体` が厳密に取れる（⚠**M3 は依然必要**）／
**C45**＝**CT（チャージターン）が録画から読める**＝敵行動モデルの intake の目処／
**C24**＝`gauge` の BG／**C38**＝⚠**録画からの近道は無い**（バフアイコン部分表示・区間推定方針は不変）。

## アクティブ作業ライン

| ライン | 状態 | 次アクション |
|---|---|---|
| **★Phase 9** | ⏳ **P3-1b（読み取り＋検算）が次** | P2 通過 → P3-0 設計 → P3-1 アトラス取得 ✅（教える方式・14枚・フィクスチャ化済） → ⏳ **P3-1b**（ラベル検出→マスク→`readRow`→検算③④⑦）。並行で 👤 §4.3.1 の依頼①（緩い囲み2枚の教え直し） |
| sim05（M3〜M6 / C40・C41・C44） | ⏸ 凍結 | Phase 9 完成後 |
| エジソン英霊武器の追加ダメは `TOTAL` の内外か | ⏳ 未確認 | **edison 編成を扱う前に**確認（C40 の留保に直結） |
| `configC_slot.json` の受領 | ⏳ 長期未受領 | Phase 9 の **P4** で効く |
| サポート幻獣の加護が全量スタックするか | ⏳ 未確認 | シムの前提。外れると全成分が一律 ×0.83 |
| override の再fit／他ハーネスの数値再取得 | ⏳ 着手禁止 | C40/C41/C44 確定後に1回だけ |
| Phase 8 アクセ実装 | ⏳ intake ゲート | — |

## ドキュメント・ポインタ

- **開発ルール・コード地図・確定仕様・検証**: `CLAUDE.md`（本書と対で必読）
  ／**不変条件の詳細**: `ENGINE_INVARIANTS.md`（**該当コードを触るときだけ開く**）
- **Phase 9**: `PHASE9_PLAN.md`（**§4 P2 に実装の設計判断と失敗の記録**・**§10 確定仕様**・§10.5.1 エラーコード台帳）
- **較正の Cx**: `CALIBRATION_ANALYSIS.md`（**C37/C38/C40〜C49 が open**・C50 は closed）
- **実験・計測の作法**: `REPO_STANDARDS.md` §6 **E1〜E11**（着手前に通す）
- **過去の経緯**: `archive/SESSION_LOG.md`（**2026-08-19〜22 ブロックが最新**）

## 検証（作業後は必ず）

```bash
npm run test:t1       # T1 転記ツールの回帰（337件・1秒未満／[16-15] が実データ回帰）
npm run doc:check     # md 相互参照（現役層に壊れた参照があれば exit 1）
npm run test:golden   # edison/raw 202,005,923・edison/cal 215,161,915・napoleon/static 299,523,354
```
⚠**golden は実測 2分07秒**（背景実行を推奨）。`src/transcribe/` はシム本体と非結線なので **golden は不変**。
`_replayResult`/`_execKey`/`clone`/`_snapshotForReplay` を触ったら **`node tools/exp_ls_incremental_verify.mjs`（約4分）も回す**。

---

## 更新履歴

<!-- 直近5件のみ（それ以前は git log）。「波及確認」列が本体＝git が持たない情報はここだけ。 -->

| 日付 | 変更点 | 波及確認 |
|---|---|---|
| 2026-08-22 | **★P3-1 アトラス取得 完了**。切り抜き **14枚**（累計）で **1回抜き＝正80/曖昧15/誤6（101点）**。実データを **`tools/fixtures/t1_glyph_atlas_M3-1.json`** へ焼き、`npm run test:t1` [16-15] を新設（**329→337件**） | ★★**誤り6件のうち5件が2枚に集中**（`5,125,605` 誤3・`5,553,703` 誤2／他11枚は誤0）＝**照合器の限界ではなく囲みが緩い個体**の疑い。∴ 次の依頼は「枚数を足す」から **「その2枚を囲み直す」** へ変えた（§4.3.1）。★フィクスチャは**署名の数値だけ**＝§10.3 に例外として明記（画像は入っていない）。裏取り＝**ずらし許容を切ると誤 6→21 で [16-15] が落ちる**＝関門が効いている。⚠ ユーザー手元のアトラス JSON は **v0.35.0（cell 12×20・カンマ込み）で世代が古い**＝使わない。★**更新履歴を規律どおり直近5件へ**（それ以前は git log と `archive/SESSION_LOG.md` の各ブロック＝**畳み先は全件存在を確認済**）。`src/`・`gamedata/js/` 未変更＝**golden 3/3 不変** |
| 2026-08-21c | **7枚で 正36 / 曖昧10 / 誤5（51点）**。照合パラメータを実データで掃引（cell 16×26・λ=1.0・ずらし±3） | ★`5,044,101` は**2枚とも完全一致**。掃引は 24通り×51点＝**誤り最小の帯**を採り、**argmax は採らない**（差は誤り 5〜9 と小さい）。★**最適化した量は正解数ではなく誤り数**（曖昧は次工程が拾える／もっともらしい誤読は較正を汚染する）。⏳ 残る誤り `8→0`×2 `0→8`×1 `4→1`×2＝**枚数の偏り**（`0`×11 に対し `8`×3・`3`×2）。⚠ 1枚抜きは**その切り抜きの全字を同時に失う**＝悲観的な指標。シム本体未変更＝golden 不変 |
| 2026-08-21b | **実画素で詰めた＝幾何は確定・読みは 正6/誤3 へ**（`test:t1` 324→328件・T1 v0.36.0） | ★ユーザー所感「1 の幅が小さい」を**データで否定**＝1あり/なしの切り抜きを比べると **r=1.00 で 0.17% 一致**（r=0.85 なら 3.7% 食い違う）＝**細いのはインクで送り幅は等幅**。★格子は正しい（インク重心のずれ ±7%・累積なし）。★**残っていたのは署名の横ずれ**（1〜3格子）で、ずらさないと**同字 0.35〜0.50 / 異字 0.70 と順序が逆転**していた → **±2格子ずらして最良**で **正2/誤10 → 正6/誤3**。★**カンマはテンプレートにしない**（セルの8割が背景・桁区切りは文法で決まる）。⚠ ずれの出どころは未解明＝**推測で幾何を触らず照合側で吸収**した。シム本体未変更＝golden 不変 |
| 2026-08-21 | **★★実画素で決着＝探索を捨て、囲みを測定として受け取る**（`test:t1` 314→319件） | ★実切り抜き3枚を Node で回して判明: ①**背景に数字と同じ明るさ・同じ色のキャラ絵**＝**画素特徴では分離不能**（4通りで確認）②芯は**金グラデ**＝しきい値1本で**下半分が消える**（前日の「明るさ」採用は実機で最悪＝正1/18）③候補格子を重ねて目視 → **カンマ比 0.5・送り幅＝囲みの幅÷(数字数+0.5×カンマ数)** がぴたり。∴ **人が囲む＝測定**・ツールは等分に置くだけ（`rois.js` の採寸と同じ規律）。実測 73.5/73.8px（本体）・99.8px（ストリーク）・**囲みが正しい1枚では箱が全数字にぴたり乗った**。⚠⚠ **3回作り直して3回とも「機械が位置を当てる」前提で外した**＝**同じ向きに3回外したら前提を疑う**。⏳ 署名の特徴量は暫定（3枚中2枚の囲みがずれ＝評価が汚染）。シム本体未変更＝golden 不変 |
| 2026-08-20c | **★★★特徴量が誤りだったと判明＝エッジ→明るさ**（`brightField`）。**実画素のオフライン経路**を新設（`tools/t1_teach_probe.mjs`＋依存なし PNG コーデック）。`test:t1` 311→313件 | ★**実物に忠実な合成**（白縁取り＋金芯＋むらのある背景）で測ると、**エッジでは送り幅 89/73/57（真値80）・1枚抜き 0/25**、**明るさでは 81/81/80・13/25**。⭐⭐ **帯（縦）の不安定も一緒に消えた**＝**症状の場所と原因の場所は違う**（切り出しの問題として2度直そうとして外した）。⚠ P2-2 の「金色では判定できない」と矛盾しない＝あれは ROI 全体での有無判定、ここは**人が数字だけを囲んだ中**。⏳ 残り＝1枚抜きで 正13/曖昧4/誤8（形が近い組）＝**実画素で詰める**。シム本体未変更＝golden 不変 |

<!-- doc_refs:begin ── 自動生成。手で編集しない（node tools/doc_refs.mjs --write が再生成する） -->
## この md を参照している文書（現役層 5）

- [CLAUDE.md](../CLAUDE.md)
- [DOC_RELATION_PLAN.md](../DOC_RELATION_PLAN.md)
- [REPO_STANDARDS.md](../REPO_STANDARDS.md)
- [simulation/sim05/README.md](../simulation/sim05/README.md)
- [workspace/TODO.md](./TODO.md)

_他に 凍結sim/archive/essays から 2 件（更新対象外）_
<!-- doc_refs:end -->
