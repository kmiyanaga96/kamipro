# 引継ぎ — 現状スナップショット

> **種別**: 引継ぎ（現状スナップショット・有界） ／ **用途**: 新セッション起動時に **CLAUDE.md と本書のみ**を読めば「今どこにいて次に何をするか」が分かる状態を保つ。
> **次タスクの詳細リスト**は `workspace/TODO.md`。過去の経緯・provenance は `archive/SESSION_LOG.md`（オンデマンド）。
> **更新規律（セッション末）**: 本書は「今」だけを短く保つ。現状化した記述は `archive/SESSION_LOG.md` の先頭へ1ブロック畳み、本書は最新状態へ上書きする（規律は REPO_STANDARDS §6）。
>
> 最終更新: 2026-07-31

---

## 現フェーズ
**Phase 4 = 統計的較正 × 反復可能ボス**（PHASE4_PLAN §3.5.1）。全分析を Claude Code 担当。

**sim05 は方針転換済み**: **ダメージ較正（C3/C5）は継続／ナポ・アリアンの押し順較正は先送り**。
押し順側は **C37（探索の不安定性）・C38（buffCount で tier 機能停止）** が未解決で意味をなさない。
較正はリプレイ突合で測るため探索の不安定性に汚染されない＝独立に進められる。

## ⏳ 次セッションの第一手

**実走（trialNN）— 推奨押し順は抽出済み**
`simulation/sim05/data/g3_recommended_order_v3_loki.md`（G3 v3・2026-07-31）。ロキ×configC placeholder で production 同経路から抽出。
- **撃破 T4**（T3 で HP の 98.7%）＝**ユーザーの実機実績「T3〜T4 安定討伐」と一致**。∴ **実走は T1〜T4 のみでよい**（T5 以降は 4.57倍のオーバーキル）。
- ✅ **押下数が最大41回/ターン**でも**戦闘の時間制限には余裕がある**（ユーザー確認 2026-07-31）＝実行可能。
- ⚠ **ATK は暫定のまま**。**proper configC はユーザー側で計算・保存が完了済み（2026-07-31）**＝**表示ATK/HP と装備パネルを受領して registry と `configC_gear_panel.md` を更新し、`tools/extract_order_loki.mjs` で再抽出する**のが次の具体作業。
- **最優先の取得物は §B（tier 実発動・バフアイコン数）＝C38 の解決データ**。押し順の順位検証は識別可能性が低い（C1/§12）ため二の次。

## 3トラックとゲート状況
| トラック | 内容 | ゲート |
|---|---|---|
| **A. ダメージ較正** | C3/C5（追撃・実機比×2.3〜6.9） | ⏳ **proper configC 待ち**（ボス切替は✅完了） |
| **B. 探索の安定化** | C37 | ✅ **ボス切替で回避＋受入検証 B4 済**／✅ **LS 実装済**（2026-07-30）。⚠**根治はしていない**（×1.25 で押し順52.8%・代理採点は手つかず）＝C37 は open |
| **C. 押し順・tier** | C38（buffCount）・予測探索 | ⏳ 実機データ待ち（`record_skeleton` §B） |

## 直近セッションの成果（2026-07-31・詳細は SESSION_LOG）
- **✅ 探索の2段実行（LS の重複除去）**: ユーザーから「エンジンが30分以上演算している」と指摘。診断＝**暴走ではない**（進捗表示の評価回数は
  **全ルートの合計**で、1ルート上限 `LS_MAX_EVALS=500000` は守られている）が、**同一ルートに LS を重複実行していた**のが実態。
  **①全 prefix をビームのみ → ②キー列で重複除去 → ③一意ルートのみ LS** の2段構成に変更。**結果は不変**（LS は (キー列,config) の決定的な純関数）でコストのみ削減。
  進捗表示にも「平均/上限」を併記して合計値の誤読を防いだ。
- **✅ G3 v3 推奨押し順を抽出**（ロキ×configC placeholder・production 同経路）。**撃破T4 が実機実績と一致**。
  副産物として **§12: loki 条件では prefix 分散が完全に空回り**（8本中6本が1円単位で同一・LS 後は上位3本が評価回数まで一致）＝C37 の編成依存**6例目**。LS 利得 +0.904%（7例目）。
- **✅ configC 向け新規3体を登録**（main から受領した一次情報 md 起点＝ROADMAP §5 の md-first intake）:
  - **メタトロン[神想真化]**（`CHAR_REGISTRY.metatron`）＝アビ3種＋バースト＋アシスト2種。**A0〜A10 を起票**（md §3.2）。
    **⚠運用前提が判明（ユーザー 2026-07-31）＝アルテミスと並べた"サブ運用"がセオリー**。∴ 意味を持つのは **AnotherLink（`subAssists`）のみ**で
    **アビリティ実装は凍結**（A1/A4〜A6/A8/A9）。HP依存機構は `反逆`（低HP維持）編成のもので**スコープ外＝A2/A3 クローズ**（md §3.4）。
    **残る要実機は ★A7（AnotherLink の重複規則）のみだが、これが configC 較正の前提になる**。
  - **ラジエル**（`SUMMON_REGISTRY.rasiel`）＝メイン 属性値枠+1.60／サブ +0.50（**条件付き**＝サブ幻獣が全て光属性のとき。汎用の `condition` 機構を新設）。
  - **カイザーゴッドドラグーン**（同 `caesar_god_dragoon`）＝**常時効果なし**（回復/防御のみ）。**採用理由は ATK/HP の高さ**で効果目的ではない（ユーザー 2026-07-31）＝`onSummon` 未実装は採用のブロッカーではない。⚠ATK寄与は**UIの手動合計入力**。
- **engine**: `subAssists` の枠を4つ拡張（`sub_na_dmg`/`sub_na_cap`/`sub_abi_dmg`/`sub_abi_cap`）＝AnotherLink[メタトロン]の全攻撃性能UPを表現。既定0で golden 不変。
- **登録中にバグ1件を摘出**: `rocket`（1アビ）は押下時に CD7 を張るため「天真爛漫時の同ターン2回目」が**構造的に発動不能**だった → アリアン `elegant_re` と同型の別キー `rocket_re` へ分離。

## 前セッションの成果（2026-07-30・詳細は SESSION_LOG）
- **✅ 較正ボスを `walpurgis_loki` へ切替**（sim05 README §4.4.0 新設／Q2 クローズ／両面宿儺は不採用＝`abilCapPerTurn:19` が探索の土台を壊すため生存ゲート以前に失格）。
  **受入検証 B4 も完了**: loki 実敵で**非単調が完全に消え**（厳密単調）、**運用域±10%の押し順は同位置95.7%/構成100%**（B1 の 88.8%/85.8% より良い）。1ルート143〜145s と安価・FB 全条件10/10。⚠**×1.25 では 52.8%**＝**根治ではなく回避**（C37 は open のまま）。
- **✅ LS 進捗表示**（LS 区間が無反応に見える UX 回帰を解消。フックが探索に影響しないことを検証済み）。
- **✅ 局所探索を production 実装**（`_localSearchRoute` が `_refineRoute` を置換・全 prefix に適用）。**golden 再fit: raw 201,909,711（+2.09%）／cal 214,213,430（+1.30%）**・napoleon/static は不変。ENGINE_VERSION → `sim05-ls-route-C37`。
- **付随して幽霊キーのバグを発見・修正**: `_execKey` が不成立キーを黙って no-op し、`greedyTakeTurn` が**押せなかったキーも押した扱いで記録**していた（LS ルートに実際に2件混入＝T6/T10）。実行できたキーのみ記録するよう正規化（総ダメージ不変）。
- **改善幅も条件依存と判明**: B3b の実gear条件 +0.78% に対し golden の default gear では **+2.09%**＝C37 の一般則に**5例目**。
- ⚠ **golden の所要が 116秒 → 約10分**（背景実行が必須）。

## 前セッションの成果（2026-07-28・詳細は SESSION_LOG）
- **②a configC ブロッカー解消**: 原因＝napoleon/arianrhod の override 未登録（実機比 −70.6%/−58.9%）→ **per-formation 構造**へ改修。napoleon 5/5 が 0.00% 一致・golden 不変。
- **G3 推奨押し順 v2**（v1 は英霊武器/サブ枠欠落で撤回）。⚠ATK 非不変が判明したため **proper configC 受領後は再抽出が必要**。
- **探索品質の実験9件**（BW掃引/実験1・2・4/局所探索5〜5d/A1/B1/B2/B3/B3b/C1）→ **C37・C38 起票**。
- **原因特定**: 不安定性は **abilCap 19 のナイフエッジ**（アリアン holy は無罪）。**エジソンは健全**。
- **押し順の識別可能性が根本的に低い**: 上位6本の差 0.13% はモデル誤差にも乱数(C28)にも埋もれ、さらに **1円単位で同値の別ルートが7本以上存在**（C1）。∴ 試走の意義は順位検証ではなく**構造情報**の取得。
- **編成依存が4例**（`BEAM_W=64` / Phase7 §7.2 / `CALIB_GRID` / C27）＝**運用原則として扱う段階**（→ TODO）。
- **インフラ**: `tools/` をルートへ昇格＋実験ハーネス14本をコミット（再現性確保）／**REPO_STANDARDS §6 に E1〜E8 制定**／C37 の肥大化解消（16,393→4,170字・根拠は `search_quality_experiments.md` へ分離）／実測コストの是正（golden 全体116秒・旧コメント「edison ~2s」は29倍の誤り）。

## アクティブ作業ライン
| ライン | 状態 | 次アクション |
|---|---|---|
| **① loki へボス切替** | ✅ **完了（2026-07-30）** | sim05 README §4.4.0 新設・Q2 クローズ。受入検証 B4 実施 |
| **A. sim05 ダメージ較正** | ⏳ configC 待ち | proper configC → override 更新 → D走で C3/C5 fit |
| **両面宿儺 試走** | ❌ **不要**（ボス切替で確定） | 較正には使わない。ただし**取得済みなら §B（tier実発動・バフアイコン数）は C38 の解決データとして有効** |
| **★メタトロン A7（AnotherLink 重複規則）** | ⏳ **要実機・最優先** | configC は metatron+artemis を両方サブ＝同枠が必ず競合。`final_dmg` 1.10 vs 1.21。**確定前に configC 絶対値較正をやると較正が誤差を吸収する** |
| **Phase 8 アクセ実装** | ⏳ intake ゲート | §6 intake をユーザーと確定後 |

→ 全タスクの優先順・チェックは **`workspace/TODO.md`**。

## ドキュメント・ポインタ（詳細は必要時のみ）
- **開発ルール・コード地図・確定仕様・検証・実測コスト**: `CLAUDE.md`（本書と対で必読）
- **較正の確定値・根拠・Cx バックログ**: `CALIBRATION_ANALYSIS.md`（**C37/C38 が最新**）
- **探索品質の実験の全数値**: `simulation/sim05/search_quality_experiments.md`（C37 の根拠アーカイブ）
- **計測ハーネス**: `tools/`（`README.md` が索引＝どのスクリプトがどの数値を出したか）
- **実験・計測の作法**: `REPO_STANDARDS.md` §6 の **E1〜E8**（着手前に通す）
- **sim05 の計画・押し順・記録様式**: `simulation/sim05/`（README・`data/g3_recommended_order.md`・`data/record_skeleton.md`・`data/configC_gear_panel.md`・`buffcount_sim_side_diag.md`）
- **Phase 一覧**: `ROADMAP.md` ／ **現行フェーズ**: `PHASE4_PLAN.md` ／ **規約**: `REPO_STANDARDS.md`
- **過去セッションの経緯**: `archive/SESSION_LOG.md`

## 検証（作業後は必ず）
```bash
npm run test:golden   # edison/raw 201,909,711・edison/cal 214,213,430・napoleon/static 299,534,299（全 FB10/10）
```
⚠**実測 約10分**（C37 局所探索の導入で 116秒から増加）＝ツール実行の600秒上限を超えるので**背景実行が必須**。docs のみの変更なら golden は不変。
