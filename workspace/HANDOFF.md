# 引継ぎ — 現状スナップショット

> **種別**: 引継ぎ（現状スナップショット・有界） ／ **用途**: 新セッション起動時に **CLAUDE.md と本書のみ**を読めば「今どこにいて次に何をするか」が分かる状態を保つ。
> **次タスクの詳細リスト**は `workspace/TODO.md`。過去の経緯・provenance は `archive/SESSION_LOG.md`（オンデマンド）。
> **更新規律（セッション末）**: 本書は「今」だけを短く保つ。現状化した記述は `archive/SESSION_LOG.md` の先頭へ1ブロック畳み、本書は最新状態へ上書きする（青天井化させない・規律は REPO_STANDARDS §6）。
>
> 最終更新: 2026-07-28

---

## 現フェーズ
**Phase 4 = 統計的較正 × 反復可能ボス**（PHASE4_PLAN §3.5.1）。全分析を Claude Code 担当。

**sim05 は方針転換済み（2026-07-28）**: **ダメージ較正（C3/C5）は継続／ナポ・アリアンの押し順較正は先送り**。
理由＝押し順側は **C37（探索の不安定性）・C38（buffCount で tier 機能停止）** が未解決で意味をなさない。
一方 **較正はリプレイ突合で測るため探索の不安定性に汚染されない**＝独立に進められる。

## ⏳ 次セッションの第一手
**実験5＝C27 リファインの一般化（局所探索）の効果測定**。着手承認済み・ただし **workspace 整理を先に実施（本コミットで完了）**。
- 内容: 確定ルートに対しアビの位置交換/移動を総当たりし、**リプレイして厳密に総ダメージが増えるときだけ採用**（単調安全）→改善が尽きるまで反復。
- 測ること: **BW64 の出力 2,007,021,635 に適用して、BW384 の 2,120,186,028 との 5.6% 差をどれだけ埋められるか**。埋まるなら探索幅を上げずに（＝UI体感を犠牲にせず）安定性と品質を同時に得られる。
- **リポジトリには触らず scratchpad で試作**（既存 export `_replayResult` のみで可能・数分見込み）。効果が出たら実装を改めて提案。

## 3トラックとゲート状況
| トラック | 内容 | ゲート |
|---|---|---|
| **A. ダメージ較正** | C3/C5（追撃・実機比×2.3〜6.9） | ⏳ **proper configC 待ち**（月末・目前）。ナポ/アリアン編成が必要（sim04 はエジソンで解けないと結論済み） |
| **B. 探索の安定化** | C37(a)（BW軸/ATK軸の非単調） | ✅ **なし＝今すぐ着手可能・編成非依存**（＝実験5） |
| **C. 押し順・tier** | C38（buffCount）・予測探索 | ⏳ A の実機データ待ち（record_skeleton §B） |

## 直近セッションの成果（2026-07-28・詳細は SESSION_LOG）
- **②a configC ブロッカー解消**: 原因＝napoleon/arianrhod の override 未登録（実機比 −70.6%/−58.9%）→ **per-formation 構造**（`DISPLAY_ATK/HP_OVERRIDE_BY_FORMATION`）へ改修。napoleon 5/5 が 0.00% 一致・**golden 3/3 不変**。GEAR は修正不要（パネル×1.8 で厳密一致）。
- **G3 推奨押し順 v2 確定**（v1 は英霊武器・サブ枠欠落で撤回）。最良prefix=`[factor]`・全ターン19手・撃破見込み T5。
- **C37 起票**: `BEAM_W=64` は編成依存＝本編成では最悪点（BW384 で +5.64%・**非単調**）。実験1で `PREFIX_TOPK=8` は転移確認（0.017%）。実験2で「順は絶対スカラに不変」が**不成立**（ATK1割で押し順7割変化・**ダメージが非単調**）。実験4で**上位順の差0.13% はモデル誤差に埋もれる**＝順位付け不能。
- **C38 起票**: `buffCount` が実機と別のものを数えている＝全tier常時到達で tier ロジック機能停止。**予測探索の前提**。
- **Phase 7 のクローズ根拠の一部が編成依存と判明**（§7.2「BW64 が大域最良＝実用上限」はエジソン測定）＝再評価の対象に戻った。

## アクティブ作業ライン
| ライン | 状態 | 次アクション |
|---|---|---|
| **B. 探索の安定化** | ⏳ 着手可 | **実験5（局所探索の効果測定・scratchpad）** |
| **A. sim05 ダメージ較正** | ⏳ configC 待ち | proper configC 受領 → override 更新 → 押し順再抽出 → D走で C3/C5 fit |
| **両面宿儺 試走** | ⏳ 保険として取得 | `record_skeleton` K モードで §A/§B/§D のみ記録 |
| **Phase 8 アクセ実装** | ⏳ intake ゲート | §6 intake をユーザーと確定後 |

→ 全タスクの優先順・チェックは **`workspace/TODO.md`**。

## ドキュメント・ポインタ（詳細は必要時のみ）
- **開発ルール・コード地図・確定仕様・検証**: `CLAUDE.md`（本書と対で必読）
- **較正の確定値・根拠・Cx バックログ全文**: `CALIBRATION_ANALYSIS.md`（**C37/C38 が最新**）
- **sim05 の計画・押し順・記録様式**: `simulation/sim05/`（README・`data/g3_recommended_order.md`・`data/record_skeleton.md`・`data/configC_gear_panel.md`・`buffcount_sim_side_diag.md`）
- **Phase 一覧・採番**: `ROADMAP.md` ／ **現行フェーズ台帳**: `PHASE4_PLAN.md` ／ **規約**: `REPO_STANDARDS.md`
- **過去セッションの経緯**: `archive/SESSION_LOG.md`

## 検証（作業後は必ず）
```bash
npm run test:golden   # edison/raw 197,775,394・edison/cal 211,462,826・napoleon/static 299,534,299（全 FB10/10）
```
docs のみの変更なら golden は不変。napoleon fixture は静的greedy回帰ガード＝モデル変更時は再fit。
