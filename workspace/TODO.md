# TODO — 次タスク（優先順）

> **種別**: to-do リスト（有界・作業台帳） ／ **用途**: 「次に着手するもの」を優先順で1か所に。各項＝1行コンテキスト＋ゲート状態＋ポインタ。
> **更新規律**: 完了項は `[x]` にしてセッション末に `archive/SESSION_LOG.md` の該当ブロックへ畳む（**本書からは削除する**）。新規タスクは REPO_STANDARDS §1 の振り分けを通してから追加。
> 現状の全体像は `workspace/HANDOFF.md`。**クローズ済みタスクの一覧は `archive/SESSION_LOG.md` が正**（本書に控えを置かない）。
>
> 最終更新: 2026-08-02（セッション末）

---

> **sim05 方針**: 主題は【追撃 C3/C5 較正】＋【tier 観測（C38）】＋【鬼神障壁の実測】。
> 押し順の序数検証は目的から外した（測り分け不能）＝**実走は実機勘のフリー押しでよい**が、
> **引き換えに「実際に押した順のターン別記録」が必須要件**（無いとリプレイできず較正が成立しない）。
> **3トラック**: A=ダメージ較正（**pre-trial 待ち**）／B=探索の安定化（LS 実装済・C37 は根治せず open）／C=押し順・tier（実機データ待ち）。

## ★次セッションの第一手 ── pre-trial.md の受領

- [ ] **`pre-trial.md` を受領して読む**（ユーザーが main へ push・両面宿儺の試走）。
- [ ] **★T1 局在の乖離を切り分ける**（sim05 README §4.4.1）。増分比 **T1 ×2.43 / T2 ×1.07 / T3 ≧×0.92** ＝**一律スカラでは説明できない**。
      仮説(1)（cap 19 が T1 で不成立）は**定量化済＝乖離の約2/3まで**（cap を外しても ×1.52・30手で自然枯渇／`tools/exp_t1_abilcap_sweep.mjs`）。
      **残り ×1.60 は (2)T1バースト過小 /(3)鬼神障壁 rate /(4)ベタイア cap 過小 の取り分**。⚠(1)と(3)は逆向き＝押下数と成分別ダメなしに配分不能。
- [ ] **`abilCapPerTurn` を「無敵中は無制限」へ条件化するか判断**（同 仮説1）。実機は T1 にテトラ1アシの無敵で鬼神一擲を無効化＝**cap は T1 に適用されないのが正しい**。
      ⏳ゲート＝**`record_skeleton` §2 の実 T1 押下数**（シムは cap 無しでも30手で自然枯渇＝実機が30を大きく超えるなら「手数モデル自体の不足」という別課題）。⚠既定敵は `enemy_abil_cap=null` ＝**golden への影響なし**。
- [ ] **高HPボスの再確認をユーザーへ**: 当初外した理由「①即死 ②未解放」のうち**①は被ダメ0の実績で弱まった**。②が解けるなら clean ターン不足（宿儺で2・要件4）が根本解決する。

## A. sim05 ダメージ較正（C3/C5・本丸）★pre-trial 待ち

- [ ] **C5（追撃cap）→ C3（式）** を**非会心・非急所アンカー**で fit。⚠ナポ/アリアン編成が必要（sim04 はエジソンで「非決定的fit・per-component fudge」と結論済＝戻すと袋小路）。
- [ ] **ベタイア cap の実機値を確定**（新仮説4・sim05 README §4.3）。シムは configC で **cap 飽和**（素点 1,213,875 → cap 800,000＝34%切り捨て・20ヒット/T）。**1ヒットの表示ダメージがそのまま実機 cap の下界**＝最も費用対効果が高い。
- [ ] アリアン絶対値 fit・A8 急所倍率。
- [ ] **★`search_calibrate.mjs` の再fit**（**C39 でダメージモデルが動いた**＝CLAUDE.md「モデル変更時は再fit必須」に対し**未履行**）。override `{judg:145,pactcore:1}` は据置のまま出荷中。⚠実験5c で「開始点が違うと LS 後の質が 3.8% 違う」と判明済＝選択最適 override が動く余地は大きい。
- [ ] **総ダメージの走間分散を実測**（C28 の理論見積り「サブ1%オーダー」は**未検証**。同一押し順の複数走で総ダメを控えれば副産物として取れる）。
- 詳細: `simulation/sim05/README.md`

## C. 押し順・tier ★実機データ待ち

- [ ] **C38: `buffCount` 差替 or 閾値再スケール**を決定（`record_skeleton` §3 受領後）→ napoleon golden 再fit。
      **取得方式＝tier 効果の成立/不成立から bc を区間推定**（アイコン計数は実機UI上 非現実的）。**consort は倍率 `2+0.5×bc` ＝表示ダメージから bc を直接逆算できる**（cap 250万に未到達なら）。
- [ ] **ナポアビ予測探索の実装**（1アビ=tier境界で即撃ち／2アビ=15未満予測なら最速／3アビ=20超え次第／4アビ=CD短縮の別軸）。⚠**C38 が前提**（未解決なら全ルールが即撃ちに縮退）。設計案＝`buffcount_sim_side_diag.md`（**検討のみ・未承認**）。
- [ ] **バースト係数 5.5/3000**（一次情報が正の見込み・**要実機**）: 確定まで 5.0/2500 据え置き。

## configC の前提を潰す（較正より先）

> **運用前提（ユーザー提供・2026-07-31）**: ①メタトロンは**アルテミスとサブに並べて火力底上げ**がセオリー＝**アビリティ実装は凍結**（意味を持つのは AnotherLink のみ）。②カイザーゴッドドラグーンは **ATK/HP 目的**の採用。

- [ ] **★A7: AnotherLink の重複規則を実機で確定する（最優先）**。configC は metatron＋artemis を**両方サブ**＝**同枠が必ず競合**。現行 `Math.max`（非累積）だが、加算なら `sub_burst_dmg` 0.25→0.45 / `sub_burst_cap` 0.10→0.20 / **`final_dmg` 1.10→1.21（全ダメージに乗る）**。⚠**確定前に絶対値較正をやり直すと較正スカラが規則の誤差を吸収する**。詳細 `gamedata/md/神姫/metatron.md` §3.3。
- [ ] **サブ枠入替の副作用を評価**: `[freyja_christmas, artemis]` → `[metatron, artemis]` で**フレイヤの `streak_dmgup: 1.1` を失う**（→1.0）。ストリークは全FBに乗る＝メタトロン採用の得失はこの喪失込みで見る。
- [ ] **カイザーゴッドドラグーンの ATK 寄与はUIの手動合計入力**（registry の `atk` はダメージ計算に使われない）。configC で採用するなら**UI側の幻獣ATK合計に含める**こと。

## 長時間ジョブ（①②は完了・残り）

- [ ] **③LS 内部の投機並列**（**実装コスト高**）。LS は first-improvement で受理順が結果を決めるため素朴な並列化は結果を変える。**スキャン順の先頭K個を投機評価→改善した最初の1件だけ採用・残りは破棄**なら受理列・着地点・評価回数まで同一にできる。4コアで 2.5〜3x 見込み。
- [ ] **他ハーネスへの横展開**（`exp_beam_width_sweep` / `exp_prefix_sweep` / `exp_atk_sensitivity_*` は掃引条件が独立＝`tools/lib/parallel_map.mjs` をそのまま適用できる）。
- [ ] **ローカルPCへの移行**（コア数で②③が素直に伸びる／揮発しない／600秒上限が無い／キャッシュが温まる／チェックポイント可能）。⚠移行しなくても①②の効果は現環境で出ている。

## 実機転記の半自動化（**後日検討**）

- [ ] `TRANSCRIPTION_DESIGN.md` の **S0（総和チェックのみ・AI不要）** から着手するか判断。転記コストが較正の律速＝走数を増やせない原因。
- [ ] 同 §6 の未確認事項をユーザーに確認（録画の解像度/fps・ポップアップの重なり頻度・成分の見分けが表示だけで付くか）。

## LS 実装の follow-up（低〜中優先）

- [ ] **探索中の進捗表示が LS 区間で止まって見える**（UX 回帰・高速化で待ち時間は縮んだが解消はしていない）。`_runRootPlan` は10ターン完遂**後**に LS を回すため、進捗バーが 10/10 に達したあと無反応に見える＝ハングと区別がつかない。対処案＝`_runRootPlan` に LS 用コールバックを足し worker→app.js で「最適化中… sweep n」を出す（既存 progress protocol への additive 変更）。**中断ボタンの応答性も要確認**（`_simCancelled` は worker のメッセージ境界でしか効かない）。
- [ ] **LS の sweep 間プルーニング（⚠golden 再fit が必要）**: 毎 sweep で全近傍を再走査しているが受理は sweep が進むほど稀。「前回評価以降に変更のあったターンを含む候補だけ再評価」で大幅に減らせる可能性。⚠**走査順・受理順が変わる＝別の局所最適へ着地する**ので結果が変わる（③の投機並列＝結果不変とは性質が違う）。
- [ ] **ターン跨ぎ swap の費用対効果**（評価の73%を消費して採用は raw 4件/cal 0件。ただし raw ではその4件が改善の32%）。近傍を |t1-t2| で窓制限した場合の品質/コストは**未測定**。

## 構造課題（C37 由来・要相談）

- [ ] **`CALIB_GRID` の構造的欠落**（ナポ/アリアンのアビを1つも含まない＝新編成の主役2人の静的スコアを較正できない）。
- [ ] **Phase 7 の再評価**（クローズ根拠 §7.2「BW64が大域最良＝実用上限」がエジソン依存と判明＝ROADMAP 上の扱いを要相談。ただし §7.1「浅ビームは full と反整合」は未検証＝編成非依存かもしれない）。

## 保留（概算時間が大きい・ユーザー判断 2026-07-28）

- [ ] **実験3**: `calibrateStaticScores` の評価ズレ（単一ビーム採点 vs 本番8prefix。較正が `{}` を選んだのに別 override が 0.93% 良かった現象を観測済み）。約51分〜2時間。**安全に回せる**（並列化で短縮可）。
- [ ] **実験1b**: `BEAM_DIVERSITY_K` の寄与測定。約70分＋**`constants.js` の一時改変が必要**（revert 漏れが golden を汚すリスク）。実験1で「入口は健全」と示唆済みゆえ優先度低。

## フェーズ着手待ち

- [ ] **Phase 8（アクセサリー実装）**。⏳ゲート＝§6 intake をユーザーと確定。詳細 `PHASE8_PLAN.md`。

## 並行・後続（低優先）

- [ ] **`_configSig` に `DMG.affinity` が入っていない**（2026-07-30 発見・低severity）。UI の属性相性トグルや、def/HP が同じで affinity だけ違う敵の間で**結果キャッシュのキーが衝突**する。実害はリプレイ検証で evict されるため**誤答にはならない**＝キャッシュが無駄になるだけ。
- [ ] **陳腐化したハーネスの整理**: `extract_order_loki`（敵が loki のまま・override が C37 世代）と `archive/caches/sim05_*.json` を読む3本（engineVersion 2世代前＝E2 が通らない）。再実行が必要になった時点で現行エンジンのアンカーを取り直す。
- [ ] C24（ゲージ±5〜10 の系統乖離）は較正走に相乗りで診断更新。
- [ ] CHARACTER_ANALYSIS §2（ヤマト vs アリアン）を較正確定後に再測。
- [ ] essays 更新（次の較正確定後）。
