# TODO — 次タスク（優先順）

> **種別**: to-do リスト（有界・作業台帳） ／ **用途**: 「次に着手するもの」を優先順で1か所に。各項＝1行コンテキスト＋ゲート状態＋ポインタ。
> **更新規律**: 完了項は `[x]` にしてセッション末に `archive/SESSION_LOG.md` の該当ブロックへ畳む（本書からは削除）。新規タスクは REPO_STANDARDS §1 の振り分けを通してから追加。
> 現状の全体像は `workspace/HANDOFF.md`。
>
> 最終更新: 2026-07-31

---

> **sim05 方針**: ダメージ較正（C3/C5）は継続／ナポ・アリアンの押し順較正は先送り（C37/C38 未解決では意味をなさない）。
> **3トラック**: A=ダメージ較正（configC＋ボス切替待ち）／B=探索の安定化（**LS 実装済 2026-07-30・残=ボス切替**）／C=押し順・tier（実機データ待ち）。

## ★次セッションの第一手

- [ ] **実走（trialNN）**: G3 v3 の推奨押し順が出た（`data/g3_recommended_order_v3_loki.md`）。**T1〜T4 のみ実行すれば足りる**（T4 撃破見込み）。⚠**§B（tier 実発動・バフアイコン数）の記録が C38 の解決データ＝押し順の検証より優先度が高い**。⚠**押下数が最大41回/ターン**（`abilCapPerTurn` 無しで上限が効かない）＝**実行可能性はユーザー確認が必要**。
- [ ] **proper configC で G3 を再抽出**（ATK 非不変＝実験2）。ハーネスは `tools/extract_order_loki.mjs`（引数=LS対象本数）。⏳**ユーザーから proper configC の表示ATK/HP と装備パネルを受領する必要がある**（2026-07-31 時点でユーザー側UIでの計算・保存は完了済み。`DISPLAY_ATK/HP_OVERRIDE_BY_FORMATION.napoleon` と `data/configC_gear_panel.md` の更新が前提）。あわせて §4.4.2 の placeholder（`elem 1.04` のサブ幻獣全光条件など）も実値へ差し替える。

## LS 実装の follow-up（2026-07-30・低〜中優先）
- [ ] **探索中の進捗表示が LS 区間で止まって見える**（UX 回帰）。`_runRootPlan` は10ターン完遂**後**に LS を回すため、各ルートの進捗バーが 10/10 に達したあと **数分間 無反応に見える**（LS は edison で 177〜324秒・ナポではさらに長い見込み）。ハングと区別がつかない。対処案＝`_runRootPlan` に LS 用コールバックを足し worker→app.js で「最適化中… sweep n」を出す（追加6行程度・既存 progress protocol への additive 変更）。**中断ボタンの応答性も要確認**（`_simCancelled` は worker のメッセージ境界でしか効かない）。
- [ ] **LS の評価コスト削減（任意・第2弾）**: 近傍評価は毎回10ターン全リプレイだが、ターン t の変更は **t 以降しか変えない**＝各ターン開始時の `Sim` スナップショットを持てば約2倍速くなる見込み。⚠`clone()` は `planDepth+1` するため静的greedy経路へ落ちる＝そのまま流用不可（要 planDepth/T のリセット）。**結果は不変**（評価値は同じ）なので golden 再fit は不要。
- [ ] **LS の sweep 間プルーニング（要検討・⚠golden 再fit が必要）**: 現在は毎 sweep で全近傍を再走査するが、受理は sweep が進むほど稀になる。「前回評価以降に変更のあったターンを含む候補だけ再評価」で大幅に減らせる可能性がある。⚠**走査順・受理順が変わると別の局所最適へ着地する＝結果が変わる**ため golden 再fit が要る。上の「評価コスト削減」（結果不変）とは性質が違う点に注意。
- [ ] **ターン跨ぎ swap の費用対効果**（評価の73%を消費して採用は raw 4件/cal 0件。ただし raw ではその4件が改善の32%）。近傍を |t1-t2| で窓制限した場合の品質/コストは**未測定**。

## configC 新規3体の登録 follow-up（2026-07-31 登録）

> **運用前提（ユーザー提供のプレイヤー集合知 2026-07-31）**: ①メタトロンは**アルテミスとサブに並べて火力底上げ**がセオリー＝**アビリティ実装は凍結**（意味を持つのは AnotherLink のみ）。②カイザーゴッドドラグーンは **ATK/HP 目的**の採用で効果目的ではない。③`反逆`（低HP維持）編成というアーキタイプが存在しメタトロンはその一員だが、**サブ運用のため本シムでの考察は現時点で不要**。
- [ ] **★A7: AnotherLink の重複規則を実機で確定する（configC 較正の前提・最優先）**。configC は **メタトロン＋アルテミスを両方サブ**に置く構成が前提（ユーザー 2026-07-31）＝**同枠が必ず競合する**。現行は `Math.max`（非累積）だが、実機が加算なら `sub_burst_dmg` 0.25→0.45 / `sub_burst_cap` 0.10→0.20 / **`final_dmg` 1.10→1.21（全ダメージに乗る）**。⚠**この規則を確定させる前に configC で絶対値較正（C25系）をやり直すと、較正スカラが規則の誤差を吸収してしまう**。詳細 `gamedata/md/神姫/metatron.md` §3.3。
- [ ] **サブ枠入替の副作用を評価**: configC が `[freyja_christmas, artemis]` → `[metatron, artemis]` になると **フレイヤのサブアシスト `streak_dmgup: 1.1` を失う**（→1.0）。ストリークは全FBに乗るため、メタトロン採用の得失はこの喪失込みで見ること。
- [ ] **カイザーゴッドドラグーンの ATK 寄与はUIの手動合計入力**（registry の `atk` はダメージ計算に使われない）。configC で採用するなら**UI側の幻獣ATK合計に含める**のを忘れないこと（採用理由が ATK/HP そのものであるため実質必須）。

## A. sim05 ダメージ較正（C3/C5）★proper configC 待ち
- [ ] **proper configC 受領 → `DISPLAY_ATK/HP_OVERRIDE_BY_FORMATION.napoleon` を更新**（現在は暫定・`data/configC_gear_panel.md` §2 も同時更新）。
- [ ] **押し順の再抽出**（上記「第一手」と同一項目）。実験2 で ATK 非不変が判明＝`g3_recommended_order.md` はそのまま流用不可。**ボスは loki へ切替済み**（2026-07-30）。
- [ ] 固定＝シム推奨順で D走 → **C5（追撃cap）→C3（式）** を非会心非急所アンカーで fit。⚠ナポ/アリアン編成が必要（sim04 はエジソンで「非決定的fit・per-component fudge」と結論済＝戻すと袋小路）。
- [ ] アリアン絶対値 fit・A8急所倍率。override 再較正・golden 戦略（napoleon fixture の再fit）。
- [ ] **`search_calibrate.mjs` の formal 再fit を判断**（LS 実装の follow-up・留保付き）。override `{judg:145,pactcore:1}` は「override はルート**選択**を制御し LS は選択後の**単調**パス」として据え置いたが、実験5c で「開始点が違うと LS 後の質が 3.8% 違う」と判明した以上**選択最適 override が動く余地は C27 時より大きい**。根拠 CALIBRATION_ANALYSIS C37。
- [ ] **総ダメージの走間分散を実測**（C28 の理論見積り「サブ1%オーダー」は**未検証**。同一押し順の複数走で総ダメを控えれば副産物として取れる）。
- 詳細: `simulation/sim05/README.md`

## C. 押し順・tier ★実機データ待ち
- [ ] **C38: `buffCount` 差替 or 閾値再スケール**を決定（`record_skeleton` §B のデータ受領後）→ napoleon golden 再fit。
- [ ] **ナポアビ予測探索の実装**（1アビ=tier境界で即撃ち／2アビ=15未満予測なら最速／3アビ=20超え次第／4アビ=CD短縮の別軸）。⚠**C38 が前提**（未解決なら全ルールが即撃ちに縮退）。設計案＝`buffcount_sim_side_diag.md`（**検討のみ・未実装・未承認**）。
- [ ] **バースト係数 5.5/3000**（一次情報が正の見込み・**要实机**）: 確定まで 5.0/2500 据え置き。

## 両面宿儺 試走（優先度低下・保険）
- [ ] ボスが loki へ移るため**必須ではない**。既に取得済みなら §B（tier実発動・バフアイコン数）が **C38 の解決データ**として有効。

## 運用原則として明文化すべき事項（本セッションで4例目に到達）
- [ ] **「エジソン編成で確立した機構・定数は、新編成では必ず再測する」を運用原則として明文化**（置き場所は REPO_STANDARDS §6 か CLAUDE.md 開発ルールか要相談）。該当4例＝`BEAM_W=64`(C16)／Phase 7 §7.2「BW64が大域最良」／`CALIB_GRID`（ナポ/アリアンのアビを1つも含まない）／**C27リファイン**（`deploysRobot`/`prelude` タグ不在で発火せず）。個別 Cx では捉えきれない横断的性質。
- [ ] **`CALIB_GRID` の構造的欠落への対処**（ナポ/アリアンのアビを含まない＝新編成の主役2人の静的スコアを較正できない）。
- [ ] **Phase 7 の再評価**（クローズ根拠 §7.2「BW64が大域最良＝実用上限」がエジソン依存と判明＝ROADMAP 上の扱いを要相談。ただし §7.1「浅ビームは full と反整合」は未検証＝編成非依存かもしれない）。

## 保留（概算時間が大きい・ユーザー判断 2026-07-28）
- [ ] **実験3**: `calibrateStaticScores` の評価ズレ（単一ビーム採点 vs 本番8prefix。較正が `{}` を選んだのに別 override が 0.93% 良かった現象が観測済み）。約51分〜2時間。**安全に回せる**。
- [ ] **実験1b**: `BEAM_DIVERSITY_K` の寄与測定。約70分＋**`constants.js` の一時改変が必要**（revert 漏れが golden を汚すリスク＝慎重に）。実験1で「入口は健全」と示唆済みゆえ優先度低。

## フェーズ着手待ち
- [ ] **Phase 8（アクセサリー実装）**。⏳ゲート＝§6 intake をユーザーと確定。詳細 `PHASE8_PLAN.md`。

## 並行・後続（低優先）
- [ ] **`_configSig` に `DMG.affinity` が入っていない**（2026-07-30 発見・低severity）。UI の属性相性トグルや、def/HP が同じで affinity だけ違う敵の間で**結果キャッシュのキーが衝突**する。実害はリプレイ検証（`tryResultCache` が保存 dmg と再生 dmg を突合）で evict されるため**誤答にはならない**＝キャッシュが無駄になるだけ。ボス切替のたびに気づきにくいので署名へ追加しておきたい。
- [ ] C24（ゲージ±5〜10 の系統乖離）は較正走に相乗りで診断更新。
- [ ] CHARACTER_ANALYSIS §2（ヤマト vs アリアン）を較正確定後に再測。
- [ ] essays 更新（次の較正確定後）。

---

## 参考: 最近クローズした主タスク（詳細は archive/SESSION_LOG.md）
- ✅ sim04 絶対値較正（2026-07-21・C25/C30/C31/C34）
- ✅ sim05 前タスク①②（鬼神障壁＋アビ上限）実装＋両面宿儺登録（2026-07-24）
- ✅ アリアン holy 無限ループ修正＋編成別 golden＋ナポ闘気実装（2026-07-25）
- ✅ **②a configC ブロッカー解消＝per-formation override 導入**（2026-07-28）
- ✅ **G3 推奨押し順 v2 抽出**（2026-07-28・v1 は英霊武器/サブ枠欠落で撤回）
- ✅ **C37 起票＋実験9件**（BW掃引/実験1・2・4/局所探索5〜5d/A1/B1/B2/B3/B3b/C1）（2026-07-28）
- ✅ **C38 起票**（buffCount が実機と別のものを数えている）／**C28 に確定仕様追記**（乱数±5%・11通り）（2026-07-28）
- ✅ **`tools/` ルート昇格＋実験ハーネス13本コミット**（再現性確保）（2026-07-28）
- ✅ **REPO_STANDARDS §6 に E1〜E8 制定＋ID接頭辞 Ex 登録**（2026-07-28）
- ✅ **C37 肥大化解消**（16,393→4,170字・根拠を `search_quality_experiments.md` へ分離）（2026-07-28）
- ✅ **② 局所探索の production 実装**（2026-07-30・`_localSearchRoute` が `_refineRoute` を置換・golden 再fit raw 201,909,711 / cal 214,213,430）
- ✅ **LS 進捗表示の実装**（2026-07-30・`onLS` フック→worker→UI。フックが探索に影響しないことを n=4/6 で検証＝キー列・ダメージ・評価回数まで一致）
- ✅ **探索の2段実行（LS の重複除去）**（2026-07-31・ビーム→キー列で重複除去→一意ルートのみ LS。**結果不変でコストのみ削減**）
- ✅ **G3 v3 推奨押し順の抽出**（2026-07-31・ロキ×configC placeholder。撃破T4＝実機実績と一致／prefix 分散が空回りと判明＝§12）
- ✅ **configC 向け新規3体の登録**（2026-07-31・メタトロン[神想真化]／ラジエル／カイザーゴッドドラグーン。golden 3/3 不変）
- ✅ **① 較正ボスを `walpurgis_loki` へ切替**（2026-07-30・sim05 README §4.4.0 新設／Q2 クローズ／**受入検証 B4 完了**＝単調性は完全回復・運用域±10%で同位置95.7%。⚠×1.25 で52.8%＝根治ではなく回避）
