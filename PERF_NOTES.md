# 探索エンジン高速化メモ（PERF_NOTES）

> シミュ実行（`runSim`）の待ち時間に関する**調査結果・実装済み施策・採否判断**の記録ハブ。
> Antigravity 協働のための共有ドキュメント。一次情報は [CLAUDE.md](./CLAUDE.md)、本書はその性能面の詳細台帳。
> （Phase 3 計画の原本は `archive/PHASE3_PLAN.md`。本書は実測・PoC を踏まえた現行の到達点を示す。）

## 1. 待ち時間の支配式（計測ベース）

```
待ち時間 ≒ ⌈ ルートタスク数 ÷ コア数 ⌉ × 1ルート約13秒
```

- `runSim` は `enumerateRootPrefixes()` が返す開幕強制ルート（基準形成で23本）＋baseline 1本を Worker プールへ分配し並列実行。
- 1ルート＝`_runRootPlan`＝10ターン×`greedyTakeTurn`。各ターン planDepth=0 で `_beamSearch(BEAM_W=32)`。
  各ステップで候補ごとに clone→`_finishStatic`→`_objective`（**残り全ターンを静的greedyロールアウト**して採点）。
- 高スペック機でも `navigator.hardwareConcurrency` は概ね8〜16。実効8コアなら ⌈24/8⌉=3波×13s ≈ **約40秒**（体感と一致）。
- 1ルートの per-turn コスト（基準形成）: T1≈3.3s（残9ターン先読み＝最重）→ T10≈0.3s。
- per-op の約50%が `use`/`burst`/`_stepStatic`（数百万回/ルート）。`_decay`/`_na` は多回数だがアロケーション無し。

**効きどころは2軸**: ①タスク数（ルート数）と ②per-task時間。コア数は不可制御。

## 2. ゴールデン値（92,031,195）の位置づけ

- 検証ワンライナー（CLAUDE.md）は **単一 `Sim.takeTurn()`（ルート分散なし）** で 92,031,195 を出す。**`enumerateRootPrefixes`/`_selectRootPrefixes` を経由しない**ため、ルート選抜やビーム幅の変更の影響を受けない＝**回帰ゲートとして常に有効**。
- 一方 `runSim`（実UI）の最終ダメージはルート分散で 92,031,195 を上回りうる（基準形成では勝者 `divinus` で 92,541,906）。**「92,031,195」は探索設定依存のスナップショットであり厳密最適ではない**（ビーム幅を変えると上振れもする）。挙動保存＝この設定下での完全一致を指す。

## 3. 実装済み施策

| 記号 | 内容 | 効果 | 挙動保存 | コミット趣旨 |
|---|---|---|---|---|
| Phase3-1 | `_stepStatic`/`_candidates` のアロケフリー化（事前計算マップ） | 約17% | ○ | 第一手 |
| D | 死コードの内部ビーム（`BEAM_W_INNER`）除去 | 0（未実行コード） | ○ | greedyTakeTurnにdepth=1が存在せずロールアウトは静的greedy固定と判明 |
| E | `clone()` の二重コピー排除（snap非経由・buf単一slice・キー事前計算） | 約8〜10% | ○ | per-task 短縮 |
| ①-A | 静的proxyによる2段ルート選抜（`_selectRootPrefixes`・`PREFIX_TOPK=10`） | タスク数を約1/2〜1/2.4 → 待ち約2〜3倍改善 | △（品質≤0.013%） | タスク数軸 |

### ①-A の仕組み
全prefixを `_staticPrefixDmg`（開幕強制＋全ターン静的greedy・ビーム不使用・約数ms）で安価に採点し、
上位 `PREFIX_TOPK` 本のみ本選（BW32）へ回す。空prefix（単一ビーム＝回帰基準）は常に確保。
`runSim` と `_fallbackRunSim` の双方が `_selectRootPrefixes(n)` を使用。

## 4. 路線①（品質トレードオフ）PoC 実測

真値＝BW32で全prefix探索（＝改修前の本番挙動）。loss% ＝ 2段選抜の上位Kの本選最良が真値最良からどれだけ落ちるか。4形成で測定。

| 戦略 | 最大loss | 本選タスク数 | 形成別loss |
|---|---|---|---|
| 静的proxy 上位3 | 8.35% | 3 | [0.88, 0.02, 0.00, 8.35] |
| 静的proxy 上位5 | 8.29% | 5 | [0.88, 0.01, 0.00, 8.29] |
| **静的proxy 上位8**（採用Kの下限） | **0.013%** | 8 | [0, 0.01, 0, 0] |
| BW4 proxy 上位3 | 3.84% | 3 | [0, 0.05, 3.84, 0] |
| union(静的top2 + BW4top2) | 0.017% | ≤4 | [0, 0.02, 0, 0] |
| 単一ビームのみ（ルート分散撤廃） | 8.35% | 1 | [0.88, 0.01, 3.82, 8.35] |

- **静的とBW4 proxyは「外す形成」が逆＝相補的**。単一proxyの小Kは2〜8%＝著しい劣化で却下。
- **静的proxy 上位8で最大loss 0.013%**（押し順・火力指数グレードに不可視）。採用は安全側に `PREFIX_TOPK=10`（上位8の上位集合のため単調に loss≤0.013%）。
- union(静的2+BW4 2) はさらに少タスク（≤4）で同等品質だが BW4 ランキングpassを要するため、最小コードの静的K10 を採用。より攻めるなら union を再検討する余地（`archive/PHASE3_PLAN.md` の将来候補）。
- 参考: PoC 4形成の勝者prefixは全て**単一キー**（divinus/consort/alone/puvoir）。ロボ順列等の多手prefixは未勝利だが、4形成では「常に不要」とは断定せず prefix集合の構造的削減は保留。
- ロスターは小規模（光神姫5＋英霊2）で本番使用空間の被覆は実用上妥当だが、新キャラ追加時は `PREFIX_TOPK` の余裕を再確認すること（下記）。

## 5. 路線②（WASM化）— 最終手段・実現可能性は極小

- per-task を 2〜5倍にしうる本命だが、今回の Worker フリーズ不具合の経験を踏まえ **大規模アーキ移行のリスクを忌避**。以下より優先度を大きく下げ、**他に取りうる選択肢が尽きた段階での最終手段**とする。
- 主な不利（詳細は `archive/PHASE3_PLAN.md` §2.4）:
  - 外部ビルド工程の追加が「外部ビルド不要」方針（CLAUDE.md/AGENTS.md）と衝突。
  - JS/WASM 二重実装の恒常的同期コスト。`characters.js` の約79個のキャラ挙動関数の橋渡しが未解決（JS据置きなら境界跨ぎで速度優位が消失／WASM移植なら「CHAR_REGISTRYが唯一の編集先」基盤を破壊）。
  - 跨言語の浮動小数点パリティ担保が困難（タイブレーク・丸めのドリフト）。
  - **今回の40秒症状はタスク数軸が支配的で、per-op改善（WASM）は的が甘い**。

## 6. 今後の指針

- まず **①-A（実装済）＋E＋D** で待ち約2〜3倍改善。なお不足なら次に検討する順:
  1. `PREFIX_TOPK` の調整（品質と速度の単純トレード・可逆）。
  2. union(静的+BW4) 選抜への置換（少タスク化・要BW4 pass）。
  3. アロケーション削減の継続（tick の map/filter 等・逓減・低リスク）。
  4. （最終手段）WASM。
- **新キャラ追加・`abilities`/`cands` 変更時**は、PoC（`_selectRootPrefixes` の上位Kに真の勝者が残るか）を数形成で再実行し、`PREFIX_TOPK` の余裕を再確認すること。検証スクリプトは scratchpad の `poc.js`（真値=BW32全探索 vs 静的/BW4 proxy 上位K）に準拠。
