# Phase 5 計画 — 探索UX刷新（待機画面の本格リニューアル）

> Phase 4（C9）でビーム幅を BW32→128 に拡張し探索量が約4倍になった。装備の充実で探索コストは
> さらに増大しうる（C9で実証＝強ギアほど最適枝が深い）。本フェーズは**探索中のユーザー体験**を
> 本格的に作り直し、「重い探索でも待てる／状況が分かる／中断できる」UIを整える。
> 起点はバックログ C10（ユーザー起案 2026-06-28）。一次台帳は本書。火力モデル・探索品質には**一切手を入れない**（UX専任フェーズ）。
> 旧 Phase 5（敵行動・味方生存）は **Phase 6** にリネーム（ROADMAP §1）。

## 1. 目標と非目標

- **目標**: 探索（runSim）実行中の待機画面を刷新し、(a) 進捗の可視化、(b) 残り時間の見通し、(c) 中断可能性、(d) 体感を保つ演出 を提供する。
- **非目標**: 探索アルゴリズム・ビーム幅・火力モデル・ゴールデン値（155,756,325）には触れない。**挙動・数値は完全保存**。
  - ∴ 本フェーズの全コミットで検証ワンライナー（CLAUDE.md §検証方法）が **155,756,325 不変**であることをアサートする。

## 2. 現状（C10 起点）

- 実行入口: `runSim()`（`index.html` ~1588行）。Worker プールで `_selectRootPrefixes()` の各 prefix を `_runRootPlan(prefix,n)` に投げ並列実行。フォールバックは `_fallbackRunSim()`（非並列・メインスレッド）。
- 待機UI: `#sim-loading`（CSS `.sim-loading` 189行 / HTML 250行）＝スピナー＋「最適押し順を計算中…」＋ `#spin-progress`。
- 進捗の粒度: **prefix（ルート）単位のみ**。`prog.textContent = ルート ${done}/${total} 計算中(${poolSize}並列)…`（1659行）。
  - ⚠ **ここが本フェーズの肝**: 各 `_runRootPlan` は 10ターンを一気に完遂して初めて結果を返す＝**ターン単位の進捗は現状ワーカーから出ていない**。per-turn 進捗・ETA・バーを出すには進捗イベントの粒度を上げる必要がある。

## 3. スコープと設計方針

### 3.1 進捗モデル（粒度の引き上げ）
- **総作業量の定義**: `総ステップ = 採用prefix本数 × ターン数(n)`（＝ルート×ターンのグリッド）。これを 100% の母数とする。
- **per-turn 進捗イベント**: `_runRootPlan` のターンループ（`for t…`）末でコールバック/postMessage を発火し、`{rootId, t}` を通知。
  - Worker 経路: `_runRootPlan` 内から `self.postMessage({type:'progress', rootId, t})` を送れるよう、**進捗通知をフック関数として注入**する（エンジン本体に `self` 直書きしない＝下記不変条件）。
  - フォールバック経路: 同じフック関数をメインスレッド版に渡し、`requestAnimationFrame` で UI 更新（同期ループでも描画されるよう yield する）。
- **⚠ Worker コード抽出不変条件（CLAUDE.md §2）**: `_runRootPlan` は engine slice（`// ===== ゲーム定数` 〜 `// ===== UI HELPERS`）内にある。進捗通知は `self.postMessage` を直書きせず、**`globalThis.__onTurnProgress?.(rootId,t)` のようなオプショナルフック**にして、Worker ラッパ側（`_buildWorkerCode` が前後に付ける部分）でそのフックを `postMessage` へ束ねる。これにより engine slice は `document`/`self` 非依存を維持（フリーズ回避）。検証は scratchpad の worker 再現スクリプト。

### 3.2 UI コンポーネント（本格版）
1. **プログレスバー**: 総ステップに対する完了率（ルート×ターン）。確定的バーにできる（母数が既知）。
2. **ターン別インジケータ**: T1..Tn のチップ列。代表ルート（baseline/空prefix）の到達ターンをハイライト（並列のため「最も進んだルートのT」を採用）。
3. **経過/推定残時間**: 経過は実測。ETA は「完了ステップ数あたりの平均所要 × 残ステップ」で逐次更新。
4. **キャンセル**: 「中断」ボタン。Worker 経路は `worker.terminate()` でプール破棄、フォールバックは中断フラグをループで監視。中断時は前回結果を保持し UI を元に戻す。
5. **演出刷新**: スピナー＋進捗の二段表示、完了時のスムーズな遷移。長時間でも不安にならない trSタイル。

### 3.3 段階実装（小さく入れて各段でゴールデン値不変を確認）
- **S1（足場・低リスク）**: 進捗フックの配管（engine slice にオプショナルフック追加 → Worker/フォールバックで postMessage 化）。UI は既存テキストのまま。**数値・挙動不変を確認**。
- **S2（可視化）**: プログレスバー＋ターン別チップ＋経過/ETA を `#sim-loading` に追加（CSS/HTML/更新ロジック）。
- **S3（中断）**: キャンセルボタン＋Worker terminate／フォールバック中断フラグ。中断後の状態復帰。
- **S4（演出）**: アニメーション・レイアウト刷新。アクセシビリティ（aria-live で進捗読み上げ）。

## 4. リスクと不変条件

- **最重要**: Worker コード抽出不変条件を壊さない（engine slice に `document`/`self`/`postMessage` を直書きしない・フック注入で疎結合化）。違反は**ページフリーズ**。
- ゴールデン値 155,756,325 不変（UX専任のため必ず保つ）。各段で検証ワンライナーを実行。
- 進捗イベントの過剰発火で探索が遅くならないこと（per-turn＝最大 prefix×n 回程度で軽微だが、postMessage 頻度に注意）。
- キャンセルの競合（terminate と結果受信のレース）を安全に扱う。

## 5. 検証

- **回帰**: 全段で検証ワンライナー＝155,756,325・FullBurst 10/10。
- **Worker 再現**: scratchpad の `document` 無しサンドボックスで `init`→`root`→`baseline` が 155,756,325 を返す（フック注入後も engine slice が自立すること）。
- **手動UX確認**: 強ギア（exp12設定＝重い探索）で待機画面の進捗・ETA・中断が機能するか実機（ブラウザ）で確認。

## 6. 次アクション

- S1（進捗フックの配管）から着手。まず `_runRootPlan` のターンループにオプショナルフック点を設け、Worker/フォールバック双方で postMessage 化 → ゴールデン値不変を確認 → S2 へ。
