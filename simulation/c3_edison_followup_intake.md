# C3 intake — エジソン英霊武器バースト追撃 倍率特定（2〜2.5倍）

> エジソン英霊武器の**バースト追加ダメージ（追撃）倍率**を実機データで特定する probe（C3・CALIBRATION_ANALYSIS.md）。
> シム実装: 追撃 = `_decay('abi', _na() × edison_burst_extra_mult, 800000)`（アビ枠・slope0.04・上限80万固定・現状 mult=2.5暫定）。
> 逆算式（追撃>80万時）: `_na()×mult = 800000 + (追撃 − 800000)/0.04` → `mult = それ / _na()`。∴ **naB（＝_na相当）が要る**。

## 測定条件（ノイズ最小化・重要）
1. **中立属性の敵**を選ぶ（有利でない）＝**急所を消す**（急所は有利属性のみ発動＝これで急所補正が全て外れ逆算がクリーン）。
2. **被ダメカット・耐性ギミックの無い敵**（ヴァルプルギス・ロキのようなHP依存カットは避ける。普通の雑魚でよい）。
3. **バフなし**でエジソンを単独バースト（アサルト/legend/puvoir等が乗る前＝T1の早い位置が理想）。
4. **2〜3回**取得（別戦闘・別ターンでも可）。**会心なしの回**が1つでもあるとベスト（cap clampが緩く倍率感度が高い）。

## 記録項目（1サンプル＝1ブロック）
各回、同じ会心状態で以下を読む（会心の有無を必ず明記）:

```
### サンプルN
- 敵: （名前・属性＝中立か / 被ダメカットの有無）
- エジソン表示攻撃力: （任意・分かれば。naB baseline照合＝B1のgear-vs-model切り分けにも流用）
- エジソン通常攻撃（単発）: ______万  [会心: 有/無]   ← naB
- エジソンバースト本体: ______万        [会心: 有/無]
- エジソン英霊武器追撃: ______万        [会心: 有/無]
```

> ⚠ 通常攻撃・本体・追撃は**同一の会心状態**で揃えること（3つとも会心 or 3つとも非会心）。揃っていれば会心倍率が比率で相殺 or naBに内包され逆算可能。
> ※ 通常攻撃が複数段の場合は各段の値を全部書いてよい（単発naBはそこから復元する）。

## 受領後の処理（Claude Code）
naB（通常攻撃・非会心単発）と追撃実測から `mult` を逆算。2.0 / 2.5 のどちらに寄るか、サンプル間のブレ幅も評価し、CALIBRATION_ANALYSIS.md C3（investigating→fixed）と `DMG.edison_burst_extra_mult` を更新（golden再fit）。
副産物: エジソン表示攻撃力＋非会心通常攻撃があれば、シム `_na(edison)` の baseline 一致度を検証（B1のnaB絶対ギャップ＝gear vs model 切り分けの傍証）。

---

## 第1回実測（2026-07-11・ユーザー実機・すべてT1＝テトラ/エレインのバフ未除外）
- エジソン表示攻撃力: **92,873**（全サンプル共通）／敵: 中立・被ダメカットなし（∴ affinity=1.0・急所なし）／全サンプル**会心あり**。

| # | 本体(会心) | 追撃(会心) | 通常攻撃(会心・三段) | 追撃/本体 |
|---|---|---|---|---|
| 1 | 304万 | 220万 | — | 0.72 |
| 2 | 283万 | 222万 | — | 0.78 |
| 3 | — | — | 45/46/42万 | — |
| 4 | — | — | 47/46/46万 | — |

### 分析（第1回）
1. **追撃はcap飽和している**（最重要）: 本体が304→283万（−7%）と変動しても**追撃は220→222万（±0.9%）とほぼ不変**。capされた値はバフ変動で動かない＝**追撃は上限に張り付いている**。∴ C3の元の問い「mult=2.0か2.5か」は**副次的**——飽和下ではmultはcapを満たすだけでよく、**較正対象はcap本体**へシフト。
2. **capの実機値 ≈ 220万（会心）／非会心換算 ≈ 147万**（会心=×1.5と仮定）。
3. **シムのcapはアビ上限UPギアでスケールする**（`_decay('abi', raw, 800000)` → 実効cap = `800000×(1+GEAR.abi_cap)`）。∴ 素の「80万」だけ見ると過小だが、**アビ上限UP込みの実効capが実機と整合する可能性**がある（例: sim02ギア abi_cap=0.954 なら実効cap≈156万 ≈ 実機非会心147万）。**本サンプルは会心＋casual敵のギア未知**のため実効capを確定できない。
4. naB（通常攻撃・会心単発）≈ 45万だが、これは**別サンプル（#3/#4）・別バフ状態**でありバースト samples（#1/#2）とペアでない＝naB↔追撃を突き合わせられない。

### 未解決（→第2回で確定）
- capを確定するには **①非会心（会心なし）の飽和追撃値** ＋ **②その戦闘のアビ上限UP合計%（GEAR.abi_cap相当）** が要る（または**gear既知のsim02 walpurgis設定で測る**）。
- 非会心が取りにくければ、**同一バースト内で 本体＋追撃＋通常攻撃 を同一会心状態で揃えて1サンプル**でも、比率＋naBペアからcapを詰められる。

## 第2回リクエスト（cap確定用・最小1サンプル）
```
### サンプルN（cap確定）
- 敵: （中立・被ダメカットなし）
- アビ上限UP合計%: ______   ← この戦闘の装備（GEAR.abi_cap相当・分かれば）
- エジソン通常攻撃（単発）: ______万  [会心: 無 を狙う]
- エジソンバースト本体: ______万      [会心: 無]
- エジソン英霊武器追撃: ______万      [会心: 無]
```
> 会心が引けない場合は会心ありでも可（3項目の会心状態を揃えて明記）。要点は**本体・追撃・通常攻撃を同一サンプルで揃える**こと。
