# sim05 — ナポレオン/アリアン編成への移行＋追撃較正

> **種別**: 較正セッション計画台帳（simNN・REPO_STANDARDS §1「実機乖離改善→simNN」） ／ **ゴール**: 実機最強感触のナポレオン/アリアンロッド編成をシムの主編成として移行し、この編成をアンカーに**積み残しの追撃較正（C3/C5）**を解く ／ **完了条件**: (a) 新編成のコード（ナポ押し順ヒューリスティック・アリアン回数制限×短縮の条件付き+1）が入り、(b) configC で表示ATK/HP・GEAR が実機一致、(c) 追撃 C3/C5 のスカラが新編成アンカーで確定、(d) golden 戦略（§5）に沿って回帰基準が維持 or 意図的再fit される ／ **状態**: 策定済み・**Q1/Q4/Q5 回答反映（2026-07-22）**・残ゲート=Q2（両面宿儺が本命：鬼神障壁=模型化可・与ダメ交絡解決／**最終ゲート=生存・討伐再現性**[全滅時計 鬼の魔力≥10]・FB=強機獣T1窓・§4.4）/Q3（configC月末）/**sim05前タスク＝鬼神障壁実装**・実行未着手
>
> 作成 2026-07-22 ／ 関連: `simulation/napoleon_arian_migration/README.md`（編成・実機ATK/HP・**ユーザー所感=一次intake**）・CALIBRATION_ANALYSIS.md C3/C5/C25・sim04（frame calib の継承元）
>
> **実施は新セッション（Opus）**。本 README＋§0 の読み順で自己完結。**構造/コード修正と較正 fit の間でセッションを切らない**（sim04 と同じ規律）。

---

## 0. 新セッション（Opus）への読み順

1. `CLAUDE.md`（開発ルール・実機較正ステータス・進行状況）
2. `simulation/napoleon_arian_migration/README.md`（編成・実機ATK/HP表・**ユーザー所感＝押し順とアビ回数モデルの一次指針**）
3. 本 README（プロトコル全体）
4. CALIBRATION_ANALYSIS.md の **C3・C5・C25**（追撃＝この sim の主題）
5. `gamedata/md/英霊/napoleon.md` §1（アビ番号↔効果の一次情報＝§4.1 のマッピング確定に必須）・`gamedata/md/神姫/arianrhod.md` §1/§3（A1〜A9・追撃別枠2本）

## 1. 編成と主題

- **編成**: 英霊=**ナポレオン** / パーティ=**ヘカテー・テトラ・アリアンロッド・エレイン**（幻獣=freyja_christmas / artemis 継続）。sim04（エジソン基軸）との差分は 英霊 と ヤマト→アリアン の2枠。
- **なぜこの編成が較正に効くか**: **アリアンの追撃が主火力になり得る**編成＝sim04 で「低総ダメ影響ゆえ保留」した C3/C5（追撃cap 実機比×2.3〜6.9・非決定的fitで残差保留）を、追撃が支配的な環境で**アンカーとして直接解ける**。sim04 のコミット a7cec53「新編成アンカーで解く方針」の受け皿がこの sim。
- **主題**: ①新編成のコード整備（ナポ押し順・アリアン回数制限×短縮）②configC で環境固定 ③**追撃 C3/C5 較正**（本丸）④override 再較正・golden 戦略。frame calib（calib_na/calib_burst/judg_calib）は**編成非依存で自動継承**＝土台は流用し、新編成固有だけを解く。

## 2. 現状のコード地点（着手前スナップショット）

- **golden 固定編成**: `test/golden.mjs` は `buildFormation('edison', ['yamato','hecate','tetra','elaine'])`＋`DISPLAY_ATK_OVERRIDE` の configB 値。**移行編成とはキャラが2枠違う**（→ §5 の golden 干渉）。
- **ナポレオン cands**: `roy/pike/consort/factor` に **`s:` スコア未設定＝既定静的スコア**。押し順ヒューリスティックは**未反映**（§4.1 で設計）。
- **アリアン**: `holy`（手動2回/T・`arianHolyFire`）・`elegant`（CD8）・`elegant_re`（guard `T.elegant<(t===1?4:3) && g>=40`）。回数制限は実装済みだが、**短縮由来の上限超過（条件付き+1）は未実装**（§4.2）。A1〜A7/A9 は実機確定済み（追撃=別枠2本・onBurst実装済み）。
- **DISPLAY_ATK/HP_OVERRIDE**: sim04 configB 値（edison/yamato/hecate/tetra/elaine）。移行実機値は移行README の表にあり（napoleon 102262 / arianrhod 77297 / hecate 75558 / tetra 83718 / elaine 85054・HP も）。
- **✅ C36（ダメージ上限UP枠）実装済み（2026-07-22・sim05前構造修正）**: cap-UP を na/burst/abi 各減衰枠へ加算（アブソ/プヴワール/ARRIVE ＋ 特別減衰別枠）。golden は default gear で cap 未到達につき不変。**⚠これにより abs-calib スカラ（calib_na/burst/judg・configB で cap-UP 無しに fit）は cap binding する実gearで過大方向に動く＝G4 で cap-UP 込みの再検証・必要なら再fit**（C31/C34 と同じ「構造→スカラ」順）。

## 3. 依存チェーン（実施順・sim04 と同型）

```
[G0] 移行 intake（§6 Q）── ✅Q1/Q4/Q5 反映済み／⏳残 Q2(ボス)・Q3(configC)
  ↓
[G1] コード整備（golden 非破壊で実装＝新編成キャラのみ触る）★Q1/Q5 確定で着手可能
  ↓
     ├ ナポ押し順 cands.s（§4.1）
     └ アリアン条件付き+1（§4.2）
  ↓
[G2] configC 受領（表示ATK/HP override 更新＋新装備パネル→GEAR export）── ユーザー
  ↓
[G3] 較正前 基準順を headless 再探索で取得（序数 diff の基準）
  ↓
[G4] 追撃データ取得（§4.3 の走）＋ C3/C5 fit（本丸）
  ↓
[G5] override 再較正・golden 戦略決定（§5）・新推奨順 export＋序数 diff 記録
```

**構造/コード修正（G1）とスカラ fit（G4）の間でセッションを切らない。**

## 4. 設計

### 4.1 ナポレオン押し順ヒューリスティック（cands.s）

**Q1 解決（2026-07-22・ユーザー確定＝一次情報が正）**: アビ番号↔キーは napoleon.md §1.2 が正＝**1アビ=roy / 2アビ=pike（buffCount≥15で確実会心）/ 3アビ=consort（≥20で2hit）/ 4アビ=factor（≥10で全体CD-1）**。移行README ユーザー所感の「2アビ＝≥10ゲート」は番号の取り違えで、**ゲート値は一次情報（pike≥15・factor≥10）を採る**。所感の定性的な意図（短縮レバーは**CD中アビが最多の位置で**＝早撃ちしない・ダメ/tier系は後半）はキーを正して踏襲:

- `factor`（4アビ・≥10で全体CD-1）: **ifishant と同型の動的スコア＝CD中アビ数が最多のターンで浮上**（`s=(CD中数)²`）。≥10 は CD-1 が効くゲートで下回れば低優先。**⚠『≥10で最速』は誤り（2026-07-22 ユーザー訂正）**＝短縮効果ゆえ**早撃ちは機会損失が大きい**、CD中アビが最も多い位置で打つのが理想（イフィシャントの打ち方を踏襲）。
- `pike`（2アビ・≥15で確実会心）: **buffCount がゲート(15)付近で高スコア＝会心を早く確定**させ後続を底上げ。
- `roy`（1アビ・全攻撃ダメプラス tier）・`consort`（3アビ・赤ダメ ≥20で2hit）: **終盤寄せの低〜中スコア**（バフ数が乗るほど tier/hit が伸びる）。
- 実装は各 cands に `s:` を付与するのみ（エンジン本体・探索ロジックは非改修）。**golden 非影響**（ナポレオンは golden 編成に不在）。buffCount 依存スコアは静的スコアの動的評価（cands.s を関数化 or exec前フックで参照）が要るか要確認＝現行 cands.s が定数のみなら小改修。

### 4.2 アリアン回数制限×短縮の条件付き+1

ユーザー所感§2:「アリアンのアビ回数制限は、自身の短縮効果／ifishant／ナポ4アビ(factor) の CD 短縮で**上限超過発動**が可能」＝**エレインの ifishant リキャスト（`ifAlone/ifLegend/ifKnights/ifPactcore`）と同型**。

- **Q5 解決（2026-07-22・ユーザー確定）**: 上限超過は**1回に留まらない**＝実機で**1アビ(holy=ホーリーターボ)が1ターン最大5回ほど**発動した例あり。∴ +1 枠は**単一フラグでなく累積カウンタ**（短縮ソースの発火数だけ上限を押し上げる）。
- 設計: `holy`（手動2/T）・`elegant_re`（回数ゲート）の上限を、**短縮ソースが「クォータ消化済みのアリアンアビ」に作用した回数だけ引き上げる累積カウンタ**（`arian_holy_plus` 等・エレイン if枠の数値カウンタと同型）で表現。C21 の教訓（発動可能状態のアビは増やせない・クォータ消化済み/CD中のみ復活）を踏襲しつつ、**複数回の上限超過を許す**（Q5）。holy は手動2＋短縮由来の追加で同ターン多数回（実機で〜5回）撃てる状態を再現。
- 短縮ソース: アリアン2アシ（光3バースト毎の自CD-1）・ナポ factor（全体CD-1）・イフィシャント系が編成にあれば同様。**実機の発火優先順・具体例**（どの短縮がどのアビを何回復活させたか）は G4 の走で裏取りし、カウンタ増分規則を確定。
- state: エレイン if枠と同型の数値カウンタ（`arian_holy_plus` 等）を宣言し、`guard` で参照。
- ⚠**HP参照項（A9・HP80%ゲート）は現状フルHP前提で常時発動**。厳密化は**未確定Phase(i)＝生存/現在HPモデル**以降（arianrhod.md 注記どおり保留）。

### 4.3 追撃較正（C3/C5・本丸）

- **対象成分**（arianrhod.md A1 で別枠2本確定＋エジソン系とは別）: ①アリアン バースト効果「追加ダメージ」 ②1アシ トランジェントマイト「追撃」 ③（参考）エジソン英霊武器追撃は本編成では英霊がナポなので不在＝**交絡が1つ減る**。現実装は①②とも 3倍/cap50万（アビ枠）で近似。
- **sim04 で判明済みの数値**: 追撃系は成分別 実機/sim ×2.3〜6.9・**非決定的 fit**（会心/急所RNG が乗り単発 raw を直読できない）で残差保留。
- **sim05 での解き方**: sim04/C25 と同じ「**非会心・非急所の非飽和アンカー**」原則。この編成は追撃が支配的なので、追撃単発が総ダメに効く＝アンカーが取りやすい。走の設計:
  - **モード D（全深測定・撃破まで）×5走**を基本（sim03 と同型・イベント列決定性を利用して5走で追撃成分を平均）。较正ボスの affinity/隠れ耐性がクリーンなこと（§4.4）が前提。
  - 各走で**追撃ヒットの会心/急所マーク**を記録（record_skeleton に列を用意）＝マーク一致ヒット間で raw を平均し、成分別（①②）の cap/slope を fit。
  - fit 順: **C5（追撃 cap レベル）→ C3（追撃式・合流）**。sim04 の judg/burst と同じく pre-cap raw に乖離が局在するか（深飽和一致か）をまず確認。

### 4.4 較正ボス（**Q2: T1撃破リスクが判明・要再選定**）

- **Q2（2026-07-22・ユーザー）**: ナポ+アリアン編成は**キャスパリーグを T1 で撃破しうる**。T1キル=**測定ターンが1つ・オーバーキル切断が深い**＝追撃成分を拾う前にHPが尽き、D×5 の統計（複数ターン・複数走）が成立しない。∴ **cath_palug（400M）続投は不適の公算**。
- **方針**: 追撃を複数ターン観測するため、**T1で落ちない硬さ/高HPの反復可能ボス**を選ぶ。条件は cath_palug 同等以上のクリーンさ＝**光有利で隠れ耐性が非適用と保証される敵**（ライトレジスト型 or affinity 既知）を優先。候補選定は G2 でユーザーと確定（敵DB intake `gamedata/md/敵/`）。
- max_hp 推定は sim03 と同じ T1終了HP%×ceil規約。**T1でオーバーキルしない**ことがアンカー品質の前提（sim03 フィンブル全滅・sim04 cath_palug と同じ『確実に反復』要件＝反復不能ボスは較正に使わない）。
- **候補プール intake（2026-07-22 ユーザー実機所見）**。**推奨戦闘力**（ゲーム内難易度指標）で生存ターン数がほぼ決まる＝**本編成≈92,000**（数値は確定以外は推定）:

| 候補 | 推奨戦闘力 | 生存/ターン | 反復 | 隠れ耐性クリーン | 交絡（絶対較正の汚染源） |
|---|---|---|---|---|---|
| 両面宿儺（CATACLYSM闇レイド・HP**9.8億**） | 100,000+ | ⚠ 多ターンだが**全滅時計**（鬼の魔力≥10で味方HP0・T2/3全滅リスク） | ○ **1日1回** | ◎ **ライトレジスト＝光有利クリーン** | def=**20**／✅**鬼神障壁**（枠別final-dmg cap: na600万/burst3000万/streak300万/abi不明・70%）＝**静的＝模型化可**・追撃(≤50万)非該当＝**測定クリーン**／⚠**鬼神一擲(アビ20回でバフ全消去+特大)**は≤19で回避／⚠**攻撃DOWNは鬼の魔力≥4=T8初出＝T1-T7出力クリーン**（詳細 `gamedata/md/敵/ryomen_sukuna.md`） |
| フィンブル | 98,000 | △ 全滅前科 | ？ | ？ | 敵防御UP＋ゲージDOWN（交絡2） |
| イヴ | 98,000 | ◎ T6撃破・複数回 | ◎ 無制限 | ？ 未確認 | ⚠⚠ 防御バフ＋対プレイヤー属性デバフ＋T6でHP削り（交絡2＋リスク） |
| **強機獣** | 90,000（≈編成） | ○ 2-3T安定 | △ 1日1回 | ◎ **有利属性以外の耐性UP＝光有利で非適用保証** | ⚠ 防御UP＝**HP50%以下トリガー（動的）**＝**50%超では未発動＝T1クリーン** |
| キャスパリーグ | 87,000（確定） | ✕ T1撃破（本編成） | ◎ 無制限 | ◎ ライトレジスト | 交絡ゼロだが**T1キルで測定不能** |
| 弱機獣 | 80,000 | ✕ 速攻（T1-2） | △ 1日1回 | ◎（機獣系） | 速攻＝ターン不足 |

- **現時点の評価＝両面宿儺が本命（#4=鬼神障壁 判明・模型化可）／強機獣がフォールバック**。両者とも**隠れ耐性クリーン（光有利保証）**を満たす。差はデータ量と要実装:
  - **両面宿儺（本命・データ品質は最良／生存が最終ゲート）**: def=20静的・HP=9.8億確定＋高難度＝**多ターンの clean データ**＝追撃 C3/C5（sim04で非決定fit・残差保留）に最も効くデータ量。**与ダメ出力の交絡は解決済み**: #3 鬼神一擲は ≤19アビ/T で回避／#4 鬼神障壁は静的枠別cap＝模型化可（追撃≤50万は非該当＝クリーン・streakは binding するが再アンカー不要）／**攻撃DOWN は鬼の魔力≥4＝T8初出＝T1-T7 は出力クリーン**（早期決着ならこの窓に収まる）。**残る技術ゲート＝abi上限/補正率70%解釈を第1走で実測**。
    - ⚠**ただし最終ゲート＝生存/再現性（fimbulvetr型）**: 自己強化＋デバフばら撒き＋**全滅時計「鬼の魔力≥10 で味方HP0」**（有志表で ~T16、鬼神躍動を踏むと加速）ゆえ、プレイヤー間セオリーは「**≤19アビ内で最大火力・早期決着**」。**基礎火力/回復/状態異常回復が不足すると T2・T3 全滅が濃厚**（テトラT1無敵でT1壊滅は回避）。∴ **ナポ/アリアン編成が安定討伐（5走再現）できるかが採用の最終判断**＝実機で要確認。全滅頻発なら fimbulvetr 同様に不適。
  - **強機獣（フォールバック）**: 防御UPは HP50%動的（模型化不可）だが 50%超＝T1は完全クリーン＝**cath_palug 同等の T1 を1本**（90,000≈編成でT1即死せず）。機構が少なく確実だが clean ターンが T1のみ＝データ量は両面宿儺に劣る。
- **⚠ sim05 前タスク①＝鬼神障壁の実装**: 敵側 final-dmg 枠別cap を enemy 特性として実装（例 `barrier:{na:6e6, burst:3e7, streak:3e6, abi:?, rate:0.70}`＋ダメージ確定後に枠別クランプ）。C36（味方側 cap-UP）と対をなす**敵側 cap**。追撃 fit 自体は非依存（追撃非該当）だが、HP追跡/streak照合に必要。golden は default gear で cap 未到達＝両面宿儺選択時のみ有効＝**golden 不変の見込み**。
- **⚠ sim05 前タスク②＝アビ回数上限制約（鬼神一擲回避＝シム推奨順の生成）**: enemy 特性 `abilCapPerTurn:19` を新設し、探索の候補生成で **per-turn アビ回数（`T.ability`）が上限に達したらアビ候補を剪定**（バースト/通常は継続可）＝「≤19アビで最大火力」の押し順をシムが返す。**計数ルール（2026-07-22 ユーザー確定）**: 手動押下＋**再発動（tenya_re/elegant_re）は数える**／**アリアン自動発動（holy auto）は数えない**（＝`_countAbilityUse` を呼ばない・A3改訂で反映済）。制約であって目的関数変更ではない＝**kill_turn には踏み込まない**（早期決着＝kill_turn は別・後回し可）。⚠**シムは火力最適化＝生存(回復/状態異常無効)は目的外**＝返る順は火力スケルトン・生存プレイは実機で上乗せ（§3 生存ゲート）。golden 不変見込み（既定敵に `abilCapPerTurn` 無し）。
- **推奨プロトコル**: 鬼神障壁を実装 → 両面宿儺で **≤19アビ/T＋毎T最低1閾値（鬼神躍動回避）＋状態異常無効/テトラT1無敵で生存** → **第1走で ①生存/討伐再現性 ②abi上限/補正率 を検証** → 追撃 per-hit で C3/C5 較正（多ターン=最良）。**全滅頻発 or abi上限が追撃を軽減するなら強機獣 T1窓へフォールバック**（sim03 fimbulvetr 第1走で全滅検知→即転換したのと同型）。
- **不採用**: フィンブル（全滅+交絡2）・イヴ（交絡2＋HP削り＝識別不能リスク）・弱機獣/キャスパリーグ（戦闘力<編成＝ターン不足）。
- ⚠**動的機構の模型化可否ルール**: 静的 `enemy_def`（両面宿儺=20）は畳めるが、**動的（HP/アビ回数/被ダメ量トリガー）は敵行動（未確定Phase i）未実装＝模型化不可**＝汚染窓のデータは使わない（両面宿儺なら#3を回避し#4該当hitを除外、強機獣なら<50%を捨てる）。

### 4.5 override

- config 別に自動較正（`calibrateStaticScores`）。移行README の暫定は **{judg:145, pactcore:1, effond:107}**（effond が新レバー・prefix=["miti"]）。G4 後に確定値を再fit。judg は frame calib 継承のため sim04 と同傾向の見込み。

## 5. golden 干渉と戦略（**要ユーザー決定**）

`test/golden.mjs` は edison 編成＋configB ATK に固定され、**hecate/tetra/elaine の ATK を移行値（75558/83718/85054）へ更新すると golden が動く**（この3キャラは両編成に居るため）。取り得る戦略:

- **案A（推奨・golden は edison 固定のまま凍結）**: `DISPLAY_ATK_OVERRIDE` を**移行編成用に切り替えず**、configC は sim05 のUI/config 側で持つ。golden は sim04 の回帰基準として据え置き＝frame calib 変更時のみ再fit。sim05 の新編成検証は**別 fixture**（headless replay ケース）で担保。→ 回帰の連続性を最優先。
- **案B（golden を新編成へ移行）**: golden.mjs を napoleon 編成へ書き換え＋ATK 更新＋override 再fit（sim03→sim04 と同じ「環境が変わったら golden も移す」流儀）。ツールの主編成が名実ともに移る。→ 移行を主編成の乗り換えと位置づけるなら自然だが、回帰基準が一度死ぬ。

- **Q4（2026-07-22・ユーザー）: 案B が望ましい**（主編成を napoleon へ移す）**が、将来エジソン編成へ再帰する可能性も僅かに残る＝悩みどころ**。
- **推奨する折衷＝二重 fixture golden**: golden.mjs を napoleon 編成の新基準（案B）へ移しつつ、**edison 編成の従来値（raw 197,775,394 / cal 211,462,826）を凍結 fixture として併存**させる（1ファイルで2編成を assert）。→ 主編成は napoleon へ移りつつ、edison への再帰は fixture が生きているので**低コストで戻せる**＝案B の『回帰基準が一度死ぬ』欠点を消し、Q4 の迷いを設計で吸収する。
- ⚠**実装上の注意**: 共有キャラ(hecate/tetra/elaine)の ATK が両編成で異なる（装備強化）ため、**単一グローバル `DISPLAY_ATK_OVERRIDE` では両編成を同時に持てない**。→ golden.mjs 側で**編成ごとに ATK を注入する小改修**が要る（per-formation override 適用フック or 各ブロックで setDisplayAtk 相当）。frame calib（絶対値の土台）は編成非依存で共有。
- 注意: 追撃 cap 変更（C5/C3）は edison golden の追撃成分＝低割合にも効くため、**edison 凍結値も sim05 fit で微動しうる**（意図した変動として台帳に記録）。凍結 fixture の期待値は G4 の fit 確定後に両編成ぶん確定する。

→ **G2 で最終確定**（案Bベース＋二重fixtureを推奨として提示済み）。

## 6. 未確定・ユーザー確認事項（intake・着手ゲート）

| # | 質問 | 状態（2026-07-22 ユーザー回答） | 効く先 |
|---|---|---|---|
| Q1 | ナポの**アビ番号↔効果**の実機確認 | ✅**解決＝一次情報が正**（1=roy/2=pike≥15/3=consort≥20/4=factor≥10）。所感の「2アビ≥10」は番号取り違え | §4.1 |
| Q2 | 較正ボス | ⚠**両面宿儺が本命／強機獣フォールバック**（§4.4・敵MD `ryomen_sukuna.md`）。光有利クリーン・def20/HP9.8億・与ダメ交絡は解決（鬼神障壁=模型化可・追撃非該当・T1-T7出力クリーン）。**残ゲート＝①鬼神障壁実装(sim05前)②abi上限/補正率③生存/討伐再現性**（全滅時計 鬼の魔力≥10・T2/3全滅リスク＝ナポ/アリアンが安定討伐できるか実機確認）。全滅頻発なら強機獣T1窓へ | §4.4 |
| Q3 | configC（新装備パネル→GEAR・表示ATK/HP）の export | ⏳**月末まで待ち（ステイ）**＝ウェポン更に強化中。sim05 実行は configC までゲート | §3 G2 |
| Q4 | golden 戦略 案A/案B | ✅**案B希望**（napoleon移行）**だがedison再帰の余地あり**→**二重fixture**を推奨（§5） | §5 |
| Q5 | アリアン上限超過の具体例 | ✅**解決＝複数回可**（1アビ holy が実機で最大〜5回発動）→ +1は累積カウンタ | §4.2 |

**残ゲート**: Q2（**強機獣T1窓で確定可**・両面宿儺の素性次第で切替）・Q3（configC月末待ち）。Q1/Q4/Q5 反映済み・**G1＋C36 構造整備は完了**＝残るは実機データ取得（configC・ボス確定後）。

## 7. 受入基準

1. **G1 コードは golden 非破壊**: ナポ cands.s・アリアン+1 実装後、`npm run test:golden` = raw 197,775,394 / cal 211,462,826 不変（新編成キャラのみ触るため不変のはず）。
2. **configC 整合**: 表示ATK/HP・GEAR がパネルと10/10枠一致（sim04 の configB_gear_panel と同型の検証）。
3. **追撃 fit**: C5/C3 のスカラが新編成アンカーで確定し、深飽和一致 or pre-cap 局在が sim03/sim04 と整合。非決定性が残るなら残差を明記（保留可・sim04 と同じ誠実さ）。
4. **golden 戦略の実行**: §5 の案A/案B いずれかに沿って回帰基準を維持 or 意図的再fit（変動は理由つきで CLAUDE.md 台帳へ）。
5. **序数 diff 記録**: 較正前後で新推奨順の変化を記録（sim04 と同型）。
