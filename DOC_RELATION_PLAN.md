# DOC_RELATION_PLAN — md 相互参照の整備計画（更新漏れインシデントの根絶）

> **種別**: 計画
> **ゴール**: md 間の相互参照を**機械検証可能**にし、「関連 md の更新漏れ」インシデントを構造的に発生させない状態にする。
> **完了条件**: ①`tools/doc_refs.mjs --check` が CI 相当で常時グリーン ②全 md が種別別テンプレに適合 ③常駐サブタスク（§7）が REPO_STANDARDS に組み込まれ2周以上回った → 達成後 archive/ へ。
> **状態**: **S1〜S5 完了（2026-08-05）＝運用フェーズへ。完了条件の残りは §7 の常駐が2周回ること**
> 作成 2026-08-05 ／ 関連: REPO_STANDARDS §3・§4・§5・§6（E10）／ CLAUDE.md「ドキュメント体系」

---

## 0. 本書の位置づけと★決定事項

起草時のユーザー指示（2026-08-05）: 「全163本に一括適用するが、ユーザー承認を得るまで作業は禁止。今は、計画のみ。」
→ **同日、§8 の未決6件＋常駐化の方針が全件決着した**（下表）。以降は本表が設計の正。

| # | 未決だった論点 | **決定（2026-08-05 ユーザー）** | 効果 |
|---|---|---|---|
| 1 | 本書の置き場所 | **(a) root の計画 doc のまま**・完了後 archive | ROADMAP の Phase 採番はしない |
| 2 | 末尾ブロックを「本文外のメタ領域」と定義してよいか | **✅ 定義してよい** | 一次情報の原文ママ原則・archive 不変原則と両立＝一括適用の前提が成立 |
| 3 | 凍結 trial 約90本（sim01〜04）への適用 | **❌ 不要** | **対象が 163本 → 約70本へ縮小**。差分90本を削減 |
| 4 | 更新履歴の保持件数 | **直近5件**（提案どおり） | git log と重複させない |
| 5 | 裸 basename 53件をパス付きへ書き換えるか | **✅ 書き換える** | 曖昧参照が消え、機械検証が全参照へ及ぶ |
| 6 | 値の複製をどこまで ID 参照へ置換するか | **数値は ID 置換しない。情報量が多いものだけ ID 参照とし、軽量な数値は処置しない** | §4.2 を改訂（下記）。可読性を保ちつつ温床だけ潰す |
| 7 | 常駐化のトリガと管理場所 | **イベント駆動＋5セッション毎のフラグ**。管理場所は **`workspace/TODO.md`**（①頻繁に参照・書き換えされる ②いかなるセッションでも読み込まれる） | 別ファイルの state を作らない＝管理対象を増やさない |
| **8** | **archive/ への適用**（2026-08-05 追加指示） | **❌ md の変更は一切不要**。archive は**歴史資料として安置する**とリポジトリ規定が定めているため（REPO_STANDARDS §2） | **適用対象が 約70本 → 52本へ確定**。被参照ブロックすら入れない＝S4 の差分がさらに縮む |

**適用対象の確定値**: **52本**（現役層のみ）。内訳の除外＝凍結 sim01〜04 **86本**／archive **15本**／essays **4本**／TEMPLATE **7本**。

**着手状況**: ✅**S1〜S5 すべて完了**（§6.1〜§6.3）。⏳ 残＝**§7 の常駐サブタスクが2周回ること**（完了条件③）。

---

## 1. 現状の実測（★推測ではなく測った＝E1）

| 指標 | 実測値 |
|---|---|
| md 総数 | **163本** |
| REPO_STANDARDS §4 ヘッダ（種別/ゴール/完了条件）を持つ | **16本（9.8%）** |
| **更新履歴セクションを持つ** | **0本** |
| md 内の md 参照 総数（テンプレのプレースホルダ 57件を除外） | **503件** |
| └ 相対パスで解決 | 201件 |
| └ リポジトリ根で解決 | 133件 |
| └ 裸 basename・一意に解決 | 58件 |
| └ **裸 basename・曖昧（機械検証が効かない）** | **53件** |
| └ **★真のリンク切れ** | **約50件**（下表・検出58件のうち8件は連結表記の検出ノイズ） |
| markdown リンク形式 `[表題](パス)` の使用 | **root 主要6本で計2件**＝ほぼ未使用（主流は backtick と裸テキスト） |
| 被参照トップ | `CLAUDE.md` **51ファイル** ／ `CALIBRATION_ANALYSIS.md` 34 ／ `configC_gear_panel.md` 11 |
| basename 衝突 | `README.md` **15本** ／ `integrated_analysis.md` 5 ／ `record_skeleton.md` 4 | <!-- doc_refs:ignore-line -->

**§4 の大テンプレは既に存在するが、事実上使われていない**（§4 自身が「既存MDへの遡及適用はしない」と明記しているため）。
∴ 本計画は「テンプレを新設する」ではなく「**既存テンプレを実効化し、末尾ブロックと統一文言を足す**」が正確な位置づけ。

### 1.1 リンク切れ 約50件の内訳（全件は §6 S1 のツールが出す）

<!-- doc_refs:ignore-begin ── 以下は「壊れた参照そのもの」を資料として引用する区間 -->

| 原因 | 件数の目安 | 実例 |
|---|---|---|
| **リネーム追従漏れ** | 7 | `enemies/cath_palug.md`・`enemies/walpurgis_loki.md`・`enemies/fimbulvetr.md`（→ `gamedata/md/敵/` へ移動済） |
| **sim 内の相対起点ズレ** | 約20 | `analysis/per_trial/*_quant.md` が `data/trial01.md` と書く（正 `../../data/trial01.md`） |
| **略記の文脈依存** | 5 | `workspace/HANDOFF.md` が `analysis/PROVISIONAL_ANALYSIS.md`（sim05 前提の略記＝HANDOFF の位置では解決しない） |
| **統合追従漏れ** | 3 | `ARIANROD_REGISTRATION.md`（→ `gamedata/md/神姫/arianrhod.md` §3 へ統合済） |
| **消えたファイル** | 5 | `g3_recommended_order.md`・`calibration_prep.md`・`simdata02.md`・`AGENTS.md` |
| **大文字小文字違い** | 1 | `search_quality_experiments.md`（正 `SEARCH_QUALITY_EXPERIMENTS.md`） |

<!-- doc_refs:ignore-end -->

---

## 2. ★最重要の設計判断 ── テンプレ単体ではインシデントは消えない

実際に起きたインシデントを型に分けると、**末尾テンプレで消える型はゼロ**だった。

<!-- doc_refs:ignore-begin ── インシデントの実例＝解決してはいけないパスを含む -->

| # | インシデントの型 | 実例（すべて実在） | 末尾テンプレで防げるか | 何が効くか |
|---|---|---|---|---|
| 1 | **値の二重管理**（md→md） | サブ枠が `configC_gear_panel.md` と `sim05/README.md` で食い違い（2026-08-03） | ❌ | **②統一文言＝単一の正**（§4） |
| 2 | **リネーム追従漏れ** | `enemies/` → `gamedata/md/敵/` が7件残存 | ❌ | **機械検査**（§3） |
| 3 | **略記の文脈依存** | HANDOFF の `analysis/…` が解決しない | ❌ | **機械検査** |
| 4 | **統合追従漏れ** | `ARIANROD_REGISTRATION.md` 3件 | ❌ | **機械検査** |
| 5 | **台帳の数え漏れ** | TODO「他5本」が実際は15本（2026-08-05） | ❌ | ❌ どれも防げない（§8.1） |
| 6 | **md→コード** | `gear_panel` の GEAR をハーネスが読まず結論が反転 | ❌ | **E10**（2026-08-05 対処済・別軸） |
| 7 | **波及先の見落とし** | 実装したのにキャラ md §2 が「未モデル化」のまま等 | ⭕ | **被参照リスト**（§3） |

<!-- doc_refs:ignore-end -->

**∴ 末尾ブロックは目的ではなく手段**であり、本命は次の2本柱:

- **(A) 参照を機械検証可能にする**（型 2/3/4 が構造的に消える）
- **(B) 単一の正を徹底する**（型 1 が消える。REPO_STANDARDS §3 に既にある原則だが**守られていない**）

末尾ブロックは **(A) の産物を置く場所**であり、型 7 に効く。この順序で説明できないと「テンプレを整えたのに
インシデントが減らない」という結末になる。

---

## 3. 設計原則

### 原則1: 被参照リストは**手書きしない**（自動生成する）

「このmdを参照している md 一覧」を手で維持すると、**リスト自体が更新漏れの新しい発生源**になる
（A→B の参照を足すたびに B の末尾も直す必要があり、忘れれば嘘の台帳が残る＝自己言及的な失敗）。
`CLAUDE.md` は51ファイルから参照されており、手書きは現実的でない。

∴ **`tools/doc_refs.mjs` が全 md を走査して生成/検証する**。思想は **E10（config は台帳から読む・ハードコードしない）と同一**。
生成ブロックは machine-managed マーカーで囲み、人間は編集しない。

### 原則2: 参照は**機械が解決できる形**で書く

現状は markdown リンクがほぼゼロで、backtick と裸テキストが主。裸 basename 53件は
`README.md` が15本ある以上、原理的に一意解決できない。∴ **統一文言（§4）を「機械可読」の要件として規定する**。 <!-- doc_refs:ignore-line -->

### 原則3: 更新履歴は**有界**にする（git log と重複させない）

「いつ・誰が・何を」は git が既に持つ。**git に無いのは「このとき何を波及確認したか」**。
∴ 履歴は **直近5件で打ち切り**（それ以前は git log に委ねる）、**「波及確認」列を必須**にする。
これはリポジトリ既存の作法（HANDOFF は有界・押し出された分は SESSION_LOG へ）と同じ形。

### 原則4: 「原文ママ」「archive 不変」との衝突を明示的に解消する

REPO_STANDARDS §4 は一次情報を「**本文は書き換えない**」、§2 は archive を「**内容は書き換えない**」と規定している。
末尾ブロックの追加はこれに抵触する。∴ **末尾ブロックを「本文外のメタ領域」と定義する §4 改訂が要る**。
**★2026-08-05 決定2 で「定義してよい」と承認済**＝一括適用の前提が成立した（改訂の実施は S2）。

---

## 4. ② 統一文言の規定（引用は最低限・詳細は参照）

> ユーザー指示②「引用情報は最低限記載し、『詳細はxx.md参照』のような統一文言を規定しておく」

### 4.1 参照の書式（機械可読を必須要件にする）

```markdown
→ 詳細は [CALIBRATION_ANALYSIS.md](CALIBRATION_ANALYSIS.md) C37
→ 詳細は [sim05 README §4.8](simulation/sim05/README.md) 
```

- **リポジトリ根からの相対パス**を markdown リンクで書く（現在の「裸 basename」「文脈依存の略記」を廃止）。
  ＝ §1 で測った**曖昧53件・略記由来のリンク切れ5件が構造的に消える**。
- ID がある台帳（Cx/Dx/Ax/Mx/Hx/Ex）は**必ず ID まで書く**（本文コピー禁止＝REPO_STANDARDS §3 の再掲）。

### 4.2 値・結論を他文書から引くときの規律（★2026-08-05 決定6により改訂）

**決定: 数値は ID 置換しない。情報量が多いものだけ ID 参照とし、軽量な数値は処置しない。**

| 引用の中身 | 扱い | 例 |
|---|---|---|
| **軽量な数値**（1つの値・その場で意味が閉じる） | **そのまま書いてよい**（置換しない） | `golden = 202,005,923` ／ `betaia_cap = 800,000` ／ `BW=64` |
| **情報量が多いもの**（複数行の表・手順・結論の連なり・状態遷移） | **ID 参照にする**（本文コピー禁止） | C40〜C44 の成分別突合表 ／ M1〜M7 の測定メニュー ／ Cx の open/fixed 状態 |
| どちらとも言えない場合 | **出所と日付を併記して書く**（E1 と対） | `バースト本体 ×0.77（CALIBRATION_ANALYSIS C44・2026-08-03 時点）` |

**判定基準**: 「その情報が**更新されたとき、ここも直さないと嘘になるか**」。
軽量な数値でも更新で嘘になるものはあるが、**1値なら検知も修正も容易**で、ID 参照にすると可読性の損失が上回る。
**表・手順・状態のような塊は、部分的にズレたまま気づかれない**＝これが型1インシデントの実体なので、こちらだけを潰す。

- **状態（open/fixed/investigating）だけは例外なく ID 参照**。REPO_STANDARDS §3 が既に
  「起票・状態遷移は台帳文書の1箇所だけを正とする」と規定しており、本項はその再掲。
- **起動時必読2本**（`CLAUDE.md` / `workspace/HANDOFF.md`）は要約を持ってよい。
  ただし**「要約（正は◯◯）」と明示**する。この2本は「読むのはこれだけ」という設計上、要約の保持が本質だから。

⚠ 一括置換はしない。§6 S2 で**「情報量が多い」に該当する箇所を列挙してユーザーに諮る**。

---

## 5. テンプレート設計

### 5.1 大テンプレ（全 md 共通）

冒頭 = 既存 REPO_STANDARDS §4 ヘッダ（**変更なし**）。末尾に以下を追加:

```markdown
---
## 更新履歴
<!-- 直近5件のみ。それ以前は git log。「波及確認」は必須（git に無い情報はここだけ） -->
| 日付 | 変更点 | 波及確認 |
|---|---|---|
| 2026-08-05 | C44 の残ゲートを更新 | CLAUDE.md 較正ステータス／workspace/HANDOFF.md を同時更新。sim05 README は影響なしと確認 |

<!-- doc_refs:begin ── 自動生成。手で編集しない（node tools/doc_refs.mjs --write が再生成する） -->
## この md を参照している文書（現役層 4）

- [CLAUDE.md](./CLAUDE.md)
- [REPO_STANDARDS.md](./REPO_STANDARDS.md)
- [workspace/HANDOFF.md](./workspace/HANDOFF.md)
- [workspace/TODO.md](./workspace/TODO.md)
<!-- doc_refs:end -->
```

### 5.2 小テンプレ（種別別に末尾ブロックの要否を変える）

| 種別 | 更新履歴 | 被参照（自動） | 理由 |
|---|---|---|---|
| **規定・生きた台帳**（REPO_STANDARDS / CALIBRATION_ANALYSIS / ROADMAP / tools/README） | ✅ 必須 | ✅ | 更新頻度が高く被参照も多い＝インシデントの震源 |
| **現状スナップショット・to-do**（HANDOFF / TODO） | ✅ 必須 | ✅ | 毎セッション更新される |
| **分析**（simNN/analysis/*） | ✅ 必須 | ✅ | 既存 §4 の「入力と出力を明記」を履歴側へ寄せる |
| **一次情報**（gamedata/md/* / sim05/data/*） | ❌ **不要** | ✅ | **原文ママ＝更新されないのが正**。受領日ヘッダで足りる。訂正は既存の「冒頭注記」方式を維持 |
| **★履歴・archive**（SESSION_LOG / archive/*） | ❌ | ❌ **対象外** | **決定8**＝archive は**歴史資料として安置する**規定（REPO_STANDARDS §2「内容は書き換えない」）。被参照ブロックも入れない。リンク切れ検査は**警告のみ**（当時のパスとして正しい記述が混ざる） |
| **★凍結 sim（sim01〜04 の trial・analysis 約90本）** | ❌ | ❌ **対象外** | **決定3**＝二度と更新されないため被参照ブロックすら不要。リンク切れ検査は**警告のみ**（歴史記述として当時のパスが正しい場合がある） |
| **読み物**（essays/） | ❌ | ❌ | 開発に拘束力なし（§1 で唯一ヘッダ免除の区分） |
| **テンプレ原本**（simulation/TEMPLATE/*） | ❌ | ❌ | プレースホルダを含む＝検査対象外（除外規則が必要） |

**∴ 適用対象の実体（決定3・決定8 反映後）＝52本**（起草時の「全163本」から大幅縮小）。
被参照ブロックが入るのは現役層のみ（root・workspace・gamedata/md・simulation/sim05・simulation/README・tools）。
**手書きの更新履歴が発生するのは約25本**（規定・台帳・分析・workspace）。
**凍結 sim 86本・archive 15本・essays 4本・TEMPLATE 7本は一切触らない。**

---

## 6. 段階計画（★S1 着手済・S2 以降は未着手）

| S | 内容 | 触るもの | 概算 | リスク |
|---|---|---|---|---|
| **S1** | ✅ **完了 2026-08-05**。`tools/doc_refs.mjs`（`npm run doc:check`）＝参照グラフ／壊れた参照／曖昧参照／被参照ランキング。**`--write` 無し＝リポジトリ無改変**。結果は §6.1 | 新規1ファイル＋npm script | 実績1セッション | 低 |
| **S2** | ✅ **完了 2026-08-05**。REPO_STANDARDS **§4.1（末尾ブロック＋メタ領域の定義）**・**§7（参照の統一文言）**・**§6-6（常駐サブタスクをセッション定型へ）**を制定 | REPO_STANDARDS | 実績 同一セッション | — |
| **S3** | **リンク切れ 約50件の修正**。S1 が全件を機械検出済み＝作業は機械的。⚠ archive/ 内の切れ（SESSION_LOG に12件）は「歴史の記述として正しい」ものがある＝**一律に直さない**（当時のパスが正しい記録） | 約20 md | 1セッション | 中（archive の扱いに判断が要る） |
| **S4** | ✅ **完了 2026-08-05**。`--write` を実装し**現役層 51本**へ挿入（TEMPLATE 除く）。冪等・生成部はマーカー内。①中核14本 ②残り37本 の2コミット | 51 md | 実績 同一セッション | — |
| **S5** | ✅ **完了 2026-08-05**。規約側（REPO_STANDARDS §6-6）＋カウンタ（workspace/TODO 冒頭）＋`doc_refs.mjs` の超過警告 | REPO_STANDARDS §6 / workspace/TODO | 実績 同一セッション | — |

**⚠ 全体で 4〜5セッション相当**。メインタスク（Aトラック＝キャスパリーグ M2〜M7 の較正）とは**独立に進められる**が、
**実機走が届いたセッションは較正を優先**する（本計画は待てるが、実機データの鮮度は待てない）。

---

### 6.1 S1 の結果（2026-08-05）

```
対象 164 md ／ 参照 1,524 件
  相対で解決 576 ／ 根で解決 446 ／ 裸 basename 一意 199
  ⚠ 曖昧（裸・衝突） 60
  （地の文・連結表記 54 ＝参照ではないと判定）
  ★ 壊れた参照 189（現役層 42 / 凍結・archive 147）
```

**★ツール設計で最も重要だったのは「解決できない＝壊れている、ではない」という分離**。
初版は **487件**（実数の約10倍）を報告した。原因は4つで、いずれも実際に踏んだ:

| 誤検出の原因 | 対処 |
|---|---|
| `git ls-files` が**非ASCIIパスを8進エスケープ**する（`gamedata/md/その他/…` が永久に壊れて見える） | `-z` で受ける |
| 正規表現の交替が leftmost-first ＝ `package.json` が `package.js` にマッチ | 拡張子は**長い方を先**に並べる |
| 地の文（`Node.js`）・連結表記（`CLAUDE.md/AGENTS.md`）・命名パターン（`<CHAR>_REGISTRATION.md`）を参照と誤認 | `prose` として分離し**失敗にしない** |
| `git ls-files` は**追跡済みしか出さない**＝同一コミットで新設したファイルへの参照が壊れて見える | 実ファイル存在も併せて見る |

**過剰報告する検査は使われなくなる**ので、確度の高いものだけを `broken` と呼ぶ設計にした
（パス形で解決不能／裸の `.md` で実在しない、の2つだけ）。

**現役層 42件の性質**（S3 の作業内容）:

<!-- doc_refs:ignore-begin ── 壊れた参照を実例として引く区間 -->

- **`data/` → `gamedata/` リネームの追従漏れ 約15件**（`data/characters.js` 等）。2026-07-16 のリネームが
  CALIBRATION_ANALYSIS / PHASE4_PLAN / ROADMAP へ伝播していなかった＝**まさに本計画が対象とするインシデント**
- **`enemies/` → `gamedata/md/敵/` の追従漏れ**／**`ARIANROD_REGISTRATION.md` の統合追従漏れ 4件**
- **`workspace/HANDOFF.md` の文脈依存の略記 7件**（`analysis/…` は sim05 前提＝HANDOFF の位置では解決しない）
- **未作成ファイルへの前方参照**（`gamedata/js/accessories.js` は Phase 8 で作る予定）＝**規約が要る**（S2 の論点）
- **構造の説明**（`simulation/README.md` が simNN の中身を `data/config.json` と総称）＝ignore 区間か言い換え

<!-- doc_refs:ignore-end -->

<!-- doc_refs:ignore-line ── 次行は「誤ったパスを実例として引く」ため -->
⚠ **自分自身も踏んでいた**: 本セッションで書いた `lib/config_c.mjs`（正 `tools/lib/config_c.mjs`）を
CLAUDE.md と HANDOFF に残していた。**ツールが即座に検出**＝検査が機能している証拠なので記録しておく（修正済）。


### 6.2 S2・S3 の結果（2026-08-05）

**S2＝規約の制定**（[REPO_STANDARDS.md](REPO_STANDARDS.md)）:
- **§4.1 末尾ブロック**＝更新履歴（直近5件・「波及確認」列が本体）＋被参照ブロック（自動生成）。
  **「本文外のメタ領域」と定義**したので一次情報の原文ママ原則と抵触しない。**archive は対象外**と明記（決定8）。
- **§7 参照の統一文言**＝7.1 書式（リポジトリ根からのパスを markdown リンク・裸 basename 禁止・文脈依存の略記禁止）
  ／7.2 引用の規律（決定6＝軽量数値は据置・情報量が多い塊と**状態**のみ ID 参照）／7.3 検査の除外規則。
- **§6-6**＝常駐サブタスクをセッション定型へ組み込み（イベント駆動＋5セッション定期・カウンタは TODO）。
- §4 の「既存MDへの遡及適用はしない」を**取り消し**、現役層 52本へ適用すると改訂。

**S3＝現役層の参照を全て解決**（`npm run doc:check` グリーン）:

<!-- doc_refs:ignore-begin ── 処置した「壊れた参照」を実例として引く区間 -->

| 分類 | 件数 | 処置 |
|---|---|---|
| `data/` → `gamedata/js/` リネーム追従漏れ | 15 | パスを現行へ（CALIBRATION_ANALYSIS 10 / PHASE4_PLAN 2 / ROADMAP 3） |
| `enemies/`・`敵/` の省略／旧パス | 3 | `gamedata/md/敵/…` のフルパスへ |
| `ARIANROD_REGISTRATION.md`（統合済で消滅） | 4 | 歴史的言及＝`ignore-line`／現行フローの記述は arianrhod.md §3 へ差し替え |
| `workspace/HANDOFF.md` の文脈依存の略記 | 8 | `simulation/sim05/…` のフルパスへ |
| **構造の説明**（simNN のスキーマを指す総称） | 20+ | **`simNN/` を前置**＝記述が正確になり、既存のプレースホルダ規則で自動的に検査対象外になる |
| 未作成ファイルへの前方参照 | 5 | §7.3 に従い `ignore-line` ＋**いつ作るか**を併記 |
| 散文・コード片の誤検出 | 3 | 言い換え／`ignore-line` |

<!-- doc_refs:ignore-end -->

<!-- doc_refs:ignore-line ── 次行は「言い換え前の総称」を実例として引く -->
**★「構造の説明」は言い換えが最良手だった**: `data/config.json` のような総称は、`simNN/data/config.json` と書けば
**記述として正確になり、かつ機械検査からも自然に外れる**（プレースホルダ `NN` を含むため）。
除外マーカーを足すより、**曖昧な記述を正確にする**ほうが本質的だった。

⚠ **ツール側も1件直した**: `ignore-line` が「マーカーのある行」しか飛ばさず、見出し行の直前に置けなかった。
**単独行なら次行を無視**（eslint の `disable-next-line` 流）を追加。

**残 28件の曖昧参照は全て 凍結 sim / archive / essays**＝**決定3・決定8 により対象外**。現役層は 0。

### 6.3 S4・S5 の結果（2026-08-05）

**S4＝末尾ブロックを現役層 51本へ挿入**（52本から `gamedata/md/敵/TEMPLATE.md` を除く）。

- **冪等**が不変条件＝`node tools/doc_refs.mjs --write` を2回走らせて **変更 0 / 変更なし 51** を確認。
- 種別で挙動が変わることを実測で確認: **一次情報（`gamedata/md/*`・`sim05/data/*`）には更新履歴が入らない**
  （原文ママ＝更新されないのが正）／規定・台帳・分析・workspace には雛形が入る。
- **★実装で1点直した ── 生成ブロックを抽出対象から除外する**。被参照ブロックの中身は他 md へのリンクの羅列なので、
  これを参照として数えると**「全 md が全 md を参照している」状態に自己増殖する**。除外を入れたうえで、
  `archive/SESSION_LOG.md` の被参照に（そのブロックからリンクしている）`buffcount_sim_side_diag.md` が
  現れないことを直接確認した。参照総数も 1,515 のまま動かない。
- **★載せるのは現役層の参照元だけにした**。当初は全参照元を並べたが、**CLAUDE.md は48件中32件が凍結 sim の trial**で、
  最も読まれる md がノイズで埋まった。凍結・archive は「二度と更新しない」と決めた文書だから、
  波及先として並べても実際に直すことはない＝**件数だけ添えて一覧からは外す**。48件 → **実際に直しうる16件**に圧縮。
  このブロックの目的は「ここを直したら、どこを直すか」であって網羅ではない。

**S5＝常駐サブタスクの運用開始**。3点セットで「忘れる」を「警告が出る」に変えた:
1. **規約**: REPO_STANDARDS §6-6（セッション末の手順に組込み・イベント駆動＋5セッション定期）
2. **状態**: `workspace/TODO.md` 冒頭のカウンタ（決定7＝別 state ファイルを作らない）
3. **機械**: `doc_refs.mjs` がそのカウンタ行を読み、**5 到達で警告**する

## 7. 常駐サブタスクの設計（「数セッションに1回」をどう担保するか）

> ユーザー意図: メインタスクは従来どおり。**数セッションに1回の頻度で md リレーションの見直しを行う。**

⚠ **「数セッションに1回」は主観トリガ＝必ず忘れる**（本リポジトリは既に「override の再fit が未履行」「C37 の
BW 掃引が旧 config のまま」等、主観トリガの取りこぼしを複数抱えている）。∴ **2種のトリガを併用する**:

| トリガ | 条件 | 動作 |
|---|---|---|
| **イベント駆動（必須）** | md の**新設・リネーム・archive 移動**があったセッション | セッション末に `node tools/doc_refs.mjs --check` を通す。**REPO_STANDARDS §6「セッション末」の手順へ追加** |
| **定期（棚卸し）** | 前回 `--check` から **5セッション経過**（★決定7） | 全体走査＋被参照ブロック再生成＋「情報量の多い複製」の目視棚卸し |

**★管理場所＝`workspace/TODO.md`（決定7）**。理由はユーザー判断のとおり
①**頻繁に参照・書き換えが行われる** ②**いかなるセッションでも読み込まれる**（起動時必読の2本目）。
∴ 起草時に案として挙げた**別ファイルの state（`workspace/doc_refs_state` 相当）は作らない**
（管理対象を増やすこと自体が新しい更新漏れの発生源になる＝§3 原則1 と同じ理屈）。

**様式（TODO 先頭付近に常駐）**:

```markdown
## 常駐: md リレーション点検（DOC_RELATION_PLAN §7）
- 前回 `npm run doc:check`: **2026-08-05 / セッション 0**  ← 5 経過で棚卸し
- カウンタ: `0/5`（セッション末に +1。md の新設・リネーム・archive 移動があった回は**即実行**して 0 に戻す）
```

**機械に数えさせる**: `doc_refs.mjs` がこの行をパースし、**5 超過なら警告を出す**（＝忘れても警告が出る）。
TODO は毎セッション読まれるので、人間側にも同時に見える。

**⚠ ただし本リポジトリに CI は無く、実行は Claude Code のセッション規律に依存する。**
`--check` を `npm run test:golden` に相乗りさせる案もあるが、**golden はエンジンの回帰テストであり
docs の検査を混ぜると責務が濁る**ため、`npm run doc:check` として分離することを推奨する。

---

## 8. 未決事項 ── ★2026-08-05 に全件決着

**§0 の決定表が正**。本節は「何が論点だったか」の記録として残す（provenance）。

| # | 論点 | 決定 | 未決時に想定していた懸念とその解消 |
|---|---|---|---|
| 1 | 本書の置き場所 | **(a) root 計画 doc** | ROADMAP の Phase は全てエンジン/機能で毛色が違う、という懸念どおりの決着 |
| 2 | 末尾ブロック＝本文外メタ領域 | **✅ 定義してよい** | 「認めないと一括適用が原則と衝突して実行できない」という最大のブロッカーが解消 |
| 3 | 凍結 trial 約90本 | **❌ 対象外** | 適用対象が 163→約70本へ。**S4 の差分規模が想定の半分以下**になり、レビュー困難という懸念（§9）も同時に軽減 |
| 4 | 更新履歴の件数 | **直近5件** | — |
| 5 | 裸 basename 53件 | **✅ パス付きへ書き換え** | 「書き換えないと機械検証が53件を素通りする」＝検査の穴が塞がる |
| 6 | 値の複製の扱い | **数値は据置・情報量が多いものだけ ID 参照** | 「可読性が下がる」懸念を回避しつつ型1の温床（表・手順・状態）だけを潰す設計へ（§4.2 改訂） |
| 7 | 常駐化のトリガ・管理場所 | **イベント駆動＋5セッション毎・`workspace/TODO.md`** | 別 state ファイル案を破棄＝管理対象を増やさない（§7） |

**残る判断は S2/S3 の実施時に個別に発生する**:
- S2: §4.2「情報量が多い」に該当する複製箇所の**リスト承認**（一括置換はしない）
- S3: archive/ 内のリンク切れのうち**「当時のパスとして正しい歴史記述」をどれとみなすか**

---

## 9. 想定される反対意見と回答（先に潰しておく）

| 懸念 | 回答 |
|---|---|
| 「更新履歴は git log と重複＝無駄では」 | 重複するのは日付と変更点だけ。**「波及確認」列は git に無い**。逆に言えば、この列を書かせないなら履歴は不要（§3 原則3） |
| 「163本に末尾ブロックは冗長」 | 手書きが発生するのは約35本。残りは自動生成でコスト実質ゼロ（§5.2）。ただし**未決3**で凍結分を外す判断はありうる |
| 「テンプレを整えてもインシデントは減らないのでは」 | **そのとおり**（§2）。だから本計画の本体は機械検査と単一の正であり、テンプレはその置き場所にすぎない |
| 「巨大な差分でレビューできない」 | S4 を①現役35本 ②残りの2コミットに割り、②は自動生成のみ＝機械的に検証可能にする（§6） |
| 「メインタスクが止まる」 | 独立に進行可能。実機走が届いたセッションは較正を優先する（§6 末尾） |

---

## 更新履歴

<!-- 本書は §5.1 の様式を自ら適用した最初の md（ドッグフーディング） -->

| 日付 | 変更点 | 波及確認 |
|---|---|---|
| 2026-08-05 | 新規作成（計画のみ・未着手）。実測 §1、インシデント型 §2、段階計画 §6、未決 §8 | CLAUDE.md「ドキュメント体系」へ登録行を追加（REPO_STANDARDS §5.1 の同一コミット規律）。REPO_STANDARDS/workspace は**S2 まで変更しない**＝未承認のため |
