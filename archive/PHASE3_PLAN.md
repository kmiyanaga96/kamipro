# 編成最適化エンジン — Phase 3 高速化計画（実測ベース）

> [!IMPORTANT]
> Node 実測により、当初想定の「clone()/GC がボトルネック」は**否定**された（clone は全体の 2.8%）。
> 真のボトルネックは `_candidates()`（探索全体の約43%）。よって Phase 3 の主標的を
> **ホットパス（`_candidates`/`_stepStatic`）のアロケーション削減**とする（§1＝第一手・現行計画）。
> §2 は後続候補と棚上げアーカイブ: §2.1/§2.2（Apply/Revert・Flat State）は実測で棚上げ、
> §2.3/§2.4（Bytecode VM・**WebAssembly化＝第二手の大幅改善候補**）は per-op コスト削減の将来選択肢。
> **第一手（§1.4-1・§1.4-2）は実装完了（2026-06）**。挙動完全保存をゴールデン値ゲートで担保済み。
> **第二手（§1.4-3＝§2.3 VM / §2.4 WASM）は計画中・未着手**（着手前にまず第一手後の再計測でボトルネックが
> per-op 側へ移ったか確認すること）。着手時は Phase 2 と同じゴールデン値ゲート（後述）で挙動完全保存を担保する。

---

## 1. 実測ボトルネック再評価と高速化主計画（現行・2026-06）

### 1.1 計測方法
- 環境: Node 単体。`index.html` のエンジン領域 + `data/*.js` を eval 連結（CLAUDE.md 検証ワンライナーと同方式）。
- 対象形成: `edison + [yamato, hecate, tetra, elaine]`（基準形成・FB 10/10・91,723,594）。
- 計測: フル探索1回（`takeTurn(1..10)`）の総時間、メソッド呼出回数（ラッパ計装）、主要メソッドの単体スループット（μs/op）。

### 1.2 主要結果
- **フル探索1回 = 約12.6秒**（Node・基準形成）。
- **clone() は全体のわずか 2.8%（約356ms / 56,286回 × 6.335μs）。** → Apply/Revert・Flat State の費用対効果は極めて低い。
- **`_candidates()` が全体の約43%（約5,406ms / 2,971,405回 × 1.819μs）。** → 最大のボトルネック。

#### メソッド呼出回数（フル探索1回）
| メソッド | 呼出回数 |
|---|---|
| `_decay` | 4,421,444 |
| `_na` | 3,845,423 |
| `_candidates` | 2,971,405 |
| `_stepStatic` | 2,936,126 |
| `use` | 2,821,555 |
| `burst` | 1,349,412 |
| `_attackPhase` | 142,937 |
| `greedyTakeTurn` | 114,794 |
| `clone` | 56,286 |
| `_objective` | 28,143 |
| `_finishStatic` | 28,143 |
| `_beamSearch` | 10 |
| `_primeLookaheads` | 10 |

（探索定数: `BEAM_W=32` / `BEAM_W_INNER=4`）

#### 単体コストと推定総時間
| メソッド | μs/op | × 呼出回数 | 推定総時間 | 全体比 |
|---|---|---|---|---|
| `_candidates` | 1.819 | 2,971,405 | **5,406 ms** | **~43%** |
| `_na` | 0.078 | 3,845,423 | 300 ms | ~2.4% |
| `_decay` | 0.046 | 4,421,444 | 202 ms | ~1.6% |
| `clone` | 6.335 | 56,286 | 357 ms | ~2.8% |
| 残り(use/burst本体・制御フロー等) | — | — | ~6,300 ms | ~50% |

### 1.3 根本原因 — なぜ `_candidates()` が突出するか
`_candidates()` は呼出毎に以下を**全て新規アロケート**する:
1. `Object.entries(ABIL)` … 全アビ分の `[key,[...]]` 配列を毎回生成。
2. 合法候補ごとに候補オブジェクト `{s,key,col,exec,deploysRobot,prelude}` を生成。
3. 候補ごとに `exec` **クロージャ**を生成（`()=>cand.exec(...)` / `()=>sim.use(...)`）。

これが約297万回 → 数百万のオブジェクト/クロージャ/配列が生成・破棄され GC が累積。
さらに `_stepStatic`（294万回）は結果を `reduce` で**最大s 1件だけ**選んで実行しており、
候補配列の完全生成は本質的に不要。`_candidates` 297万回の約99%が `_stepStatic` 由来（残りは beam/`_execKey`/root 列挙）。

### 1.4 高速化の主標的（優先度順）
1. **【最優先・✅実装完了 2026-06】`_stepStatic` のアロケーションフリー化**（挙動完全保存・ゴールデン値検証済）:
   - `buildFormation` 時に事前計算（キャラ名リテラル不使用・Phase2フック方針と整合）— **index.html に実装済**:
     - `ABIL_KEYS = Object.keys(ABIL)`（毎回の `Object.entries` を排除）
     - `ABIL_CANDS[key] = CHAR_REGISTRY[owner]?.cands?.[key]`（毎回のネスト参照を排除）
     - `ABIL_KC[key]`（契晶コスト＝`ABIL[key][3]` の事前展開）
     - `ABIL_BASE_S[key]`: `cand.s` が定数/未定義のものは `computeBaseScore` 結果を事前確定（関数 `s` は走査時評価=null）。
   - `_stepStatic` を「候補配列を作らず `ABIL_KEYS` を1パス走査して最大s候補を直接実行」する形へ書換え済。
     - **タイブレーク厳密一致**: 現行 `reduce((a,b)=>b.s>a.s?b:a)` は先頭最大を残す。`bestKey===null||s>bestS`（厳密 `>`）も
       先頭最大を残すため選択は完全同一。`Object.keys` の順序＝`Object.entries` の順序（挿入順）で走査順も同一。
   - **実測結果**: 単一ビーム(空prefix)で **14.87s → 12.36s（約17%短縮）・dmg完全同一**。
     当初見込み(~30-40%)に届かなかったのは、削れたのが `_candidates` のアロケーション分のみで、
     残コストの約50%が `use`/`burst` 本体の per-op 側にあるため（→ 第二手=3. の領域）。
2. **【次点・✅実装完了 2026-06】`_candidates()`（beam用・フル候補列挙）の軽量化**: 事前計算マップ(1.)を共用し
   `Object.entries`/ネスト参照/`computeBaseScore` 再計算を排除。返却shape(`{s,key,col,exec,deploysRobot,prelude}`)は不変で
   beam・`_execKey`・`enumerateRootPrefixes` と完全互換（root prefix列挙の回帰一致を確認済）。beam由来は呼出数が少ない(~万)ため副次的。
3. **【第二手・将来・計画中/未着手】per-op コスト削減（§2.3 Bytecode VM / §2.4 WebAssembly化）**: `use`/`burst` 本体（~280万/~135万回）
   の per-op 削減策。第一手（1.・2.）が効いた後に再計測し、なおボトルネックが per-op 側に残る場合に着手。
   特に **§2.4 WASM 化は C5（大幅な演算改善）を満たしうる本命候補**（2-5x見込・「ビルド禁止」解除で開く）。

### 1.5 検証方法（着手時）
- Phase 2 と同じゴールデン値方式（4形成: 全機構/Hecate無/Tetra無/Edison無 ＋ Tetra入り8形成のターン毎詳細一致）。
- `_stepStatic` 書換えは**選択完全同一**が条件のため、基準値 91,723,594 を含む全形成一致をアサート。
- 計測再現スクリプトは scratchpad の bench 系（フル探索1回+clone計数 / メソッド呼出計数 / 単体μs）に準拠。

---

## 2. 後続フェーズ候補・棚上げアーカイブ

> **§2.1/§2.2（Apply/Revert・Flat State）は実測（clone 2.8%）により費用対効果が低く当面棚上げ**（採らない理由の記録）。
> **§2.3/§2.4（Bytecode VM・WebAssembly化）は第一手のホットパス最適化後の per-op 削減策＝将来の選択肢**。
> 記録目的: 採否の根拠を後続セッション／Antigravityエージェントへ残すため。

### 2.1 差分エミュレーター（Apply/Revert）— 棚上げ
clone を廃し、単一の `Sim` 状態に対しアビリティ実行時に変更ログを記録、バックトラック時に逆再生（Revert）で
状態を復元する構想。**棚上げ理由**: clone は実測2.8%のため上限効果が小さい一方、全 mutation 点（エンジン＋
Phase2で移管したキャラフックの mutation も含む）のログ化が必要で、リグレッションリスクが効果に見合わない。

### 2.2 状態のフラット化（Float64Array）— 棚上げ
全状態を1つの型付き配列へシリアライズし、clone を `new Float64Array(old)` で高速化する構想。
**棚上げ理由**: (a) clone が非ボトルネック、(b) `buf`（可変長スタックの辞書）はフラット固定長へ写すと
バフのスタック意味論を全面再設計する必要があり、Phase2で整理したキャラフック群と整合させるコストが大。

### 2.3 アビリティのバイトコード化（VM）— 将来の選択肢
`cands[key].exec` の命令的JSロジックを命令オブジェクト配列（バイトコード）へ置換し、エンジンの VM ループ
（`switch-case`）で実行する構想。例:
```js
inori: { s:160, exec:[
  { op:'USE_ABIL', key:'inori' },
  { op:'ADD_G', target:'owner', amount:100 },
  { op:'ADD_BUF', key:'inori_burst', duration:'dur_inori_burst' },
]}
```
**位置づけ**: `use`/`burst` 本体の per-op コスト削減には寄与しうるが、`data/characters.js` 全面書換え＝規模・
リスク大。§1 のホットパス最適化を先行し、再計測でボトルネックが per-op 側へ移った場合に初めて検討する。
§2.4 WASM とは「同じ per-op 削減目標への別アプローチ」の関係（VM=JS内・WASM=ネイティブ寄り。下記参照）。

### 2.4 クライアント側 WebAssembly 化（第二手・C5を満たしうる大幅改善候補）

> **背景**: 「ビルドしない」制約の解除で**実際に開く価値はここ**。外部DB／サーバ演算オフロード（Next.js/Supabase/
> Vercel/Google）は別途定量評価し、**いずれも不適と判断**した（無料サーバレス枠 ~10s 上限が1ルート ~15s に収まらず、
> かつブラウザの無料マルチコア並列に勝てず保守だけ増える／データ38KB・件数十数・関数主体でDBの規模問題が不在）。
> よって**演算改善はクライアント側に閉じる**。その最有力が WASM 化。

- **対象**: ホットパス（探索ループ＝`use`/`burst`/`_na`/`_candidates`/`_decay`）を **Rust or AssemblyScript → WASM** へ移植。
  数値計算中心のループは JIT 非依存・型確定・GC回避により **2〜5倍**が現実的（C5＝大幅改善を満たしうる本命）。
- **メリット**:
  - **同じ無料クライアントコア上**で動作。外部インフラ・アカウント・クォータ・鍵管理が一切不要（維持保守最小）。
  - 生成物は静的 `.wasm`。配信は GitHub Pages / Cloudflare Pages 等の**無料静的ホストで保守ほぼゼロ**
    （※Google Drive は直接Webホスト不可＝2016廃止。Drive 5TB は検証ログ/スクショ等の**保管庫用途のみ**）。
- **デメリット / 留意**:
  - WASM ツールチェーン（ビルド工程）追加＝初期コスト。ただし生成物が静的のため**運用保守はほぼゼロ**。
  - エンジンを WASM/JS で二重実装する場合の**同期コスト**に注意。挙動完全保存（ゴールデン値）を跨言語で担保する仕組みが要る。
  - `data/characters.js` の関数（振る舞い 79箇所）を WASM 側へどう橋渡しするかが設計課題（全移植 vs ホットループのみ移植）。
- **着手条件（重要）**: **まず第一手（§1.4 の JS ホットパス最適化・無料・無ビルド・~30-40%）を実施して再計測**し、
  なお探索時間が UX 上不足する場合にのみ WASM 化を判断する。VM（§2.3）と WASM は同目標の二択であり、
  到達上限（WASM が高い）と導入コスト（WASM が高い）のトレードオフで選ぶ。
