# 編成最適化エンジン — Phase 2 / 3 設計暫定メモ

> [!NOTE]
> 本ドキュメントは、Antigravityセッションでの議論およびユーザーフィードバックに基づき、次回 Claude Code と作業を再開した際にスムーズに設計・実装へ移行するための暫定計画メモです。

---

## 1. 探索アルゴリズムの課題とアプローチの評価

### 1.1 静的優先度 `s` / 静的ペナルティの限界と対策
- **課題**: 
  アビリティの静的優先度 `s` や、特定の押し順を禁止・推奨する静的ペナルティは、編成（英霊×神姫の組合せ）ごとに最適なシナジーバランスが変化するため、一意に決定することが困難です。特定の編成で最適化できても、別の編成で探索漏れや不合理な枝刈りが発生するリスクがあります。
- **対策（動的コンテキスト指向優先度 / Dynamic Context-Aware Priority）**:
  アビリティの優先度ルールを静的にハードコードするのではなく、**「編成構築時（`buildFormation`）」にアビリティ間のメタデータ（タグ）を解析し、シナジー関係に応じた優先度評価ポリシーを動的に生成・補正**します。
  - キャラDB側には「このアビはロボット起動である（`deploy_robot`）」「このアビはアサルトバフである」「ロボット起動後に使用するとシナジーがある（`recommendsAfter: 'deploy_robot'`）」といった**純粋な性質のみをタグとして宣言**します。
  - 編成内にエジソン（ロボ起動持ち）とヤマト（黄アビ多数）が同席したことをエンジンが検知すると、「ロボ起動アビの優先度を極大化する」「ロボ起動後は黄アビの動的スコアをブーストする」といった評価式を動的にバインドします。

### 1.2 差分エミュレーター（高速化）による本質的解決
- **方針**:
  探索漏れが発生する根本原因は、計算リソース（JavaScriptオブジェクトの生成・クローン・ガベージコレクションのオーバーヘッド）の限界により、ビーム幅（`BEAM_W=32`）やロールアウトの探索深度を十分に広げられない点にあります。
  シミュレーションの実行速度を **10〜100倍** に高速化することで、ペナルティ等のアドホックな枝刈りルールに頼らず、より広いビーム幅（例：`BEAM_W=256`）で「一時的に火力が下がるが将来シナジーが爆発するルート」を漏らさずブルートフォース的に導出可能になります。

---

## 2. Phase 2 / 3 実行計画案

### 【Phase 2】エンジン内ハードコード汎用化 ✅ **完了（2026-06）**
アビリティ記述を宣言化（Phase 3）する前段階として、エンジン本体（`class Sim`）に残存するキャラクター名リテラルや固有カウンタを汎用化し、キャラDB側へ移管した。**実装方針は既存フックを再利用**（新フック名 `onAbilityUsed`/`onMburstMilestone` は新設せず、既存の `onAbility`/`onPartyBurst`/`turnEnd` に集約）。挙動完全保存（4ゴールデン形成＋Tetra入り8形成ターン毎詳細でバイト一致）。

1. **カウンタのtick汎用化** ✅:
   - `droid`/`banoshik_robot`/`mooncode` の個別デクリメントを、キャラDBの `tickStates: ['key', ...]` 宣言→`TICK_STATES`集約→`tick()`汎用ループに置換。`mooncode`/`mburst`はHecate固有stateへ移管しconstructor/snap/cloneからキャラ名連動を排除。
2. **反応アビリティのフック化** ✅:
   - `use()`内のロボ反応（赤/黄）→`edison.def.onAbility`、`procR()`内のムーンコード再発動→`hecate.def.onAbility`、コヴァレント連理魔力→`tetra.def.onAbility`(アビ側)＋`onPartyBurst`(バースト側)へ移管。`procR()`メソッドは削除。
3. **バーストマイルストーン/タイマーの汎用化** ✅:
   - モビウス（バースト5回毎CDリセット）→`hecate.def.onPartyBurst`、天矢乱舞の遅延解禁（inori_pタイマー）→`yamato.def.turnEnd`へ移管。

### 【Phase 3】差分エミュレーター（高速化）とアビリティ宣言化（将来検討）
シミュレーターのクローン生成・GCコストをゼロにするための抜本的なリファクタリング計画です。

1. **固定バッファ状態（Flat State）による差分適用と復元**:
   - `class Sim` の複製（`clone()`）を廃止。
   - 状態をフラットな配列（`Int32Array`）等で表現し、アビリティ実行時は「状態のコピー」ではなく「**差分の適用（Apply）**」を行い、探索のバックトラッキング（別ルートの評価）時には「**差分の復元（Revert / Undo）**」を行う構造に換装。
   - これにより、メモリアロケーションが完全にゼロになり、GC（Garbage Collection）のオーバーヘッドが消失します。
2. **アビリティの宣言的コマンド（バイトコード化）**:
   - キャラ定義のアビリティ実行ロジック（`exec`）から命令的JSコード（関数）を排除し、以下のような単純な命令オブジェクトの配列（バイトコード）として定義します。
     ```js
     // 例: アビリティ定義
     puvoir: {
       s: 140,
       effects: [
         { type: 'addBuf', name: 'puvoir', duration: 3 },
         { type: 'addBufIfState', condition: 'mooncode>0', name: 'puvoir_acute', duration: 3 },
         { type: 'useAbility' }
       ]
     }
     ```
   - エンジン側は、この配列を高速に処理するインタープリタループ（単純な switch-case 分岐）としてアビリティを実行します。これにより、JS関数のコールスタック生成コストがなくなり、極限の実行速度を達成します。

---

## 3. 動的コンテキスト指向優先度のリスク・対策・実現可能性

### 3.1 プレイヤー経験値（戦術セオリー）の完全反映は可能か？
**結論：技術的に十分可能です。**
プレイヤーの戦術知識（例：「デバフ先行」「ゲージ溢れの回避」「ロボ起動中の黄アビ集中」）は、以下のプリミティブに分解してキャラDBのアビリティ定義にメタデータ（タグ）として宣言できます。
- **アビリティの性質定義 (Tags)**: `tag: 'def_down' | 'atk_buff' | 'charge_gauge' | 'nuke'`
- **順序関係の定義 (Relations)**: `precedes: ['nuke']` (デバフはヌークの前に使用)
- **状態依存の定義 (State dependencies)**: `requiresActive: 'banoshik_robot'`
- **資源溢れの抑止 (Overflow guard)**: `maxResource: { key: 'g', target: 'self', limit: 100 }` (ゲージ100未満でのみ使用推奨)

これらを `buildFormation` 時に探索用スコア補正ポリシーとして動的にコンパイル（関数バインド）することで、プレイヤーの持つセオリーや経験則を完全に反映した探索が可能になります。

### 3.2 想定されるデメリットと対策案

#### ⚠️ デメリット1：メタデータの「記述ミス」による探索の破綻
- **詳細**: キャラDB側で依存関係（例: `precedes`）の記述を誤ると、探索エンジンは非最適な手順を「最良」と誤認し、シミュレーション全体の品質が低下します。
- **対策**: **依存性バリデーターの導入**
  `buildFormation` 実行時に、依存関係グラフに循環参照（Aの前にB、Bの前にA）が発生していないか、また「デバフはヌークより前」といったゲームの基本セオリーに反する矛盾がないかを自動検査し、コンソールに警告・エラーを出力して開発時に検知できるようにします。

#### ⚠️ デメリット2：極端な例外（エッジケース）での誤判断による「最適解の切り落とし」
- **詳細**: 「ゲージ100でのゲージチャージは無駄（ペナルティ）」という基本セオリーに対し、あえて無駄撃ちしてでも「アビ発動回数カウント（ヤマトの1アシ用等）」を稼ぐ方が全体の期待値が高くなる極端な状況下で、ペナルティのせいで最適解が早期に枝刈りされる懸念があります。
- **対策**: **動的コンディション（エスケープハッチ）のサポート**
  依存・ペナルティ条件に単純な静的タグだけでなく、`penaltyCondition: (sim) => sim.ycount % 4 === 3` のように、特定の危機的/例外的な状況下ではペナルティを無効化（バイパス）できる条件式をハイブリッドで許容します。

#### ⚠️ デメリット3：動的評価に伴う「探索パフォーマンス（実行速度）の低下」
- **詳細**: 探索の毎ステップでアビリティごとの複雑な動的依存関係を走査すると、CPUオーバーヘッドが増大し、シミュレーション完了までのウォールクロックタイムが延びます。
- **対策**: **ポリシーの事前フラット化とビットマスク化**
  `buildFormation` の時点で、静的および動的ペナルティ条件を「ビットマスク」や「事前計算されたルックアップ配列」に変換し、シム実行中の `_candidates` 生成時は単純な配列アクセスとビット演算のみで評価を完結させます。
