# 設計レポート — 押し順最適化の精緻化（C12 ＝ C9-②「賢い枝刈り」本設計）

> 対象: sim2 推奨押し順の準最適（ユーザー指摘 2026-06-28・ユーザー実ギアで再現済）。索引は `CALIBRATION_ANALYSIS.md` C12。
> 前提: 火力モデルは正（effond の defdown 登録済・amplifa は +10万固定で正＝Q1:b・nights は C11 で非累積化済）。**残るのは探索の順序付け・粒度の問題**。
> 不変条件: ゴールデン値 **150,445,920** 不変を全変更で確認 / `ABIL` 走査順・`>` 厳密タイブレーク（CLAUDE.md §2）/ Worker コード抽出（engine slice 自立）を壊さない。

---

## 1. 対象問題の総括（5症状）

ユーザー実ギア・BW128 の T1/T2 で再現（scratchpad `sim2dump.js`）。

| # | 症状 | 種別 |
|---|---|---|
| ① | T1 amplifa(エジソン4 #8)が赤アビ(alone#3/judg#7)の後 | 表示信頼性（火力は誤差・Q1:b） |
| ② | effond(ヘカテー2)がデバフ持ちなのに赤アビ群の中で優先されない（s=70低） | 順序付け（デバフ先行） |
| ③ | T2 judg リキャスト空転（judgCap 到達後の連理魔力 proc reset が死蔵） | 機構・無駄リキャスト |
| ④a | T2 ヘカテー順: mobius(パーティB5回毎CDリセット)が sleur 使い切り前に発動 | 順序付け |
| ④b | T2 tenya(ヤマト2)×3 が atomic 実装で 3発の間に他アビを挟めない | 探索粒度 |

共通テーマ＝**「バフ/デバフを先・ダメージを後」「無駄リキャストを避ける」「多段アビを分割して interleave 可能にする」**。C9 で保留した「②賢い枝刈り」の具体化。

## 2. 各症状の根本原因

- **①②（バフ/デバフ先行が選ばれない）**: 探索の目的関数は総ダメージ（`_objective[0]`）で、静的 `s` は greedy 既定＋タイブレーク。強ギアでは「デバフ先・ダメージ後」と「逆」の総ダメージ差が cap飽和で僅差になり、BW128 でも僅差の中で非定石順が残る（C9 と同根＝幅依存の僅差）。effond は s=70（赤の中位）で、デバフ価値が静的スコアに反映されていない。
- **③（judg 空転）**: 連理魔力 proc（アビ12/バースト2毎）が `cd.judg=0` を付与するが、同ターン `judgCap=JUDG_REACT+(開始時cd0?1:0)` を超えると judg を撃てず、reset が死蔵。さらに judg ph2(通常攻撃)が低価値の埋めになる場合がある。
- **④a（mobius×sleur）**: mobius は `onPartyBurst` でパーティバースト5回毎にヘカテー全 CD を0に。sleur を使い切る前にこれが起きると、本来 mobius 後に温存して撃てた sleur 回数が変わる。ヘカテー内の発動順（puvoir/sleur/effond）と mobius 発火タイミングの相互作用。
- **④b（tenya 一括）**: `tenya.exec` が while ループで最大3バーストを一括実行（`characters.js`）。探索は tenya を1候補として扱うため、3発の間に effond(デバフ)/judg を挟む順を**構造的に表現できない**＝粒度不足。

## 3. 設計案

### 3.1 デバフ/バフ先行（①②）— 「賢い枝刈り」本体
2案を比較。

- **案A: 静的スコアの精緻化（軽量）**
  - effond 等「デバフを付与する赤/青アビ」に、デバフ未適用時のみ +α の先行ボーナスを与える動的 `s`（例: `defdown 系バフが薄い間は s を judg 並みに引き上げ、適用後は通常値へ戻す`）。divinus が既に s=90 で先行する設計と同型。
  - 長所: 実装小・速度不変。短所: `s` は greedy 既定＆タイブレークのみで、ビーム本選は総ダメージ駆動のため効果限定的（僅差問題は残りうる）。
- **案B: ビーム目的関数のタイブレーク拡張（本命・採用）**
  - `_objective` の第1要素（総ダメージ）を**相対許容（log-bucket）**で丸め、僅差オーダーを「同点」扱いにする。同点時に第2要素＝**定石性スコア**で定石側へ寄せる。
  - **僅差判定（gear非依存）**: 第1要素 = `round( ln(dmg+1) / ln(1+REL) )`。差が ±REL 以内なら同バケット＝同点。REL は「強ギア僅差(≈0.03〜0.1%)は同点・弱ギア有意差(≈数%)は別点」を満たす値（PoC で 0.2%前後を起点に較正）。
  - **定石性スコア（ユーザー定義 2026-06-28＝報酬−減点）**:
    - ＋報酬「**バフ/デバフ先行**」: ダメージ行動（judg ph0・effond・各バースト=alone/tenya/judg ph1/攻撃フェイズ）が、攻撃/防御DOWN（divinus_def/effond_def）・アサルト(absolute/banoshik)・nights 等の**有効中に撃たれた度合い**を加点。＝「バフが乗った状態でダメージを出す」ほど高い。
    - −減点「**無駄リキャスト・機会損失**」: ① 非累積バフ(nights等)の有効中の再発動（refresh無駄／C11で nights は guard 済）、② judgCap 到達後の judg CD リセット死蔵（proc空転）、③ tenya 多段の冗長（④bの分割前は近似減点）、④ デバフ/バフを撃たずダメージを先行させた取りこぼし（報酬の裏返し）。**機会損失はなるべく減らす方向**。
  - 長所: 総ダメージを損なわず（僅差時のみ作動）表示を定石化＝表示信頼性（①含む）と②③④a に同時に効く。短所: REL と各報酬/減点の重みが要較正。`>` 厳密比較・`ABIL`走査順の不変条件は崩さない（cmpVec は語彙拡張＝要素追加のみ）。
  - ⚠ ゴールデン値（150,445,920）への影響: 弱ギア（generic harness）はオーダー間 dmg差が REL より大きいため**別バケット＝従来どおり dmg で選択＝不変**の見込み。強ギアのみ僅差→定石性が作動。**PoC で generic golden 150,445,920 不変を必ず実測**（同点が生じて選択が変わると golden が動くため）。
  - **計測方式**: 定石性は turn 実行中の状態に依存するため、`T.orthodoxy` を `use()`/`burst()`／判定点で加減算（hotpath）。PoC ではまず報酬項のみで golden不変＆強ギア改善を確認し、減点項を順次追加して較正する。

#### 案B の PoC 結果（2026-06-29・不採用）
- scratchpad `poc_c12.js`: 定石性報酬＋log-bucket(0.2%)許容で僅差を同点化→定石性で選択。
- **結果: generic golden 150,445,920 → 149,768,263（−0.45%）＝実ダメージを犠牲**。同バケット内で定石側が低ダメージのため、ターン毎の僅差損が10ターンで累積。
- ∴ **案B（許容タイブレーク）は「0.1%でも追跡＝最大ダメージ優先」方針と矛盾するため不採用**。定石順と最大ダメージ順が一致しない（＝定石順がモデル上僅かに低ダメージ）ケースでは、タイブレークは必ずダメージを削る。

#### 案C（採用方針）＝探索ガイダンス（ダメージ無犠牲）
- 定石性スコアを**選択基準にしない**。ビームの枝刈り段で**定石枝を一定数 保持（多様性確保）**し、**最終選択は純ダメージ最大のまま**。→ ダメージは決して下がらず、枝刈りで取りこぼしていた「同等以上ダメージの定石順」があれば拾える（C9のBW増と同系統＝探索品質の改善）。
- **適用ゲート（必須の事前切り分け）**: 各症状（②effond先行/③judg空転/④a hecate順）について、定石順 vs 最大ダメージ順を**フォース計測**し、`定石 ≥ 最大` のものだけ案Cで拾う。`定石 < 最大` は最適化器が正しく、定石表示はダメージとのトレードオフ＝「不具合ではない」と結論（ユーザーへ提示）。
- **S1フォース計測結果（2026-06-29・scratchpad poc_c12b.js）= 案C にとって決定的好結果**:
  - **強ギア（ユーザー実ギア）10T総和: baseline 1,007,410,534 → 定石寄り 1,011,839,025（+0.44%）**。
  - ∴ **強ギアでは定石順が真に高ダメージ**＝ビーム(BW128)が最適枝を取りこぼしている（C9と同種の探索ミス）。generic は逆（定石側が低ダメージ）＝ギアで regime が異なる。
  - **案Cはダメージ単調**: 純ダメージ選択のまま定石枝を保持するため、強ギア=+0.44%を拾い、generic=真の最大(150,445,920)を選び golden 不変。案Bの犠牲（generic −0.45%）は起きない。
  - → **案C を実装する価値が確定**（ユーザーの「0.1%でも追跡」に合致＝むしろ上積み）。

### 3.2 judg 空転の抑止（③）
- judg の動的 `s` / guard に「`T.ju>=judgCap` なら 0/不可」は既にあり。**proc reset の死蔵**は機構上避けにくいが、(a) judgCap の定義見直し（proc で増えた分を活かせるか）か、(b) ph2(NA)埋めを低優先化して「撃てるなら ph0/ph1 を優先」する選別を検討。要・実機の judg 実回数と突き合わせ（sim2 データで「JD空転」の実体を確認してから着手）。

### 3.3 ヘカテー順 × mobius（④a）
- mobius 発火（パーティB5回目）の直前に sleur/effond/puvoir をどう並べるかは**ビーム探索が本来解ける**範囲。①②のタイブレーク拡張（3.1案B）で「バフを使い切ってから reset を受ける」順が僅差で選ばれやすくなる可能性。専用ロジックは持たず、3.1 の副産物で改善するか先に検証。

### 3.4 tenya 多段の分割（④b）— 探索粒度の引き上げ
- tenya を「初回 tenya」＋「tenya 再発動（40ゲージ・最大2回）」の**別候補**に分割し、探索が3発の間に他アビを挟めるようにする。
  - 実装: `tenya` exec を1発に縮小し、再発動を別キー（例 `tenya_re`）またはガード付き同キー再選択で表現。`T.tenya` カウンタと40ゲージ消費は維持。
  - ⚠ これは探索空間を広げる＝**BW128 を更に重くする**（Phase 5 の待機UXと相乗で重要）。また `ABIL` 走査順・ラベル採番（ヤマト2×N 表示）への影響に注意。golden 値も変わりうる。
  - 優先度: ①②③④a を先に片付け、④b は効果（実機で interleave が有意か）を確認してから。

## 4. 影響度・リスク

- **ゴールデン値**: 3.1案B（タイブレーク）は総ダメージ不変なら 150,445,920 維持の見込み（要実測）。3.2/3.4 は値が変わりうる→変更時は新基準として固定＆ユーザー承認。
- **不変条件**: `>` 厳密比較・`ABIL` 挿入順・Worker slice 自立を厳守。タイブレーク追加は `cmpVec` の語彙拡張で対応（既存の辞書式に項を足す）。
- **性能**: 3.4（tenya分割）は探索増。Phase 5（待機UX）と並行で体感を担保。
- **検証**: 強ギア再現フィクスチャ（exp12設定）を回帰に追加し、(i) divinus/effond が赤アビ前、(ii) amplifa の扱い、(iii) tenya 分割後の interleave、(iv) golden 不変 をアサート。

## 5. 実装計画・引継ぎ

1. **S0（PoC・完了2026-06-29）**: 案B（許容タイブレーク）を PoC→**不採用**（generic golden −0.45%＝ダメージ犠牲）。案C（探索ガイダンス）へ軌道修正。
2. **S1（切り分けゲート・次の一手）**: 各症状（②effond先行/③judg空転/④a hecate順/①amplifa）について、**定石順 vs 最大ダメージ順をフォース計測**（強ギア・10T総和）。`定石 ≥ 最大` = 探索ミス（案Cで拾える）／`定石 < 最大` = 最適化器が正でトレードオフ（不具合でない・ユーザーへ提示）。
3. **S2**: S1で「探索ミス」と判定された症状のみ、**案C（定石枝の保持＋純ダメージ選択）**を実装。golden 150,445,920 不変を保証（選択は純ダメージのまま）。
4. **S3**: ③judg 空転は sim2 実機データで実体確認後（judgCap 見直し or ph2低優先）。④b tenya 分割（探索粒度）は効果と性能コストを測り Phase 5（待機UX）と足並みを揃える。
5. 各段で検証ワンライナー（150,445,920）＋強ギアフィクスチャをアサート。完了項目は CALIBRATION_ANALYSIS.md C12 を更新。

> 注記: ① amplifa は Q1:b より火力的に不要（強ギアで+10万固定は誤差）。S1 で `定石<最大` が確定すれば、amplifa前出しは**ダメージを削ってまで追わない**＝表示のみの嗜好として保留（案Cは適用しない）。
