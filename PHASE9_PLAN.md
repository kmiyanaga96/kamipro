# PHASE9_PLAN — 実機観測 intake の自動化（録画 → 検証済みデータ）

> **種別**: 計画（フェーズ級・ROADMAP で採番）
> **ゴール**: **実機の観測を、人手の転記を介さずに、検証可能な形でリポジトリへ入れられる状態**にする。
> 達成されたら真になること＝「**ユーザーは遊びながら録画するだけでよく、転記・検算・整形は一切発生しない**」。
> **完了条件**: §9（受入基準）を満たし、1本の実走が「録画 → trial データ → 突合」まで**人手の数値入力ゼロ**で通ること。
> **状態**: **進行中（P0・P1・★P2 ✅完了＝2026-08-19 に出口確認を通過 / P2-2 ❎不要で close / P2-4 ➡️P3-2 へ移設 / 次は P3）**
>
> 作成 2026-08-07 ／ 関連: `TRANSCRIPTION_DESIGN.md`（設計の一次・本 Phase へ編入）・`REPO_STANDARDS.md` **E11**
> ・ROADMAP §4(iii)（ワークフロー系の未確定Phase・本 Phase が intake 部分を吸収）・sim05（凍結中・§8）
>
> **⚠ 方針（ユーザー決定 2026-08-07）**: **進行スピードより網羅性・正確性を優先する。**
> 想定外の追加ツールが必要になることを**前提**とし、§7 に受け入れ手順を定めてある。

---

## 1. 目的とスコープ境界

**解く問題**: 転記コストが較正全体の律速になっている（`TRANSCRIPTION_DESIGN.md` §0）。
sim05 は残り約10走（M3 4〜6 / M4 1〜2 / M5 1〜2 / M6 2）で、**C40 / C41 / C44 はすべてデータ量に依存**する。
1 trial あたり「5分の録画をコマ送りで手入力」では、走数を増やすほど人が詰まる。

**根本原因**（2026-08-07 の棚卸し）: **分担が逆**だった。
人間しかできないこと（実機の観測・判断）と、機械がやるべきこと（転記・検算・整形）が混ざり、
**人間が機械的作業を担っていた**。その結果、記録様式は事故のたびに**足し算だけ**され続けた。

### 1.1 分担の原則（★本 Phase の憲法）

> **観測と判断はユーザー。導出・分析は Claude Code。転記・検算・整形はツール。**
> 系として: **既に記録されたものから導けることは、二度と人に訊かない。**

- **ユーザーが持つもの**: 実機で何が起きたか（ground truth）／config を変えたという事実／優先順位の判断／**責任**
- **Claude Code が持つもの**: 導出・分析・設計・文書化
- **ツールが持つもの**: 転記・検算・整形・版管理（**AI 知識が不要な機械的作業はすべてここ**）

⚠ **LLM に転記させるのは本原則の違反**（2026-08-07 に一度提案し、ユーザー判断で取り下げ）。理由＝
**非決定的**（同じ画像で違う答えが出うる）／**再現不能**（後から根拠を追えない）／
**毎回コンテキストを食う**（走が増えるほど劣化）／**Claude の可用性に律速される**。
✅ **例外は設計判断のみ**＝ROI 座標の決定・可読性の判定・テンプレート検出の可否など、
「**転記ではなく、ツールの作り方を決めるために画像を見る**」場合（P1 がこれ）。

### 1.2 スコープ境界

| 含む（intake） | 含まない |
|---|---|
| 録画 → 数値・ヒット・フラグの抽出 | **ダメージモデルの較正**（Phase 4／sim05） |
| 抽出結果の**検算**と不合格箇所の提示 | **エンジン改修**（`src/`） |
| **config の版管理**と走の無効化判定 | 探索アルゴリズム |
| trial データの生成（機械可読） | 分析・Cx の起票（Claude Code の仕事のまま） |

**∴ 本 Phase は「データがリポジトリに入るまで」で終わる。** 入った後は Phase 4 の仕事。

## 2. 全体フローとゲート

```
[P0] 先行整備 ──────────────────── ✅完了（2026-08-07）
     無効化条件（E11）／録画時チェックリスト／設計メモの解凍・陳腐化修正
  ↓
[P1] ★実測ゲート ───────────────── ✅**通過（2026-08-14）＝進む**
     静止画 ＋ ffprobe 出力 → §9.2 の撤退条件3つとも**非該当**
  ↓
[P2] 抽出基盤（AI 不要） ────────── ⏳**現在ここ**
     フレーム抽出・**canvas 正規化**・ROI クロップ・重複除去・HP%検算
  ↓
[P3] 数値抽出 ＋ 検算 ──────────── OCR は候補生成のみ。検算不合格だけ人へ
  ↓
[P4] config 版管理の統合 ────────── slot JSON / 版の自動判定 / 走の自動無効化
  ↓
[P5] 受入 ──────────────────────── §9 の受入基準（実走1本を人手ゼロで通す）
```

**⚠ ゲートは「前段の出口条件を満たすまで次に進まない」**。各段の入口/出口は §4 に明記する。

## 3. 情報の所在（★正確性の担保の中核）

**過去の事故はすべて「その情報がどこにあるか決まっていなかった」ことが原因**だった
（GEAR ハードコード＝結論が符号ごと反転／幻獣枠が未記録＝逆算でしか復元できなかった／
ランクが記録項目に無かった／holy 回数が2箇所にあって食い違った）。
∴ 本 Phase では**すべての事実に「唯一の正」を割り当てる**。

| 情報 | 唯一の正 | 誰が入れるか | 検証手段 |
|---|---|---|---|
| **実機で何が起きたか** | 録画（映像） | ユーザー | — （原本） |
| 押下・ヒット・値・フラグ | 抽出結果（機械生成） | **ツール** | §5 の検算4本 |
| **config**（GEAR/幻獣/サブ/ATK/ランク） | **config 版ファイル**（1ファイル・日付版） | ユーザーが**映す**／ツールが読む | E2（既知値との bit 一致） |
| 敵パラメータ | `gamedata/js/enemies.js` | Claude Code | 一次情報 md |
| モデル（式・係数） | `src/` ＋ `CALIBRATION_ANALYSIS.md` | Claude Code | golden |
| 乖離の状態 | `CALIBRATION_ANALYSIS.md` の Cx | Claude Code | — |

**規律3点**:
1. **二重管理を作らない**。同じ数が2箇所にあったら、片方は必ず導出にする。
2. **導出できるものは記録項目にしない**（記録項目を増やす提案は、まずこの関門を通す）。
3. **復元不能な情報は必ず原本に残す**。⚠ 実例＝**幻獣枠は `_configSig` から復元できない**
   （畳んだ GEAR しか持たないため）＝この種の項目を §6 で洗い出しておく。

## 4. 段階計画

### P0 — 先行整備 ✅ 完了（2026-08-07）

**目的**: ツールを待たずに「後手」を止める。単独で価値があり、後戻りしない範囲だけ先に入れる。

| # | 成果物 | 所在 |
|---|---|---|
| 1 | **走の無効化条件**（何が変わったら走を捨てるか・数値基準） | `REPO_STANDARDS.md` **E11** |
| 2 | **録画時チェックリスト**（録画を自己完結させる） | 本書 §4.0.1 |
| 3 | 設計メモの**解凍＋陳腐化修正** | `TRANSCRIPTION_DESIGN.md`（archive から復帰） |

**出口条件**: ✅ 3点すべて所在が確定し、`npm run doc:check` がグリーン。

#### 4.0.1 ★録画時チェックリスト（ユーザー用・これだけ守れば後から何でもできる）

**狙い**: **config を「書く」から「映す」に変える**。映像が自己完結していれば、転記はいつでも後追いできる
＝**ユーザーのゲームプレイと Claude の可用性が完全に分離する**（＝「後手」の解消）。

**録画の開始前に、1回ずつ映すだけ**:

1. **編成画面**（幻獣枠 メイン/サポート/サブ5 ＋ サブ神姫が見える状態）
2. **装備パネル**（ウェポン効果一覧＝GEAR の素）
3. **キャラ5人の表示ATK/HP**（ステータス画面）
4. **プレイヤーランク**が見える画面

**戦闘中に守ること**:

5. **敵HP%が見える状態**を維持（検算の材料＝§5-1）
6. **右側UI（ゲージ・CD）を隠さない**（tier 観測と C24 の材料＝ROI が固定で最も安い）
7. 途中で**装備・幻獣・サブ枠を変えない**（変えたら**そこで録画を切る**＝別の走にする）

⚠ **ランクアップは録画を切る理由にならない**（E11＝ノイズ床の 1/1000）。**そのまま続行してよい。**

### P1 — ★実測ゲート（進む / 降りるの分岐）✅ **通過（2026-08-14）**

**目的**: 机上の設計を**実物で検証**する。ここを通さずに P2 以降へ進まない。

**入口条件**: P0 完了。
**ユーザーに依頼したもの**（`TRANSCRIPTION_DESIGN.md` §6.1 が正）: 静止画 ＋ `ffprobe` 出力 ＋ フレームタイムスタンプ列。

⚠ **動画そのものは git に格納しない**（1.37MiB のリポジトリに 50〜500MB は不可逆汚染）。
セッションへ共有するのは問題ない。`.gitignore` に動画拡張子は追加済み。

#### P1 結果 ── **判定＝進む**（撤退条件3つとも非該当）

**実測サンプル**: `M3-1.mp4`（Microsoft Game DVR・2026-08-09 録画・4分36秒）

| 項目 | 実測値 |
|---|---|
| 解像度 / コーデック | **2288×1440**（SAR 1:1・DAR 143:90）／ h264 Main / yuv420p |
| ビットレート | **16,348 kb/s**（高品位＝OCR に有利） |
| フレームレート | `avg 29.718fps` / `r_frame_rate 60` / `tbn 30k` ＝ **VFR** |
| 総フレーム | 8,210（276.27秒） |

**★VFR の実害は無い**（2026-08-14 に 90フレーム連続のタイムスタンプで実測）:

| 間隔 | 出現 |
|---|---|
| 33.33ms（＝60fps グリッド **2目盛**） | 88 |
| 50.00ms（＝**3目盛**） | **1** |

**最大ギャップ 50ms・クラスタリング無し**。欠落は「30fps フレームの脱落（66.7ms になるはず）」ではなく
**60fps グリッド1目盛（16.7ms）のスリップ**で、毎秒 約0.6回・一様に散っている（全体の 0.94% 欠損と整合）。
∴ **ポップアップが 50ms 未満しか表示されない場合のみ取りこぼす**。
**実測での裏付け**（2026-08-14 第2便・真解像度フレーム5枚）＝**サンプル内で読めなかった値はゼロ**
（`602,052` / `595,900` / `596,45x` / `605,266` / `6,012,442` がいずれか1枚以上で判読可）。
⚠ ただし**寿命そのものは未測定**（下記「第2便の発見」⑧）。

**★設計判断（P2 へ）**: **CFR 変換をしない**。当初案の「固定fps へ正規化」は、
**再エンコードでフレームの複製/脱落を作り、重複除去（§P2）と検算を逆に汚す**。
native フレーム＋タイムスタンプをそのまま扱う（`ffmpeg -fps_mode passthrough`）。
⚠ **`-vsync 0` は FFmpeg 7.1 で削除された**（`Unrecognized option 'vsync'` ＝2026-08-14 に実機で遭遇）。
**後継は `-fps_mode passthrough`**。⚠ **無指定にしない**＝ffmpeg が VFR を埋めるために
**フレームを複製することがあり**、ポップアップの表示継続長の測定が水増しされる。

#### 撤退条件（§9.2）の照合

| 撤退条件 | 判定 | 根拠 |
|---|---|---|
| ① 数字が判読できない | ❌ **非該当** | 0.64倍に縮小された共有画像でも `602,052` / `595,900` が判読可＝**実フレームは 1.56倍鮮明** |
| ② 30fps で取りこぼす | ❌ **非該当** | 最大ギャップ **50ms**（上表） |
| ③ 1ヒットも読めない成分がある | ❌ **非該当** | 重なった5個のうち **同値ヒットが2組**＝`TRANSCRIPTION_DESIGN` §3.5 の「同値の繰り返し＝1つ読めれば足りる」が実物で成立 |

#### ⚠ 共有画像の経路に関する注意（今後の依頼様式に反映）

初回共有分は `ゲーム(2288×1440) → 録画 → VLC 再生（縮小表示）→ デスクトップキャプチャ(1920×1080)` の経路で、
**VLC の表示段で 0.64倍に再標本化**されていた（ジオメトリで裏取り＝画像内の Chrome ウィンドウ幅 ≈1464px ÷ 2288）。
**可読性の判定は安全側に外れる**ので結論は覆らないが、**画素幅の絶対値は確定できない**。

★**以後、静止画は「デスクトップキャプチャ」ではなく `ffmpeg` の抽出フレームで受け取る**
（撮影タイミングの技能が不要になり、真解像度が保たれる）:
```
ffmpeg -hide_banner -i "M3-1.mp4" -ss <秒> -t 2 -fps_mode passthrough "frames\f_%03d.png"
# 任意: -frame_pts 1 を足すとファイル名が PTS 由来になり、連続性と間隔を検算できる
# ⚠ FFmpeg 7.1 未満は -fps_mode passthrough ではなく -vsync 0（版は ffmpeg -version で確認）
```

#### 新発見（P2/P3 の要件に反映済み）

| # | 発見 | 影響 |
|---|---|---|
| ① | **ラベルが「他ヒット」の数値に重なる**（縦ピッチ < ラベル高+数値高） | §4-4 の「真下の数値へ固定オフセット割当」は**そのままでは誤対応と画素汚染**。→ **P3 でテンプレのマスク/減算を先に行う** |
| ② | ⚠**ROI を固定クロップにできない** | 録画対象がブラウザウィンドウ全体で、**ページスクロール位置・ウィンドウサイズ・ズーム**で canvas が動く（共有画像にスクロールバーと下部バナーが写っている）。→ **P2 に canvas 外枠検出＋アフィン正規化を追加**（全 ROI 由来の検算 1/3/4/5 の前提条件） |
| ③ | `NEXT n TURN` 表示が画面にある | **ターン境界がヒューリスティック無しで読める**＝検算②（リプレイ照合）のアラインメントが容易に |
| ④ | 画面右上のタイマーは **`hh:mm:ss`**（1Hz） | フレーム精度の時計としては**使えない**（ユーザー回答 2026-08-14）。ただし②の実測で VFR 正規化自体が不要になったため**問題にならない** |
| ⑤ | ⚠**バフ/デバフアイコン列は一部しか表示されない**（ユーザー回答 2026-08-14） | **§3 規律3「復元不能な情報」に該当**＝**録画からバフ状態は全量復元できない**。∴ **C38 の近道にはならない**（従来方針どおり tier 効果の成立/不成立から区間推定する） |
| ⑥ | 録画メタデータに `creation_time` / `title`（ウィンドウ名）/ `artist` がある | **走の provenance が自動で取れる**＝**P4 の config 版判定・E11 の自動無効化判定の錨**に使える |

#### 第2便の発見（2026-08-14・真解像度フレーム5枚 ＝ `ffmpeg` 抽出）

**★★⑦ `TOTAL` ポップアップが存在する ── §5 検算①の分解能問題（§3.4 R5）を解消しうる最大の発見**

多段ヒットの終端に **`TOTAL STING! 6,012,442`** が表示される（個別ヒットは 596〜605千＝**約10ヒットぶんの合計**）。
`TRANSCRIPTION_DESIGN.md` §3.4 は「総和チェックは **HP 1% = 980万**の分解能しか無く、
**個別ヒットの取りこぼしは原理的に見えない**（holy 8ヒット丸ごと落としても 0.065%）」と結論していたが、
**画面に合計値が1円単位で出ている**なら話が変わる:

| | 従来（HP%由来） | **TOTAL 表示由来** |
|---|---|---|
| 分解能 | ±980万（holy 122ヒット相当） | **1円** |
| 取りこぼし検出 | 不可能 | **個別ヒットの合計と突合すれば1ヒットでも検出できる** |

**✅ 集計単位が確定（ユーザー回答 2026-08-14）**:

| 単位 | `TOTAL` の中身 |
|---|---|
| **アビリティ** | 多段ヒットの合計 |
| **バースト** | **本体 ＋ 追加ダメージ** |
| **攻撃**（通常） | 多段攻撃の合計 |

★**含まれないもの**＝**追撃（エジソンのロボ追撃・アリアンの1アシ追撃 等）／バーストストリーク**。

**★★これが2つ目の価値を生む ── `TOTAL` は「成分の二分」を供給する**

`TRANSCRIPTION_DESIGN.md` §3.3 R4 は「**成分の見分けは表示だけでは付かない**（ユーザーは演出で判断している）」
を**未解決**としていたが、**`TOTAL` に含まれる/含まれない は画面から機械的に読める境界**である。∴:

| Cx | `TOTAL` が与えるもの |
|---|---|
| **★C47** | シムはアリアンの**①バースト追加ダメージ**と**②1アシ追撃**を同一値で加算している（実機 ①=②×5.71）。**① は TOTAL の内側・② は外側**＝**シムが混同している当の2つを、画面が分離してくれる** |
| **★C40** | バースト単位で `追加 = TOTAL − 本体` が**厳密に取れる**（どのポップアップがどちらか読めなくても差で出る）。⚠ **M3 は依然必要**（本体連動 vs バフ量連動の共線は測定精度ではなく実験計画の問題） |
| **C3 / C5** | 追撃は TOTAL の外側＝**追撃だけを他成分と混ぜずに拾える** |

⚠ **未確認**: エジソン英霊武器の「追加ダメージ」（`onBurst`・abi 枠）が内側か外側か。
**ロボ追撃とは別物**なので回答からは決まらない＝**エジソン編成を扱う前に確認する**。
⚠ **§10.2 の受け入れ関門**により、これを使うツールは **§5 の表に行を足してから**作る（§5-7 に追加済み）。

**⑧ ⚠ ポップアップの寿命は想定より短い可能性（要測定・P2 の最初の実測項目）**

5枚のうち frame2 の値（`602,052`/`595,900`）が frame3 では**消えて別の値に入れ替わっている**。
**フレーム間隔が未確認**（`-frame_pts` 無しで抽出されたため連続性を検算できない）ので断定できないが、
**連続フレームだとすれば波の入れ替わりは 33ms 級**で、当初想定より速い。
- **P1 の判定は変わらない**（サンプル内で読めなかった値はゼロ・最大ギャップ 50ms も不変）
- **ただし margin の見積りは下方修正する**。⑦の TOTAL が**安全網**として効く（個別を落としても合計が残る）
- → **P2 で最初に測る**（`-frame_pts 1` 付き抽出で PTS 付きの連続フレームを取る）

**⑨ その他（P2 の実装に直接効く）**

| # | 発見 | 効き方 |
|---|---|---|
| ⑨-a | **canvas の装飾外枠（金色）が高コントラストで固定アスペクト** | **canvas 正規化の検出ターゲットとして良好**＝新発見②の実装可能性を確認 |
| ⑨-b | **HPバー幅 ≈ 630px**（真解像度・目測 ±5%）＝1% ≈ 6.3px | サブピクセル推定は**有望**。⚠ 区切りドット5個は x 固定＝マスク可能 |
| ⑨-c | **HP% の 100→99 遷移をフレーム間で捕捉** | %表示は**即座に更新**される。⚠ バー側の遅延追従は目視では判定不能＝**P2 で機械測定** |
| ⑨-d | frame5 の Ability パネルに **`Turn 10`** の文字 | ✅**CD 表示と確定**（ユーザー回答 2026-08-14）＝画面右側にキャラの各アビリティが**色枠**で並び、**使用後はグレーアウト＋そのすぐ下に残ターン数**。∴ **CD 状態が全アビリティぶん読める**＝`record_skeleton` の CD 項目が自動化でき、さらに**押し順の錨**になる（→ §5 検算8 を起票） |
| ⑨-e | バフ/デバフアイコン列が**フレーム間で増減している** | ユーザー回答「一部しか表示されない」と整合＝**動的な切り詰め**であることを確認（C38 の近道が無いことの裏取り） |

**★★⑮ 右パネルは「同じ領域に2状態」だった ── モード判定が全 ROI 解釈の関門**（ユーザー回答 2026-08-14）

`gauge` クロップ（5キャラ＝ナポレオン/ヘカテー/テトラ/エレイン/アリアンロッド）で確定:

| モード | 中身 | いつ |
|---|---|---|
| **list** | **5キャラの HP・BG・Ability の色・バフ列**が同時に読める | **初期状態** |
| **detail** | 個別キャラの Status / Burst / **Ability 2×2＋CD残ターン** | **アビリティ発動のたび** |

★**1本の動画内で2状態を無数に行き来する**。

⚠ **∴ モードを判定せずに ROI を解釈すると、別レイアウトを読んで静かに壊れる**。
`abil` ROI は list モードでは**一覧の3〜5行目**を指してしまう（`gauge` に内包されているため）。
**モード判定は全 ROI 解釈の前に置く関門**として実装した（`src/transcribe/panel_mode.js`）。

★★**そしてこれは押下シグナルそのもの**: `list → detail` の遷移は「**どのキャラが操作されたか**」を直接示す。
`TRANSCRIPTION_DESIGN.md` §3.3 の💡「**演出を押下境界の検出に使えるかもしれない（要検証）**」に対し、
**演出のフレーム差分より遥かにきれいなシグナルが UI 側に存在していた**ことになる＝この💡は**上位互換で解決**。

⚠ **判定アルゴリズムの落とし穴（実際に踏んだ）**: 「行プロファイルが周期 h/5 で自己相関を持つ」だけでは
**detail を list と誤判定する**。detail にも **Ability 2×2 グリッド**という縦の繰り返しがあり、
その行ピッチが h/5 に近いと単一ラグでは区別できない。
∴ **調和成分（L・2L・3L）まで揃うことを要求**する＝「4回以上繰り返す」という list 固有の性質だけを拾う。
セルフテストの detail フィクスチャは**この誤判定を再発させないための回帰ガード**（グリッド行ピッチを
list のスロット高さにわざと近づけてある）。

⚠ **`listThreshold` は未較正**（合成フィクスチャ由来の暫定値）。判定が閾値に近いと `T1-ROI-003` を WARN で出し、
**実フレームの score 分布を集めて決め直す**（FATAL にはしない＝1フレーム捨てるより較正材料を残す）。

**⑯ list モードから直接読めるもの**（同クロップで確認）:
- **5キャラの HP 実数値**（45370 / 40236 / 37876 / 40334 / 38788）＝**味方生存**（ROADMAP 未確定Phase）の観測値
- **5キャラの BG**（全員 100）＝**C24** の観測経路
- **Ability の◆が色分けされている**（赤/黄/青/暗）＝敵の限定行動条件（「赤色アビリティを使用した」等）に対応しうる
- バフ列は各キャラ 9〜10 個で**切り詰められている**＝**C38 の近道にならない**ことの再確認

#### 残った未確認（★いずれも P2 の設計で回避可能＝ゲートを塞がない）

| 項目 | 扱い |
|---|---|
| **ポップアップの寿命** | ⑧＝**P2 の最初の実測項目**。取りこぼしが起きるなら ⑦（TOTAL）が安全網 |
| HPバーの**遅延追従** | ⑨-c＝P2 で機械測定。回避策＝ターン境界の**安定後**フレームを採る |
| `TOTAL` の**集計単位** | ⑦＝**ユーザー確認事項**。確定するまで検算⑦は実装しない（§10.2 の関門） |
| `Turn 10` の**意味** | ⑨-d＝**ユーザー確認事項**。record_skeleton の自動化範囲が変わる |

### P2 — 抽出基盤（★AI 不要）⏳ **現在ここ**

**目的**: コマ送りを消す。数値 OCR の**受け入れ検査**になる部分を先に作る。

- フレーム抽出（左ROI の差分が大きい所だけ）／**native フレームのまま扱う**
  （⚠ **CFR 変換はしない**＝P1 結果。再エンコードが重複除去と検算を汚す。実測ギャップは最大 50ms）
- ★**canvas 正規化**（P1 新発見②）: ゲーム canvas の外枠を検出しアフィン変換で基準座標へ。
  **固定クロップは不可**（スクロール位置・ウィンドウサイズ・ブラウザズームで canvas が動く）。
  ⚠ **これが全 ROI 由来の検算（§5 の 1/3/4/5）の前提条件**＝最初に作る
- ROI クロップ（上部HP・右CD/ゲージ・左ダメージ）。⚠ HPバー長は**区切りドット5個をマスク**してから測る
- **重複除去**（ポップアップは静止＝「同一位置・同一値の連続フレーム」を1イベントに畳む）
- **HP% 抽出 → 総和チェック**（§5-1）

**出口条件**（★**2026-08-15 にユーザー承認で改訂**）: **HP% が自動で取れること**と、
canvas 正規化・モード判定・ROI が**実フレームで成立していること**。
⚠ **AI をここで入れない**（順序を逆にすると受け入れ検査が無くなる）。

> **★改訂の理由（旧: 「人が見るフレーム数が1桁減る」）**
>
> **実測により、フレーム削減は P2 では達成できないことが確定した**（下記 v0.14.0 実走）。
> そして P2-4 の定義そのもの「**同一位置・同一値**の連続フレームを1イベントに畳む」の
> ★**「同一値」は値を読むこと＝OCR を要求している**。∴ **イベント単位の畳み込みは P3 の後段に属する**。
>
> ∴ 削減の要求は **P3-2** へ移し（「**OCR 後のイベント数**が1桁減る」）、P2 は上記2点で通過とする。
> ⚠ **要求を下げたのではない**＝**置き場所が間違っていた**（同じ削減を、値が読める段でやる）。
>
> ⭐ **副次的に P2-2（フレーム選別）の長期棚上げが決着した＝「不要」**。
> 削減の担い手が P3 の後段へ移ったため、**OCR 前にフレームを間引く動機そのものが消えた**
> （実運用は放置実行＝所要時間は二の次・ユーザー確定）。
> ⚠ v2（色による存在検出）は**採用でも棄却でもなく「用途が消滅」**として閉じる。
> 不利な証拠（同一窓3走で Fisher 1.92〜1.96・最良キーが走ごとに入れ替わる）は台帳に残す。

#### P2 の進捗

| # | 中身 | 状態 |
|---|---|---|
| **P2-1** | **canvas 正規化**（`src/transcribe/canvas_detect.js`）＋ **T1 ページの足場**（`transcribe/index.html`） | ✅ **完了（2026-08-14）＝実フレームで検証済** |
| **P2-1b** | ★**右パネルのモード判定**（`src/transcribe/panel_mode.js`）＝全 ROI 解釈の関門（⑮） | ✅ **実装（2026-08-14）**。⏳ 実フレームで `listThreshold` を較正 |
| **P2-2** | フレーム抽出 | ❎ **不要として close（2026-08-15・ユーザー承認）**＝削減の担い手が P3-2 へ移り、**OCR 前に間引く動機が消えた**。⚠ v1（変化検出）は失敗・v2（色）は同一窓3走で Fisher 1.92〜1.96＝**採用でも棄却でもなく用途消滅**。実装は残置（分布は診断に出続ける） |
| **P2-3** | ROI クロップ（HPバーは区切りドットをマスク） | ✅ **ROI 採寸 完了（2026-08-14）**＝`src/transcribe/rois.js`（5枠・provenance 付き） |
| **P2-4** | 重複除去 | ➡️ **P3-2 へ移設（2026-08-15・ユーザー承認）**。実測で**等間隔の間引きでは削減できない**と確定（L≦2＝stride 1）。`src/transcribe/dedup.js` の**測定部分は P2 に残す**（`lagProfile`＝発見⑧の実測値・`eventContrast`）。**完全重複の除去（情報損失ゼロ）はそのまま有効** |
| **P2-5** | HP% 抽出 → 総和チェック（§5-1） | ✅ **実走で成立（v0.12.0）**＝単調性違反 118→**5（0.16%）**。原因2種を実プロファイルで特定し、右端を階段フィットへ／ROI 汚染は「読めない」へ。⏳ 残5件は**遮蔽が空区間検査を通る**型（優先度低） |
| **P2-5b** | ★**CT（チャージターン）ドット抽出** | 🔶 **検出器の穴を3つ塞いだ（2026-08-18・v0.19.0）**＝`src/transcribe/charge_dots.js`。**幾何は走全体から決める**（★塗り境界は動きドットは動かない）。★**実走が探索の誤りを都合4つ示した**＝①周期 21.3px は探索下限 45px の外（v0.16.0 で修正） ②構造はバー幅の 16% にしかなく全域では希釈（同） ③★★**帯探索の窓が固定幅 0.25W ＝探せる周期が 43px で頭打ち**（v0.19.0） ④**平坦な帯を標準化して score 0.995 の偽構造を作る**（同）。⏳ **CT の居場所は未確定**／点灯の読みは未較正 |
| **P2-1b′** | モード遷移の**除振** | ✅ **実装（2026-08-15・v0.15.0）**＝最小滞在 0.25秒。★閾値の根拠は**人の操作の下限**（実測の谷: 偽の往復 ≦160ms / 本物の滞在 1.9〜7.2秒）。生の遷移列も併記 |

**★★P2-4 の設計判断（2026-08-15・v0.13.0）── 問いを変えた**

P2-2 は「**変わったか**」を問うて失敗した（採用率 61.8%）。ゲーム画面は背景・立ち絵・光が常時動くので
**「変わっていないフレーム」が存在しない**。閾値を p90 へ上げれば 10% になるが**本物を捨てる**ので採らない。

∴ P2-4 では問いを変えた: **「取りこぼさないと保証できる最大の間引きはどれか」**。

⭐ **等間隔の間引きには証明がある**＝s フレームおきに採ると、任意の連続 s フレームの窓に採用フレームが
ちょうど1枚入る。∴ **s フレーム以上表示され続けるものは必ず1回は捕まる**。
賢い検出器はこの保証を持たない（P2-2 が実際に取りこぼした）。
**検出が信用できない場面では、鈍いが証明のある方法を採る。**

⚠⚠ **この保証は `s ≦ ポップアップの寿命 L` のときだけ成り立つ**。そして **L は未測定**＝**発見⑧**
（「寿命は想定より短い可能性」＝連続フレームなら入れ替わりが **33ms 級**）。
**L が 1〜2 フレームなら間引きは一切できない**＝出口条件に**間引きでは到達できない**ことになる。

∴ **測定を先に出し、間引きは測定が許した分しかしない**という形にした:

| 部品 | 役割 |
|---|---|
| `LagProfile.lags` | **ラグ k フレーム離れた2枚の距離** d(k) を全フレームで集める。★**膝の自動抽出はしない**（それ自体が新しいヒューリスティック）＝曲線を生データで返し、人／Claude が読んで決める |
| `stateRuns` | ★**状態が続く長さの分布＝L の直接の候補**。隣接距離が cut を超えたフレームを遷移とみなし、遷移間の長さを数える。**cut を1つに決め打ちせず p50/p75/p90 の3通りで測る**（`popup_probe` で確立した「格子で分布を測ってから決める」作法）。**p50 と p90 が近い cut ほど長さが揃っている＝そこが L を読むべき行** |
| `cellDeltas` | セル単位 \|Δ\| の分布＝**ノイズ床を分布に語らせる**（`zeroFraction`＝まったく動かない画素の割合） |
| `EventDeduper` | `stride` が **null（未較正）のうちは間引かない**。落とすのは**完全重複（距離ちょうど 0）だけ**＝同じフレームを二度デコードした場合のみ＝**情報を一切失わない** |
| `eventContrast` | **正解ラベル無しの健全性検査**＝ラグ1 の **p90/p50**。**1 に近い＝滑らかに動いているだけで「出現・消滅」が無い**＝捕まえる対象が信号に出ていない（`T1-DEDUP-003`）。合成で **出来事あり 6.2〜6.3 / 出来事なし 1.02〜1.03** と明確に割れる |

⚠⚠ **実装中に指標を1つ作って、自分で反証して捨てた**（記録として残す）:
初版の `persistenceRatio`（ラグ1 p50 ÷ ラグ20 p50）は、**ポップアップが1つも無い背景だけの列で 0.537**
＝「持続性あり」に見えた。**背景アニメの遅さとポップアップの寿命を区別できない**指標だった。
★**ラグ曲線の低い側（p10/p50）は背景の床であって、ポップアップの信号は上側（p90）にしか出ない。**
同じ罠に戻らないよう、**背景のみの列を「出来事なし」と判定できることを回帰に固定**した。

⚠ **`stateRuns` に自動判定を足すのも一度やって撤去した**。「cut を変えても run 長が安定なら L が実在する」
という検査を書いたが、**cut を下げれば遷移が増えて run が短くなるのは構造上あたりまえ**で、
真の L=10 の合成でも発火した（[1,3,9]）。**どの cut で読むかは出来事の発生率に依存し、走ごとに違う**
＝**人が読んで決める**（膝の自動抽出をしないのと同じ理由）。

★**`stride` を推測で埋めない**＝**「読めないときは読めないと言う」の間引き版**
（P2-5 で7回踏んだ「正常値はこうなるはず」の型に戻らないための構造）。
較正後は ROI や `listThreshold` と同じく **provenance 付きの定数として固定**する。

⭐ **回帰フィクスチャは実走の距離分布に合わせて作った**（`tools/t1_selftest.mjs` §11）＝
隣接フレーム距離の分位点を **実走の p50 6.72 / p90 40.34 / p99 112.63 に一致させた**合成列
（実測 p50 6.70 / p90 41.7 / p99 110）。∴ この合成は **P2-2 を実際に失敗させた動きを持つ**
（テストでも「変化検出は出口条件を満たせない」ことを固定してある）。

⚠ **テストを書いて主張が1つ修正された**: 「stride=L/2 なら全部捕まる」は**偽**で、正しくは
**「寿命が stride 以上あるものは必ず捕まる」**。走の末尾で切れた 3 フレームだけのポップアップで
実際に落ちた。**弱めたのではなく定理どおりに書き直した**（弱いほう＝stride>L で取りこぼすことは反証として固定）。

**★★v0.13.0 実走の結果（2026-08-15）── P2-5 は成立、寿命の測定器は作り直し**

**①P2-5（HPバー）は実走で成立した**:

| | v0.11.0（3走とも） | **v0.13.0** |
|---|---|---|
| 単調性違反 | **118 / 118 / 118** | **5**（3088フレーム中 **0.16%**） |
| 読めず捨てた | 108（flash のみ） | 423（flash 108 ＋ **noEmptyRegion 315**） |

★**v0.11.0 が3走とも寸分違わず 118 だった**ので、**5 への低下はノイズではなく実装の効果**と断言できる。
残る5件は**すべて「低く誤読した直後の復帰」**（`from` が 0.12〜0.50 で `to` が正しい）＝
**バーの一部を暗く覆う遮蔽が「バーの終わり」に見える**型。⚠ 遮蔽部は暗い＝空区間検査を通ってしまう。
`noEmptyRegion` 315件（9%）は**設計どおりの拒否**（HP は毎秒30回測るので 12% 捨てても実害なし）。

**②寿命 L の測定器は、3つ作って2つ捨てた**:

| 設計 | 結果 |
|---|---|
| ①ROI 全体のラグ距離の膝（`lags`） | ❌ **背景の動きと混ざる**。実走では k=1→2 で p50 +110% と**平坦部が無い**が、それは「持続しない」の証拠にならない |
| ②セル単位「次の jump までの長さ」 | ❌ **真の L が 10 / 6 / 3 のすべてで p50=3** を返した（合成で反証） |
| ③**セル単位「jump 直後に \|Δ\|=0 で凍る長さ」（長さ1は除く）** | ✅ **真値 10 / 6 / 3 を J によらず厳密に復元** |

★①は**撤去した `persistenceRatio` とまったく同じ構造の誤り**（現象を分離できていない指標で現象を語る）。
**3度目だったので、指標をいじるのをやめて測定器そのものを作り直した**。

★③が効く理由は計画の文言どおり「**ポップアップは静止**」＝
ポップアップに覆われたセルは**表示中まったく動かない**が、背景セルは跳ねても次のフレームには動く。
**長さ1を除く**のは調整つまみではなく**定義**（「凍っていない」ものは freeze ではない）で、
これにより背景の寄与が構造的に落ち、**J をどう選んでも同じ答え**になる。
⚠ 実走を模した汚染（**静止セル 38%＋途中で一度切替**）を入れても L=10/6/3 で壊れないことを確認済み。

⭐ **受入条件＝3つの J での一致**。**両方向を既知の正解で検証**した:
L が実在すれば3つの J で完全一致し、**実在しない場面（真 L=1・汚染あり）では 13/13/21 と食い違う**。
∴ 一致は「測れた」の証拠、不一致は「この走からは決まらない」の証拠になる（`T1-DEDUP-004`）。

⚠ **v0.13.0 の走には ③ が入っていない**（③は v0.14.0）＝**L はまだ未測定**。
v0.13.0 が出した `eventContrast 6.731`（＝離散的な出来事は実在する）と
`exactDuplicates 283`（8.1%＝**seek が同じフレームを二度返している**・VFR の 50ms 保持と整合）は有効。

⚠ **`T1-DETECT-005`（サンプリング被覆）の死角**: 採用 fps は**重複を含めて**数えている。
`3511 − 283 = 3228` 枚が一意＝**26.9fps（native 29.72 の 90.6%）**で、**警告閾値 90% のすれすれ**だった。
⚠ ただし `exactDuplicates` は `dmg` ROI の署名一致で数えており、**別フレームが偶然一致した分を含みうる**
（実測で 38% のセルが不動）＝**この数字で合否を切り替えるのは早い**。両方を出して判断材料にする。

**★★★v0.15.0 実走（2026-08-17）── 除振は成功、CT は「探す場所が違った」**

**①モード遷移の除振は予測どおりに効いた**:

| | 値 |
|---|---|
| 生の遷移 | 12 |
| 除振後 | **8** |
| 落とした | **4** ＝ `28.450→28.483`(33ms) と `110.033→110.066`(33ms) の**2往復ちょうど** |

★**事前に「これは人には不可能な速さだから偽」と名指ししていた2件が、そのまま落ちた**。
残った8件の滞在は **2.23 / 1.93 / 7.15 / 1.92 秒**＝すべて本物の操作の尺度。

**②CT ドットは「見つからない」で返った。そして集約プロファイルが理由を答えた**:

`meanProfile`（走全体で集約した中央帯・120bin）を読むと:

| bin（px） | 中身 |
|---|---|
| 64, 67, 71, 75, 79, 83（**x 341〜443px**） | ★**6つの山が ~4bin = 21.3px 間隔で等間隔に並ぶ** |
| 92〜107（x 493〜569px） | 値が 18.0→5.8 と減衰＝**HP バーの塗り境界の掃引**（HP 77.0%〜88.9% に**厳密に一致**） |
| 106〜119 | ほぼ平坦＝塗りが一度も届かない領域 |

⭐ **掃引区間が HP の実測値とぴったり一致した**ことが、プロファイルの読み方が正しい証拠になっている。

★**検出できなかった理由は2つとも「探し方」だった**（検出器の性能ではない）:
- **周期 21.3px は探索下限 45px の外**＝**最初から範囲に入っていなかった**。
- **構造がバー幅の約 16% にしか無い**＝全域の自己相関では希釈され、スコアが**負**（-0.047）になる。
  同じ列を **窓 x320〜480px に絞ると 0.477**（floor 0.35 超え）。

∴ ①下限を **1/40（16px）** へ実測に基づいて広げ ②**窓を切って探す**ようにした。
⚠ **窓には「周期が4回以上入ること」を要求する**（`panel_mode` の list 判定と同じ根拠）＝
これが無いと**窓に1周期しか入らない状態で無意味なスコアを最良に選ぶ**（合成で実際に踏んだ）。

⚠⚠ **その6つの山が CT ドットである確証はまだ無い**。位置はバーの **53〜69%**（右端ではない）で、
`hp_bar.js` が想定する `100%` 文字の位置とは違うが、**文字の字送りも 21px 級**でありうる。
★**これは観測（＝ユーザー）でしか決まらない**＝確認待ち。

**★★★v0.22.0 実走（2026-08-18d）── HP 完全復旧／★CT の「7枠」は撤回（山の数え方が誤りだった）**

**①HP は旧採寸の水準へ完全復旧**＝違反 **5**／読めた **3084**／`flash` **109**／`noEmptyRegion` **315**、
違反の時刻も v0.18.0 と一致。**`hpbar` を戻した判断は正しかった**。
★新設の `hpSkipSamples` が効いた＝`flash` フレームは **peak=0 / red=0 / 山ゼロ**＝
**ROI が本当に真っ暗**＝演出であり採寸の問題ではないと**推測なしで**確認できた。

**②⚠⚠ 「CT は 7枠・5点灯」は誤りだった（撤回）**。全120値:

| 領域 | 中身 |
|---|---|
| bin 12〜88 | ★**5つの山**（重心 17.40 / 33.65 / 49.98 / 66.38 / 82.59）＝**間隔 22.00px・CV 0.4%** |
| bin 91〜119 | **56〜63 の平坦**（幅 7）＝**山は1つも無い** |

★「6・7枠目」と読んだ 99/108/110/114/116 は、**平坦な背景の上の ±1 のノイズ**だった。

⭐⭐ **原因は digest がプロファイルを「上位10山」に畳んでいたこと**＝
**局所最大は、平坦なところでも必ず見つかる**。上位N個を取れば「山が N 個ある」ように必ず見える。
∴ **山は「局所最大か」ではなく「どれだけ高いか」で数える**＝`rawHumps()`
（**そのプロファイル自身の (最小+最大)/2 でしきり、連結した塊の重心**）。
実データで 5個・CV 0.45% を再現し、平坦領域では 0 個と答える。

⚠ **この誤りは、2026-08-17 の「見つけた ≠ 探していたものを見つけた」と同じ型**＝
**構造が有るように見える表現を作って、それを観測と取り違えた**。今回は畳み方が犯人だった。
★∴ **畳み方そのものが観測に影響する**＝要約の設計は測定器の設計の一部。

**③★新たな未決着＝`ct` ROI は左端で切れている疑い**:

| | 水準 |
|---|---|
| 山と山の谷（bin 26/42/58/75） | **67.0** |
| 左端（bin 0〜9） | **63.0**（谷とほぼ同じ）・**bin 0 は 78 で端に向かって上昇** |
| 右端（bin 100〜119） | **56.6**（谷より 6.4 低い＝**真の背景**） |

★**左右が非対称**＝**ROI の左外に山がある**（外挿位置 canvas x≈694／ROI 左端は 693）。
⚠ ただし **`modebar`（x 361〜706）と `ct`（x 693〜855）は 13px 重なる**＝
その構造は**6個目のピップかもしれないし、モードゲージの右端かもしれない**。
★**決着方法＝`modebar` にも同じ生プロファイル抽出をかける**（v0.23.0 で実装・走査コストはほぼ増えない）。
⭐ これは**モードゲージ自体を読む**という本来の目的（未モデル化メカニクス）とも一致する。

**④⚠ `sigmaProfile` は設計と逆に振る舞う**＝山の中心 **29.6〜30.4（最小）**／谷 36〜38／背景 39.9。
**ピップは最も安定した列**だった。∴「変化した列の localizer」**ではない**が、
**「動かない UI 要素の検出器」としては有効**＝意味を読み替えて使う（捨てない）。

**⑤点灯のエンコードは未確定**。5つの山は平均輝度で**ほぼ同じ（126〜133）**・120秒ほぼ不変。
⚠ ただし標本 t=56.4 / 124.9 で **1番目の山だけ 210/198 に跳ぶ**。
∴ v0.23.0 で **山ごとの明るさの分布**（p05〜p95・max・中央値+30 を超えるフレームの割合）を出す＝
**二峰かつ「明るい割合」が山ごとに階段状なら「明るい＝点灯」**が確定し、**それが CT の時系列そのもの**。

**★★★v0.20.0 実走（2026-08-18）── CT は「離散ピップ 7枠・5点灯」と読めた／HP は ROI 変更で退行**

**①CT は連続ゲージではなく離散ピップだった**（`ct` ROI 162×37px・生の輝度の平均）:

| bin | 18 | 34 | 50 | 66 | 83 | 99 | ~115 |
|---|---|---|---|---|---|---|---|
| 高さ | 133 | 127.9 | 126.3 | 127.3 | 127.5 | **58.3** | **56.8 / 56.9** |
| canvas x | 717 | 739 | 761 | 782 | 805 | 827 | ~849 |

★**明るい5つは間隔 21.94px（CV 2.7%）・高さのばらつき ±2.6%**＝**等間隔・等輝度の離散ピップ**。
（連続ゲージなら「境界が1つ」になるはずで、そうなっていない。）
★★**その格子を右へ延ばすと 6枠目 bin 99.3・7枠目 bin 115.5**＝**暗い山（~57）の位置と一致する**。
8枠目は bin 131.8＝**ROI の外**（ROI は 120bin）。
∴ 読みは **枠は 7・点灯 5・消灯 2**＝**CT 5/7**。暗い側は明るい側の **45%** の輝度。
⭐ **2026-08-14 の目視「丸ドット5個」と一致する**（人は点灯している方を数えた）。

⚠ **未確定（要・完全JSON）**: 暗い群には **bin 108/110 という格子外の山**もある。
digest は山を上位10個に畳むので、**`rawProfile` の 120 値全部**を見れば
「7枠＋ノイズ」か「もっと枠がある」かが確定する。**走査は不要**（保存済み JSON にある）。

**②CT は 120秒間ほぼ不変だった**。標本8本すべてで bin 18/34/50/66/83 が 126〜129。
⚠ ただし **t=56.5 と t=124.9 に、1枠目の位置だけ 210/198 の明るい二重山**が出る＝
**点灯の瞬間の演出**の可能性。∴ 「点灯/消灯のエンコード」は**中央帯の平均輝度では決まらない**かもしれない。

**③⚠ `sigmaProfile` は今のところ localizer として機能しない**。
σ の山は 3/9/26/42/75/92/96/99/103/106 と**ROI 全体にほぼ一様（39〜42）**で、
**ピップ位置（18/34/50/66/83）に立っていない**＝**ROI 全体を覆う事象（演出・ポップアップ）が
分散を支配している**。★設計意図（状態が変わった列だけが立つ）は成立していない。
⏳ 直すなら「flash フレームを除く」か「MAD など頑健な散布度」だが、**推測で入れない**。

**④★HP は ROI を変えただけで退行した（同一コード）**:

| | 旧 `hpbar` 640×54（y 121〜175） | 新 `hpbar` 650×29（y 117〜146） |
|---|---|---|
| `flash`（バーが見えない） | **109** | **2152**（20倍） |
| 読めたフレーム | 3085 | **1020** |
| 単調性違反 | **5** | **36** |

★**原因は `hp_bar.js` の帯の取り方**＝`stripTop [0.06,0.26]` / `stripBottom [0.74,0.94]` は
**ROI の上下端**を見るので、**ROI が「バーぴったり」だと帯がバーの縁（枠線・グラデ）に乗る**
（新 ROI の帯は上下とも**縁から 1.7〜7.5px**）。旧 ROI の上帯は y 124〜135＝**バーの内側**だった。
⭐ 旧 ROI の下帯（y 161〜172）は**バーの外**だが**無害**＝階段フィットは**絶対水準に依存しない**
（既記載の「完全に塗られた列でも占有率 0.375〜0.86」は、**行の約半分がバー外**であることと整合する）。

∴ **`hpbar` は 2026-08-14 の値へ戻した**。バーそのものの実寸は `HP_BAR_TIGHT` として保存
（帯の割合を内側へ寄せる作業の一次情報。**フィクスチャ再検証を伴うので推測で動かさない**）。
⚠ 旧 ROI が CT の行を含むことは**もう問題にならない**（CT は `ct` で別に読む）。

**⑤★診断の欠陥を1つ塞いだ**＝`flash` が 20倍になったのに**捨てた側の生データが無く**、
原因を**推測でしか説明できなかった**。∴ **`hpSkipSamples`**（捨てた原因ごとに最初の3件の
`colProfile` / `peak` / `redFraction` / `bands`）を追加。
⭐ **「読めなかった」は最も情報量の多い出来事なのに、それを捨てていた**のが欠陥だった。

**⑥再現性**: モード遷移は **3走連続で同じ8件**（10.58 / 12.82 / 14.75 / 16.68 / 62.48 / 69.63 / 81.77 / 83.40）。
⚠ `T1-ROI-009`（CT が毎秒 1.735 回変化）は **|高域通過| 経路の `filledPrefix`** の値＝
**当てにしない**と digest に明記済みの数字。分布は {4: 3171} で大半が 4。

**★★★2026-08-18 採寸受領 ── 「バーの目盛り」は CT だった（2026-08-17 の判定を撤回）**

ユーザーが4枠を採寸（`M3-1.mp4` t=23・canvas 1684×1129）。画素へ落とすと:

| ROI | canvas 画素 | |
|---|---|---|
| `hpbar`（採り直し） | x 352〜1002 / y **117〜146** | **650×29** |
| `modebar` | x 361〜706 / y 135〜177 | 345×42 |
| **`ct`** | **x 693〜855** / y 138〜175 | **162×37** |
| `debuff` | x 365〜995 / y 164〜216 | 630×52 |
| （旧 `hpbar`） | x 359〜999 / y **121〜175** | 640×**54** |

★★**2026-08-17 に「バーの目盛り・模様」と判定された等間隔6山は canvas x 700〜802**＝
**採寸された `ct`（x 693〜855）の中に完全に入る**。**あれは CT だった。**

⚠ **誤りの本体は回答ではなく質問だった**。旧 `hpbar` は **54px 高**で、実際のバー（**29px**）の
**下にある CT の行まで含んでいた**。`charge_dots.js` が見ていた中央帯（0.30〜0.70）は
**y 137〜159＝バーの下の CT 行そのもの**。∴ 「**HPバーの中の**構造ですが CT ですか」と尋ねれば、
「バーの模様でしょう」という回答になるのが自然だった。**検出器は最初から正しい場所を見ていた。**

⭐⭐ **この Phase で最も高くついた教訓＝1つの回答から恒久的な抑止規則を作らない**。
2026-08-17 は「誤報告は見つからないより悪い」（正しい）への対策として、
その位置を `knownDecorX: [0.50, 0.72]` に**ハードコードして抑止**した。
これは**静かで恒久的なブロッカー**になり、しかも新しい `ct` ROI の中では
**中央付近＝ピップそのものを拒否する**規則になっていた。∴ **2026-08-18 に撤回**（既定 `null`）。
機構だけは残す（本当に確認できた装飾は呼び出し側が明示的に渡せる）が、**既定で何かを抑止することはしない**。
★位置の問題は**抑止規則ではなく採寸**で解く。

**★★合成での事前確認（preflight）が、本番前に読み取り経路の誤りを捕まえた**

採寸された実寸（**162×37px**）で合成を通したところ:

| 経路 | 結果 |
|---|---|
| `found` / `bestPeriod`（|高域通過|＋自己相関） | ❌ ドット5・6個で **found=false**（score 0.15〜0.27 < floor 0.35）／7個では **周期を真値の約半分**（縁）に取り、個数も 5 と誤る |
| `peakRun`（等間隔の山の列） | 🔶 間隔はおおむね正しいが**個数を外す**（縁を拾って 11.5px を掴むことがある） |
| ★**`rawProfile`（生の輝度の平均）** | ✅ **6ケース全部で正解**＝5個→山5つ（21.6px）／6個→山6つ（21.1px）。真値 21.3px |
| ★**`sigmaProfile`（列ごとの時間σ）** | ✅ **走の間に状態が変わった列だけ**が立つ＝**点灯のエンコードを仮定しない位置特定** |

★**理由は構造的**＝`meanProfile` は **|高域通過| の平均**なので、**山がドットの縁に立つ**。
広いバーの中から「動かない等間隔構造」を探すには正しい道具だったが、
**ROI が要素そのものになった今は、生の輝度をそのまま読むのが正しい**。

⚠⚠ **ここでアルゴリズムの作り込みを止めた**＝合成の見た目（12px の四角・仮の輝度）は**私の推測**で、
それに対して調整を続けると**自分の推測に過適合する**。∴ 判定を確定させず、
**`rawProfile` / `sigmaProfile` / 時刻を散らした生プロファイル標本**を出力に足して、
**次の走査1回が「離散ピップか連続ゲージか・何個か・点灯はどちら向きか」を答えられる**ようにした。
digest には**どの数字を信じるか**（found/bestPeriod は当てにしない）を毎回書く。

⚠ **走査後に必ず見る**: 新 `hpbar` の**下帯（`stripBottom` 0.74〜0.94＝y 138.5〜144.3）が
`ct` の縦範囲（138〜175）に入る**。実害の有無は**単調性違反の数**が答える（旧採寸で 5 件）。

**✅✅✅ P2 出口確認（2026-08-19）── 通過**

出口条件は2つ（2026-08-15 にユーザー承認で改訂）。実走の証拠と突き合わせる。

**① HP% が自動で取れること ── ✅ 満たす**

| 指標 | 実測（v0.27.0 走） | 判定 |
|---|---|---|
| 単調性違反 | **5 / 読めた 3090 ＝ 0.16%** | ✅ v0.11.0 の 118 から 96% 減 |
| 違反の時刻 | 12.30 / 51.60 / 69.63 / 72.52 / 118.25 | ✅ **6走連続で同一**＝再現する |
| first → last | 0.8891 → 0.7703 | ✅ 全走一致 |
| 読めず `flash` 110 | **peak=0 / red=0 / 山ゼロ**＝ROI が本当に真っ暗 | ✅ 演出であり抽出の失敗ではない（`hpSkipSamples` で確認） |
| 読めず `noEmptyRegion` 315 | 満タン・遮蔽時の**設計どおりの拒否**（数値を捏造しない） | ✅ |

**② canvas 正規化・モード判定・ROI が実フレームで成立 ── ✅ 満たす**

| 対象 | 実測 | 判定 |
|---|---|---|
| canvas 検出 | box = `{290,198,1684,1129}` が**全走で同一** | ✅ |
| ROI | **9枠すべて採寸済み**（hp / hpbar / modebar / ct / debuff / dmg / abil / turn / gauge） | ✅ |
| モード判定＋除振 | 除振後の遷移が **6走連続で同一の8件**（10.58 / 12.82 / 14.75 / 16.68 / 62.48 / 69.63 / 81.7 / 83.40） | ✅ 生は 12〜20 とぶれるが**出力は再現する** |

**★出口条件を超えて得たもの**（P2 の当初計画に無かった観測経路）:

| 観測値 | 状態 | 効き先 |
|---|---|---|
| **CT（チャージターン）** | ✅ **5ピップ・点灯が実機の正解と一致**（1個目のみ・3回） | **C45**（敵の行動モデル） |
| **モードゲージ** | ✅ **塗り率が読める**（満102 / 空−1・左から溜まる） | **一次情報にある未モデル化メカニクス**（`ryomen_sukuna` 等の「モードリミット」） |

**⚠ P2 を通過させるが、持ち越す既知の穴**（いずれも出口条件ではない）:

| 項目 | 内容 | 決着させる段 |
|---|---|---|
| 決定性の残差 | 走ごとに 3508 / 3510 / 3515 フレーム＝**0.2%** | **P5 の受入**（§9.1-3） |
| `T1-DETECT-005` の死角 | 採用 fps を完全重複込みで数えている | P5 |
| HP 違反の残り5件 | 遮蔽が空区間検査を通る型（0.16%） | 優先度低 |
| CT の偽区間 | `bg` ≥ 20（演出・遷移）と単一フレーム | **読む側が `bg`/`coin` で判別できる**＝運用で吸収 |

∴ **P2 通過。次は P3（OCR ＋ 検算）。**

**★★★2026-08-18 の方針転換（ユーザー提案）── CT の「探索」をやめて「採寸」にする**

ユーザー提案＝**`hp` 周辺を4要素に分けて、`hp`/`gauge`/`dmg` と同じように人が採寸する**:
**①HPバー ②モードゲージバー ③CTゲージ ④バフ・デバフアイコン**。

★**この提案は、2セッション続いた失敗の原因そのものを外している**。
「どの行に CT があるか」を**幾何で当てにいく**機構（v0.15.0〜0.19.0 の帯探索）は、
**偽陽性（2026-08-17＝バーの目盛りを CT と誤認）と偽陰性（2026-08-18＝探索上限 43px の外）を
1回ずつ出した**。⚠ **1分の採寸で確定することを機械に当てさせようとしていた**＝
§1.1 の憲法（**観測と判断はユーザー／転記・検算・整形はツール**）から外れていた。

∴ 採寸後は、幾何の機構の仕事が**「探す」から「読む」へ変わる**（個数を数える／塗り率を測る）。
v0.19.0 で直した3点は読み取り側で引き続き効く（**捨てるのは探索の役割だけ**）。

**★★②モードゲージは装飾ではない ── 一次情報にある未モデル化メカニクスだった**（2026-08-18 に確認）

| | |
|---|---|
| 一次情報 | `gamedata/md/敵/ryomen_sukuna.md`「**モードリミット**＝我々の**敵モードゲージ操作**を減少」／`variant_chimera.md`・`variant_chimera_chi.md` にも同記述 |
| シム実装 | **ゼロ**（`src/`・`gamedata/js/` に `modeGauge` 系の grep ヒット無し） |
| 立場 | ★**CT とまったく同じ**＝**C45（敵の行動モデル）の観測経路**。録画から読める未モデル化の状態変数が**2つ**あることになる |

⚠ **命名の衝突に注意**: `panel_mode.js` の `mode`（右パネルの list/detail）とは別物。ROI 名は `modebar`。

**採寸の要件**（既存コードの制約から出るもの＝推測ではない）:
- ★**要素の周囲に数 px の余白を持たせる**。ドットの判定は「両隣の谷が ROI 内にあること」を要求するので、
  **ROI の端ぎりぎりにあるドットは落ちる**（`charge_dots.js` に既記載）。
- ★**中間状態の時刻で採寸する**（満タン/空だと端が見えない）。HP バーが「**満タンは読めないと言う**」のと同型。
- ⚠ **1点採寸（E9）**＝別の敵・別のウィンドウサイズでは未再測。採寸時刻と canvas box を必ず残す
  （「ROI をコピー」の JSON が provenance ごと出す）。

⏳ 受領後に決めること＝**③が離散ドットか連続ゲージか**。
2026-08-14 の記録は「**バー上の丸ドット5個**」、2026-08-18 の呼称は「**CT ゲージ**」で食い違う。
★**仮定して読み取りを書かない**＝`ct` ROI の集約プロファイルを見て決める
（山が離散的に並ぶか／境界が1つだけ動くか）。

**★★★v0.18.0 実走（2026-08-18）── 「見つからない」は検出器が見られない範囲の話だった**

走査は正常に完了した（119.9秒 / 3510フレーム / 実効 29.27fps / FATAL 0 ERROR 0）。
帯探索の結果は**事前に決めた読み方に従えば「CT は `hp` ROI の外＝ROI 採寸のやり直し」**だった:

| 帯 | 最良周期 | score | |
|---|---|---|---|
| [0,0.125] / [0.125,0.25] | 41 / 43 | 0.158 / 0.143 | |
| [0.25,0.375] | 38 | 0.220 | |
| [0.375,0.5] / [0.5,0.625] / [0.625,0.75] | **22 / 22 / 22** | 0.541 / **0.643** / 0.124 | decor（バーの目盛り） |
| [0.75,0.875] / [0.875,1] | 43 / 43 | 0.056 / 0.122 | |

⚠⚠ **その読み方を適用しなかった。測定器の可視範囲を先に確かめたら、穴が2つあった**:

**①★★探せる周期の上限が 43px に潰れていた**（v0.17.0 で帯探索を足したときに入った）。
帯探索は**固定幅 `0.25 × W` の窓**の中で自己相関を測る。そこへ「窓に `minRepeats`(=4) 周期入ること」
という**測定の成立条件**が掛かるので、上限は **W/16**＝`hp` ROI 687px なら **43px** になる。
★**指紋は結果そのものに出ていた**＝**8帯中5帯が上限側の 43/43/43/41** を報告している
（最良値が探索範囲の端に張り付くのは、**範囲の外に真の構造がある**ときの典型）。
⚠ そして**一次情報の観測は「バー上の丸ドット5個」**（2026-08-14 のクロップ受領・⑭）＝
640px のバーに5個なら間隔 **≈107px** で、**最初から探索範囲の外**だった。
∴ **この走の「見つからない」は、CT が無いことの証拠にはなっていない。**

**②平坦な帯が偽の構造を作る**。何も写っていない帯は高域通過後の残差が 1e-14 級の浮動小数のゴミになり、
それを「分散1」へ標準化すると **score 0.995 が最良候補として浮上する**（合成で実際に、空の帯が
本物のドット列（0.50）を押しのけて1位になった）。実走では帯に中身があったため顕在化しなかった＝
**次に ROI を広げたら踏んでいた**。

**★直し方は「閾値をいじる」ではなく「窓の決め方を物理に合わせる」**（v0.19.0）:
窓幅 = `max(minRepeats × period, 0.25W)` ＝**試している周期ごとに窓を広げる**。
∴「4周期入ること」という成立条件は保ったまま、**上限だけが外れる**（687px なら 17〜171px）。
あわせて ③**周期は自己相関で「見つけ」、櫛フィルタで「測り直す」**（整数ラグは繰り返しが4〜5回だと
数%外し、その誤差が格子を伸ばすうちに累積して**5個を3個と数えていた**）／
④**等間隔の山の列を自己相関に依らず直接拾う**（`evenlySpacedRun`）＝**少数のドットは自己相関には出ない**。

| | v0.18.0 | **v0.19.0** |
|---|---|---|
| 帯探索が間隔 106px のドット列を報告 | ❌（43/17 しか返せない） | ✅ **P=106** |
| `solveGeometry`（687px ROI・5ドット） | ❌ found=false（0.3253 < floor 0.35） | ✅ found=true・**dotCount 5** |
| 空の帯 | ⚠ score **0.995**（偽） | ✅ score 0 |

⭐⭐ **この回の教訓＝「見つからない」を結論にする前に、測定器が何を見られるのかを確かめる**。
2026-08-17 の教訓（**見つけた ≠ 探していたものを見つけた**）の裏返しで、
**範囲外の「無し」は情報ではない**。∴ **`searchRange`（探索できた周期の範囲）を digest に常時出す**ようにした
＝測定器の可視範囲を、測定結果と同じ画面に置く。

⚠⚠ **未解決の食い違いが1つある（ユーザー確認が要る）**:
**2026-08-14**「`hp` クロップに**バー上の丸ドット5個**が写る＝それが CT」（⑭）と
**2026-08-17**「バー幅 53〜69% の等間隔6山は**目盛り・模様**」が**両立しない可能性がある**。
①同一の物を指していて 2026-08-14 の解釈が誤りだった／②別物で、5個のドットは**もっと広い間隔で**
バー上に並んでいる（＝v0.18.0 では原理的に見えなかった）――のどちらか。
★**これは観測でしか決まらない**＝`hp` ROI クロップ PNG（§10.3 の経路）で確定させる。

**★★★v0.14.0 実走 ── 寿命 L が測れた。そして「間引きでは出口条件に届かない」が確定した**

`lifetimeFrames = 2`（**3つの J すべてで p50=2 が一致**＝受入条件を満たしている）。

⚠ ただし**交絡が1つ見つかり、測定を修正した**: **seek が同じフレームを二度返す**（実測 261/3510＝**7.4%**）と、
**重複は全セルの \|Δ\| を 0 にする＝直前に跳ねたセル全部に「長さ2の freeze」を作る**。
見積りでは実測 `frozenCount` の **45〜61%** がこれで説明でき、しかも **p50 がちょうど 2** だった。
∴ **重複フレームを除いた列で測る**よう直した（v0.14.0 の測定値は未修正版＝**真の L は 2 以下・おそらく 1**）。

★**どちらに転んでも結論は同じ**:

| L | `stride = floor(L/2)` | 削減率 | 出口条件（10×） |
|---|---|---|---|
| 2 | **1** | **1.0×＝間引きゼロ** | ❌ |
| 2（安全率1） | 2 | 2.0× | ❌ |

∴ **等間隔の間引きでは「人が見るフレーム数が1桁減る」に到達できない**。
`frozenFraction` も **12〜16%**＝**跳ねた変化の 84〜88% は次のフレームでまた変わっている**＝画面は常時動いている。

**⚠⚠ もう1つの交絡＝「凍っている」と「読める」は別物 ── ユーザー回答で決着（2026-08-15）**

本測定器は**「画素が止まっている長さ」**を測る。合成での検証は**完全に静止するポップアップ**で行ったので、
**測定器の正しさは検証できたが「ポップアップは静止する」という前提は検証していなかった**。

★**ユーザー確定情報**: 「**わずかにフェードイン・消失があるが、位置は基本的に不動**」。

∴ 解釈が確定した:
- ✅ **「同一位置」は使える**＝位置アンカーでの照合が成立する（畳み込みの設計に直結）。
- ⚠⚠ **freeze 長 2 は「数字が動いている」ことを意味しない**。位置が不動なのに画素が凍らないのは、
  **数字の上に重なる演出**（P1 発見⑫「**キラキラが数字に重なる**」）がセルを動かし続けるため。
  ★**読める長さは 2 フレームより長い。ただし画素の凍結では測れない。**

★**どちらにせよ結論は同じ**＝**「同じポップアップか」は値を読まないと判定できない**。
そして P2-4 の定義自体が「**同一位置・同一値**」であり、★**「同一値」は OCR を要求している**。
∴ **イベント単位の畳み込みは P3 の後段（P3-2）に属する**。**2026-08-15 にユーザー承認で移設した。**

**P2-2 の設計判断**:
- **ROI ごとに独立して差分を見る**（全画面差分では**背景アニメと立ち絵の揺れ**に埋もれる）。
- **署名は縮小輝度グリッド**（24×24）＝JPEG ノイズや微小な揺れに鈍く、構造変化に敏感。
- ★**閾値は実走でしか決まらない**ので、判定だけでなく**距離の分位点を必ず診断 JSON に載せる**
  （Claude は画面を見られない＝分布が唯一の較正材料）。
- ⚠ **出口条件（採用率 10% 以下）は合成フィクスチャでは検証できない**（イベント密度が恣意的になる）。
  セルフテストが固定するのは**機構**（静止中は捨てる／変化点を取りこぼさない／削減が起きる／分布を返す）だけで、
  **出口条件の判定は実走の `summary.meetsExitCriterion` で行う**。
- ⭐ 走査中に**モード遷移（list⇄detail）の回数と時刻**も同時に採る＝**押下シグナルの実データ**（⑮）。

**★★同一条件3走の比較（2026-08-15・v0.11.0・`M3-1.mp4` 120秒）── 再現性の実測**

同じ動画・同じ設定で3回走らせた結果が揃ったので、**何が再現し、何が再現しないか**を確定させた。

| 項目 | 走A(01:28) | 走B(03:54) | 走C(07:40) | 判定 |
|---|---|---|---|---|
| 総フレーム | 3510 | 3509 | 3506 | **0.11% ぶれる**（既知の残差・§9.1-3 は P5 で確定） |
| HP `violations` | **118** | **118** | **118** | ✅ **完全一致** |
| HP first→last | 0.8906→0.7719 | 同 | 同 | ✅ **完全一致** |
| HP `skippedFrames` | 108 | 108 | 109 | ✅ ほぼ一致 |
| 距離 p50 / p90 | 6.72 / 40.34 | 6.02 / 39.60 | 6.06 / 39.30 | p90 は安定（±1.3%）・**p50 は 11% ぶれる** |
| `popupProbe` best | y160g80 (1.964) | y160g80 (1.921) | **y190g80** (1.940) | ⚠ **最良キーが走ごとに入れ替わる** |
| `panelTransitions` | **18** | **14** | **12** | ⚠⚠ **50% ぶれる** |

★**①HP 抽出は極めて再現性が高い**（フレーム数が違っても違反数・first/last が完全一致）。
∴ **v0.12.0 の効果は次の走で明確に判定できる**（118 が変われば、それは実装変更の効果である）。

⚠⚠**②モード遷移が再現しない**（18 / 14 / 12）。フレーム数のぶれ 0.11% では説明がつかない。
中身を突き合わせると構造が分かる:

- **走Cの12件は3走すべてに現れる**＝**安定な芯**（10.5834 / 12.8168 / 14.7501 / 16.6834 / 28.45 / 28.4833 /
  62.4832 / 69.6332 / 81.4831 / 83.3998 / 110.0331 / 110.0664）。
- 余分な分は**必ず対で現れ、30〜160ms で元の状態へ戻る**（走A: 12.23〜12.78 に2対＋81.72 に1対／走B: 63.77 に1対）。

★**この揺れは閾値ぎりぎりが原因ではない**＝**3走とも `T1-ROI-003` が1件も出ていない**
（判定はどのフレームでも確信的だった）。∴ **その1〜2フレームは本当に反対のモードに見えている**
＝**遷移アニメーションの途中フレーム**と考えるのが自然で、対処は**閾値の調整ではなく除振（debounce）**。
そして**1〜2フレームの excursion は、そのフレームが標本に入るかどうかで出たり消えたりする**
＝フレーム数の 0.11% のぶれが遷移数の 50% のぶれに増幅されている。

⚠ **⑮に効く**＝`list→detail` は「**どのキャラを操作したか**」の signal として使う予定なので、
**幻の遷移は幻の操作イベントになる**。P3 へ進む前に除振が要る（TODO 起票済み）。

⚠**③色による存在検出（P2-2 v2）は、この窓では否定的**。**全フレーム被覆の3走すべてで Fisher 1.92〜1.96**
（採用基準 2.5 に届かない）で、しかも**最良キーが走ごとに入れ替わる**＝特徴量として不安定。
以前の「窓によっては 3.02」は**別の窓**の値であり、**同一窓を3回測ると 1.92〜1.96 に収束する**。

**★P2-2 v1 の失敗と作り直し（2026-08-14・`M3-1.mp4` 120秒＝1,275フレーム）**

| 実測 | 値 |
|---|---|
| 採用率 | **88.1%（1123/1275）**＝出口条件（10%以下）を大きく外した |
| 距離の分位点 | min 0 / **p50 16.9** / p90 67.6 / p99 143.8 / max 237 |

**原因**: **中央値 16.9 が閾値 6 の 2.8倍**＝**`dmg` ROI は静止しない**。
598×798 の中に**敵スプライト・床グリッド・光エフェクト**が入っており、
「背景に埋もれないよう ROI ごとに差分を見る」という v1 の対策では足りなかった。

⚠ **閾値を p90（67.6）へ上げれば採用率は 10% になる**が、**採らない**。
それは**この1走に当てた数字**で、低コントラストの本物のポップアップも一緒に落とす＝**症状に合わせただけの修正**。

★**根本原因は問いの立て方だった**:
欲しいのは「**前フレームから変わったか**」ではなく「**このフレームに数字が出ているか**」。
前者は背景アニメが常に真にしてしまう。後者は背景が動いていても影響を受けない。
∴ **変化検出（temporal）→ 存在検出（appearance）へ切り替える**（`src/transcribe/popup_probe.js`）。

**v2 の進め方（閾値を先に決めない）**: ダメージ数字は**金グラデ＋縁取り**＝輝度が高く青が相対的に低い。
候補しきい値の**格子（輝度3×金らしさ3）で分布を測り**、**二峰に割れる組み合わせを選んでから**閾値を決める。

⚠ **品質指標を1度間違えた**: 当初は大津法の分離度（クラス間分散/全分散）で二峰性を測ろうとしたが、
**分割点を最大化して選ぶ量なので単峰でも高く出る**（実測: 一様 **0.750** / 正規 **0.669**）＝
閾値 0.5 の警告が**原理的に鳴らない**。→ **Fisher 型の分離 `|m1−m0|/(sd0+sd1)`** に差し替えた
（実測の目安: 一様 1.73 / 正規 1.32 / 分離した2峰 10+）。セルフテストに**一様分布の回帰ガード**を置いた。

**★★P2-2 v2（存在検出）も失敗 ＋ 走査そのものが壊れていた（2026-08-14・2回目の実走）**

**(a) 金色画素の割合は使えない**: しきい値格子 **9通りすべてで単峰**（Fisher 分離 1.57〜1.95）。
単峰の目安（一様 1.73 / 正規 1.32）と同水準で、**二峰に割れる組み合わせが1つも無い**。
原因は明快で、**バースト演出そのものが金色**（強い光・きらめき）＝金色画素は数字を切り出さない。
∴ **色ベースの存在検出は棄却**。

**(b) ⚠⚠ もっと重大 ── 走査が全フレームの 1/3 しか見ておらず、しかも非決定的だった**

| 走査 | フレーム数 | 実効 fps | 動画(29.72fps)に対する被覆 |
|---|---|---|---|
| 1回目 | 1275 / 120.05s | **10.62** | 35.7% |
| 2回目 | 1174 / 120.02s | **9.78** | 32.9% |

**同じ入力で 7.9% 違う**＝**受入基準 §9.1-3「同じ録画を2回処理したら同じ結果」に違反**していた。
原因は再生ではなく **1フレームあたりの処理が 33ms に間に合っていない**こと
（処理を一切しない間隔実測ループは 29.3fps 取れていた＝切り分け済み）。

⚠ **∴ (a) の分布も v1 の採用率も、1/3 に間引かれた標本の上での値だった**。
結論（dmg ROI は静止しない／色では割れない）は覆らないが、**数値は再取得が要る**。

★**対策（v0.8.0）**: **再生ベース → seek ベースの決定的走査**へ作り直した。
- 1フレームずつ seek して踏む。歩幅は公称間隔の少し上から始め、**進まなければ広げる**（VFR のスリップ対策）
- **ROI だけを小さい canvas へ切り出す**（フル 2288×1440 の `getImageData` は 3.3M 画素＝重い）
- **実効サンプリングが動画 fps の 90% を切ったら `T1-DETECT-005` を ERROR**＝二度と黙って通さない
- ⚠ **実時間より遅くなる**が、intake は無人実行なので**速度より再現性を採る**（§9.1-3）

**★P2-5 の設計（3度の誤りを経た形）── `src/transcribe/hp_bar.js`**

合成フィクスチャで **最大誤差 0.07 百分点（≈1画素）**＝**画面の整数%表示より1桁細かい**
（`TRANSCRIPTION_DESIGN.md` §3.4 対処2「バー長のサブピクセル推定」が成立する見込み）。

到達までに3回間違えており、いずれも**同じ型の誤り**だったので記録する:

| # | 誤り | なぜ壊れたか |
|---|---|---|
| 1 | 敵アイコンを色でバーと分離しようとした | **アイコンも赤系**＝色では分離不能。HP 20% を 100% と誤読した |
| 2 | バーの全長を「赤が終わる位置」から求めた | ★**分母を分子と同じ信号から作った**＝HP が減ると分母も縮み、**塗り率が常に 1.000** |
| 3 | 遮蔽（CT ドット・`100%` 文字）を「穴の許容」で埋めた | 累積許容が塗り境界を越え、**系統的に過大**（最大 +0.19） |

★**共通の教訓＝「測るものの基準系を、測る対象そのものから作らない」**。最終形は:
- **幾何は人が採寸した ROI から与える**（`ROIS.hpbar` ＝ バーそのもの）＝1 は構造的に消える
- **分母は ROI 幅**（塗りとは独立）＝2 が消える
- **走査は バーの上下の細い帯**＝**CT ドットも `100%` 文字も縦中央にある**ので
  遮蔽が構造的に起きず、**穴を埋める小細工そのものが要らなくなる**＝3 が消える

**★正解ラベル無しの健全性検査**: **敵HPは戦闘中に増えない**＝抽出値の系列が単調でなければ壊れている
（`T1-ROI-005`）。P2-2 の二峰性テストと同じ発想で、**ユーザーに正解を訊かずに自分で判定できる**（§1.1）。

**★★決定的走査の検証 ＋ 前回結論の撤回（2026-08-14・3回目の実走）**

**(a) seek 方式は効いた**: **実効 29.813 fps / 動画 29.72 fps ＝ 100.3% 被覆**（`T1-DETECT-005` 不発）。
実時間は **6.0倍**（29.4秒ぶんに 174.9秒）＝276秒の全走で約27分。
⚠ **ユーザー確定情報（2026-08-14）＝実運用はマルチディスプレイで放置実行するため所要時間は二の次**。
∴ **seek 方式は恒久的に正しい選択**（速度と再現性のトレードオフは、この一言で決着している）。

**(b) ⚠★前回の「色ベースの存在検出は棄却」を撤回する**

| しきい値 | 間引き標本（1/3） | **全フレーム** | 判定 |
|---|---|---|---|
| `y190g80` | 1.922 | **3.023** | ★2.5 超 |
| `y160g80` | 1.950 | **2.951** | ★2.5 超 |
| 他7通り | 1.57〜1.94 | 1.83〜2.29 | 未達 |

**間引き標本の上で「9通りすべて単峰＝棄却」と結論していた**が、**全フレームでは2組が閾値を超える**。
∴ **棄却は誤り**＝サンプリング欠陥が分析を汚していた。採用率も **88.1% → 46.5%**、
距離の中央値も **16.9 → 3.07** と変わった（隣接フレームが本当に隣接になったため）。

⚠ **ただし今度は逆方向に読み過ぎない**。二峰性は**良い特徴量であるための必要条件**であって、
**2つの山が「数字の有無」に対応している保証はない**（「大きな金色演出」と「それ以外」かもしれない）。
`otsuCut 0.201` は p90(0.077) と p99(0.470) の間＝**上位5〜10%しか拾わない**ので、
「数字が出ている全フレーム」より狭い可能性が高い。**次は2つの山の中身をクロップで確認する**。

⭐ **本件の教訓**: **測定系の欠陥は、その上で回した分析を全部無効にする**。
`T1-DETECT-005`（サンプリング被覆の検査）を入れたのは、これを二度と黙って通さないため。

**★★P2-5 の実走検証 ── 失敗し、原因が特定でき、直った（2026-08-14）**

`M3-1.mp4` 120秒（3,503フレーム・実効 29.212fps）で `T1-ROI-005` が **単調性違反 237回**。
値は 0 と 1 の間を乱高下し、`drop` が **−0.567（＝増えている）**。

**★診断に載せた `colProfile`（生プロファイル）が原因を一発で示した**
（「外していても次の一手が決まるよう生データを必ず返す」という設計方針が効いた）:

| 観測 | 値 |
|---|---|
| **完全に塗られた列の占有率** | **0.375〜0.86**（★**1.0 にならない**） |
| 空の列の占有率 | **厳密に 0** |
| 閾値 0.50 の ±0.13 に入る列 | **76%** |

∴ **絶対閾値 0.50 が実データ分布のど真ん中に刺さっていた**。わずかな明滅で列が閾値を跨ぎ、
塗りの右端が 0 と 1 を往復していた。**占有率が 1.0 にならない理由**は、
人が採寸した ROI に対してバーがわずかに上下へずれ、上下帯が部分的に外れるため
＝**人の採寸に 1px 精度を要求するのは筋が悪く、アルゴリズム側で吸収すべき**だった。

**★修正2点**:
1. **閾値を絶対値から相対へ**: `threshold = max(0.10, peak × 0.20)`（peak＝列占有率の p95）。
   **空が厳密に 0 なので分離は本来易しい**。実走プロファイルに適用すると **HP 77.9%**
   ＝独立な目安（0 の列の割合から 77.0%）と一致（旧ロジックは 57.4%＝20ポイント外していた）。
2. ★**「読めない」を返せるようにした**: バースト演出中は画面が白金色にフラッシュして
   **バーが物理的に読めない**。従来は無理に数値を返して嘘の HP を報告していた。
   `visible:false` ＋ `fillRatio:null` を返し、系列側は `skippedFrames` に計上する
   ＝**数値を捏造しない**。

⭐ **本件の教訓（P2-5 で4回目の同型）**: **「正常値はこうなるはず」という思い込みで閾値を置かない**。
セルフテストのフィクスチャも「塗りは占有率 1.0」という理想形だったため通ってしまっていた。
→ **実走で観測された条件（占有率 0.375〜0.86・演出フラッシュ）をフィクスチャに焼き直し**、
**絶対閾値だと外れることを回帰として固定**した。

**★P2-5 修正後の実走（2026-08-15）── 大幅改善・残り 3.4%**

| | 修正前 | **修正後** |
|---|---|---|
| 単調性違反 | 237 | **117**（3399 フレーム中 **3.4%**） |
| `drop` | **−0.567**（増えている＝壊れている） | **+0.119**（減っている＝正しい向き） |
| first → last | 0 → 0.567 | **0.8906 → 0.7719**（もっともらしい） |
| 演出で読めず捨てた | （機能なし） | 108 フレーム |

★**平常値は 0.8875〜0.8922 と非常に安定**＝本物の HP（約89%）は正しく読めている。

**★残る違反の正体が絞れた**: 先頭10件のうち **9件が「ぴったり 1.0 へ跳ぶ」**
＝**ROI の最右列まで塗りと判定されている**。バーが満タンになったのではなく、
**何かが最右列を赤と判定させている**（バーの端飾り／演出の赤い光／`%` 文字の縁 など）。

⚠ **ここで推測して閾値をいじらない**（P2-5 で4回やった失敗の型）。
∴ **ツール自身に違反フレームの生プロファイルを持ち帰らせる**ようにした（v0.11.0）＝
`hpViolationSamples`（違反時の `colProfile` / `peak` / `threshold` / `redFraction`）と
`hpNormal`（正常フレームの同じ値）を並べれば、**人にクロップを頼まずに原因が特定できる**。

⚠ **決定性の残差**: 同じ 120秒の走で総フレーム数が **3503 vs 3507（0.11%差）**。
再生ベース時代の 7.9% からは激減したが**厳密一致ではない**（seek の歩幅適応が走ごとにわずかに違う）。
§9.1-3 の「同じ結果」をどこまで厳密に要求するかは **P5 の受入で確定させる**。

**★★P2-5 完了 ── 生プロファイルが原因を一発で示した（2026-08-15・v0.12.0）**

v0.11.0 で持ち帰った `hpViolationSamples` を `hpNormal` と並べたところ、
**推測を一切せずに原因が確定した**（＝v0.11.0 の設計判断がそのまま効いた実例）。

★**真因は閾値の高さではなく「右端の探し方」だった**。v0.11.0 の実装は

```js
for (let x = 0; x < width; x++) if (colProfile[x] >= threshold) edge = x + 1;   // 閾値を超えた「最後の列」
```

＝**塗りが連続しているかを一切見ていない**。実測プロファイルでは真の境界（idx 105）より右に
**孤立した 1〜2 列**が 0.11〜0.31 で立つことがあり（間の列は厳密に 0）、それだけで右端が末尾へ飛ぶ。

| t | 真の境界 | 末尾の異常 | threshold | v0.11.0 の出力 |
|---|---|---|---|---|
| 15.3834 | idx 105（0.375 が平坦） | **最終列だけ 0.3125** | 0.10 | **1.0** |
| 18.25 | idx 104（0.4583 が平坦） | **末尾2列 0.1083 / 0.2917** | 0.10 | **1.0** |
| 12.3001 | idx 105 | 末尾ほぼ 0 | 0.10 | 0.8906（**正しい**＝`from` 0.3016 の方が誤読） |
| 14.0834 | — | **プロファイル全体が別物** | 0.20 | **1.0** |

∴ **違反は1種類ではなく2種類**だった:

1. **孤立列型（9/10）** ── 境界の探し方の問題。
2. **ROI 汚染型（t=14.0834）** ── 平坦部が 0.2083（正常は 0.375〜0.4583）で、中央と末尾に
   **占有率 1.0 のブロック**＝赤い演出が ROI を覆っている。**境界を直しても解けない**＝
   このフレームは**読めないと言うのが正解**。旧実装は「HP 100%」と報告していた。

**修正（v0.12.0）── 閾値を1つ減らした**:

| | v0.11.0 | **v0.12.0** |
|---|---|---|
| 右端 | `max(0.10, peak×0.20)` を超えた**最後の列** | **2区間の階段フィット**（`fitStepEdge`）＝**閾値パラメータなし** |
| 可読性 | `peak < 0.15`（白フラッシュ）のみ | ＋**空の区間が本当に空か**（右区間平均 < `absFloor`） |
| 実測余裕 | — | 正常フレームの空区間平均 **0.000〜0.046** に対し判定 0.10 |

★**階段フィットが強い理由**は、孤立列を「例外扱い」しているのではなく、
**そこへ境界を動かすと左区間にほぼ 0 の列が入って残差が跳ね上がる**から自然に選ばれないこと。
`peak` にも塗り水準（0.375 でも 0.86 でも）にも依存しない。

⚠ **代償を1つ受け入れた**: **満タンのバーも「読めない」になる**。空の区間が無いフレームは
「満タン」と「ROI 全体が覆われている」を**プロファイルからは区別できない**（どちらも一面が赤）。
取りこぼすのは戦闘開始前の 100% 区間だけ＝**推測する価値が無い**ので、`visible:false` とする。
読めなかった理由は `skipCauses`（`flash` / `noEmptyRegion`）で内訳が出る。

⭐ **回帰の作り方をここで一段変えた**＝**実走のプロファイルを現物のままリポジトリに入れた**
（`tools/fixtures/t1_hp_profiles_M3-1.json`・実フレームの**画像ではなく 120個の集計値**なので §10.3 に抵触しない）。
合成フィクスチャで通っていたものが実走で崩れる事故を**2世代続けた**ため、
**壊れた当のプロファイルで「旧実装なら 1.0 に跳ぶ」ことまで固定**した（証拠が消えると同じ型に戻れる）。
`npm run test:t1` は **75件 → 93件**。

**P2-1 の設計判断**:
- **検出器は純関数**（DOM に触れない）＝**Node でセルフテストできる**（`npm run test:t1`）。
  ⚠ Claude はブラウザを見られない（§10.1）ので、**検証可能性を実装の都合より優先した**。
- **ROI は canvas 基準の正規化座標 [0,1] で持つ**＝マジックな解像度定数を置かない。
  ∴ スクロール・ズーム・録画解像度が変わっても**同じ ROI が同じ物を指す**（新発見②への構造的な答え）。
- **ROI の採寸は人がページ上でドラッグして行う**＝Claude が画面を見られない問題を、
  「**人が測って正規化座標をテキストで渡す**」に置き換えた（§1.1 の憲法どおり＝観測は人）。
- ⚠ **合成フィクスチャのテストは「実フレームで検出できる」ことの証明にはならない**（→ 下記で実測した）。

**★P2-1 実フレーム検証（2026-08-14・T1 v0.1.0 / `pic.mp4` 2288×1440）**:

| 項目 | 結果 |
|---|---|
| 診断 | **FATAL 0 / ERROR 0 / WARN 0** |
| canvas box | `x:290 y:198 w:1684 h:1129` |
| アスペクト | **1.4916**（既定の想定値 1.49 と **0.1% で一致**＝目測の暫定値が実測で裏付いた） |
| 面積比 | 57.7% |

→ 既定 `expectAspect` を実測値 **1.4916** に更新し、**測定条件を併記**した（E1）。
⚠ **1本の録画からの1点測定**＝別の録画・ウィンドウサイズでは未再測（E9）。

**v0.2.0 で足したもの**（採寸の手数を減らすため）:
- **ROI の名前つき登録・一覧・全件 JSON 書き出し**（1つずつ貼らせない）
- **登録済み ROI を画面に重ねて表示**（採寸のやり直しと確認）
- ★**ROI クロップの PNG 保存**＝§10.3 が「Claude へ渡すのはフル画面ではなく ROI クロップ」と
  決めた経路を、ページ側に実装した
- `progress.done` の更新漏れを修正（「0/1 完了」という矛盾表示が出ていた）

**★フレーム間隔の実測（2026-08-14・T1 v0.3.0・ブラウザ内 `requestVideoFrameCallback`）**

| | 値 |
|---|---|
| フレーム / 実効 fps | 89 / 3.000秒＝**29.334 fps** |
| 間隔の分布 | **33ms × 84 ／ 50ms × 4**（それ以外なし） |
| 最大ギャップ | **50.03ms** |

★**ffprobe（第1便・別窓）の結果と構造が一致**＝`33.33ms × N ＋ 50.00ms × 少数`。
**独立な2手法（コンテナのタイムスタンプ / ブラウザのフレーム提示）で同じ結論**が出たので、
**最大ギャップ 50ms は偶然ではなく構造**（60fps グリッドを1目盛スリップする以上の欠落が起きない）と確定した。
⚠ スリップの頻度は窓によって変わる（第1便 1/89 ＝1.1% ／ 本便 4/88 ＝4.5%）が、**上限は動かない**。

∴ **§9.2 の撤退条件②は完全にクリア**（ポップアップが 50ms 未満しか出ない場合のみ取りこぼす）。

**★ROI クロップの実物確認（2026-08-14・native 解像度）── P3 の設計に効く4点**

`dmg`（598×798）と `hp`（687×127）のクロップを受領。**§10.3 が設計した経路が実際に機能した**
（フル画面ではなく ROI クロップだけで中身が判定できた）。

| # | 観測 | 設計への影響 |
|---|---|---|
| ⑩ | **数字の桁高 ≈ 70〜80px** | テンプレートマッチングに**余裕**。可読性は問題にならない |
| ⑪ | ⚠**フォントは「金グラデ塗り＋濃い縁取り＋わずかな斜体」の装飾体** | §10.1 は「**固定ビットマップフォント**＝テンプレートマッチング」と書いたが、**字形は固定でも塗りは一定でない**。∴ **塗りでマッチさせず、縁取り（エッジ）でマッチさせる**か輝度正規化する。**結論（AI 不要）は変わらない** |
| ⑫ | ★★**キラキラ（4条星）のエフェクトが数字の上に重なる** | **オクルージョン源が「他の数字」だけではなかった**（設計に無かった要素）。ただし星は**局所的で形が既知**＝除去可能。⚠ P3 の受け入れ検査に「星が乗ったヒット」を必ず含めること |
| ⑬ | 重なりは**半透明合成**（不透明な上書きではない） | 両方が部分的に残る＝**分離の余地がある**。⑫と合わせ、P3 は「欠損に強いマッチング」を前提に設計する |

**★`hp` から出た設計判断（P2-5）**: バー上の丸ドット5個は
**「塗り画素の計数」は壊すが「塗りの右端エッジ位置」は壊さない**（位置は中央付近に固定）。
∴ **HP% はエッジ位置で測る**。当初案の「区切りドットをマスクする」は、
残 HP がドット位置に来てエッジ検出が取り違えるときだけの局所対処に降格する。

**★★⑭ そのドットは「チャージターン（CT）」だった ── 邪魔物ではなく観測値**（ユーザー回答 2026-08-14）

CT は**敵の限定行動の発動条件**であり（`gamedata/md/敵/*` に「**チャージターンが最大でない**」という条件が
`ryomen_sukuna` / `fimbulvetr` / `variant_chimera` / `cath_palug` など多数）、**最大になると強力な攻撃をすることが多い**。

⚠ **シムには未実装**（`src/`・`gamedata/js/` に `charge` 系の実装がゼロ＝2026-08-14 に確認）。

| 効き方 | 内容 |
|---|---|
| **C45**（反撃が未モデル化・**本体は敵の行動モデル**） | **敵行動モデルの主要な状態変数が、録画から直接読める**。`反撃の発数`（＝敵が何ヒット当てたかの唯一の観測値）と合わせると、**敵の行動が観測可能になる** |
| ROADMAP 未確定Phase（敵行動） | 同上＝intake の目処が立つ |
| 走の妥当性 | CT 最大時の強攻撃は味方生存・反撃数に効く＝**走の文脈情報**として記録する価値がある |

∴ `hp` ROI の処理は **HP% と CT の両方を出す**（同じクロップから取れる＝追加コストがほぼ無い）。
⚠ ただし **CT のモデル化そのものは Phase 9 のスコープ外**（§1.2）＝ここでは**読めることの確認と記録**に留める。

### P3 — 数値抽出 ＋ 検算 ⏳ **現在ここ**

- `CRITICAL!` / `STING!` のテンプレ検出 → **真下の数値へ割り当て**（固定オフセット＝決定的）
  ⚠ **P1 新発見①**: ラベルは**1つ上のヒットの数値にも重なる**（縦ピッチ < ラベル高+数値高）。
  ∴ ①検出 → ②**既知グリフなのでマスク/減算** → ③その後に数値 OCR、の順にする。
  **割当先が「自分の真下」とは限らない**前提で対応付けを検証すること
- 数値 OCR（**候補生成のみ**）
- §5 の検算を全部回し、**不合格の行だけ人にレビューさせる一覧**を出す

⚠ **全ヒットを読む必要はない**（`TRANSCRIPTION_DESIGN.md` §3.5）。
**重なりが激しい成分ほど必要な情報量が少ない**（betaia も holy も**1ヒット読めれば足りる**＝残りは同値の繰り返しで、
ヒット数はシムが知っている）。**この非対称性が本 Phase の成立根拠**。

**出口条件**: 1走ぶんの抽出が検算を全部通り、人のレビューが**数行以内**に収まること。

#### P3-0 — 設計（2026-08-19 確定・実装は `src/transcribe/glyph.js`）

**★段取りを3つに割る**。フォントは実機のものなので、**テンプレートは実機の録画からしか得られない**。
⚠ Claude が形を推測して書いたテンプレートは、**合成テストだけ通って実機で静かに外す**
（本 Phase で3回踏んだ「測定器の性質を観測対象の性質と取り違える」型）。

| # | 工程 | 担当 | 中身 |
|---|---|---|---|
| 1 | **採取** | 🔧 | 走から「グリフらしい塊」を集め、**似た見え方をまとめる**（形の分類まで） |
| 2 | **ラベル付け** | 👤 | 代表画像を見て「これは 7」と**1回だけ**答える。★憲法＝観測と判断はユーザー |
| 3 | **照合** | 🔧 | 以後は登録済みアトラスとの照合＝**二度と人に訊かない** |

**★実測で決めた設計判断**（合成フィクスチャ・3背景 × 3塗り × 10字 = 90標本・2026-08-19）:

| # | 判断 | 根拠（測った数字） |
|---|---|---|
| ① | **切り出しは「谷で切る」ではなく「等間隔の格子を当てる」** | 射影をしきる方式は**背景を変えるだけで 10文字が 10 / 4 / 3 に化ける**。閾値を 3×3 掃引しても両立しない（字間の谷は背景しだいで谷にならない）。格子なら**背景3種すべて P=24＝真値**・10個 |
| ② | **色は使わない**（輝度の勾配だけ） | P2-2 v2 で**金色画素の割合は9通りすべて単峰**（Fisher 分離 1.57〜1.95）＝バースト演出そのものが金色 |
| ③ | **明るさで重みを付けない** | 「縁取り↔背景」の境界を消したくて試したが**読み正解が 6/9 → 2/7 に悪化**（低コントラストの構造まで消える）＝棄却 |
| ④ | **採否は score の絶対値ではなく「1位と2位の差」** | **誤読 9件は 9件とも `ambiguous`**。score は誤読 0.564〜0.632 が正解 0.539〜0.999 と**重なる**＝絶対値では分けられない |
| ⑤ | **アトラスは1字につき複数の見え方を持つ**（variants） | 1条件アトラス **54/90** → 3条件アトラス **88/90** が読める（**誤りはどちらもゼロ**） |
| ⑥ | **採取のクラスタリングは「同じ字を1つに」を諦め、純度で測る** | 閾値 0.62 では**5クラスタに別の字が混ざる**（最大4字が同居）／0.85 で混入 0。⚠ 混ざると**人のラベルが誤ったテンプレに付く**＝一番高い代償 |

★**通底する原則＝間違って読むくらいなら「読めない」と言う**。読めない（`?`）は次工程が拾えるが、
**もっともらしい誤読は較正を静かに汚染する**（本 Phase の成果物は較正の入力そのもの）。

#### P3-1 — アトラス取得（👤 実機録画が要る・⏳ **ユーザー待ち**）

**ツール側は実装済**（T1 v0.29.0＝走査に相乗りして採取し、代表をシートに描く）。
残りは**ユーザーが1回だけラベルを付ける**こと。手順は本節末の「録画・採取の依頼」。

**出口条件**: `validateAtlas` が通る（0〜9 とカンマが揃い、provenance がある）こと。

##### 4.3.1 ★アトラス採取の依頼（👤 ユーザー用・これだけ）

⚠ **新しい録画は要りません**（既存の録画でできます）。数字は毎ターン出るので、
**普通の戦闘が映っていれば 0〜9 は全部出そろう**見込みです。

1. `npm run dev` → `/transcribe/` を開き、録画を1本読み込む。
2. **「★走を通しで選別」を 60〜120秒**で回す（今までと同じボタン。採取は相乗りします）。
3. 下の「**4. ★グリフ採取**」に代表が並ぶので、**番号順にラベルを空白区切りで**入力する。
   数字は `0`〜`9`、桁区切りは `,`、**数字でない／分からないものは `-`**。例: `7 - 0 , 3 8 - -`
4. 「アトラスJSONを保存」→ できた JSON と、**digest**（★コピーボタン）をセッションへ貼る。

★**できれば 2〜3 窓ぶん**（別々の時間帯 or 別の録画）をやると精度が上がります。
実測で**1条件だと 90文字中 54 しか読めず、3条件にすると 88 読めた**（誤りはどちらもゼロ）ため。
狙って混ぜたい条件は次の3つ:

| 条件 | なぜ |
|---|---|
| **通常攻撃だけの時間帯**（背景が暗い） | 基準になる見え方 |
| **バースト中**（画面が金色に光る） | ★数字と背景の明暗が逆転しうる**いちばん厳しい条件** |
| **キラキラ（4条星）やラベルが数字に重なっているヒット** | P1 発見①⑫＝受け入れ検査に必ず含める |

⚠ **動画は共有しないでください**（§10.3）。出てくる JSON と digest は数値だけです。
⚠ 代表シートに**数字がまったく無い**ときは、ラベルを付けずに digest だけ貼ってください
（＝切り出しが実物に合っていない＝`glyphStats` の「割り方」「格子の合致度」で直します）。

#### P3-1b — 読み取り＋検算（アトラス取得後）

- ラベル（`CRITICAL!`/`STING!`）の検出 → **マスク**（潰した画素は「白でも黒でもなく分からない」＝票を持たない）
- 行ごとの読み取り＝`readRow`（候補生成＋カンマ文法検査）
- 検算③（値域）・④（ラベルのテンプレ）・⑦（`TOTAL` ↔ 個別ヒットの合計）を回す

#### P3-2 — イベント畳み込み（旧 P2-4・2026-08-15 にユーザー承認で移設）

**同一位置・同一値の連続フレームを1イベントに畳む**。⚠ **ここでなければできない**＝
「同一値」の判定は**値を読んでいること**を前提にする。

**出口条件**: **OCR 後のイベント数**が、走査フレーム数に対して**1桁以上少ない**こと
（旧 P2 の「人が見るフレーム数が1桁減る」を、達成可能な段へ置き直したもの）。

**移設の根拠（実測）**:
- 寿命 L ≦ 2 フレーム＝`stride = floor(L/2) = 1`＝**等間隔の間引きは原理的に不可能**。
- ★**ユーザー確定情報（2026-08-15）**: ダメージ数値は「**わずかにフェードイン・消失があるが、位置は基本的に不動**」。
  ∴ ①**「同一位置」は使える**（位置アンカーでの照合が成立する）
  ②⚠ **freeze 長 2 は「数字が動いている」ことを意味しない**＝**数字の上に重なる演出**
  （P1 発見⑫「**キラキラが数字に重なる**」）がセルを動かし続けるため。
  ★**読める長さは 2 フレームより長い。ただし画素の凍結では測れない。**
- ∴ 畳み込みの判定材料は**画素の静止**ではなく**読んだ値と位置**＝OCR 後が正しい置き場所。

⚠ **設計時に効く既知事項**: `dedup.js` の `EventDeduper` は**完全重複（距離ちょうど 0）の除去**だけは
P2 で有効なまま（実測 7.4%・情報損失ゼロ）。ただし判定は `dmg` ROI の署名一致なので
**別フレームの偶然一致を含みうる**（実測でセルの 38% が不動）。

### P4 — config 版管理の統合

- `data/config.<日付>.json` を config の唯一の正にし、trial は**版を名乗るだけ**にする
- ⏳ **`configC_slot.json`（UI の編成保存スロット JSON）は長期未受領**＝これ1本で
  **幻獣枠・サブ枠・ウェポン・ボックス補正**が埋まり、記録項目を4つ消せる
- 走の**自動無効化判定**（E11 を機械化）

**出口条件**: config の取り違えが**構造的に起こりえない**こと（E2 が自動で落とす）。

## 5. 検算の設計（★これが精度を担保する）

**OCR を信用するのではなく、検算する**。互いに独立な検算があるので、AI は候補を出すだけでよい。

| # | 検算 | 何を捕まえるか | 状態 |
|---|---|---|---|
| 1 | **総和チェック**: 成分別合計 = HP減少量 | まるごと1押下級の大穴。⚠**分解能は粗い**（HP 1% = 980万＝holy 122ヒット相当） | ⏳ P2 |
| 2 | **★リプレイ照合**: 押し順をシムで再生し「この押下で何ヒット出るはず」と突合 | **個別ヒットの取りこぼし・成分の誤ラベル**＝**主役** | ✅ **ほぼ実装済**（`calib_replay_compare.mjs` の `LAYOUT` / `FB_GROUPS`・2026-08-07） |
| 3 | **値域チェック**: 各成分は cap で頭打ち＝値域が既知 | 桁違いの誤読 | ⏳ P3 |
| 4 | **`CRITICAL!`/`STING!` はテンプレ**＝固定グリフ | フラグの誤り／**数値探索の錨** | ⏳ P3 |
| 5 | **ゲージ増分 ↔ 通常攻撃ヒット数**（1ヒット +10） | 通常攻撃の転記漏れ（sim04 で実績2件） | ⏳ P2 |
| 6 | **反撃の発数 ↔ 敵のヒット数** | 敵行動の段数漏れ（pre-trial T3 で実績） | ⏳ P3 |
| **7** | ★**`TOTAL` 表示 ↔ 個別ヒットの合計** | **個別ヒットの取りこぼし（1ヒット単位）**＝検算1 の分解能問題（§3.4 R5）を解消する。**さらに成分の二分**（下記） | ⏳ **P3**（2026-08-14 に集計単位が確定＝実装可） |
| **8** | **CD 表示（グレーアウト＋残ターン）↔ シムの押し順再生** | **押下の取り違え・押し順のアラインメントずれ**。検算2 の錨になる。⚠ **映るのは「選択中のキャラ」のぶんだけ**＝全キャラ同時ではなく**機会的**。⭐ 逆に「どのキャラのパネルが映っているか＝操作対象」自体が手がかりになる | ⏳ P3 |

**⚠ 1 と 2 は独立**（1 は HPバー由来・2 はシムの構造由来）＝両方通れば信頼度は高い。
**ただし 1 単独では弱い**＝**2 が主役**。
⭐ **7 が成立すれば 1 の弱さは解消する**（画面表示の合計は1円単位＝HP% の ±980万とは桁が違う）。
**7 は 2 とも独立**（画面由来 vs シム構造由来）＝**3本目の独立な検算**になる。

⚠ **循環に注意**: 2 は「シムの構造」を仮定してラベルを付ける。
**ヒット数と成分の種類＝構造**（golden が守っている）／**値＝較正対象**、と切り分ければ許容できる。
**ヒット数そのものが未確定な成分**（`consort` の 1hit/2hit は bc 依存＝まさに測りたいもの）は
**表示回数で独立に数える**こと。

## 6. 未確認事項（P1 で潰す）

**✅ 解決済**（2026-08-02 ユーザー提供）: ポップアップは**静止**（追跡不要）／ラベルは数値の**真上**（固定オフセット）／
重なりは**成分で予測可能**（単発は被らない・多段は確実に被る）／HP は**整数%＋バー**／
**成分は表示だけでは判別できない**（ユーザーは演出で判断している）。

**✅ P1 で解決**（2026-08-14・詳細と根拠は §4 P1「P1 結果」）:
- **解像度・fps・VFR か** → 2288×1440 / 29.718fps / **VFR だが実害なし**（最大ギャップ 50ms）
- **多段ヒットの重なり具合** → **1つは読める**（同値ヒットの繰り返し＝§3.5 の前提が実物で成立）
- **演出開始の検出** → より確実な代替を発見＝**`NEXT n TURN` 表示**でターン境界が直接読める
- ★**復元不能な項目の洗い出し** → **1件発見＝バフ/デバフアイコンは一部しか表示されない**
  （∴ 録画からバフ状態は全量復元できない＝**C38 の近道にはならない**）。
  ⭐ 逆に**録画メタデータ**（`creation_time`・ウィンドウ `title`）は走の provenance として使える

**⏳ 残り**（★いずれも **P2 の設計で回避可能**＝ゲートは塞がない）:
- **HPバーの画素幅**（推定 ~620px・±10%）＝サブピクセル推定の可否。⚠ **区切りドット5個の重畳**をマスクする必要
- HPバーは**アニメーションで遅延追従するか**（回避策＝ターン境界の**安定後**フレームを採る）
- ポップアップの**表示継続フレーム数**（取りこぼしリスクは P1 で消えた＝重複除去の設計にのみ影響）

## 7. 追加ツールの受け入れ方（★想定外を前提にする）

**ユーザー方針（2026-08-07）**: 「現時点で想定できていない追加のツールが必要になることもあるだろう。
**PJ 進行スピードよりも網羅性・正確性を重視**」。∴ **計画を後から広げられる形にしておく**:

1. **新しいツールは必ず「どの検算を強くするか」を宣言してから作る**（§5 の表に行を足す）。
   検算に紐づかないツールは**作らない**（＝スコープが無限に広がるのを防ぐ）。
2. **AI 不要のものを先に作る**（S0〜S2.5 の原則）。AI を使うツールは、**その受け入れ検査が先に存在する**こと。
3. **段（P2〜P4）の中に差し込む**。新しい段を横に足さない（ゲートが増えると進まなくなる）。
4. **本書 §4 の表に追記し、更新履歴に「なぜ必要になったか」を残す**（後から経緯を追えるように）。

## 8. sim05 との関係（★凍結範囲を明示）

**ユーザー決定 2026-08-07**: **sim05 の残り（M3〜M6）は凍結し、Phase 9 を先行させる。**

| 項目 | 扱い |
|---|---|
| **M3〜M6 の実走** | ⏸ **凍結**。ただし**録画は貯めてよい**（§4.0.1 のチェックリストを守れば後から処理できる） |
| **C40 / C41 / C44 ほか** | ⏸ **open のまま**。Phase 9 完成まで進展しない（**これは意図的に受け入れたコスト**） |
| **M1 / M2 の成果** | ✅ 確定済み・不変（`simulation/sim05/analysis/integrated_analysis.md`） |
| **sim05 の記録様式** | 現状のまま凍結。**Phase 9 完成後に「skeleton 廃止＋menu 最小仕様＋汎用正規化」へ移行**（sim06 以降の構造解） |
| **golden / エンジン** | 影響なし（Phase 9 は `src/` を触らない） |

⚠ **凍結の解除条件**: P5（受入）通過。または P1 で降りた場合は §9.2 の代替路線へ。

## 9. 受入基準と撤退条件

### 9.1 受入基準（P5）

1. **1本の実走が「録画 → trial データ → 突合」まで人手の数値入力ゼロで通る**。
2. §5 の検算が**全部**回り、不合格箇所が**行単位で特定**される。
3. **同じ録画を2回処理したら同じ結果**になる（決定的＝再現可能）。
4. **config の取り違えが構造的に起こりえない**（E2 が自動で落ちる）。
5. 既存の trial01（手転記・検証済み）を**基準として再現**できる、または差分の理由が説明できる。

### 9.2 撤退条件（★先に決めておく）

**P1 の実測で以下が判明したら、OCR 路線から降りる**:
- 録画の解像度が低く**数字が判読できない**（人が見ても読めないなら機械にも読めない）
- ポップアップの表示が**30fps で取りこぼす**ほど短く、かつ録画設定を上げられない
- 多段ヒットの重なりが激しく、**1ヒットも読めない**成分がある

**代替路線**（降りた場合）: **入力補助に切り替える**。
＝転記自体は人が行うが、①ROI クロップした静止画を並べて**見る負担だけ**減らす
②入力と同時に §5 の検算を回して**間違いを即座に指摘**する。
**検算（§5）は OCR の成否に関わらず価値がある**ので、投資は無駄にならない。

## 10. 確定仕様（2026-08-07 ユーザー承認）

> 実装は次セッション以降。本節は**着手前に固定した仕様**であり、変更にはユーザー承認を要する。

### 10.1 実行場所 ── **データの所在で決まる**

**原則**: ツールは**入力データがある場所で動かす**。Claude 経由の呼び出しは挟まない。

| ツール | 入力の所在 | 実行場所 | 理由 |
|---|---|---|---|
| **録画転記** | ユーザーPC（数GB） | **ブラウザ（Webアプリの新規ページ）** | 動画が PC から出ない／Claude 環境では原理的に動かせない |
| **検算** | リポジトリ内 trial ＋ シム | **両方**（ブラウザで即時／`tools/` で回帰） | シムは ESM＝Node/ブラウザ両対応 |
| **config 版管理** | UI の編成スロット | **Webアプリ** | 既にそこにある |
| **陳腐化検出・doc 検査** | リポジトリ | **`tools/`** | Claude が回帰を回せる |

**★Webアプリ統合の根拠**（当初想定より強い）:
- **シムエンジンが既にある**（`src/sim.js`）＝**検算②（リプレイ照合＝主役）がその場で回る**
- **動画が PC から一歩も出ない**（`<input type=file>` → `<video>` → `canvas`）＝**§10.3 の問題が構造的に消える**
- Python/OpenCV のインストール不要（Windows のツールチェーン整備は事故りやすい）
- ★**ゲームの数字は固定ビットマップフォント**＝汎用OCRではなく**テンプレートマッチング**で解ける
  ＝**決定的・再現可能**で、**パイプラインから AI が完全に消える**（§1.1 の憲法どおり）

**★配置（ユーザー決定）**: **新規の別ページ**として作る（同一 Vite ビルド内・シム本体とは独立）。
シム本体の UI と golden に**一切干渉しない**こと。共有するのは `src/` の ESM を import する形だけ。

⚠ **開発ループの制約**: Claude はブラウザ画面を見られない。
∴ **ツールは診断をテキスト/JSON で出す**（§10.5）＝これが唯一のデバッグ経路。

### 10.2 ツール類型と受け入れ関門

**列挙ではなく類型で押さえる**（網羅性のため）。既存はすべてこの表のどこかに属する。

| 類型 | 目的 | 既存 | 追加候補 |
|---|---|---|---|
| **A. intake** | 観測 → データ | — | **録画転記**（T1） |
| **B. 検証** | データ → 信頼 | `golden` / `calib_replay_compare`（検算②）/ `config_c.mjs`（E2） | 検算 ①③④⑤⑥（T2） |
| **C. 版管理** | 何がいつ変わったか | `config_c.mjs`（読むだけ） | **config 版**・**engine 版と台帳の鮮度**・**走の無効化判定（E11 の機械化）**（T3） |
| **D. 陳腐化検出** | 文書と実体の乖離 | `doc_refs`（参照のみ） | ★**数値の provenance 検査**（T4） |
| **E. 運用** | 工数削減 | — | セッション定型の機械的部分のみ（T5） |

**★D が構造的な穴だった**: 本セッションだけで「2世代前の golden 値」「陳腐化した期待値」
「retire した原則への参照」を**3件**検出したが、いずれも**人間が偶然気づいただけ**。
「リポジトリ内の数値がどの台帳のどの版に由来するか」を検査するツール＝**E1 の機械化**にあたる。

**⚠ E の禁止事項**: TODO/HANDOFF の**自動更新はしない**。
「波及確認」列は **git が持たない唯一の情報**で、自動化すると必ず失われる。
自動化してよいのは**機械的部分のみ**（カウンタ、鮮度フラグ、リンク切れ）＝**判断は自動化しない**。

**受け入れ関門**（§7 の拡張）:
- **検算系のツール**: 「**どの検算を強くするか**」を §5 の表に行として宣言してから作る。
- **非検算系のツール**: 「**どの手作業を、何回/セッション 消すか**」を宣言してから作る。
- **どちらも答えられないものは作らない**（スコープの無限膨張を防ぐ唯一の歯止め）。

### 10.3 動画の扱い ── **共有しない**

**結論**: 動画は**リポジトリにもセッションにも入れない**。**アーキテクチャで解消する**（転送手段の問題ではない）。

**誰が動画を必要とするか**で分けると消える:
- **ツール**が必要 → ユーザーPC で動く（§10.1）→ **動画は1バイトも移動しない**
- **Claude** が必要なのは動画ではなく、下表のものだけ

| 場面 | Claude へ渡すもの | 頻度 | コスト |
|---|---|---|---|
| **P1 設計時** | 静止画3枚（PNG） | **1回だけ** | 許容 |
| 開発中 | ツールの**診断ログ／抽出 JSON**（§10.5） | 都度 | 軽い |
| 不具合時 | **ROI クロップした小さな PNG**（フル解像度不要） | 稀 | 軽い |

**⇒ 仕様**: ツールは「人が見るべきフレーム」を **ROI クロップした小さな PNG で出力する**。
これは**人のレビュー用**であると同時に、**Claude へのデバッグ経路を兼ねる**。
⚠ **git-lfs は使わない**（有料枠・履歴汚染は同じ）。外部ストレージも不要。

### 10.4 trial / analysis の新構造

**前提が変わる**: 真の原本は**動画**であり、リポジトリに入るのは**再生成可能な抽出物**。

```
data/trialNN/
  source.md      ← ★人が書く唯一のもの（走の意図・封じたもの・所感・演出で判断した成分ラベル）
  extract.json   ← ツール出力（機械生成・再生成可能・ツール版と設定を刻む）
  verify.md      ← 検算結果（機械生成）
analysis/per_trial/
  trialNN_{quant,quali}.md   ← Claude Code（解釈のみ）
```

**規約の変更点2つ**:
1. **「不可侵」の対象が動画へ移る**。抽出物は**ツールを直したら作り直してよい**
   （現行の「原本は加工せず」の縛りが外れる）。
2. **人が書くのは再生成できないものだけ**＝走の意図・封じたもの・所感・
   **演出で判断した成分ラベル**（`TRANSCRIPTION_DESIGN.md` §3.3＝数値の周辺に成分情報が無く、**人にしか出せない**）。

⚠ **再生成可能性の条件**: `extract.json` に**ツール版と設定を刻む**（provenance）＋
**検算を通るまで動画を消さない**を運用規律とする。
⚠ **analysis の2層構造（map-reduce）は維持**＝目的はコンテキスト有界化で、そこは変わらない。

### 10.5 ★エラー・診断の出力仕様（実装前に固定）

**前提**: 実装初期は何度も止まる。**「どのツールが・どの段階で・何に躓いたか」が分からないと直せない**。
⚠ Claude はブラウザ画面を見られないので、**これが唯一のデバッグ経路**。

**ツールIDと段階（stage）**:

| ツールID | 名称 | stage |
|---|---|---|
| **T1** | 録画転記（ブラウザ） | `LOAD` → `DECODE` → `ROI` → `DETECT` → `MATCH` → `PAIR` → `DEDUP` → `EMIT` |
| **T2** | 検算 | `PARSE` → `CONFIG` → `REPLAY` → `CHECK` |
| **T3** | config 版管理 | `LOAD` → `DIFF` → `JUDGE`（E11） |
| **T4** | provenance 検査 | `SCAN` → `RESOLVE` → `COMPARE` |

**エラーコード**: `<ツールID>-<stage>-<3桁>`（例 `T1-MATCH-003`）。
**本書 §10.5.1 にコード台帳を作り、増えたら追記する**（コードは再利用しない＝過去ログの意味が変わらないように）。

**severity**: `FATAL`（処理停止）／ `ERROR`（その行/フレームだけ無効・続行）／ `WARN`（続行・要確認）／ `INFO`。

**出力の形**（1回の実行につき1つ）:

```json
{
  "tool": "T1", "version": "0.3.1", "ranAt": "2026-08-08T12:34:56Z",
  "input":    { "file": "M3_run1.mp4", "frames": 9012, "fps": 30, "resolution": "1920x1080" },
  "config":   { "roi": {...}, "thresholds": {...} },
  "progress": { "stage": "MATCH", "done": 7421, "total": 9012, "completed": false },
  "diagnostics": [
    { "code": "T1-MATCH-003", "sev": "ERROR", "where": { "frame": 1832, "roi": "dmg" },
      "expected": "3〜8桁の数字", "got": "グリフ一致なし",
      "hint": "重なりの可能性。crop を確認",
      "crop": "crops/f1832_dmg.png" }
  ],
  "summary": { "FATAL": 0, "ERROR": 2, "WARN": 5 },
  "result": { "...": "ここまでに抽出できたもの（部分成功でも必ず出す）" }
}
```

**★仕様として固定する5点**:

1. **人が読む1行サマリ**を必ず併記する（例: `T1 v0.3.1: MATCH で停止 / 7421/9012 フレーム処理 / FATAL 0 ERROR 2 WARN 5`）。
2. **部分成功を必ず保全する**＝途中で落ちても**そこまでの `result` を出力する**（走を丸ごと失わない）。
3. **`expected` と `got` を必ず対で書く**（「失敗した」だけのログは直せない）。
4. **`crop` を添える**＝不合格箇所の ROI 画像を出し、**そのパスを診断に書く**（§10.3 のデバッグ経路）。
5. **決定的であること**＝同じ入力＋同じ `version` なら**同じ診断列**が出る。`version` は出力に刻む。

⚠ **Claude へ渡すのはこの JSON（または1行サマリ）**。動画も全フレームも要らない。

#### 10.5.1 エラーコード台帳

<!-- 実装時に埋める。コードは再利用しない（過去ログの意味が変わるため）。 -->

⚠ **コードは再利用しない**（過去ログの意味が変わらないように）。廃止したら「廃止」と書いて残す。

| コード | severity | 意味 | 典型的な原因 / 対処 |
|---|---|---|---|
| `T1-DECODE-001` | FATAL | フレームの画素取得に失敗 | 別オリジンの動画で canvas が汚染された。ローカルファイルを開く |
| `T1-ROI-001` | FATAL | canvas 矩形を検出できない | ページ余白（白）が録画に写っていない。ゲームを全画面表示にすると背景推定が効かない |
| `T1-ROI-003` | WARN | 右パネルの list/detail 判定が閾値ぎりぎり | `listThreshold` の較正が要る。⚠ 既定は合成フィクスチャ由来の**未較正**値＝実フレームの score 分布で決め直す |
| `T1-ROI-002` | WARN | canvas のアスペクト比が想定外 | canvas 以外（バナー等）を掴んだ可能性。期待値 **1.4916** は 2026-08-14 の実測（1点測定・未再測）＝**FATAL にしない**（E1） |
| `T1-DECODE-002` | ERROR | フレーム間隔の実測でフレームが取れない | タブが背景にあり再生が抑制された。タブを前面にして再実行 |
| `T1-DECODE-003` | WARN | フレーム間隔に 100ms 超のギャップ | ポップアップを取りこぼしうる（§4 P1 発見⑧）。録画設定の見直しを検討 |
| `T1-DETECT-001` | ERROR | 選別で1フレームも走査できない | 背景タブでは再生が抑制される。タブを前面にして再実行 |
| `T1-DETECT-002` | **INFO**（旧 WARN） | 採用率が 10% を超えた | ★**2026-08-15 に severity を落とした**＝採用率は**もう P2 の出口条件ではない**（削減は P3-2 へ移設・ユーザー承認）。分布は較正材料として出し続ける |
| `T1-DETECT-003` | WARN | 要求秒数より短い窓しか走査できなかった | 動画の残りが足りない。⚠ **採用率はイベント密度に依存する**＝短い窓の値は出口条件の判定に使えない |
| `T1-DETECT-004` | WARN | 存在検出の特徴量が2峰に割れない | 金色画素の割合ではポップアップの有無を分けられない。特徴量を変える（縁取りのエッジ密度・連結成分など） |
| `T1-DETECT-005` | ERROR | 実効サンプリングが動画 fps の 90% 未満 | フレームを取りこぼしている。**この値が低いと分布も採用率も信用できない**（v0.7.0 で実際に 1/3 しか見ていなかった） |
| `T1-ROI-004` | ERROR | HPバーの解析に失敗 | `hpbar` ROI が未採寸／低すぎる／赤の閾値が実物と合わない。診断の `colProfile` を見て決め直す |
| `T1-ROI-005` | WARN | HP 割合が増加した | 抽出が壊れているか、演出でバーが隠れるフレームがある。⚠ **敵HPは戦闘中に増えない**＝正解ラベル無しの健全性検査。★**到達点が「ぴったり 1.0」に偏っていたら右端の探し方が壊れている**（v0.11.0 がこの型＝孤立した末尾列に釣られていた） |
| `T1-ROI-006` | WARN | 走の間に HP が減っていない | 常に同じ位置を拾っている（塗り境界を見ていない）可能性 |
| `T1-ROI-007` | ERROR | CT ドットの解析に失敗 | `hpbar` が未採寸か低すぎる／走査が0フレーム |
| `T1-ROI-008` | WARN | CT ドット列の周期性が弱い | ★**まず `searchRange`**（探索できた周期の範囲）を見る＝**範囲外の「無し」は情報ではない**（2026-08-18 に実際にここで誤りかけた）。次に `bandScan[].peakRun`＝**少数のドット列は自己相関では出ず、等間隔の山の列としてだけ出る**。それから `meanProfile` / `periodScan`。⚠ `periodicityFloor` は**合成由来の未較正値**。**どの帯にも山の列が無いときに初めて**「ドットは ROI の外」を疑う |
| `T1-ROI-009` | WARN | CT の読みが毎秒1回以上変化 | ★CT はターン境界でしか動かない＝**抽出が壊れている合図** |
| `T1-ROI-011` | WARN | モード遷移の除振で生の6割超を落とした | 落としすぎ＝`minDwellSeconds` が本物の滞在（実測 1.9〜7.2秒）に食い込んでいないか |
| `T1-DEDUP-001` | **INFO**（旧 WARN） | 間引いていない（完全重複の除去のみ） | ★**2026-08-15 に severity を落とした**＝実測 L≦2 で `stride=1`＝**等間隔の間引きは原理的に不可能**と確定し、削減を P3-2 へ移設した。∴ 未較正は**未了ではなく確定した設計** |
| `T1-DEDUP-002` | WARN | 間引いたが採用率 10% に届かない | ⚠ **現状は発火しない**（`stride` を設定しないため）。将来 stride を入れる設計に戻す場合の番人として残置 |
| `T1-DEDUP-003` | WARN | 離散的な出来事が信号に出ていない（`eventContrast` = ラグ1 の p90/p50 が 1.5 未満） | 捕まえる対象が信号に出ていない＝ROI が外れているか、その窓に何も起きていない。⚠ **この状態で stride を決めてはいけない** |
| `T1-MATCH-001` | WARN | グリフ候補が0件 | 行の切り出し閾値が高すぎる／その窓に数字が出ていない。digest の `glyphStats`（割り方・行の高さ・格子の合致度）を見て決め直す |
| `T1-MATCH-006` | ERROR | アトラス（実機グリフ）が未取得・不完全 | ★**採取 → ユーザーがラベル付け → 登録**の順。推測テンプレートでは動かさない（合成だけ通って実機で静かに外れる）。⚠ アトラスを読み込んでいないときは鳴らさない（P3-1 は「これから作る」段） |
| `T1-DETECT-006` | WARN | 採取のクラスタ数が上限に張り付いた | `clusterSimilarity` が高すぎるか、切り出しの箱が字ごとにずれている。⚠ **クラスタが多いこと自体は異常ではない**（条件ごとに分かれるのは設計どおり＝variants になる） |
| `T1-DEDUP-004` | WARN | 寿命 L が J のとり方で食い違う | ★**この走からは L が決まっていない＝stride を決めてはいけない**。合成で「L が実在すれば3つの J で完全一致・実在しなければ食い違う」ことを両方向に検証済み |

### 10.6 ★一連のフロー（厳密版）

**凡例**: 👤=ユーザー ／ 🔧=ツール ／ 🤖=Claude Code

| # | 工程 | 担当 | 成果物 | **次へ進む条件（ゲート）** |
|---|---|---|---|---|
| 1 | **走の設計**（どのメニューを何本） | 🤖→👤承認 | sim README §4 の該当節 | 必須項目が menu 別に確定していること |
| 2 | **録画**（§4.0.1 のチェックリスト） | 👤 | 動画（PC内・**移動しない**） | 開始前の4画面が映っていること |
| 3 | **走の意図を書く** | 👤 | `data/trialNN/source.md`（短い） | 再生成できない情報（意図・封じたもの・成分ラベル・所感）が入っていること |
| 4 | **転記**（T1・ブラウザ） | 🔧 | `extract.json` ＋ 診断 JSON ＋ 不合格の crop | **FATAL 0**。ERROR/WARN は残ってよい（次工程で潰す） |
| 5 | **検算**（T2） | 🔧 | `data/trialNN/verify.md`（未作成）<!-- doc_refs:ignore-line 実装後に生成されるファイルへの前方参照 --> | **検算①〜⑥が全部走り**、不合格が**行単位で特定**されていること |
| 6 | **不合格箇所のレビュー** | 👤（crop を見る） | `extract.json` の修正 or 再実行 | 残り不合格が**数行以内** |
| 7 | **config 判定**（T3・E11） | 🔧 | 走の有効/無効・config 版 | **走が有効**と判定されること（無効なら 2 へ戻る） |
| 8 | **リポジトリへ投入** | 👤（push） | `data/trialNN/` | — |
| 9 | **分析** | 🤖 | `analysis/per_trial/trialNN_{quant,quali}.md` | 入力は 3・4・5 の成果物のみ |
| 10 | **統合・Cx 更新** | 🤖 | `integrated_analysis.md` ／ `CALIBRATION_ANALYSIS.md` | rollup のみを入力とする（2層構造） |

**★このフローの含意**:
- **👤 が数値を入力する工程が1つも無い**（3 は言葉・6 は目視確認のみ）＝§1.1 の憲法が満たされる。
- **🤖 は 9 から登場する**＝**intake は Claude の可用性に律速されない**（ユーザーは貯め続けられる）。
- **失敗は 4→6 のループに閉じ込められる**＝走をやり直す必要がない（§10.5-2 の部分成功保全が効く）。
- ⚠ **工程 7 が 8 の前にある**のが重要＝**無効な走をリポジトリに入れない**
  （幻獣枠・GEAR の取り違え事故を構造的に防ぐ）。

---

## 更新履歴

<!-- 直近5件のみ（それ以前は git log）。「波及確認」列が本体＝git が持たない情報はここだけ。 -->

| 日付 | 変更点 | 波及確認 |
|---|---|---|
| 2026-08-14 | **★P1 実測ゲートを通過＝「進む」を確定**（`M3-1.mp4` の実測）。§4 P1 に結果節を新設し、§2 のフロー図・§6 未確認事項・§4 P2/P3 の要件へ反映 | **撤退条件3つとも非該当**。★**VFR は実害なしと実測で確定**（90フレーム連続のタイムスタンプ＝最大ギャップ **50ms**・クラスタリング無し・欠落は 60fps グリッド1目盛のスリップで一様分布）。∴ **P2 の「固定fps 正規化」を撤回**＝再エンコードが重複除去と検算を汚すため **native フレームのまま扱う**（設計の単純化）。★**新発見2件が P2/P3 の要件を変えた**: ①ラベルが**他ヒットの数値にも重なる**＝テンプレのマスク/減算を OCR の前に置く ②**ROI を固定クロップにできない**（スクロール/ズームで canvas が動く）＝**canvas 正規化を P2 の最初に**。★**復元不能項目を1件発見**＝**バフアイコンは一部しか表示されない**＝**C38 の近道にはならない**（従来の区間推定方針は不変）。⚠ 初回共有画像は VLC 再生のデスクトップキャプチャ＝**0.64倍に再標本化**されていた（可読性判定は安全側に外れるため結論は不変・画素幅のみ未確定）。∴ **以後の静止画は `ffmpeg` 抽出フレームで受け取る**様式に変更。`src/` 未変更＝golden 3/3 不変 |
| 2026-08-07 | **§10 確定仕様を追加**（実行場所／ツール類型と関門／動画の扱い／trial・analysis の新構造／**エラー診断の出力仕様**／**一連のフロー**）。実装は次セッション以降 | ユーザー承認済＝**変更にはユーザー承認を要する**。★**動画は共有しない**（ツールをユーザーPCで動かす設計で問題が構造的に消える）／★**録画転記はWebアプリの新規ページ**（シム本体と golden に非干渉）／★**エラーは `<ツールID>-<stage>-<3桁>` で構造化し部分成功を必ず保全**（Claude はブラウザを見られない＝唯一のデバッグ経路） |
| 2026-08-07 | **新設**（ユーザー承認＝Phase 9 として正式採番）。P0 を先行実装し、P1 をゲートに設定 | 契機＝「ゲームプレイと並行してデータ取得を進めているため後手に回る」。**根本原因は分担が逆だった**ことと診断（人間が機械的作業を担っていた）。`TRANSCRIPTION_DESIGN.md` を archive から解凍して一次設計に据え、**C49 による陳腐化**（非会心アンカー原則の retire）を冒頭注記で訂正。ROADMAP §4(iii) の intake 部分を吸収。**sim05 は凍結**（C40/C41/C44 は open のまま＝意図的コスト） |

<!-- doc_refs:begin ── 自動生成。手で編集しない（node tools/doc_refs.mjs --write が再生成する） -->
## この md を参照している文書（現役層 7）

- [CALIBRATION_ANALYSIS.md](./CALIBRATION_ANALYSIS.md)
- [CLAUDE.md](./CLAUDE.md)
- [REPO_STANDARDS.md](./REPO_STANDARDS.md)
- [ROADMAP.md](./ROADMAP.md)
- [TRANSCRIPTION_DESIGN.md](./TRANSCRIPTION_DESIGN.md)
- [workspace/HANDOFF.md](./workspace/HANDOFF.md)
- [workspace/TODO.md](./workspace/TODO.md)

_他に 凍結sim/archive/essays から 2 件（更新対象外）_
<!-- doc_refs:end -->
